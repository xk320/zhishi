# 任务-000078：阶段1最终审计重算与门禁裁决

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 执行分支：`codex/task-000078-final-audit-v1`
- 开始时间：`2026-08-08T03:46:00+08:00`
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“基于最新不可变证据复用阶段1最终审计器并裁决八个研究叶子门禁”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅使用仓库中的已合并合同、报告、配置和不可变批次，不请求方案选择或重复批准
- 并行规则：禁止并行；唯一前序依赖任务-000077；不得与其他阶段1最终审计、数据补采或阶段门裁决任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000077已完成，并已把数据库元数据身份批次追加到最新`main`。
- 输入依赖：任务-000029至任务-000077已合并的任务合同、不可变批次、配置、缺口台账和阶段1审计报告；执行时必须从最新`main`读取。
- 当前阻塞原因：无。若任何上游指纹、批次、状态计数或执行器不能复算，必须停止相关裁决并发布失败安全事实，不得猜测补齐。
- 解除条件：八个BTC/ETH研究叶子在相同证据基线下分别满足全部阶段1硬门；任一叶子存在`拒绝`、`无法判定`、`失败`、`未成熟`、`失效`或其他缺口时，阶段1和阶段2继续阻塞。

## 背景

任务-000074的阶段1最终审计是在任务-000075至任务-000077新增证据前生成的。总体计划要求在新增证据合并后重新生成最终审计并裁决阶段2门禁。本任务只复核和重算现有证据，不把数据库结构观察、服务器可达性或文件描述解释为完整身份、时间、质量、重放、成本或研究准入证明。

## 任务目标

1. 冻结最新`main`、任务-000029至任务-000077的合并事实、合同版本、配置、执行器和不可变批次指纹，形成可复现的最终审计输入基线。
2. 将任务-000075、任务-000076和任务-000077的真实批次及其候选资产/身份成员分母分别纳入最终审计，不覆盖历史批次，不跨BTC与ETH补偿缺失。
3. 以BTC、ETH分别对应4小时、8小时、24小时、48小时的八个研究叶子生成一次追加式不可变最终审计批次，保留所有候选总体、分母、已观察、拒绝、无法判定、失败、未成熟、失效和缺口原因。
4. 依据既有硬门裁决阶段1与阶段2状态；只有八个叶子全部通过才可解除阶段2，否则如实保持阻塞并精确列出解除条件。

## 固定执行方案

1. 从最新`main`只读加载AGENTS、README、研发中心入口、任务规范、任务-000029至任务-000077及其可追溯配置、报告和不可变批次，按任务编号和提交祖先顺序建立输入清单。
2. 复用现有`脚本/审计/执行阶段1最终审计.py`的确定性叶子、状态语义和硬门逻辑；必要的任务-000075至任务-000077接线仅以追加式、可回放方式实现，不改变历史批次和门槛。
3. 以固定的八个叶子（BTC/ETH × 4小时/8小时/24小时/48小时）逐项裁决；15分钟和1小时只能作为事后结果观察窗口，不能成为市场状态、事件探测或交易许可尺度。
4. 生成新的不可变批次、输入/规则/执行器/任务快照指纹和审计报告；任何指纹漂移、缺失、未来时间、状态不守恒、跨标的补偿或输出超限均失败关闭。
5. 只在本任务交付PR和独立状态闭环PR均合并后标记任务完成；任务完成不等于阶段1通过。

## 默认工程决策

- 证据、可复现和失败安全优先于覆盖率；普通工程取舍按安全、证据、简单、可回滚顺序自主决定。
- 复用标准库、既有审计器和既有测试，不引入新服务、数据库客户端或复杂质量门；所有资源串行、有界，最多两个只读评审者，测试保持单进程，Node堆上限256MiB，不创建额外工作树。
- 资产候选分母与身份成员分母分开统计；BTC、ETH独立裁决，任何一方缺失都不得由另一方补偿。
- 描述性差异不得解释为因果、预测优势、胜率、收益、研究准入或交易许可；不得事后更换分类、筛选事件、修改分组、缩小分母或寻找最佳窗口。
- 远端、数据库和真实市场数据不属于本任务输入；所有不足均保留`无法判定`或阻塞。

## 允许停止条件

- 需要访问服务器、数据库、真实市场数据、生产服务、账户权限、凭据、原始数据或任何不可逆操作。
- 发现必须改变既有硬门、任务合同、历史批次、原始数据或阶段边界才能继续。
- 输入指纹、提交祖先、批次内容、时间语义或状态计数无法复算，或出现未来数据、敏感信息、跨标的补偿、输出超限或资源超限。
- 任何叶子证据不足、拒绝、无法判定、失败、未成熟或失效；此时停止放行判断，但必须完成失败安全批次并记录精确解除条件。

## 输入合同

- `AGENTS.md`、`README.md`、`docs/研发中心/README.md`、`docs/研发中心/任务规范.md`、`docs/研发中心/看板.md`、`docs/研发中心/总体计划.md`及任务-000029至任务-000077完整任务文件。
- 任务-000029至任务-000077已合并提交的合同、配置、测试、报告、缺口台账和`artifacts/`下不可变批次；输入必须绑定最新`main`和精确提交SHA。
- `scripts/审计/执行阶段1最终审计.py`及其现有专项测试；仅允许仓库只读证据作为审计输入。
- 既有阶段1硬门、八个叶子定义、时间可见性和数据缺口语义；不得从聊天、Issue、PR描述或推测补齐证据。

## 输出合同

- `docs/superpowers/specs/task-000078-phase1-final-audit-design.md`：定义输入身份、八叶子、状态守恒、硬门、尺度边界和失败安全规则。
- `artifacts/审计/阶段1最终审计/<批次>/`：不可变批次清单、八叶子逐项结果、输入/规则/执行器/任务快照指纹和资源事实。
- `scripts/审计/执行阶段1最终审计重算.py`及专项测试：复用既有阶段1审计器并接入任务-000075至任务-000077批次。
- `docs/审计/阶段1最终审计报告.md`、`docs/审计/数据缺口与补采清单.md`：只追加本次重算事实、精确缺口和阶段门裁决，不改写历史结论。
- 本任务文件、`docs/研发中心/看板.md`、交付PR、双只读评审证据、验证命令和合并后状态闭环记录。

## 工作范围

- 复算任务-000029至任务-000077的输入链、提交祖先、批次指纹和状态计数。
- 复用既有阶段1最终审计器，并通过任务专用重算入口使任务-000075至任务-000077证据进入同一不可变审计快照。
- 生成八个研究叶子的候选总体、分母、状态、失败安全原因和阶段1/阶段2裁决；运行专项、审计、数据、研发中心、编译、MarkdownLint、敏感扫描和差异检查。

## 不在范围

- 不访问服务器、数据库、真实市场数据或订单簿；不迁移、删除、覆盖或修改原始数据。
- 不实现运行时、模型、回测、模拟交易、真实交易、策略参数、阈值、方向、仓位、订单、胜率、收益或交易信号。
- 不改变阶段硬门、研究尺度、任务历史合同、历史批次、生产系统、权限、防火墙、凭据或账户。
- 不进入阶段2；不把阶段1审计任务完成、描述性差异或结构证据解释为研究准入或交易许可。

## 安全边界

- 所有输入只读；批次追加式、原子、不可覆盖；任何输入漂移、未来数据、敏感输出、跨标的补偿、状态不守恒和越权均失败关闭。
- 不保存远端地址、用户名、令牌、密码、密钥、原始业务响应或真实市场记录；只保存必要脱敏指纹、计数、定位和原因码。
- 资源预算绑定批次：单进程、有限内存、有限输出、无额外工作树；资源不足时停止并记录，不通过降低门槛继续。
- 阶段2只有在八叶子硬门全部满足且经过现有可信合并流程后才可讨论；本任务不得自动启动阶段2。

## 验收标准

1. 最新`main`和任务-000029至任务-000077的提交、合同、配置、批次及其输入指纹可复算，历史字节不变。
2. 新批次仅包含八个BTC/ETH研究叶子，主研究尺度严格为4小时、8小时、24小时、48小时；15分钟和1小时不升级为状态或许可尺度。
3. 每个叶子和每个上游证据保留候选总体、分母、已观察、拒绝、无法判定、失败、未成熟、失效及缺口原因；计数守恒、BTC与ETH独立。
4. 不因结果可见而更换分类、筛选事件、修改分组、缩小分母或选择最佳状态/事件/窗口；不产生因果、预测、胜率、收益或交易许可结论。
5. 阶段1和阶段2裁决与全部硬门一致；任一未知或失败安全状态均明确阻塞及解除条件。
6. 任务要求的专项、研发中心、审计、数据测试，Python编译，MarkdownLint，敏感信息扫描和`git diff --check`均通过并记录真实结果。
7. 交付PR经双只读评审、精确头SHA可信复验并自动合并；独立状态闭环PR合并后任务才可标记`已完成`。

## 验证命令

```bash
python3 -m unittest tests/审计/test_阶段1最终审计.py tests/审计/test_阶段1最终审计重算.py -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/执行阶段1最终审计.py scripts/审计/执行阶段1最终审计重算.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须执行一次仅基于仓库不可变证据的真实、串行、有界最终审计批次，并保存批次身份、输入/规则/执行器/任务快照指纹、八叶子结果和资源事实；不得把审计批次伪装成市场数据验证。

## 完成定义

最终审计合同、重算入口、一次新的不可变审计批次、精确报告/缺口派生记录和全部验证证据已提交；交付PR经双只读评审、可信复验并自动合并，随后独立状态闭环PR合并，将任务-000078标记`已完成`。若任一阶段1硬门未满足，任务仍可完成但阶段1和阶段2必须保持阻塞。

## 需要人工决策

- 无。普通工程取舍和数据不足按本合同自主失败安全处理；只有新增生产权限、凭据、不可逆操作或合同冲突才可暂停并报告。
