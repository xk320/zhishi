# 任务-000119：解耦任务115合同修订后的阶段1测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 执行分支：`codex/task-000119-task115-fixture-repair-v1`
- 开始时间：`2026-08-15T02:48:59+08:00`
- Pull Request：[#338](https://github.com/xk320/zhishi/pull/338)
- 实现提交SHA：`6695319c2692392addc1a0f25a24c64b68b149a3`
- 合并时间：2026-08-14 18:55:20 +0000
- 合并提交SHA：`c892c4b0d22a42991fdb1e2f02736334bc38831b`
- 类型：测试
- 阶段：研发中心可信合同治理
- 优先级：P1
- 执行方案：方案A“固定合成基线并兼容合同生命周期”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；只修改研发中心测试夹具，不修改任务-000115/000116/000106合同或状态

## 依赖与阻塞条件

- 唯一前序依赖：任务-000116已完成并进入main。
- 当前阻塞原因：任务115执行会把自身从待执行更新为执行中/待评审，并把任务106追加覆盖受限规则；现有资格测试直接读取当前文件，导致2项正向夹具失效。
- 解除条件：测试内合成任务115待执行基线和任务106未修订基线，且真实任务状态变化不再使正向测试失败。

## 背景

任务115的固定合同修订会改变任务115状态元数据并追加任务106适用规则。现有测试仍把工作树当前任务文件当作未执行基线，形成生命周期耦合。该耦合会阻止任务115在不跳过测试的前提下推进。

## 任务目标

1. 为任务116单字段基线修复测试构造稳定的任务115待执行合同基线。
2. 为阶段1覆盖受限合同修订测试构造稳定的任务115执行前和任务106未修订基线。
3. 保留任务116完成前置、任务115状态迁移、任务106固定章节、路径白名单和合同漂移失败关闭断言。

## 固定执行方案

1. 仅在测试夹具内移除执行元数据、合成待执行状态，并按固定章节指纹逆向得到未修订任务106基线。
2. 不读取服务器、数据库、市场数据或凭据，不修改可信资格器和工作流。
3. 运行研发中心全套专项测试；任何失败均停止，不以跳过测试替代修复。

## 默认工程决策

- 复用Python标准库、现有unittest和可信规则常量，不新增依赖。
- 合成基线只存在于测试内，不进入任务事实、审计批次或研究输出。
- 兼容逻辑固定为任务115待执行基线和任务106未修订基线，不泛化为任意任务。

## 允许停止条件

- 需要修改可信资格器、自动合并工作流、任务合同、看板事实、服务器、数据库、权限、原始数据或生产路径。
- 无法以固定文本/字段构造确定性基线。
- 修复会放宽未来数据、跨标的补偿、研究准入、交易许可或评审失败关闭。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`中的任务116和阶段1正向/负向测试。
- 任务115执行前后状态元数据和任务106覆盖受限固定章节规则。

## 输出合同

- 与真实任务生命周期解耦的测试夹具。
- 研发中心资格、工作流、看板和范围专项验证结果。
- 任务119执行记录及看板派生状态。

## 工作范围

- 仅修改研发中心测试文件、任务119执行记录和看板状态字段。

## 不在范围

- 不修改可信资格器、自动合并工作流、任务-000115/000116/000106合同、历史批次、服务器、数据库、市场数据、凭据、生产或交易逻辑。

## 安全边界

- 合成输入不进入生产、审计或研究结论。
- 保留状态、合同、路径、看板和安全边界负向测试。
- 不使用未来数据，不跨BTC/ETH补偿，真实资金交易保持关闭。

## 验收标准

1. 任务116基线修复和阶段1覆盖受限合同修订正向测试在任务115/106真实状态变化前后均通过。
2. 任务115待执行/待评审、任务106固定章节和任务116完成前置的越权变体仍失败关闭。
3. 研发中心资格、工作流、看板和范围专项测试全绿。
4. Python编译、MarkdownLint和`git diff --check`通过，无生产、数据或凭据变更。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

夹具修复进入main，专项测试全绿，任务-000119状态闭环为已完成；任务115/116/106合同、状态和历史证据不被改写。

## 数据与安全影响

仅修改测试夹具和研发中心治理记录；未访问服务器、数据库、市场数据、原始数据、生产服务、凭据或真实交易。

## 已知限制

本任务只修复测试生命周期耦合，不代表覆盖受限成本证据、研究准入或交易许可通过。

## 回滚方式

回滚本任务PR即可恢复旧测试夹具，不触碰任务115/116/106合同、状态或历史批次。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000119-task115-fixture-repair-v1`
- 开始时间：`2026-08-15T02:48:59+08:00`
- 实现提交SHA：`6695319c2692392addc1a0f25a24c64b68b149a3`
- Pull Request：[#338](https://github.com/xk320/zhishi/pull/338)
- 交付物：测试夹具合成基线、任务119状态记录、研发中心看板迁移。
- 验证结果：研发中心专项167项通过；Python编译通过；变更Markdown 2份为0 issues；`git diff --check`通过。
- 已知限制：不改变任务106阻塞、研究准入或交易许可；不提供多年主网真实执行延迟证据。
- 数据与安全影响：仅内存合成测试文本和治理文档，无服务器、数据库、原始数据、凭据、生产或真实交易访问。
- 回滚方式：回滚PR #338，恢复旧测试夹具，不触碰任务115/116/106合同及历史证据。
