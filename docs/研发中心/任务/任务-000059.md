# 任务-000059：修复看板模式回归测试夹具漂移

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：测试
- 阶段：阶段1 研发自动化治理
- 优先级：P1
- 执行方案：方案A“从当前机器合同同步回归夹具并保持失败关闭”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 自动合并范围：治理自动化
- 并行规则：禁止并行；任务-000058完成后执行；不得与其他任务并行
- 执行分支：`codex/000059-board-schema-regression-v1`
- 开始时间：`2026-08-05T09:36:22+08:00`
- Pull Request：[#139](https://github.com/xk320/zhishi/pull/139)
- 实现提交SHA：`2be538f65f1fd1d32ef2578b8749c2c27c20749b`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000058已完成并进入main。
- 当前阻塞原因：无；问题可在仓库内通过测试夹具修复，不需要外部事实。
- 解除条件：从最新main创建分支，修正夹具并完成验收验证。

## 背景

研发中心看板机器合同已将当前各分区表头固定为三至五列。现有
`test_重复表头失败关闭`仍查找历史五列表头分隔行，而当前看板只有三列表格，因此测试输入
没有发生变化，无法证明重复表头会失败关闭。这是测试合同与当前看板模式的状态漂移。

## 任务目标

1. 使重复表头负向测试基于当前看板中真实存在的机器合同分隔行。
2. 保持校验器的失败关闭逻辑、看板机器合同和所有任务状态不变。
3. 通过范围、阶段门和任务中心回归，证明修复没有引入SOL前向范围或状态漂移。

## 固定执行方案

1. 仅修改研发中心看板模式回归测试的夹具定位，使用当前三列表格分隔行或等价的机器合同
   事实，确保重复行确实注入测试输入。
2. 不修改 `docs/研发中心/任务看板模式.md`、可信资格器实现或看板状态内容。
3. 运行任务合同列出的全部验证并记录真实结果；任一失败保持任务未完成并修复后复验。

## 默认工程决策

- 优先复用现有测试和标准库，不引入依赖。
- 以机器合同和当前看板为唯一表结构来源，不硬编码已废弃列数。
- 只做最小可逆测试修复，不借机重写治理规则。
- 测试单进程，Node堆上限256 MiB。

## 允许停止条件

- 需要修改可信资格器、机器合同、生产系统、服务器、数据库、原始数据、权限或凭据；
- 当前机器合同与看板存在无法在本任务范围内解决的直接冲突；
- 发现修复会改变任务状态、研究范围、阶段门或交易许可。

## 输入合同

- `AGENTS.md`、任务规范、任务看板模式合同和当前 `docs/研发中心/看板.md`；
- `scripts/研发中心/验证自动合并资格.py`；
- `tests/研发中心/test_任务看板模式.py`及范围、外部状态一致性测试；
- 任务-000058在main中的合并事实。

## 输出合同

- 修正后的 `tests/研发中心/test_任务看板模式.py`；
- 本任务执行记录、看板状态、提交、PR、双评审和验证证据。

## 工作范围

- 修正重复表头负向测试夹具；
- 运行看板模式、范围与任务中心治理回归；
- 按流程提交、评审、自动合并并完成状态闭环。

## 不在范围

- 不修改生产代码、可信资格器、机器合同内容或历史看板记录；
- 不访问Ubuntu、服务器、数据库或真实市场数据；
- 不执行模型、回测、模拟交易或生成交易许可结论；
- 不改变BTC/ETH前向范围、SOL历史边界或阶段2门禁。

## 安全边界

- 仅允许测试、任务记录和看板状态变更；
- 不提交密钥、密码、令牌、地址、原始日志或市场记录；
- 失败时保持拒绝，不删除或覆盖原始数据。

## 验收标准

1. `test_重复表头失败关闭`实际注入当前合同存在的分隔行副本，并断言校验器返回失败。
2. `tests/研发中心/test_任务看板模式.py`全部通过。
3. `tests/研发中心/test_项目范围与阶段状态.py`、`test_外部状态一致性.py`和
   `test_验证跨载体冲突.py`通过。
4. Python编译、Markdown检查和 `git diff --check`通过。
5. 任务文件与看板状态、分支、提交、PR证据一致；无新的可执行状态漂移。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_任务看板模式.py -v
python3 -m unittest tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m unittest tests/研发中心/test_外部状态一致性.py -q
python3 -m unittest tests/研发中心/test_验证跨载体冲突.py -q
python3 -m py_compile scripts/研发中心/*.py tests/研发中心/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

测试夹具修复进入main，双只读评审均通过，精确SHA自动合并；随后通过独立合并后状态闭环
PR将任务-000059标记为`已完成`。未合并前不得声明完成。

## 执行记录

- 执行分支：`codex/000059-board-schema-regression-v1`。
- 开始时间：`2026-08-05T09:36:22+08:00`。
- 交付提交SHA：`2be538f65f1fd1d32ef2578b8749c2c27c20749b`。
- Pull Request：[#139](https://github.com/xk320/zhishi/pull/139)。
- 交付物：看板模式重复表头回归测试、任务状态记录和看板执行视图。
- 验收结果：看板模式9项、范围21项、外部状态10项、跨载体相关3项测试通过；Python编译、
  MarkdownLint（140文件0 issues）和`git diff --check`通过。
- 已知限制：未访问Ubuntu、服务器、数据库或真实市场数据；跨载体全量测试由可信主流程复验。
- 数据与安全影响：仅修改测试与研发中心状态文件；未改生产、原始数据、权限、凭据或账户。
- 需要人工决策：无。
- 回滚方式：回滚PR #139合并提交即可恢复旧测试夹具，不影响机器合同和历史审计证据。
