# 任务-000047：修复状态夹具在空待评审分区下的兼容性

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：测试
- 阶段：阶段1 研发中心状态一致性治理
- 优先级：P1
- 执行方案：方案A“按看板模式生成双态状态夹具”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只修改外部状态一致性测试、本任务记录和看板
- 执行分支：`codex/000047-state-fixture-empty-review-v1`
- 开始时间：`2026-08-04T14:12:03+0800`
- 合并时间：2026-08-04 06:26:34 +0000
- 合并提交SHA：`030c2b556c191c3ea71ac5bec296724915da9e68`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000043已完成；本任务修复其完成态闭环后暴露的测试夹具回归；任务-000046已合并但尚未完成状态闭环，不改变本任务的测试范围；
- 当前阻塞原因：无；
- 解除条件：任务-000043已进入`main`，现行看板的`待评审`分区可以为空，缺陷可在本地夹具中复现。

## 背景

任务-000045增加了任务-000043在`待评审`和`已完成`两种真实基线之间的反向夹具，但夹具
只会在看板已有对应表头时插入行。任务-000043完成闭环后，真实看板的`待评审`分区为“无”，
没有表头，专项测试无法构造待评审夹具并错误失败。该问题阻断研发中心全量回归，必须修复
夹具生成逻辑而不是改变任务状态或放宽一致性校验。

## 任务目标

使任务-000043状态一致性专项测试从机器看板模式生成反向夹具，在`待评审`分区为空、存在
表格或任务处于`已完成`时均能构造合法的待评审映射，同时继续拒绝错分区、缺失行、未知、
缺失和重复状态。

## 固定执行方案

1. 复用`docs/研发中心/任务看板模式.md`中的机器表头和分隔行，不复制易漂移的字符串；
2. 当目标状态章节为“无”时，在该章节位置插入标准待评审表头后再插入夹具行；
3. 保持当前任务文件和看板的真实内容不变，夹具仅在内存中构造；
4. 先复现空分区失败，再补充双态和负向断言，运行研发中心全量、编译、Markdown和差异验证。

## 默认工程决策

- 以唯一机器看板模式为表头来源；
- 使用标准库字符串操作和现有断言，不引入依赖；
- 测试夹具不写入仓库、服务器、数据库或数据目录；
- 任何模式缺失、状态重复或章节不一致继续失败关闭，不用默认值掩盖错误。

## 允许停止条件

仅当需要修改生产、服务器、数据库、原始数据、权限、凭据或可信控制面，或发现任务合同
与看板机器模式直接冲突且无法保守处理时停止。普通测试夹具和断言由Codex自主决定。

## 输入合同

- `tests/研发中心/test_外部状态一致性.py`现有任务-000043双态夹具；
- `docs/研发中心/任务看板模式.md`机器表头合同；
- 任务-000043、任务-000045、任务规范和看板当前状态；
- 不访问服务器、数据库、真实市场数据或敏感日志。

## 输出合同

- 空`待评审`分区可复用的待评审夹具；
- 双态、错分区、缺失行、未知/缺失/重复状态的回归验证；
- 本任务执行记录、分支、提交、PR、验证结果和看板同步。

## 工作范围

- 修改`tests/研发中心/test_外部状态一致性.py`中的夹具插入逻辑和必要断言；
- 更新本任务状态记录与看板派生视图；
- 运行任务合同规定的验证命令。

## 不在范围

- 不修改任务-000043、任务-000045或任务-000046的合同和状态；
- 不修改看板机器模式、可信合并脚本、生产代码、服务器、数据库、原始数据、权限、账户或凭据；
- 不训练模型、运行回测、生成交易信号、仓位、订单或真实交易；
- 不修改历史任务、历史报告或历史数据批次。

## 安全边界

只允许研发中心测试、本任务记录和看板状态记录变更；夹具必须内存隔离。测试失败必须
保留事实，不得跳过、删除或重命名失败场景。

## 验收标准

1. 任务-000043为`已完成`且`待评审`分区为空时，专项测试能够构造并通过待评审反向夹具；
2. 任务-000043为`待评审`时，专项测试能够构造并通过已完成反向夹具；
3. 错分区、看板行缺失、未知/缺失/重复状态和历史记录缺失继续有失败断言；
4. 研发中心全量测试、脚本编译、MarkdownLint和`git diff --check`通过；
5. 不产生服务器、数据库、真实数据、生产、权限或凭据影响。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_外部状态一致性.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证外部状态一致性.py tests/研发中心/test_外部状态一致性.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check
```

## 完成定义

空分区和双态夹具在专项及研发中心全量测试中通过，经双子智能体只读评审、主执行器验证和
`main`可信复验合并；再通过独立状态闭环PR标记`已完成`。

## 执行记录

- 执行分支：`codex/000047-state-fixture-empty-review-v1`
- 开始时间：`2026-08-04T14:12:03+0800`
- 实现提交SHA：`3fd3221f8a40b19d7059a0dbee95e8e0a4294477`
- Pull Request：[#75](https://github.com/xk320/zhishi/pull/75)
- 交付物：从唯一机器看板模式读取表头和分隔行，在内存中兼容空“待评审”分区并保留双态、错分区、缺失和重复状态负向断言。
- 验收结果：专项测试10项通过（含空“待评审”分区）；研发中心全量153项通过；审计全量122项通过；脚本编译、MarkdownLint（106文件）和`git diff --check`通过。
- 已知限制：数据身份冻结测试仍有2项因既有数据资产发现器SHA-256漂移失败，该漂移属于任务-000046变更后的独立后继任务，未在本任务中改写冻结合同；未访问服务器或真实市场数据。
- 数据与安全影响：仅修改研发中心测试夹具；夹具内存隔离，不写入数据、服务器、数据库、生产、权限或凭据。
- 回滚方式：回滚本PR提交即可恢复原夹具构造，不触碰任务状态机、看板模式或历史证据。
