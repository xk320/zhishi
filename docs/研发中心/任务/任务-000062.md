# 任务-000062：Ubuntu 数据对象质量与完整扫描复验

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 执行分支：`codex/000062-quality-audit-v1`
- 开始时间：`2026-08-05T14:30:41+08:00`
- 合并时间：2026-08-05 14:49:21 +0800
- 合并提交SHA：`a069c72e2d57ec288cfffec276d59e46c6f29da5`
- 类型：数据审计
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“复用持续质量审计入口执行同一输入链的只读质量复验”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000061完成后执行；不得与其他数据审计或数据工程任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000061已合并到最新`main`，来源身份和时间质量批次已形成不可变证据。
- 当前阻塞原因：无；本任务授权固定逻辑别名`ubuntu`的白名单只读质量审计。
- 解除条件：从最新`main`重新冻结任务-000061的来源身份/时间质量批次、清单和规则指纹；若
  输入成员集合不一致、探针不可达、资源边界不满足或需要读取业务正文，则记录脱敏证据并将
  本任务标记为`阻塞`，不得伪造质量批次。

## 背景

任务-000061重新冻结了630个BTC/ETH身份与时间质量成员，但身份已证明数为0，时间质量已证明
数为0。当前仍不能确认频率、业务键、迟到、重复、断档和结构异常，也不能区分完整扫描与仅
元数据观察。任务-000060的质量批次是旧一次复验，不能替代在任务-000061同一证据链上的新
质量批次。本任务只回答本次冻结输入的质量事实和未知事实，不修复数据、不修改来源合同、不
生成研究或交易结论。

## 任务目标

1. 精确绑定任务-000061来源身份批次、时间质量批次、资产清单、质量方案、审计器和作用域指纹。
2. 使用既有`scripts/审计/持续验证数据质量.py`在固定清单上生成一次真实、追加式、不可覆盖的
   只读质量批次，保存数据质量、断档、异常和验证清单。
3. 按BTC、ETH分别保留候选总体、已观察、通过、拒绝、无法判定、失败、未成熟和失效计数；
   未知不补零，不能跨标的补偿。
4. 明确区分完整内容扫描、元数据范围、输入身份漂移、超时、失败和未执行原因；不把文件名、
   字段名、记录数估计或文件修改时间解释为业务语义。
5. 生成缺口矩阵，裁决ZS-DATA-GAP-003（对象级质量）和ZS-DATA-GAP-005（完整扫描）是否
   可精确关闭；未关闭的缺口继续阻塞阶段1。

## 固定执行方案

1. 从任务-000061已合并批次读取来源身份JSON、时间质量合同JSON及其SHA-256，校验成员集合、
   BTC/ETH双标的、主尺度4小时/8小时/24小时/48小时和15分钟/1小时观察窗口不漂移。
2. 冻结`artifacts/审计/数据源清单.csv`、`config/审计/数据质量持续验证.json`、
   `scripts/审计/持续验证数据质量.py`及底层`scripts/审计/审计数据质量.py`的内容指纹；任何
   漂移在SSH前失败关闭。
3. 远端只通过逻辑别名`ubuntu`执行既有审计器，文件只读，数据库只读获批元数据，不读取业务
   正文、不远端落盘；本地只保存聚合统计、状态、元数据、指纹和脱敏错误。
4. 新批次必须写入独立目录并追加批次索引；失败、超时、输出超限、身份漂移和成员遗漏不得发布
   半批次或覆盖历史批次。
5. 生成质量报告和缺口矩阵；如果证据不足，保留`无法判定`或`阻塞`，不降低任何研究准入门槛。

## 默认工程决策

- 复用既有脚本、配置和标准库，不引入新依赖、守护进程或第二套扫描器。
- 流式处理、单进程测试，Node堆上限256 MiB；可用内存低于20%或磁盘低于5 GiB时停止新批次。
- BTC与ETH独立统计；SOL不进入前向范围；短窗口只能出现在结果观察窗口字段。
- 不确定时选择失败安全；不把无法判定、元数据估计或局部完整扫描解释为整体通过。

## 允许停止条件

- 需要读取白名单之外的路径、数据库业务正文、环境变量、原始日志或敏感信息；
- 需要修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；
- 需要新增凭据、付费数据、不可逆操作或覆盖历史批次；
- 任务-000061批次与当前清单、规则、成员集合或研究范围发生权威冲突，无法保守处理。

## 输入合同

- `AGENTS.md`、README、研发中心入口、任务规范、全部任务文件和任务-000061已合并执行记录；
- 任务-000061来源身份批次`source-identity-20260805T140150+0800-f310f869e6f2`及时间质量批次
  `time-quality-20260805T140224+0800-f84e41d1a031`的JSON、CSV和指纹；
- `artifacts/审计/数据源清单.csv`、`config/审计/数据质量持续验证.json`；
- `scripts/审计/持续验证数据质量.py`、`scripts/审计/审计数据质量.py`及既有审计/研发中心测试；
- 固定逻辑别名`ubuntu`及现有总超时、成员超时、成员数、输出、日志、内存和远端只读边界。

## 输出合同

- `artifacts/审计/数据质量增量复验/<新批次>/`不可变质量批次及追加索引；
- `artifacts/审计/数据质量增量复验/任务-000062输入/`冻结输入、来源/时间质量绑定和指纹清单；
- `docs/审计/Ubuntu数据对象质量与完整扫描复验报告.md`；
- 更新后的任务文件、看板、提交、PR、双只读评审记录和验证结果。

## 工作范围

- 冻结同一输入链并执行一次真实白名单只读质量审计；
- 保存确定性成员顺序、状态守恒、完整性、时间候选字段、断档和异常的已知/未知计数；
- 对BTC、ETH和四个主研究尺度分别报告限制与解除条件；
- 运行任务合同列出的数据、审计、研发中心、Markdown、编译、敏感信息和差异验证。

## 不在范围

- 不修复、删除、覆盖、迁移、去重、排序或重排原始市场数据；
- 不访问白名单之外的服务器路径，不修改数据库、生产服务、权限或防火墙；
- 不训练模型、不回测、不做模拟或真实交易、不生成方向、仓位、订单、交易信号或交易许可；
- 不将SOL加入前向研究，不把15分钟/1小时升级为市场状态、事件探测或交易许可尺度；
- 不因局部质量通过自动解除阶段2或扩大允许研究范围。

## 安全边界

- SSH目标只允许逻辑别名`ubuntu`；远端只读、无落盘；产物不得含IP、用户名、密钥、令牌、密码、
  原始日志或业务正文。
- 只保存聚合事实、状态、元数据、指纹、成员顺序、资源实测和脱敏错误；未知不得以零填充。
- 任何超时、资源超限、输入漂移、输出不完整和成员遗漏均失败安全，不重试为成功。
- 质量结果不构成因果、预测优势、胜率、收益、研究准入或交易许可。

## 验收标准

1. 新质量批次和输入绑定目录为追加式不可变产物，历史批次和原始数据字节未改变。
2. 来源身份、时间质量、清单、方案、规则、审计器、作用域和成员集合指纹均可复算且绑定任务-000061。
3. BTC、ETH计数分别守恒；完整扫描、元数据范围、拒绝、失败、未成熟、失效和无法判定不静默遗漏。
4. 质量、断档和异常结果中的未知保持未知；短窗口仅作为事后观察窗口。
5. 报告明确ZS-DATA-GAP-003和ZS-DATA-GAP-005是否关闭；未关闭时保留阻塞原因和解除条件。
6. 远端只读、无落盘、资源上限、敏感信息门、Python编译、数据/审计/研发中心测试、Markdown
   和`git diff --check`全部通过。

## 验证命令

```bash
python3 scripts/审计/持续验证数据质量.py \
  --inventory artifacts/审计/数据源清单.csv \
  --plan config/审计/数据质量持续验证.json \
  --ssh-target ubuntu \
  --batch-root artifacts/审计/数据质量增量复验 \
  --timeout 3600
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/数据/*.py scripts/审计/*.py tests/数据/test_*.py tests/审计/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

新质量批次、输入绑定、报告和真实验证记录形成后，创建引用任务-000062的PR并更新为`待评审`。
只有双只读评审、主执行器验证和`main`可信复验通过后自动合并，再通过独立状态闭环PR将任务
标记为`已完成`。若Ubuntu或资源边界阻止批次形成，按证据将任务标记为`阻塞`，不得提交假批次。

## 执行记录

- 执行分支：`codex/000062-quality-audit-v1`
- 开始时间：`2026-08-05T14:30:41+08:00`
- 交付提交SHA：`91de153873219139cf4236ba6321d5e1f52788b1`
- Pull Request：[#148](https://github.com/xk320/zhishi/pull/148)
- 质量批次：`dqv-20260805T143246+0800-b1c06c04eda7`；候选315，已观察310，通过0，拒绝3，无法判定312，失败0，未成熟0，失效0。
- 扫描分类：完整扫描218，仅元数据92，输入漂移3，未执行2；历史批次未覆盖。
- 交付物：质量批次目录、任务-000062输入绑定（含批次索引快照）和`docs/审计/Ubuntu数据对象质量与完整扫描复验报告.md`；既有批次索引未修改。
- 验收结果：来源身份、时间质量、资产清单、方案、规则、Schema、作用域和成员顺序指纹均已绑定；ZS-DATA-GAP-003与ZS-DATA-GAP-005仍阻塞；BTC/ETH不跨标的补偿，主研究尺度与短窗口边界未漂移。
- 验证命令与结果：质量审计成功（约79.23秒）；数据测试55项、审计测试147项、研发中心测试183项通过；Python编译通过；MarkdownLint（149个文件）0 issues；`git diff --check`通过；敏感模式扫描无命中。
- 已知限制：0个对象具备已证明的标的身份、三类时间或对象级质量通过；97个对象未形成完整扫描结论；本批次与旧批次规则/Schema指纹不同，索引比较状态为不可比较。
- 数据与安全影响：仅使用逻辑别名`ubuntu`白名单只读审计，远端无写入、无临时文件、未读取数据库业务正文；未修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；未训练模型、回测或生成交易结论。
- 需要人工决策：无。
- 回滚方式：回滚PR #148合并提交即可移除本次新增批次、输入绑定和报告，历史批次与原始数据不受影响。
