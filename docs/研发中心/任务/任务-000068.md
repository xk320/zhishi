# 任务-000068：执行任务合同冲突的双字段可信修复

<!-- markdownlint-disable MD013 -->

- 状态：待执行
- 类型：治理
- 阶段：阶段1研发中心可信交付治理
- 优先级：P0
- 执行方案：方案A“按可信Git事实执行双字段合同修复并保持目标待评审”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 自动合并范围：治理自动化
- 并行规则：禁止并行；必须在任务-000067已完成后执行；不得与其他研发中心可信治理任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000067（已完成；提供任务合同冲突修复可信入口）。
- 独立前置事实：任务-000066正文交付PR #165已进入main且状态闭环仍为待评审；该事实必须由main中的真实Git双父合并提交复算，不把PR描述或聊天当作合同来源。
- 当前阻塞原因：无；本任务只修改任务合同字段、派生看板和研发中心治理记录，不访问服务器、数据库、真实市场数据或生产系统。
- 解除条件：任务-000067已完成，且最新main可信规则仍固定绑定任务-000068→任务-000066、目标基线和头部均为待评审。

## 背景

任务-000066的正文交付PR #165已合并，但任务登记合同仍缺少可信状态闭环所需的`交付提交SHA`，完成定义也保留了登记阶段的阻塞表述。
任务-000067已建立一次性“任务合同冲突修复”入口。本任务只执行该入口预绑定的两项字段修复，恢复合同与已发生Git交付事实的一致性；不把合同修复解释为正文审计通过，也不迁移任务-000066状态。

## 任务目标

1. 从main中的任务-000066“实现提交SHA”、PR #165真实双父合并提交和祖先关系复算唯一`交付提交SHA`，写入任务-000066。
2. 将任务-000066的完成定义从登记阶段阻塞表述，替换为预绑定的完整交付后状态闭环段落。
3. 保持任务-000066的状态、看板行、依赖、范围、输入输出、安全边界、执行记录、审计批次、失败/无法判定/未成熟计数和研究结论逐字不变。
4. 仅以“任务合同冲突修复”类型创建交付PR；任务-000068本身保持待评审，合并后再通过独立状态闭环PR完成本任务。

## 固定执行方案

1. 从最新main创建独立执行分支，先把任务-000068和看板更新为执行中并记录分支与开始时间。
2. PR正文只引用任务-000068并声明“任务合同冲突修复”；目标任务-000066只允许新增可信`交付提交SHA`和替换完整`完成定义`两项内容。
3. 使用main可信资格器从PR #165的真实双父提交和祖先关系复算交付SHA；不信任PR正文、Issue、评论、聊天或手工填写的替代SHA。
4. 运行任务要求的专门夹具、研发中心全量测试、编译、MarkdownLint和差异检查，真实记录结果。
5. 提交、推送并创建PR；通过双只读评审、main可信复验和精确头SHA自动合并后，再创建独立状态闭环PR把任务-000068标记为已完成。
6. 任务-000066必须在本任务合并并闭环后，另行创建只修改状态/合并事实的完成状态闭环PR；本任务不得代替该闭环。

## 默认工程决策

- 复用main中的可信资格器、跨载体检查器、Git双父提交和已有固定段落指纹，不引入依赖、服务、数据库或常驻进程。
- 目标正文采用完整旧/新段落精确匹配；任何同义改写、部分替换、字段重复、额外章节变化或执行记录变化均停止并失败关闭。
- 目标任务仍保持待评审，看板目标行保持原样；看板只反映执行任务-000068的执行中/待评审状态。
- 研究尺度仍为4小时、8小时、24小时、48小时；15分钟和1小时只能作为事后观察窗口，本任务不创建研究结果。
- 测试单进程，Node堆上限256 MiB，不创建额外工作树；普通技术取舍按安全、证据、简单、可复现和可回滚顺序决定。

## 允许停止条件

- 无法从main中的Git对象和任务-000066既有合同确定唯一交付SHA。
- 目标基线或头部不是待评审，或任务-000066的PR、实现提交、双父关系与预绑定事实不一致。
- 需要修改交付SHA、完成定义以外的任何目标字段、执行记录、审计结果、看板目标行或任务状态。
- 需要访问服务器、数据库、真实市场数据、生产系统、权限、凭据、原始数据或执行不可逆操作。
- 需要放宽来源仓库、main目标、精确头SHA、双评审、失败关闭或研究数据硬门。

## 输入合同

- 最新main上的AGENTS.md、README、研发中心README、任务规范、看板、总体计划和全部任务文件。
- 任务-000067已合并的可信入口、任务-000066完整正文、PR #165合并提交及其双父Git对象。
- main可信资格器、跨载体检查器、自动合并工作流和研发中心测试。
- 任务-000066原有“实现提交SHA”、PR #165事实和固定旧完成定义段落。

## 输出合同

- `docs/研发中心/任务/任务-000068.md`：执行元数据、交付记录、验证结果和状态闭环信息。
- `docs/研发中心/任务/任务-000066.md`：仅新增可信`交付提交SHA`并替换完整`完成定义`段落。
- `docs/研发中心/看板.md`：仅按任务-000068状态移动；任务-000066行保持原样。
- PR正文、双只读评审证据、main可信复验结果和合并后独立状态闭环PR。

## 工作范围

- 任务-000068执行任务元数据和派生看板状态。
- 任务-000066预绑定的两项合同字段：`交付提交SHA`、完整`完成定义`。
- 固定Git事实复算、合同指纹校验、任务中心验证和PR交付证据。

## 不在范围

- 不修改任务-000066的状态、看板目标行、依赖、研究范围、输入输出、安全边界、执行记录、审计批次或任何数据结论。
- 不访问服务器、数据库、真实市场数据或订单簿系统，不创建远程账号，不修改权限、防火墙或生产服务。
- 不实现运行时、数据库、模型、回测、交易、方向、仓位、订单、胜率、收益或交易许可。
- 不合并任务-000066完成状态，不解除阶段1数据硬门或阶段2禁止条件。

## 安全边界

- 仅读取本地Git对象和仓库文件，所有变更必须通过可信PR；不提交IP、域名、用户名、密钥、令牌、密码、原始日志或账户凭据。
- 目标字段必须由main Git事实复算；任何PR正文、Issue、评论和聊天补齐的SHA都无效。
- 任务-000066的无法判定、失败和未成熟计数必须原样保留，不得以合同修复改变研究结论。
- 真实交易、生产写入、原始数据破坏和不可逆操作始终禁止。

## 验收标准

1. 任务-000068基于最新main、依赖任务-000067已完成，状态和看板执行元数据一致。
2. PR正文只引用任务-000068并声明“任务合同冲突修复”；任务-000066目标基线和头部均保持待评审。
3. `交付提交SHA`为main真实PR #165交付头，且由双父合并事实、第一父main基线和第二父祖先关系唯一复算。
4. 完成定义只从固定旧段落变为固定新段落；任何其他合同、执行记录、审计结果、计数或看板目标行变化均失败关闭。
5. BTC、ETH、SOL历史记录不做跨标的补偿；不生成任何研究指标、胜率、收益、交易信号或许可结论。
6. 专门资格夹具、研发中心全量测试、Python编译、MarkdownLint、敏感信息扫描和`git diff --check`全部通过并记录真实结果。
7. PR通过双只读评审、主执行器验证、main可信复验并以精确头SHA自动合并；合并后独立状态闭环将任务-000068标记为已完成。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_验证跨载体冲突.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/*.py tests/研发中心/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

任务-000068的双字段修复PR通过双只读评审、主执行器验证和main可信复验后合并；随后通过独立状态闭环PR标记任务-000068为已完成。
任务-000066的完成状态必须另行状态闭环PR处理；本任务不将任务-000066标记为已完成，不改变其审计结果或阶段门禁。

## 数据与安全影响

只更新任务合同字段、任务中心状态和验证证据；不访问或修改服务器、数据库、真实市场数据、原始数据、订单簿逻辑、生产服务、权限、凭据或账户。不产生模型、回测、胜率、收益、方向、仓位、订单或交易许可。

## 已知限制

本任务只修复任务合同与已合并Git事实的登记冲突，不证明任务-000066正文审计通过，不改变其无法判定、失败和未成熟计数，也不放行阶段2。跨载体检查器单独运行时的执行记录防护仍由主资格器补充拦截。

## 回滚方式

回滚本任务交付PR的合并提交即可恢复任务-000066原始两字段和任务-000068执行记录；不得重写PR #165、任务历史、原始数据或Git历史。随后任务-000066继续保持待评审并等待新的可信状态闭环。
