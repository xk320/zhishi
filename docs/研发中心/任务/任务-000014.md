# 任务-000014：知识图谱运行时设计

- 状态：待评审
- 类型：架构
- 来源：阶段规划
- 优先级：P0
- 关联任务：任务-000009
- 执行分支：`agent/000014-knowledge-graph-runtime-design`
- 开始时间：2026-07-23 23:34:43 +0800
- 完成实现时间：2026-07-23 23:44:32 +0800
- 实现提交 SHA：`f02ea79bbf8cdffb197a36e2faacd06bf427895b`
- Pull Request：[#16](https://github.com/xk320/zhishi/pull/16)

## 目标

将《知势知识图谱》从概念合同升级为未来可实现的运行时架构设计，为后续工程实现提供边界和接口规范。

## 背景

当前已完成：

- 知识图谱节点合同；
- 关系合同；
- 证据状态体系；
- 决策映射。

但尚未定义未来系统如何管理这些对象。

## 实施要求

新增：

docs/架构/知识图谱运行时设计.md

内容至少包含：

1. 节点生命周期；
2. 关系生命周期；
3. 证据状态升级流程；
4. 版本管理；
5. 血缘追踪；
6. 回滚机制；
7. 数据对象与知识节点映射；
8. 决策链路映射。

## 限制

禁止：

- 实现数据库；
- 引入具体技术选型；
- 假设数据已经存在；
- 编写真实交易代码。

## 验收标准

- 能指导未来工程实现；
- 与白皮书、决策委员会、知识图谱一致；
- 不把假设写成事实；
- Markdown检查通过；
- git diff --check通过。

## 完成定义

提交PR。

等待架构评审。

评审通过并合并后，任务状态更新为已完成。

## 执行记录

### 交付物

- `docs/架构/知识图谱运行时设计.md`
- `docs/方法论/知势词典.md`
- `README.md`
- 本任务文件与`docs/研发中心/看板.md`的状态记录

### 验收与验证

- 只读结构检查：通过。任务要求的8项主题、3条证据轨道、7个生命周期状态、
  5个证据状态、7道质量门和10个评议单元完整覆盖。
- 任务中心一致性：通过。14个任务文件状态合法，任务-000014与看板状态一致。
- Markdown检查：通过。`markdownlint-cli2`检查5个变更文件，0 issues。
- Mermaid图：通过官方CLI渲染，输出29,041字节SVG。
- PR自动化回归测试：通过。22项测试，0失败。
- `git diff --check`：通过，无空白错误。
- 首轮结构检查因校验语句要求“不选择数据库”为连续文案而失败；文档实际以
  “本文不”加列表项声明该约束，修正只读断言后完整重跑通过。
- 首轮Mermaid渲染因本地缺少工具要求的无头浏览器而失败；安装匹配版本的本地
  渲染依赖后完整重跑通过，未修改仓库依赖。

### 已知限制

- 本任务只交付技术中立架构合同，不实现数据库、接口、服务或运行时代码。
- 数据资产审计尚未完成，预测性关系不能标记为已验证或进入真实交易许可。
- 未来实现仍须由独立任务、研究准入和架构评审授权。

### 数据与安全影响

- 未访问服务器，未读取、修改或覆盖市场数据。
- 未修改数据库、生产服务、权限或防火墙。
- 未提交密钥、令牌、密码或账户凭据。
- 未使用未来数据，未虚构胜率、收益、测试结果或完成状态。
- 真实资金交易保持关闭。

### 需要人工决策

- 请对PR #16执行架构评审；Codex不自行批准或合并。
