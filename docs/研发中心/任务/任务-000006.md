# 任务-000006：设计最小数据闭环、存储方案并形成最终审计结论

- 状态：执行中
- 类型：数据架构 / 审计收口
- 阶段：阶段1数据资产审计与最小数据闭环
- 优先级：P0
- 讨论关联：GitHub Issue #7（非任务事实来源）
- 前置任务：任务-000003、任务-000004、任务-000005、任务-000022
- 阻塞解除时间：2026-07-31 06:53:48 +0800
- 合同复核时间：2026-07-31 09:03:44 +0800
- 恢复执行时间：2026-07-31 09:37:25 +0800
- 执行分支：`codex/000006-contract-blocker-20260731`
- Pull Request：复用在途[#28](https://github.com/xk320/zhishi/pull/28)，不得创建重复PR
- 执行方案：方案A“冻结证据驱动的六门闭环与参数化容量预算”
- 方案状态：已批准执行
- 执行授权：仓库所有者明确要求启动任务-000006；Codex直接执行，不得再次要求选择方案
- 并行规则：不允许并行执行其他任务

## 任务目标

基于数据发现、质量审计和历史重放证据，设计最小数据闭环，估算3个月、6个月、12个月存储需求，并明确是否允许进入基准模型阶段。

## 背景

任务-000003至任务-000005已经形成冻结资产清单、质量审计和历史重放证据。现有结果
证明315个验证单元均未形成完整来源身份、三类时间、频率、质量、可见性和闭环证据，
且没有正式验证单元进入历史重放第二门。

本任务不是为了强行得到阶段通过，而是把已有证据连接为可复算的最终审计结论，明确
最低数据闭环、存储预算、阻塞项及解除条件。证据不足时必须禁止进入基准模型阶段。

## 固定执行方案

采用方案A“冻结证据驱动的六门闭环与参数化容量预算”，仅使用Python标准库和仓库
已有冻结产物，不引入数据库、服务、队列、容器或外部依赖。

1. 冻结任务-000003、000004、000005和000022的批次、指纹、资产编号与固定列；
   输入不一致时失败退出，不得混用批次。
2. 按资产身份、三类时间、质量、重放、最小闭环和允许研究范围六道门逐项判定；
   任何关键门`失败`或`无法判定`都禁止进入基准模型阶段。
3. 最小闭环采用技术简单的分层合同：不可变原始段、标准化对象、质量与血缘记录、
   冻结快照、研究只读输入和审计产物；所有层双向引用确切版本与SHA-256。
4. 容量估算使用版本化参数配置、整数运算和固定公式，分别输出3、6、12个月规划预算。
   记录频率、记录字节数、副本和安全余量均标记为`规划假设`，不得冒充真实采集统计。
5. 最终审计报告分别记录事实、判定和建议；BTC、ETH、SOL独立结论，不投票、不平均、
   不跨标的或尺度外推。

默认工程决策依次选择只读、失败安全、证据完整、依赖最少、可复现和可回滚的实现。
普通文件组织、命名、测试和保守判定由Codex自主决定。只有需要新增生产凭据或权限、
修改服务器、数据库、原始数据或生产服务、执行不可逆操作，或者现有合同直接冲突时，
才允许停止请求人工决策。

## 输入合同

1. 基线必须包含任务-000021完成状态提交以及任务-000003、000004、000005、000022的
   合并证据。
2. 资产输入：
   - `artifacts/审计/数据源清单.csv`；
   - `docs/审计/数据源清单.md`；
   - 发现批次固定为`discovery-20260728T194022+0800`。
3. 质量输入：
   - `artifacts/审计/数据质量结果.csv`；
   - `artifacts/审计/数据断档结果.csv`；
   - `artifacts/审计/数据异常结果.csv`；
   - `docs/审计/数据质量审计报告.md`；
   - 审计批次固定为`audit-20260728T233014+0800`；
   - 清单指纹固定为
     `019010d64fe47d89c81bfaedafd458f1d886025bb34292c0c32eafcfa4392657`。
4. 重放输入：
   - `artifacts/审计/历史重放结果.csv`；
   - `docs/审计/历史现场重放验证.md`；
   - 重放批次固定为`replay-20260731T033130+0800`；
   - 快照合同固定为`replay-snapshot-contract-1.0`。
5. 规则输入：
   - `docs/研究/数据验证阶段执行规范.md`；
   - `docs/研究/研究数据合同与实验记录规范.md`；
   - `docs/研究/研究准入规范.md`；
   - `docs/架构/系统蓝图.md`。
6. 不读取未登记市场数据、数据库业务正文、配置、环境变量或凭据。现有冻结证据不足
   时输出`无法判定`和解除条件，不访问远端补造通过证据。

## 输出合同

- `docs/架构/最小数据闭环.md`
- `docs/审计/数据资产审计报告.md`
- `docs/审计/数据缺口与补采清单.md`
- `config/审计/最小数据闭环容量.json`
- `scripts/审计/评估最小数据闭环.py`
- `tests/审计/test_评估最小数据闭环.py`
- `artifacts/审计/存储容量估算.csv`

容量CSV至少包含：配置版本、估算类型、期限月数、期限天数、数据族、标的数、每标的
数据流数、采样间隔秒数、单记录预算字节数、基础字节数、质量血缘字节数、副本后字节数、
安全余量后字节数和限制。总计行必须可由明细重算，按期限和数据族稳定排序。

最终审计报告至少包含：

- 输入批次与指纹；
- 六道阶段门及证据；
- BTC、ETH、SOL独立结论；
- 是否允许进入简单基准模型阶段；
- 容量估算事实与规划假设边界；
- 阻塞项、解除条件、复验入口、限制和数据安全影响。

## 工作范围

1. 校验四份冻结CSV的批次、指纹、资产编号覆盖和固定列。
2. 生成确定性的3、6、12个月容量规划结果。
3. 设计来源至研究输入的最小数据闭环、失败、修订、冻结、备份和恢复合同。
4. 汇总任务-000003至000005的事实，形成最终数据资产审计结论。
5. 建立数据缺口、影响、安全降级、解除条件和复验入口清单。
6. 运行测试、格式、任务中心、未来数据和敏感信息检查。

## 不在范围

- 不采集、补采、清洗、去重、重排、迁移、删除或覆盖原始数据。
- 不访问数据库业务正文，不修改服务器、服务、权限、防火墙或生产配置。
- 不实现生产存储、采集服务、消息队列、数据仓库或调度系统。
- 不训练模型、不运行正式回测、不计算胜率或收益、不编写交易执行代码。
- 不把容量规划假设、文件名、字段名或当前磁盘状态写成已验证市场数据合同。
- 不创建后续执行任务；缺口只形成候选建议，必须另行建立任务文件并进入`main`。

## 安全边界

1. 所有输入只读，现有审计产物不得覆盖；新产物只写仓库指定输出路径。
2. 正式结果只保存汇总、合同、指纹和容量预算，不保存原始市场记录、未脱敏样本、
   网络地址、凭据或配置秘密。
3. 容量估算只使用非负整数和显式有理数倍率，禁止浮点舍入、隐式单位和负容量。
4. 输入缺失、列漂移、批次不一致、资产覆盖不一致、未知配置字段或结果不可重算时
   失败退出，不发布半套产物。
5. 任何必需阶段门失败或无法判定时，最终阶段结论固定为`禁止进入基准模型阶段`。
6. 不允许用BTC或ETH证据补偿SOL，不允许用总体数量补偿单元级身份、时间或重放缺口。

## 实施步骤

1. 同步最新`main`，读取任务中心、输入规范及任务-000003至000005全部证据。
2. 复用既有执行分支和Pull Request #28，补齐合同并将任务与看板更新为`执行中`。
3. 先编写失败测试，冻结输入批次、资产覆盖、容量公式、配置边界和阶段放行规则。
4. 实现最小评估器，使测试通过并生成确定性容量CSV。
5. 编写最小数据闭环、最终审计报告和缺口清单，引用真实批次和结果。
6. 核对容量总计、三标的独立结论、六道门、未来数据边界和安全降级。
7. 运行全部验收命令，提交、推送并更新Pull Request #28。
8. 将任务与看板更新为`待评审`，写入提交、PR、交付物和真实验证结果；不合并PR。

## 验收标准

1. 任务-000003、000004、000005和000022的输入批次、指纹、固定列与315个验证单元
   覆盖可复算，不存在静默遗漏或批次混用。
2. 最小闭环覆盖来源、原始记录、标准化、质量、派生、快照、研究输入、失败、修订、
   查询、失效、备份、恢复和完整性校验。
3. 手续费、滑点、资金费率、流动性和执行延迟均进入最低闭环；缺失时不得形成成本后结论。
4. 3、6、12个月容量估算由版本化配置和整数公式生成，总计可由明细重算，所有假设
   与真实证据边界明确。
5. BTC、ETH、SOL分别形成`可用`、`有限可用`、`不可用`或`无法判定`结论，不跨范围外推。
6. 最终报告依据最低关键阶段门明确给出是否允许进入简单基准模型阶段；证据不足时必须禁止。
7. 缺口清单逐项包含证据、影响、安全降级、解除条件、建议责任任务和复验入口。
8. 脚本仅使用Python标准库，输入校验、确定性、失败安全和原子发布有自动化测试。
9. Markdown、Python编译、任务中心一致性、敏感信息和`git diff --check`全部通过。

## 验证命令

```bash
python3 -m unittest tests/审计/test_评估最小数据闭环.py -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m py_compile \
  scripts/审计/评估最小数据闭环.py \
  tests/审计/test_评估最小数据闭环.py
python3 scripts/审计/评估最小数据闭环.py \
  --inventory artifacts/审计/数据源清单.csv \
  --quality artifacts/审计/数据质量结果.csv \
  --gaps artifacts/审计/数据断档结果.csv \
  --anomalies artifacts/审计/数据异常结果.csv \
  --replay artifacts/审计/历史重放结果.csv \
  --config config/审计/最小数据闭环容量.json \
  --capacity-output artifacts/审计/存储容量估算.csv
npx --yes markdownlint-cli2 \
  docs/架构/最小数据闭环.md \
  docs/审计/数据资产审计报告.md \
  docs/审计/数据缺口与补采清单.md \
  docs/研发中心/任务/任务-000006.md \
  docs/研发中心/看板.md
git diff --check origin/main...HEAD
```

另执行输入/输出列、批次、资产编号、容量总计、阶段结论、占位符、未来数据边界和常见
敏感信息模式检查。

## 完成定义

全部交付物和验收证据真实形成后，更新任务文件与看板，提交、推送并更新引用
任务-000006的Pull Request #28，状态变更为`待评审`。只有架构评审通过且PR合并后，
任务才能标记为`已完成`。最终结论为禁止或无法判定不妨碍任务交付，但不得因此降低门槛。

## 历史阻塞与解除记录

- 历史阻塞原因：等待任务-000005形成严格重放和降级重放结论。
- 解除证据：任务-000005与其确定性修复任务-000022已通过PR #26合并到`main`，
  合并提交为`14902dea55800ad789751c9b02d600f4675f9700`；本任务前置依赖已经满足。

## 合同阻塞与解除记录

- 阻塞原因：任务文件缺少`docs/研发中心/任务规范.md`要求的类型、阶段、完整输入合同、
  输出合同、工作范围、不在范围、安全边界、验收标准、验证方式、完成定义、唯一执行
  方案、方案批准状态、执行授权、默认工程决策规则和允许请求人工决策的条件。
- 风险说明：本任务需要形成最终数据资产审计结论并决定是否允许进入基准模型阶段。
  在缺少获批方案和验收合同的情况下直接执行，会由自动化自行定义正式阶段放行口径，
  违反任务文件唯一事实来源和“没有证据，不进入主决策链路”的硬门槛。
- 防重复检查：截至合同复核时间，远程没有任务-000006执行分支或未关闭Pull Request；
  任务-000021已有独立在途Pull Request #27，与本阻塞记录不构成执行授权。
- 解除条件：架构层通过独立治理变更把上述最低合同和唯一方案补齐到`main`，并将
  `方案状态`明确为`已批准执行`、`执行授权`明确为Codex直接执行；随后把本任务状态
  恢复为`待执行`并同步看板。
- 保守结果：任务-000006未执行，不形成最小数据闭环、存储容量或最终审计结论；
  不允许进入基准模型阶段。
- 解除依据：2026-07-31仓库所有者在PR #27合并后明确要求启动任务-000006，批准
  Codex在既有任务文件和PR #28内补齐唯一保守方案并直接执行；上述最低合同已写入
  本任务文件，普通工程取舍不再请求A/B/C选择。
