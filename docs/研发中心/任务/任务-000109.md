# 任务-000109：以缺失审计替代任务-000084来源身份路线

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“系统级来源注册、批次级缺失审计与历史路线替代”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；按用户批准停止重复来源身份审计，只读取已合并脱敏证据确认数据缺失，不得再次要求用户选择方案
- 并行规则：禁止并行；任务-000108是唯一前序依赖；本任务完成前不得登记第二个缺失审计替代任务
- 执行分支：`codex/task-000109-missing-data-audit-v1`
- 开始时间：`2026-08-14T04:39:34+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000108已完成，PR #299合并提交为`86230e51c90c67b70dee59f6b51eba58cf93031c`。
- 替代关系：本任务替代任务-000084的逐成员来源身份声明接入路线；任务-000084历史批次、合同和失败证据保持不可变，后续通过独立状态闭环取消旧路线。
- 当前阻塞原因：无。任务-000092至任务-000105的脱敏批次、成员顺序、连续段和叶子摘要已在`main`中可只读复验。
- 解除条件：不适用；若输入批次指纹漂移、缺失计数无法守恒或发现必须读取原始业务数据，则失败安全并保留任务-000084历史阻塞。

## 背景

任务-000084要求对旧的630个候选身份成员逐项取得外部来源声明，但订单簿采集架构已经固定使用Binance官方来源；继续要求供应商级逐成员身份证明不能增加当前研究价值，反而重复消耗执行周期。本任务将当前可验证问题收敛为“正式研究输入在哪里缺失、缺失多少、影响哪些标的/对象/尺度”，不把来源提供者声明与数据缺失混为一谈。

## 任务目标

1. 复用当前`main`中的正式成员、质量、时间、连续段、重放和阶段门禁脱敏批次。
2. 按BTC、ETH、精确数据对象和4小时、8小时、24小时、48小时主研究尺度统计候选总体、已观察、拒绝、无法判定、失败、未成熟、失效、缺失成员、缺失日期和连续覆盖段。
3. 只确认“数据是否缺失、缺失在哪里、缺失是否切断连续段”，不审计或重新证明数据提供者身份。
4. 形成可复算的缺失摘要，作为任务-000084旧路线的替代输入；不得据此宣称成本、预测优势、胜率、收益、研究准入或交易许可已通过。

## 固定执行方案

1. 冻结`main`提交、任务-000092至任务-000105相关批次路径和每个输入文件SHA-256。
2. 只读取仓库内脱敏JSON、CSV和报告摘要；禁止访问Ubuntu、数据库、网络、原始市场数据或账户。
3. 按确定性成员顺序逐组读取状态和日期边界，保留所有分母和拒绝原因；BTC、ETH、数据对象之间不得互相补偿。
4. 以`候选总体 - 已观察覆盖成员`和固定连续段规则计算缺失成员、缺失日期、缺失区间和未成熟/失效状态；任何计数不守恒时整批失败关闭。
5. 生成追加式缺失摘要、规则指纹、输入指纹、执行器指纹和资源事实；不得覆盖任务-000084旧批次。

## 默认工程决策

- 使用Python标准库和已有脱敏批次解析逻辑；不新增第三方依赖、数据库、常驻服务或网络访问。
- 以事件日期和批次中已保存的连续段为缺失判断依据，不使用文件修改时间、当前行情或结果可见后的筛选。
- 15分钟、1小时只能记录为事后结果观察窗口，不升级为市场状态、事件探测或交易许可尺度。
- 普通工程取舍按安全、证据、简单、可复现和可回滚顺序自主决定，不再次提供A/B/C选项。

## 允许停止条件

- 输入批次、成员顺序、文件指纹或状态字段发生漂移且无法保守复算。
- 必须读取原始业务记录、服务器、数据库、凭据、网络或生产系统。
- 必须降低数据、风险或研究准入门槛才能填补缺失。
- 任何缺失结果只能通过删除成员、跨标的补偿、缩小分母或事后改组得到。

## 输入合同

- `artifacts/审计/阶段1事前冻结决策重放/stage1-prior-frozen-replay-20260812T130000Z-ca8ae0a8ecd7/`及其任务-000099决策摘要。
- `artifacts/审计/阶段1逐行时间质量/stage1-time-quality-20260812T091000Z-6968246516ef/`成员、连续段和叶子摘要。
- 任务-000092、000093、000094、000099、000100、000103、000104、000105的已合并任务合同和脱敏输出指纹。
- 最新`AGENTS.md`、`README.md`、研发中心任务规范、看板和总体计划。

## 输出合同

- `docs/数据/阶段1数据缺失审计合同.md`。
- `config/审计/任务-000084数据缺失审计.json`。
- `scripts/审计/审计阶段1数据缺失.py`及专项测试。
- `artifacts/审计/阶段1数据缺失/<批次>/`中的摘要、缺失成员、缺失区间、叶子裁决、输入/规则/执行器指纹和资源事实。
- 更新数据缺口清单、阶段1最终审计报告、README、总体计划和看板；不修改任务-000084历史批次。

## 工作范围

- 只读解析已合并的脱敏证据。
- 统计缺失成员、缺失日期、连续覆盖段和八个BTC/ETH逐尺度叶子的缺失影响。
- 建立任务-000084旧路线的替代关系和后续取消依据。

## 不在范围

- 不接入或验证`source-identity-evidence-1.0`外部声明。
- 不重新证明Binance来源、不扫描原始数据、不访问Ubuntu、数据库、网络或账户。
- 不修改订单簿系统、采集器、数据库、权限、原始数据或历史批次。
- 不训练模型、不回测、不生成方向、仓位、订单、胜率、收益或交易许可。

## 安全边界

- 输入和历史批次只读；输出追加式、不可覆盖、无业务正文和无敏感信息。
- 单进程、固定内存和输出上限；任何异常、漂移、重复或计数不守恒均失败关闭。
- BTC与ETH独立裁决；不使用未来数据，不以当前值回填历史缺失。

## 验收标准

1. 全部输入文件、规则、执行器和任务合同指纹可复算。
2. BTC、ETH、数据对象和主研究尺度的状态、缺失成员、缺失日期和连续段计数守恒。
3. 所有缺失、拒绝、无法判定、未成熟和失效状态完整保留，不缩小分母、不跨标的补偿。
4. 任务-000084旧来源身份批次字节不变，替代关系在报告和看板中明确。
5. 专项、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
6. PR经双只读评审、main可信复验和自动合并；随后独立状态闭环完成任务-000109，并为取消任务-000084保留证据。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/审计/test_审计阶段1数据缺失.py -q
PYTHONHASHSEED=0 python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/审计阶段1数据缺失.py tests/审计/test_审计阶段1数据缺失.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

另须运行一次真实、只读、单进程缺失审计批次，保存输入指纹、缺失计数、连续段、叶子裁决和资源事实。

## 完成定义

缺失审计合同、配置、执行器、专项测试和真实追加式批次已交付；缺失成员、日期、区间和连续段可由当前脱敏证据复算；任务-000084旧来源身份路线没有被伪造为完成；双只读评审、main可信复验、自动合并和独立状态闭环完成。

## 数据与安全影响

只读仓库内脱敏批次，预计输出小于25MiB；不访问服务器、数据库、网络、账户或凭据，不修改原始数据、订单簿系统或生产服务。

## 已知限制

缺失审计只能回答数据覆盖和缺失位置，不能证明来源提供者身份、数据质量全部通过、历史成本、真实执行延迟、预测优势、胜率、收益或交易许可。

## 回滚方式

回滚本任务新增合同、配置、执行器、测试、缺失批次和派生文档；任务-000084旧历史批次和原始数据不删除、不覆盖。
