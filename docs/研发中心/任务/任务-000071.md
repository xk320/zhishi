# 任务-000071：订单簿共享数据源字段与时间索引证据合同

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000071-orderbook-shared-schema-v1`
- 开始时间：`2026-08-06T22:19:14+08:00`
- 实现提交SHA：`e3028dca83625cdb3297a3fab993568f5f8b52d8`
- Pull Request：[#183](https://github.com/xk320/zhishi/pull/183)
- 类型：数据治理
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“冻结订单簿源代码合同并以专用只读元数据固定入口复核共享表”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；只读本地订单簿源代码和Ubuntu白名单元数据。一次性root安装/撤销本任务root-owned元数据固定入口、生成/撤销仅绑定该入口的临时调用密钥已获授权；不得读取查询输出或修改数据库、原始数据、订单簿逻辑，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000070完成后执行；不得与其他数据审计、数据工程或共享数据库评估任务并行
- 默认工程决策：优先复用任务-000063矩阵中的16个对象清单、任务-000069专用只读身份和订单簿源代码合同；任务-000070仅复用其批次/授权/资源/代码指纹，不复用92对象清单；字段语义不确定时记录“无法判定”，不得按名称猜测，不得用新增索引或迁移掩盖证据缺口
- 允许停止条件：仅在需要长期/外部凭据或生产权限、修改数据库结构或原始数据、访问白名单外目标、任务合同冲突，或缺少会改变数据语义与风险门槛的外部事实时停止；本任务一次性临时调用密钥和固定入口安装/撤销不属于停止条件。普通字段未知、元数据不匹配和只读结果为空不得请求人工选择，必须形成失败安全证据

## 依赖与阻塞条件

- 唯一前序依赖：任务-000070。
- 输入依赖：任务-000063覆盖矩阵中的16个已登记订单簿对象、任务-000064正文/资源合同、任务-000069专用只读身份与身份探针、任务-000070最新正文批次`批次-20260806T131956Z-v3`的授权/资源/代码指纹；这些文件和指纹必须从最新`main`重新冻结。任务-000070的92对象清单、正文入口和正文协议不得复用作本任务元数据入口。
- 本地源依赖：订单簿系统仓库`orderbook-intelligence-service-release-20260731`只读快照，源代码提交`030499faca3d6955d75c75cbc59656a4981f6c05`，`src/orderbook_service/storage.py`指纹`d4fed7bf0fc89666a9836a17f144ef41d2a6d13d829124437032c9787bf9b05d`；若工作区漂移，批次失败安全。
- 当前阻塞事实：阶段1最终审计的8个BTC/ETH逐尺度叶子仍阻塞；本任务只补齐共享订单簿表的可复核身份、字段、时间和索引合同，不自动放行阶段1或阶段2。
- 解除条件：源代码快照、16个已登记共享订单簿表的远端元数据、新建固定入口、对象清单和本任务合同在同一批次中绑定；`order_book_derived_state_revisions`仅作为未登记源代码候选保留，不得伪装成任务-000063对象。任何不匹配均保留为`无法判定`或`失败`，不得补零。

## 背景

任务-000070在专用只读身份下对92个数据库对象和2个敏感日志形成了正文级脱敏批次，但91个对象仍为`无法判定`，主要原因是没有可冻结的时间字段或没有已证明的索引路径。订单簿系统仓库已经声明一组共享MySQL表及字段、主键和索引，但其中16个表出现在任务-000063冻结矩阵，`order_book_derived_state_revisions`尚未登记为数据资产。该声明尚未与Ubuntu上的真实表元数据绑定。若不先冻结这条身份链，不能把表名相似、代码声明或`created_at`字段误认为事件时间，更不能把共享数据库复用解释为研究数据已通过。

## 任务目标

1. 从冻结的订单簿源代码中确定性提取16个已登记共享表的表身份、业务键、事件/到达/采集时间候选、单位、主键和索引声明；另列出1个未登记源代码候选但不执行远端查询。
2. 在本任务新增、版本化、root-owned的专用元数据固定入口中，使用任务-000069专用只读身份仅读取`information_schema`及16个目标表结构元数据，逐项复核数据库、表名、列类型、主键/唯一键和索引顺序；不得复用任务-000070正文入口。
3. 形成对象级映射批次：匹配、漂移、未发现、无法判定和失败均单独计数；其余不在任务-000063矩阵内的对象不得由名称或跨标的推断。
4. 把字段语义与时间尺度边界固定为：主研究尺度4小时、8小时、24小时、48小时；15分钟、1小时只能作为事后结果观察窗口，不能升级为市场状态、事件探测或交易许可尺度。
5. 为后续数据质量审计提供可复算输入，但不宣称数据质量通过、研究准入、预测优势、胜率、收益或交易许可。

## 固定执行方案

1. 从最新`main`冻结任务-000063的16个对象清单、任务-000064/000069的授权和资源、任务-000070的批次/授权/资源/代码指纹；不复用任务-000070的92对象清单、正文入口或协议。
2. 读取订单簿源代码固定提交的`storage.py`，只提取16个已登记表的结构合同，并记录1个未登记源代码候选的排除事实。
3. 部署本任务版本化的元数据固定入口，使用专用只读身份逐表读取`information_schema`元数据，执行源代码与远端的确定性比较；完成批次后撤销临时调用密钥。
4. 追加不可变脱敏批次并运行全部验证；任何不匹配、超限或权限漂移都失败安全。

## 默认工程决策

- 优先复用订单簿源代码合同、任务-000063的16个对象清单和任务-000069专用只读身份；任务-000070只作为批次/授权/资源/代码指纹输入。
- 字段语义、索引路径、标的或来源无法证明时记录`无法判定`，不按名称猜测，不新增索引，不迁移数据。
- 只保存指纹、计数、状态和原因码；不保存列值或业务正文。

## 允许停止条件

- 需要长期/外部凭据或生产权限、修改数据库结构或原始数据、访问白名单外目标、任务合同冲突，或缺少会改变数据语义与风险门槛的外部事实时停止并记录原因；本任务一次性临时调用密钥和固定入口安装/撤销不触发停止。
- 普通字段未知、元数据不匹配、只读结果为空或阶段门仍阻塞不请求人工选择，继续形成失败安全证据。

## 输入合同

- 任务-000063冻结的97对象覆盖矩阵和确定性成员顺序；本任务只处理其中与订单簿源代码声明相交的16个表；`order_book_derived_state_revisions`没有资产编号，不进入本批次。
- 任务-000070批次`批次-20260806T131956Z-v3`的批次身份/授权/资源/代码指纹（不含92对象清单）；历史批次只读且不可覆盖。
- 源代码仓库提交与`storage.py`指纹见“依赖与阻塞条件”；不得读取该仓库的凭据、环境变量或未登记数据文件。
- 远端目标仅为白名单逻辑别名`ubuntu`和任务合同登记的MySQL元数据对象；请求协议、目标清单、截止时点和资源预算必须写入批次元数据。

## 输出合同

- `docs/superpowers/specs/task-000071-orderbook-shared-schema-design.md`：16个已登记表的版本化字段、业务键、时间语义边界、索引证明规则，以及未登记源代码候选的身份与排除原因。
- `scripts/审计/远程共享表元数据固定入口.py`及对应测试：本任务专用、版本化、root-owned的元数据强制入口；只允许固定16个对象和`information_schema`查询，完成后撤销临时调用密钥。
- `artifacts/审计/订单簿共享对象映射/<批次>/`：只含对象编号、表身份指纹、列/索引指纹、状态、原因码、计数、资源事实和输入指纹；不得包含列值、业务正文、连接串或凭据。
- `scripts/审计/验证订单簿共享对象映射.py`及对应测试：验证源代码快照、映射合同、状态守恒、确定性顺序、敏感信息边界和失败安全。
- `docs/审计/数据缺口与补采清单.md`如需更新，只能追加本任务批次及明确的部分解除范围，不得把未知改写为通过。

## 工作范围

- 只读订单簿源代码合同和任务-000070的不可变脱敏产物。
- 通过本任务新增的固定专用只读元数据入口读取`information_schema.COLUMNS`、`STATISTICS`和必要的表级元数据；不读取任意业务字段值。
- 对16个已登记共享表按固定顺序记录远端元数据与源代码声明的确定性比较；未登记源代码候选只记录排除原因。
- 运行数据/审计/研发中心测试、Python编译、MarkdownLint和`git diff --check`。

## 不在范围

- 除安全边界明确授权的一次性root-owned固定入口安装/撤销和仅绑定该入口的临时调用密钥增删外，不修改、创建、删除或重建数据库表、索引、用户、数据库权限、业务配置或订单簿逻辑；不迁移、清理、覆盖或删除原始数据。
- 不读取业务正文、日志正文、完整配置、环境变量、密钥、令牌、密码或账户凭据；不使用root读取业务数据。
- 不训练模型、不实现运行时、不回测、不生成市场状态、事件、方向、仓位、订单、胜率、收益或交易许可。
- 不把代码声明当作远端数据已存在的证明；不把描述性字段差异解释为因果、预测优势或研究准入。
- 不把共享表跨BTC、ETH、SOL补偿缺失；不将15分钟、1小时升级为主研究尺度。

## 安全边界

- 远端使用任务-000069验证的专用只读身份和本任务新增的root-owned固定强制入口；root仅可一次性安装/撤销入口和临时调用密钥，不得读取数据库查询输出。
- 请求只允许版本化的元数据审计操作、固定16个对象和固定字段；拒绝任意SSH命令、任意SQL、未登记对象和交互终端。任务-000070正文入口、协议和目标清单不可复用。
- 单对象最多65536字节/30秒，批次最多300秒/4MiB，串行，远端内存最多256MiB，远端临时写入关闭；超限、越权、身份或指纹漂移均失败安全。
- 批次追加式写入，历史批次字节不变；只保存指纹、计数、状态和原因码，不保存原始结果。
- 数据截止时点不得晚于任务执行时点；发现未来时间只记录失败，不删除或修正原始数据。

## 验收标准

1. 源代码提交、文件指纹、16个已登记表的声明顺序、1个未登记候选和映射合同可由脚本确定性复算。
2. 16个已登记表逐项形成远端元数据结果；匹配、漂移、未发现、无法判定和失败计数守恒，未登记候选和其他对象保持未覆盖，不跨标的补偿。
3. 业务键、事件/到达/采集时间和单位只有在源代码声明与远端结构同时匹配且语义合同完整时才可标记为已证明；否则为`无法判定`。
4. 任何索引新增、数据库写入、原始数据变更、订单簿代码变更、任意SQL或凭据输出均为验收失败。
5. 主研究尺度和观察窗口边界保持不变；本任务不关闭ZS-DATA-GAP-001/002/003/005/006/007/008/010/011，不放行阶段2。
6. 研发中心、审计、数据测试，Python编译，MarkdownLint，敏感信息扫描和`git diff --check`通过；PR经双只读评审、主执行器验证、`main`可信复验并自动合并，再经独立状态闭环标记完成。

## 回滚方式

若入口安装、身份验证、元数据批次或验证任一步失败，立即停止固定入口、撤销本任务临时调用公钥并删除本任务root-owned入口文件；只保留失败安全批次和脱敏指纹，不触碰任务-000069原始身份、数据库用户/权限、原始数据、订单簿逻辑或历史批次。撤销动作须记录入口指纹、临时公钥指纹、撤销时间和复核结果；无法完成撤销时任务进入`阻塞`，不得继续查询。

## 验证命令

```bash
python3 scripts/审计/验证订单簿共享对象映射.py --self-test
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/验证订单簿共享对象映射.py tests/审计/test_订单簿共享对象映射.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另需执行一次真实、串行、专用只读元数据批次，并复算源代码、对象清单、授权、资源和状态指纹。真实批次失败或无结果时必须保留原因，不得写入模拟结果。

## 完成定义

16个已登记共享订单簿表的源代码—远端元数据映射合同、未登记源代码候选排除记录、root-owned元数据固定入口、不可变脱敏批次、验证脚本和测试已提交；PR经双只读评审、主执行器验证、`main`可信复验并精确头SHA自动合并；再通过独立状态闭环PR将任务-000071标记为`已完成`。即使存在匹配表，也不自动关闭阶段1缺口或放行阶段2；所有未知和漂移必须保留为下一任务的解除条件。

## 执行记录

- 初始批次：`批次-20260806T143417Z-v1`（旧解析器结果仅作历史字节保留，未覆盖）。
- 交付批次：`批次-20260806T143417Z-v2`（历史真实采集，采集时入口/规则指纹原样保留；当前验证器仅回放不可变脱敏结果，不宣称重新采集）；16个对象匹配5、漂移11、未发现0、无法判定0、失败0。
- v2批次绑定了采集时的列内主键/唯一键、`NON_UNIQUE`、列/索引类型与可空性、表身份指纹、远端入口/规则脚本/授权会话指纹、源代码合同清单和任务-000070前序批次输入；当前回放验证器另外执行严格元数据白名单、敏感输出、状态声明、异常行和撤销记录校验；对象结果和状态摘要可复算。
- 追加批次：`批次-20260806T152759Z-v3`、`批次-20260806T153128Z-v3`均按当前修复后的固定入口重新真实采集并完成撤销；两批次均为匹配5、漂移11、未发现0、无法判定0、失败0，采集时规则/入口指纹与回放验证器指纹分别绑定。
- 真实批次完成后，远端固定入口、目标清单、临时强制命令密钥及本地临时密钥均已撤销；既有`zhishi-log-ro`身份保持不变。
- 验证结果：自检通过；数据测试55/55；审计测试（含批次回放）通过；研发中心测试通过；Python编译、MarkdownLint、`git diff --check`通过。
- 已知限制：结构匹配不表示数据存在、时间语义、质量通过、研究准入或交易许可；11个漂移对象需后续独立合同裁决，阶段1和阶段2门槛不变。
