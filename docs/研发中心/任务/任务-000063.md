# 任务-000063：未完整扫描对象分层授权与失败安全复验

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/000063-incomplete-scan-audit-v1`
- 开始时间：`2026-08-05T15:04:10+08:00`
- 类型：数据审计
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“复用任务-000062批次按扫描状态分层复验，不扩大正文访问边界”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000062完成后执行；不得与其他数据审计或数据工程任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000062已合并到最新`main`，质量批次、输入绑定和缺口矩阵已形成。
- 当前阻塞原因：无；本任务只授权既有`ubuntu`白名单只读探针和仓库内不可变聚合证据。
- 解除条件：若需要读取数据库业务正文、敏感系统日志正文、白名单之外路径或修改原始数据，
  必须保留失败安全状态并将该对象标记为`无法判定`或`阻塞`，不得猜测补齐。

## 背景

任务-000062形成315个验证单元的最新质量批次，其中218个完成结构扫描，92个仅元数据，3个
输入身份漂移，2个敏感系统日志未执行。剩余97个对象无法支持完整扫描结论；ZS-DATA-GAP-003
和ZS-DATA-GAP-005仍阻塞阶段1。本任务先把97个对象按证据、授权和可执行性分层，重新冻结可
复验的输入身份，并对不可执行对象保留明确的失败、未执行和无法判定证据，不把元数据观察升格
为业务质量通过。

## 任务目标

1. 精确绑定任务-000062质量批次、输入绑定、资产清单、规则、审计器和97个未完整扫描对象。
2. 对3个输入漂移对象仅执行白名单普通文件身份元数据复验；稳定身份前不得读取正文。
3. 对92个数据库元数据对象和2个敏感系统日志保留“未完整扫描”事实；不读取业务正文或敏感日志正文。
4. 生成按标的标签、资产编号、扫描状态、授权边界和失败原因分组的可复算覆盖矩阵。
5. 明确本任务不能关闭ZS-DATA-GAP-003/005时的解除条件，并保持阶段2、模型、回测和交易禁止。

## 固定执行方案

1. 从任务-000062质量批次和输入绑定读取确定性资产编号集合，校验97个对象与315个候选总体
   的集合关系及成员顺序；输入漂移不得用旧版本补齐。
2. 冻结当前资产清单、持续验证方案、底层规则和脚本指纹；漂移时在SSH前失败关闭。
3. 仅对任务合同明确的3个普通文件输入漂移对象执行既有只读身份探针；探针只读取`stat`
   元数据和已批准的数据库Schema元数据，不读取文件正文。
4. 对92个数据库元数据对象和2个敏感系统日志仅复核任务-000062的未执行边界，保存脱敏原因、
   授权缺口和解除条件，不尝试绕过限制。
5. 以新增不可变目录保存覆盖矩阵、输入绑定、状态守恒和资源事实；不得修改既有批次索引或历史产物。

## 默认工程决策

- 复用既有标准库、审计器和任务-000062输入，不引入新依赖、第二套扫描器或守护进程。
- BTC与ETH独立统计；未限定标的不得分配给BTC或ETH；SOL不进入前向范围。
- 主研究尺度固定为4小时、8小时、24小时、48小时；15分钟、1小时仅为事后结果观察窗口。
- 元数据范围、身份复验和未执行原因不等于完整扫描、对象质量通过或研究准入。
- 任何不确定性均保留`无法判定`、`失败`或`未执行`，不补零、不跨标的补偿。

## 允许停止条件

- 需要读取数据库业务正文、敏感系统日志正文、白名单之外路径、原始日志或敏感信息；
- 需要修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；
- 需要新增凭据、付费数据、不可逆操作、覆盖历史批次或扩大研究尺度；
- 任务-000062批次与当前清单、规则、成员集合或安全边界发生权威冲突且无法保守处理。

## 输入合同

- `AGENTS.md`、README、研发中心入口、任务规范、全部任务文件和任务-000062执行记录；
- 任务-000062质量批次`dqv-20260805T143246+0800-b1c06c04eda7`及输入绑定目录；
- `artifacts/审计/数据源清单.csv`、`config/审计/数据质量持续验证.json`；
- `scripts/审计/持续验证数据质量.py`、`scripts/审计/审计数据质量.py`及既有测试；
- 固定逻辑别名`ubuntu`及现有成员、输出、日志、内存和远端只读边界。

## 输出合同

- `artifacts/审计/数据质量增量复验/任务-000063范围绑定/`不可变输入绑定和覆盖矩阵；
- `docs/审计/Ubuntu未完整扫描对象分层复验报告.md`；
- 更新后的任务文件、看板、提交、PR、双只读评审记录和验证结果。

## 工作范围

- 复算任务-000062未完整扫描集合及状态守恒；
- 对3个普通文件输入漂移对象执行有界身份元数据复验；
- 对92个数据库元数据对象和2个敏感系统日志记录未执行边界、缺口和解除条件；
- 生成覆盖矩阵、缺口更新和可复算安全报告；
- 执行数据、审计、研发中心、Markdown、编译、敏感信息和差异验证。

## 不在范围

- 不读取数据库业务正文、敏感系统日志正文或任何未授权文件正文；
- 不修复、删除、覆盖、迁移、去重、排序或重排原始市场数据；
- 不训练模型、不回测、不做模拟或真实交易，不生成方向、仓位、订单、交易信号或交易许可；
- 不将SOL加入前向研究，不把15分钟/1小时升级为状态、事件探测或交易许可尺度；
- 不因3个对象身份复验或任意局部通过自动解除阶段1或扩大研究范围。

## 安全边界

- SSH目标只允许逻辑别名`ubuntu`；远端无写入、无临时文件；数据库仅获批元数据；敏感日志正文禁止读取。
- 产物只保存聚合事实、状态、元数据、指纹、成员顺序、资源实测、脱敏错误和解除条件。
- 任何超时、资源超限、输入漂移、输出不完整和成员遗漏均失败安全，不重试为成功。
- 结果不构成因果、预测优势、胜率、收益、研究准入或交易许可。

## 验收标准

1. 97个未完整扫描对象与任务-000062质量批次按资产编号确定性绑定，集合关系和状态守恒可复算。
2. 3个输入漂移对象的身份复验结果独立保存；92个数据库元数据对象和2个敏感日志的未执行边界、
   授权缺口和解除条件完整保留。
3. BTC、ETH和未限定标的分组不互相补偿；未知、失败、未执行和拒绝不静默遗漏或填零。
4. 既有批次索引、历史产物、原始数据和服务器状态未修改；新增产物不可覆盖且包含资源事实。
5. ZS-DATA-GAP-003和ZS-DATA-GAP-005只有在对象级证据完整覆盖时才可关闭，否则报告明确保持阻塞。
6. 远端只读、敏感信息门、Python编译、数据/审计/研发中心测试、Markdown和`git diff --check`全部通过。

## 验证命令

```bash
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/数据/*.py scripts/审计/*.py tests/数据/test_*.py tests/审计/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

覆盖矩阵、输入绑定、分层复验报告和真实验证记录形成后，创建引用任务-000063的PR并更新为`待评审`。
只有双只读评审、主执行器验证和`main`可信复验通过后自动合并，再通过独立状态闭环PR将任务标记为
`已完成`。若授权边界或输入漂移阻止复验，按证据将任务标记为`阻塞`，不得提交假批次。

## 执行记录

- 执行分支：`codex/000063-incomplete-scan-audit-v1`
- 开始时间：`2026-08-05T15:04:10+08:00`
- 交付提交SHA：`6fe727c0e6e09b2ab872e145adb4c0b14e15013b`
- Pull Request：[#151](https://github.com/xk320/zhishi/pull/151)
- 复验范围：97个未完整扫描对象；普通文件输入漂移3个、数据库元数据范围92个、敏感系统日志2个。
- 复验结果：3个普通文件身份复验均拒绝；92个数据库对象和2个敏感日志保持未执行；正文读取0。
- 交付物：`artifacts/审计/数据质量增量复验/任务-000063范围绑定/`覆盖矩阵、输入绑定和验证清单；`docs/审计/Ubuntu未完整扫描对象分层复验报告.md`。
- 验收结果：97个对象按资产编号确定性绑定；未知、拒绝和未执行不补零；ZS-DATA-GAP-003与ZS-DATA-GAP-005仍阻塞；主研究尺度与短窗口边界未漂移。
- 验证命令与结果：数据测试55项、审计测试147项、研发中心测试183项通过；Python编译通过；MarkdownLint（152个文件）0 issues；`git diff --check`通过；敏感模式扫描无命中。
- 已知限制：没有对象获得完整正文质量通过；需要新的正文授权、资源预算和脱敏合同才能处理数据库或敏感日志对象。
- 数据与安全影响：仅使用逻辑别名`ubuntu`执行3个对象的只读`stat`，远端无写入、无临时文件；未读取数据库业务正文、敏感日志正文或原始记录；未修改服务器、数据库、生产系统、权限、防火墙、原始数据或账户。
- 需要人工决策：无。
- 回滚方式：回滚PR #151合并提交即可移除本次覆盖矩阵和报告，任务-000062历史批次与原始数据不受影响。
