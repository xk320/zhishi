# 任务-000029：冻结数据来源与资产身份合同

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：数据治理
- 阶段：阶段1 来源与资产身份
- 优先级：P0
- 执行方案：方案A“现有清单复核、身份合同冻结与不可变登记”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领
- 执行分支：`codex/000029-freeze-data-source-identity-v1`
- 开始时间：`2026-08-03T11:14:25+08:00`
- 完成实现时间：`2026-08-03T13:22:00+08:00`
- 实现提交SHA：`37129bbd461104395ab24479355397f1890da1ba`
- Pull Request：[#44](https://github.com/xk320/zhishi/pull/44)

## 依赖与阻塞条件

- 唯一前序依赖：任务-000038通过独立子智能体评审并合并，状态为`已完成`；
- 当前阻塞原因：无；任务-000038已完成。
- 解除条件：已满足。
- 不依赖任务-000026，也不得引用尚未进入`main`的Issue或PR扩大范围。

## 背景

既有资产清单只证明候选文件和数据库元数据被发现，不能证明提供者、交易场所、市场、
标的、合约、字段语义、授权边界和修订行为。没有确切来源身份，后续时间、质量、重放、
成本和研究快照均不能形成可信血缘。

## 任务目标

对BTC、ETH候选资产建立可验证、带版本、内容寻址且不可变的数据来源与资产身份登记，
明确未知项和解除条件，不从文件名、路径、字段相似度或市场常识猜测业务身份。

## 固定执行方案

1. 冻结现有资产清单、发现脚本、规则和任务版本指纹；
2. 通过`ubuntu`白名单只读复核候选资源元数据和可公开验证的来源合同；
3. 按来源、场所、市场、标的、合约、数据对象和Schema确切版本形成确定性成员顺序；
4. 为每个成员输出`已证明`、`拒绝`或`无法判定`及证据、限制和解除条件；
5. 生成不可覆盖登记批次、中文字段映射和自动化合同检查，不修改旧清单。

## 默认工程决策

优先复用现有只读发现器和Python标准库；身份冲突时保留全部候选并输出`无法判定`；
使用SHA-256和排序后的成员清单冻结版本；任何未证明字段保持未知，不用默认值补齐。

## 允许停止条件

只有需要新增账号、凭据、生产权限、付费来源，或必须修改服务器、数据库、原始数据、
权限和防火墙时才停止请求人工决策。来源缺失、身份未知或结果为`无法判定`不属于停止条件。

## 输入合同

- 最新`main`、任务-000028合并提交及任务-000038合并后的自动评审治理版本；
- `artifacts/审计/数据源清单.csv`及其内容指纹；
- `docs/审计/数据源清单.md`、既有审计报告和缺口清单，只作为历史证据；
- `scripts/审计/发现数据资产.py`及既有白名单规则；
- SSH逻辑别名`ubuntu`，只允许任务登记的元数据读取范围；
- 现行标的仅BTC、ETH，分别判定且不得互相补偿。

## 输出合同

- `docs/数据/数据来源与资产身份合同.md`；
- `config/数据/数据来源与资产身份.json`；
- `scripts/数据/冻结数据来源身份.py`及对应测试；
- `artifacts/数据/来源身份/<批次>/来源身份清单.csv`和`身份清单.json`；
- 批次、规则、输入、成员和输出SHA-256，候选总体及三态计数守恒；
- 本任务、看板、提交、PR和验证记录。

## 工作范围

实现来源身份合同、只读复核入口、确定性登记、中文字段映射、状态守恒、指纹和测试；
执行一次有界真实只读批次，并分别报告BTC与ETH的证据范围。

## 不在范围

- 不采集或补采市场数据，不读取未授权正文；
- 不修改、清洗、去重、迁移或删除原始数据；
- 不完成三类时间、质量统计、重放、成本或最小闭环；
- 不训练模型、回测、计算胜率收益或生成交易许可。

## 安全边界

远端只读且不落盘；数据库只查获批元数据；资源使用必须有总超时、逐成员超时、成员数、
输出和日志上限；不得记录主机地址、用户名、凭据、令牌、原始业务记录或敏感配置。

## 验收标准

1. 所有候选成员均有唯一身份记录或明确无法判定原因，无静默遗漏；
2. 来源、场所、市场、标的、合约、数据对象、Schema和授权边界逐项记录；
3. BTC与ETH独立判定，成员和三态计数守恒；
4. 相同冻结输入产生相同成员顺序与内容指纹，历史批次不可覆盖；
5. 真实只读批次、失败安全、资源上限、脱敏和无远端写入有证据；
6. 全部相关测试、Markdown、敏感信息扫描和差异检查通过。

## 验证命令

```bash
python3 -m unittest tests/数据/test_冻结数据来源身份.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
npx --yes markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
```

真实批次命令必须在执行分支按冻结配置记录，禁止在任务合同阶段预填虚构批次身份。

## 执行记录

### 交付物

- `docs/数据/数据来源与资产身份合同.md`；
- `config/数据/数据来源与资产身份.json`；
- `scripts/数据/冻结数据来源身份.py`及35项专项测试；
- 真实只读批次`source-identity-20260803T131620+0800-e7bc65038f21`中的
  `来源身份清单.csv`和`身份清单.json`；
- 本任务文件、研发中心看板及Pull Request #44中的执行证据。

### 验收与验证

- 真实只读执行：通过。通过SSH逻辑别名`ubuntu`只读复核白名单文件元数据及数据库
  `information_schema`元数据；未读取业务记录、未在远端落盘或写入数据库。
- 状态守恒：通过。315个候选资产形成630个BTC、ETH独立身份成员；已证明0、拒绝12、
  无法判定618。BTC与ETH各自为已证明0、拒绝6、无法判定309，合计均为315。
- 证据边界：通过。12条拒绝来自6个候选文件的冻结元数据漂移；其余成员没有足够的
  结构化身份声明，保持无法判定，未从路径、文件名、Schema或市场常识推断身份。
- 不可变性：通过。批次身份、冻结输入、规则、确定性成员顺序、CSV、JSON和整批载荷
  SHA-256已保存并回读复算；批次目录拒绝覆盖和符号链接。
- 自动化回归：来源身份专项35项、研发中心75项、审计119项全部通过。
- 格式与差异：Markdown 85个文件0问题，`git diff --check`通过。

### 实际验证命令

```bash
python3 -m unittest tests/数据/test_冻结数据来源身份.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
NODE_OPTIONS=--max-old-space-size=256 \
  npx --yes --offline markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
python3 scripts/数据/冻结数据来源身份.py \
  --contract config/数据/数据来源与资产身份.json \
  --ssh-target ubuntu \
  --batch-root artifacts/数据/来源身份 \
  --timeout 600
```

### 已知限制

- 当前没有任何身份成员达到`已证明`，不得据此解锁质量、重放、成本、研究或交易阶段；
- 6个候选文件发生元数据漂移，本任务只保留拒绝证据，不修改或修复原始数据；
- 本任务没有证明三类时间、数据质量、可重放性、交易成本或双标的数据闭环；
- 任务类型为数据治理且包含脚本、配置、测试与真实批次，不属于当前自动合并白名单，
  双子智能体评审通过后仍须由具备权限的人工合并Pull Request #44。

### 数据与安全影响

- 未修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；
- 仓库仅保存元数据、状态、理由和指纹，不含网络地址、用户名、凭据或市场数据正文；
- 未训练模型、回测或生成胜率、收益、方向、仓位、订单和交易许可。

### 需要人工决策

- 仅需在双子智能体最终评审和当前头提交验证通过后人工合并Pull Request #44；
- 不需要新增服务器、数据库、账号、凭据、付费数据或生产权限。

## 完成定义

合同、实现、测试和一次真实只读批次均形成可复现证据后创建引用任务-000029的PR，将任务
和看板更新为`待评审`。若真实环境不能取得必要证据，如实输出无法判定；只有外部访问条件
完全阻断执行时才标记`阻塞`。PR经架构评审并合并后才能标记`已完成`并解除任务-000030。
