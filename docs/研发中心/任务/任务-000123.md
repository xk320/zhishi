# 任务-000123：修复阶段1合同修订完成态测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000123-stage1-completion-fixture-v1`
- 开始时间：`2026-08-15T04:50:00+08:00`
- 实现提交SHA：`cbbe52d14473bc2aef5fb457f01e0181f6badcbb`
- Pull Request：[#355](https://github.com/xk320/zhishi/pull/355)
- 类型：测试
- 阶段：研发中心生命周期测试治理
- 优先级：P1
- 执行方案：方案A“合成完成态夹具隔离”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务完成前不得执行任务-000115的合并后状态闭环

## 依赖与阻塞条件

- 唯一前序依赖：任务-000122已完成并进入main。
- 当前问题：任务-000115完成状态闭环会从看板移除待评审行，阶段1正向测试仍依赖该真实行而失败。
- 目标：将该正向测试改为内存合成任务115待评审/任务106未修订基线，保持对真实状态变化的负向约束。
- 阻塞条件：若修复需要修改任务-000115、任务-000106合同或放宽可信规则，立即停止。
- 解除条件：合成夹具在任务115完成前后均稳定，专项测试和可信资格复验通过。

## 背景

任务-000122完成后，任务-000115状态闭环触发既有阶段1测试夹具生命周期回归。测试不应从当前真实看板推断历史待评审状态。

## 任务目标

1. 在测试内合成固定任务115待评审行和任务106未修订合同基线。
2. 保留任务115到任务106的目标、状态、合同指纹和看板漂移失败关闭断言。
3. 不修改可信资格器规则、任务合同、真实看板或研究数据。

## 固定执行方案

1. 仅修改研发中心资格测试夹具和任务123交付记录。
2. 从现有测试辅助函数构造确定性内存文本，避免读取任务115完成后的真实看板分区。
3. 运行资格、工作流、看板、范围测试及默认MarkdownLint。

## 默认工程决策

- 使用Python标准库和现有夹具，不新增依赖、数据文件或服务。
- 合成文本只存在测试进程内，不写回仓库、数据库或服务器。
- 任务115完成态和任务106阻塞事实仍由独立状态闭环与可信规则验证。

## 允许停止条件

- 需要修改任务115、任务106、任务122合同或真实看板语义。
- 需要放宽任务状态、路径、合同指纹、未来数据、交易或生产安全门。
- 测试无法确定性合成基线或负向断言不能失败关闭。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`阶段1覆盖受限合同测试。
- 任务-000115当前为待评审；任务-000106当前为阻塞且合同未修订；任务-000122已完成，其可信规则已进入main。

## 输出合同

- 与真实任务状态解耦的阶段1正向/负向测试夹具。
- 任务-000123执行记录、验证结果和PR元数据。

## 工作范围

- 研发中心自动合并资格测试夹具与必要的辅助函数。

## 不在范围

- 不修改可信资格器、工作流、任务合同、看板、数据、服务器、数据库、生产或交易路径。

## 安全边界

- 合成夹具不构造真实市场数据、胜率、收益、订单或交易许可。
- 不使用未来数据，不跨标的补偿，不改变真实交易关闭状态。

## 验收标准

1. 任务115完成后全套研发中心专项测试不再因缺少待评审看板行失败。
2. 目标合同、任务状态、分支、PR和额外路径漂移仍失败关闭。
3. 任务115、任务106、任务122和真实看板文件在本任务中不变。
4. 测试、Python编译、MarkdownLint和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

测试夹具进入main并通过双只读评审、可信资格复验和自动合并；任务-000123状态闭环为已完成。任务-000115随后独立完成状态闭环。

## 数据与安全影响

仅修改测试夹具和治理记录，不访问或写入服务器、数据库、市场数据、原始数据、生产服务、凭据或真实账户。

## 已知限制

合成夹具只验证治理生命周期，不提供成本、延迟、覆盖、胜率、收益或交易证据。

## 回滚方式

回滚本任务PR即可恢复旧夹具；不修改或删除任何任务合同、状态和历史证据。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000123-stage1-completion-fixture-v1`
- 开始时间：`2026-08-15T04:50:00+08:00`
- 实现提交SHA：`cbbe52d14473bc2aef5fb457f01e0181f6badcbb`
- Pull Request：[#355](https://github.com/xk320/zhishi/pull/355)
- 交付物：任务115完成态兼容的阶段1测试夹具、任务123执行记录、研发中心看板迁移。
- 验证结果：研发中心专项170项通过；Python编译通过；变更Markdown 2份为0 issues；`git diff --check`通过。
- 已知限制：不提供成本、延迟、覆盖、胜率、收益或交易证据；任务115仍需独立完成状态闭环。
- 数据与安全影响：仅修改测试夹具和治理记录，不访问服务器、数据库、市场数据、原始数据、生产服务、凭据或真实交易路径。
- 回滚方式：回滚PR #355，恢复旧测试夹具，不触碰任务115/106/122合同和历史证据。
