# 任务-000114：固定root兼容模式的阻塞基线测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 合并时间：2026-08-14 07:03:22 +0800
- 合并提交SHA：`b00c7a4c7eb28d51f96c41e735c93f00cbcfc98d`
- 开始时间：`2026-08-14T06:59:31+08:00`
- 执行分支：`codex/task-000114-root-fixture-exec-v1`
- 实现提交SHA：`502d67ff49cbfdf96ef3e1ae8e0fb0dbfe404e9b`
- Pull Request：[#320](https://github.com/xk320/zhishi/pull/320)
- 类型：治理
- 阶段：研发中心自动合并可信事实治理
- 优先级：P0
- 执行方案：方案A“显式合成阻塞基线夹具”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；按任务113评审反馈自动修复，不再询问方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；唯一前序依赖为任务-000113；不得修改任务-000084/000086合同、取消状态或历史批次

## 依赖与阻塞条件

- 唯一前序依赖：任务-000113已完成；取消PR #315待本任务测试修复后复验。
- 当前阻塞原因：root兼容正向测试从当前已取消任务-000084读取状态，导致资格专项回归。
- 解除条件：测试夹具显式构造阻塞基线，专项测试通过且取消PR #315资格复验通过。

## 背景

任务-000113正确关闭了已取消目标的旧root映射，但其正向测试仍依赖历史状态，形成测试夹具漂移。本任务只修复测试输入来源。

## 任务目标

1. 固定不依赖当前任务文件状态的合成阻塞基线。
2. 保留已取消目标失败关闭负向测试。
3. 不改变生产、治理规则或任何任务事实。

## 固定执行方案

1. 在测试夹具中对读取的任务合同状态字段做确定性合成替换。
2. 运行root兼容正向与取消负向测试。
3. 如计数或合同结构不一致则失败关闭。

## 默认工程决策

- 复用现有Python unittest和标准库；不新增依赖。
- 合成夹具只存在于测试内，不写入任务文件、看板或批次。

## 允许停止条件

- 需要修改可信校验规则、任务合同、状态或权限。
- 无法保持合成夹具与真实合同结构一致。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`及任务113已合并规则。

## 输出合同

- 固定阻塞基线的测试夹具、专项验证和任务/看板记录。

## 工作范围

- 仅研发中心root兼容模式测试输入夹具。

## 不在范围

- 不修改任务084/086、任务113规则、服务器、数据库、原始数据、凭据、生产或交易。

## 安全边界

- 测试合成数据不进入生产或审计批次；取消目标仍失败关闭。

## 验收标准

1. root兼容正向测试不读取当前任务084状态，显式阻塞基线通过。
2. 已取消目标负向测试通过。
3. 资格/范围/看板测试、编译、MarkdownLint和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

测试夹具修复进入main，任务-000084取消PR #315重新通过可信资格和双评审。

## 数据与安全影响

仅修改测试和研发中心治理记录；无服务器、数据库、原始数据、凭据、生产或交易影响。

## 已知限制

合成阻塞夹具只验证历史允许路径，不代表任务084恢复阻塞或重新开放root映射。

## 回滚方式

回滚本任务PR即可恢复旧测试夹具，不改动任务113规则和任务084取消事实。

## 执行记录

- 交付状态：待评审，已完成实现，等待Pull Request合并。
- 执行分支：`codex/task-000114-root-fixture-exec-v1`
- 开始时间：`2026-08-14T06:59:31+08:00`
- 交付物：显式合成阻塞状态测试夹具，保留已取消目标失败关闭负向测试。
- 验证结果：资格专项116项通过；Python编译通过；`git diff --check`通过。
- 已知限制：仅修复测试输入来源，不改变任务084取消事实或任务113规则。
- 数据与安全影响：仅测试文件；未访问服务器、数据库、原始数据、凭据、生产或交易系统。
- 回滚方式：回滚本PR即可恢复旧测试夹具。
