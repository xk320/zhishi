# 任务-000075：来源身份输入指纹漂移修复与复采

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000075-source-identity-drift-recapture-v1`
- 开始时间：`2026-08-07T17:24:47+08:00`
- 实现提交SHA：`5e957fd74f6722ecfef6b0bd1c12d8bc03db7dfd`
- Pull Request：[#195](https://github.com/xk320/zhishi/pull/195)
- 类型：数据治理
- 阶段：阶段1来源身份证据闭环
- 优先级：P0
- 执行方案：方案A“当前main输入重签、逐成员漂移裁决与只读来源身份复采”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅使用当前main仓库证据和Ubuntu白名单只读元数据，不请求方案选择
- 并行规则：禁止并行；唯一前序依赖任务-000074；不得与其他阶段1来源身份、时间、质量、重放、成本或最终审计任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000074已完成，最终审计确认`ZS-DATA-GAP-001`仍未关闭且8个叶子全部阻塞。
- 输入依赖：任务-000029历史来源身份合同与批次、任务-000071结构证据、任务-000072复采批次、任务-000074最终审计批次，以及执行时最新`main`的资产清单、规则和缺口台账。
- 当前阻塞原因：无；本任务可以在输入存在漂移、证据不足或远端只读探针失败时发布失败安全批次，不得用猜测补齐身份。
- 解除条件：对每个明确纳入范围的BTC和ETH候选成员，来源提供者、交易场所、市场类型、标的身份、精确合约、数据对象、Schema确切版本、授权边界和字段中文映射均绑定当前输入指纹及结构化证据；未满足者继续为`拒绝`或`无法判定`。

## 背景

任务-000072的来源身份复采没有形成已证明成员，且部分成员因冻结文件元数据漂移被拒绝。任务-000074重新审计后确认来源身份门仍阻塞。当前必须先把“输入发生漂移”和“来源身份本身无法证明”分开，基于最新`main`重新冻结一条可复算、追加式、失败安全的来源身份证据链；不得覆盖任务-000029或任务-000072历史批次。

## 任务目标

1. 冻结当前`main`的资产清单、来源身份合同、任务-000071结构证据、任务-000074最终审计和缺口台账，并记录每个输入的内容指纹、版本和确定性成员顺序。
2. 对任务-000072历史输入逐项执行漂移分类：字节未变、可证明的格式化差异、真实内容变化、缺失、越界或无法判定；任何分类都不得静默改写历史批次。
3. 在同一输入版本上复用固定的只读来源身份入口，分别对BTC、ETH候选成员复采结构化身份与授权边界证据；不读取业务记录正文。
4. 生成新的不可变批次、逐成员证据定位、状态计数、输入/规则/执行器指纹和精确缺口变化；只有证据完整的成员才允许标记`已证明`。
5. 让后续时间、质量、重放、成本和最终审计能够绑定本任务的确切来源身份版本；不因本任务完成而放行阶段2。

## 固定执行方案

1. 从最新`main`读取任务-000029、任务-000071、任务-000072、任务-000074的合同和不可变批次，建立只读基线清单。
2. 为本任务建立版本化输入配置和执行器指纹；旧配置、旧脚本和历史批次只读保留，不通过刷新SHA掩盖漂移。
3. 采用确定性成员排序和逐成员状态机，先完成本地指纹与范围校验，再通过逻辑别名`ubuntu`的固定只读入口获取允许的`information_schema`元数据。
4. 远端批次串行、有界、无写入；输出只保存脱敏指纹、计数、状态、原因码、证据定位和撤销事实，不落盘业务正文。
5. 以追加式方式发布新批次和审计派生记录；对未覆盖标的、场所、合约、对象、Schema或时间范围不作部分放行。

## 默认工程决策

- 复用仓库现有标准库、固定探针、白名单和状态语义；不修改任务-000029、任务-000072历史合同和批次。
- 证据优先于描述；内容差异不能被解释为来源身份证明，证据冲突固定为`无法判定`，安全越界固定为失败关闭。
- BTC与ETH完全独立统计，禁止跨标的补偿；主研究尺度固定为4小时、8小时、24小时、48小时，15分钟和1小时仅为事后结果观察窗口。
- 不把文件路径、表名、字段名、文件修改时间、Ubuntu可达性或共同目录解释为来源、标的、合约或时间证明。
- 默认单进程、最多两个只读评审者、Node堆上限256MiB、不创建额外工作树；任何资源超限均停止相关批次并保留失败事实。

## 允许停止条件

- 需要新增账号、凭据、非白名单主机、数据库业务正文读取、生产权限、数据库写入、原始数据迁移或不可逆操作。
- 输入指纹无法复算、历史批次被覆盖、成员顺序不确定、远端探针不是固定版本或出现任何越权输出。
- 证据不足、结果为空、成员拒绝、无法判定、探针不可达或数据差异不是请求用户选择的理由；必须完成失败安全记录。
- 发现任务合同与上游硬门直接冲突且保守失败关闭仍不能解决时，停止冲突部分并记录证据。

## 输入合同

- `config/数据/数据来源与资产身份.json`、`config/数据/来源身份复采.json`及其引用文件；执行时重新计算，不信任聊天或PR描述中的旧SHA。
- `artifacts/审计/数据源清单.csv`、任务-000029和任务-000071结构证据、任务-000072身份批次、任务-000074最终审计批次；全部只读。
- `AGENTS.md`、任务中心规范、来源身份合同、最终审计报告和缺口台账；用于范围、状态和安全复算。
- 远端只允许逻辑别名`ubuntu`，且仅允许任务合同列明的`information_schema.TABLES`和`information_schema.COLUMNS`固定元数据范围；不得读取业务表正文。

## 输出合同

- `docs/数据/来源身份输入指纹修复与复采合同.md`：本任务的版本、字段、状态机、漂移分类和安全边界。
- `config/数据/来源身份输入指纹复采.json`：当前main输入、规则、成员、远端和资源指纹的冻结配置。
- `scripts/数据/复采来源身份输入指纹.py`及其专项测试：只读漂移分类、成员构建、复采和失败安全发布入口；不得改写历史入口语义。
- `artifacts/数据/来源身份输入指纹复采/<批次>/`：不可变脱敏批次、验证清单、逐成员结果和追加索引。
- `docs/审计/数据缺口与补采清单.md`、阶段1审计派生报告和任务文件/看板：只追加本任务证据和精确解除范围。

## 工作范围

- 当前main输入与历史输入的字节、规范化、内容和规则指纹复算。
- BTC、ETH候选成员的来源身份字段、结构证据、Schema版本、授权边界和状态守恒验证。
- 运行固定白名单只读入口并保存可复算、脱敏、追加式批次；为后续时间与质量任务提供确切来源身份版本。
- 运行专项、研发中心、审计、数据、编译、MarkdownLint、敏感信息和差异验证。

## 不在范围

- 不读取、复制、删除、迁移或修改市场业务记录、原始数据、数据库内容、订单簿逻辑、生产服务、权限或防火墙。
- 不执行三类时间、质量、历史重放、成本、容量、恢复、模型、回测、模拟交易或交易许可判断。
- 不生成方向、仓位、订单、胜率、收益、因果、预测优势或研究准入结论。
- 不把本任务的来源身份结果外推到未覆盖标的、市场、合约、对象、时间范围或阶段2。

## 安全边界

- 所有仓库输入、历史批次和远端元数据只读；远端写入、业务正文读取、凭据/环境变量读取和原始数据落盘固定为`false`。
- SSH仅使用无交互、固定版本、密码关闭、无PTY/转发的白名单逻辑别名；输出不得包含地址、用户名、密钥、令牌、密码或原始业务响应。
- 每批次追加式、原子、不可覆盖；输入漂移、未知、冲突、未来数据、超时、输出超限或敏感信息均失败关闭。
- 资源上限必须绑定批次、规则、授权和执行器指纹；不得为了得到已证明成员降低研究或安全门槛。

## 验收标准

1. 当前main和历史输入提交、规则、成员、文件内容及规范化指纹均可复算；任务-000029、任务-000072历史批次字节不变。
2. BTC、ETH分别覆盖确定性候选成员，逐成员保留漂移分类、身份状态、证据定位、输入指纹、Schema版本、授权边界和原因；所有状态计数守恒。
3. 只有当前版本全部身份字段和结构证据完整的成员才可标记`已证明`；缺失、冲突或漂移不得默认通过。
4. 远端查询仅为合同允许的只读元数据；无业务正文、写入、权限变更、原始数据修改或凭据泄露证据。
5. 新批次和缺口派生记录追加式、可回放、可撤销；`ZS-DATA-GAP-001`仅按精确范围部分解除或继续阻塞，其他P0缺口和阶段2结论不被改写。
6. 专项测试、研发中心全量测试、审计/数据测试、Python编译、MarkdownLint、敏感信息扫描和`git diff --check`通过。
7. PR经双只读评审、main可信复验、精确头SHA自动合并；合并后独立状态闭环将任务标记为`已完成`。

## 验证命令

```bash
python3 -m unittest tests/数据/test_来源身份输入指纹复采.py -q
python3 -m unittest tests/数据/test_冻结数据来源身份.py tests/数据/test_复采来源身份.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m py_compile scripts/数据/复采来源身份输入指纹.py tests/数据/test_来源身份输入指纹复采.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须执行一次真实、串行、专用白名单只读复采，并记录批次身份、输入/规则/授权/资源/执行器指纹、撤销事实和真实结果。

## 完成定义

当前main输入重签、漂移分类、来源身份只读复采入口、不可变批次、精确缺口更新和全部验证记录已提交；PR经双只读评审、main可信复验并自动合并，随后独立状态闭环将任务-000075标记为`已完成`。任务完成不等于阶段1通过；只有后续最终审计证明8个叶子全部硬门满足时，阶段2才可解除。

## 执行记录

- 执行分支：`codex/task-000075-source-identity-drift-recapture-v1`；开始时间：`2026-08-07T17:24:47+08:00`。
- 真实批次：`source-identity-input-drift-20260807T173144+0800-592622eddd8e`；执行器为任务专用`复采来源身份输入指纹.py`，固定探针版本`source-identity-input-drift-probe-1.0`。
- 输入漂移：6个输入中3个`未漂移`（资产清单、来源身份合同说明、任务-000071结构合同），3个`内容变化`（历史来源身份配置、阶段1最终审计报告、缺口清单）；未使用默认值或文件时间补齐身份。
- 复采结果：候选资产315、身份成员630；已证明0、拒绝12、无法判定618；BTC和ETH各已证明0、拒绝6、无法判定309；状态计数守恒。
- 远端与安全事实：仅通过白名单逻辑别名`ubuntu`执行固定只读`information_schema`元数据探针；远端写入、数据库业务记录读取、环境变量/凭据读取均为`false`，未保存地址、用户名、凭据或原始业务记录。
- 当前状态：实现与真实批次已完成，已创建PR并进入双只读评审；`ZS-DATA-GAP-001`未关闭，阶段1和阶段2均未放行。
