# 任务-000076：来源身份字段级证据与授权边界复采

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 执行分支：`codex/task-000076-source-identity-field-evidence-v1`
- 开始时间：`2026-08-07T18:05:55+08:00`
- 类型：数据审计
- 阶段：阶段1来源身份证据闭环
- 优先级：P0
- 执行方案：方案A“字段映射冻结、只读授权快照与逐成员失败安全复采”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；按本合同使用仓库证据和Ubuntu白名单只读入口，不请求方案选择
- 并行规则：禁止并行；唯一前序依赖任务-000075；不得与其他阶段1来源身份、时间、质量、重放、成本或最终审计任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000075已完成，但其批次仍显示0个身份成员已证明、12个拒绝、618个无法判定，`ZS-DATA-GAP-001`继续阻塞。
- 输入依赖：任务-000029来源身份合同与资产清单、任务-000071结构证据、任务-000074最终审计批次、任务-000075来源身份输入指纹复采批次、现行缺口台账和最新`main`。
- 当前阻塞原因：无；若字段映射、授权快照或远端只读证据缺失，必须发布失败安全批次，不得猜测或把未知标为通过。
- 解除条件：BTC、ETH分别对每个纳入成员提供来源提供者、交易场所、市场类型、标的、精确合约、数据对象、Schema确切版本、授权边界和字段中文映射的当前版本证据；缺失成员继续为`拒绝`或`无法判定`。

## 背景

任务-000075只完成当前输入指纹漂移分类和`information_schema`结构复采，未形成任何已证明身份成员。下一步必须把“字段存在”与“字段语义及授权边界可证明”分开，使用同一不可变输入链建立字段级证据；不得覆盖任务-000029、任务-000072或任务-000075的历史合同和批次。

## 任务目标

1. 冻结任务-000075当前批次中BTC、ETH各自的候选成员、字段映射、来源身份版本、授权范围和确定性顺序。
2. 通过固定只读入口复核候选对象的字段级身份元数据、Schema版本和授权快照；不读取价格、成交、订单簿、持仓量、资金费率或其他业务正文。
3. 为每个成员保留输入指纹、授权指纹、证据定位、状态、拒绝/无法判定原因和撤销事实；状态计数必须守恒且不得跨标的补偿。
4. 仅在全部身份字段、结构证据和授权边界同时满足时标记`已证明`，并让后续时间、质量、重放和最终审计绑定该确切版本。

## 固定执行方案

1. 从最新`main`只读加载任务-000029、任务-000071、任务-000074、任务-000075的合同、配置、成员和不可变批次，重新计算所有输入指纹。
2. 冻结字段映射配置：来源提供者、交易场所、市场类型、标的、精确合约、数据对象、Schema版本、授权边界和中文字段名必须逐项登记；禁止运行时新增或删除字段。
3. 远端仅使用逻辑别名`ubuntu`的无交互固定探针，范围限于`information_schema.TABLES`、`information_schema.COLUMNS`、`information_schema.TABLE_PRIVILEGES`和合同明确的身份元数据列；每成员最多一条固定排序、固定列的摘要证据，禁止全表扫描和业务记录导出。
4. 远端执行串行、有界、只读；输出只保存脱敏值指纹、字段存在性、授权快照指纹、计数、状态、原因码和证据定位，原始字段值不落盘。
5. 追加式发布新批次和缺口派生记录；任何输入漂移、授权冲突、越界、超时或证据不完整均失败关闭，不修改历史批次。

## 默认工程决策

- 复用现有标准库、白名单、确定性排序和状态语义；不修改任务-000029、任务-000072或任务-000075的历史产物。
- 证据优先于描述；文件名、表名、路径、Ubuntu可达性或共同目录不能单独证明来源、标的、合约或时间。
- BTC与ETH完全独立统计，禁止跨标的补偿；主研究尺度固定为4小时、8小时、24小时、48小时，15分钟和1小时只能作为事后结果观察窗口。
- 远端每批次最多630成员、单成员最多1条摘要、响应和本地写入均有上限；单进程、最多两个只读评审者、Node堆上限256MiB、不创建额外工作树。
- 普通工程取舍按安全、证据、简单、可复现、可回滚顺序自主决定，不向用户转交A/B/C选择。

## 允许停止条件

- 需要新增账号、凭据、非白名单主机、数据库写入、生产权限、原始数据迁移或不可逆操作。
- 远端探针读取了非合同列、业务正文、价格/成交/订单簿等市场数据，或输出包含地址、用户名、令牌、密码、密钥。
- 输入指纹、字段顺序、授权快照或撤销事实无法复算；历史批次被覆盖；出现跨标的补偿或未来数据。
- 证据不足、成员拒绝、无法判定、探针不可达或结果为空；必须记录失败安全事实，不得降低研究门槛。

## 输入合同

- `config/数据/来源身份输入指纹复采.json`及任务-000075不可变批次；执行时重新计算，不信任聊天、Issue或PR摘要。
- `config/数据/数据来源与资产身份.json`、`artifacts/审计/数据源清单.csv`、任务-000029/000071/000074合同与批次、缺口台账和阶段1最终审计报告。
- `AGENTS.md`、任务规范、来源身份合同、当前`main`提交指纹和白名单逻辑别名`ubuntu`。
- 远端只读范围以本任务配置的字段白名单为准；不读取业务正文、账户余额、真实交易或原始市场记录。

## 输出合同

- `docs/数据/来源身份字段级证据与授权边界复采合同.md`：字段、版本、授权、摘要证据、状态机和安全边界。
- `config/数据/来源身份字段级证据复采.json`：输入、字段白名单、成员顺序、探针版本、资源上限和撤销规则。
- `scripts/数据/复采来源身份字段级证据.py`及`tests/数据/test_来源身份字段级证据复采.py`：只读复采、脱敏、状态守恒和失败安全入口。
- `artifacts/数据/来源身份字段级证据复采/<批次>/`：不可变批次清单、脱敏成员结果、计数、输入/规则/授权/执行器指纹和追加索引。
- `docs/审计/数据缺口与补采清单.md`、`docs/审计/阶段1最终审计报告.md`及任务看板：只追加本任务真实证据和精确解除范围。

## 工作范围

- 任务-000075当前成员的字段映射、Schema和授权边界指纹复算。
- BTC、ETH独立的来源身份字段级只读摘要复采、逐成员状态裁决和计数守恒。
- 批次追加、证据定位、撤销记录、专项测试、数据/审计/研发中心验证、编译、MarkdownLint、敏感扫描和`git diff --check`。

## 不在范围

- 不读取、复制、保存或分析价格、成交、订单簿、持仓量、资金费率、账户余额或其他业务正文；不实现运行时、模型、回测、模拟交易或真实交易。
- 不修改服务器、数据库、生产服务、权限、防火墙、原始数据、任务历史批次或账户凭据。
- 不生成方向、仓位、订单、胜率、收益、因果、预测优势、研究准入或交易许可结论。
- 不把本任务结果外推到未覆盖标的、市场、合约、对象、时间范围或阶段2。

## 安全边界

- 所有仓库输入和历史批次只读；远端仅白名单无交互只读探针，远端写入、业务正文读取、凭据读取和原始数据落盘固定为`false`。
- 只保存脱敏摘要与SHA-256指纹；敏感输出、地址、用户名、密钥、令牌、密码和原始响应一律拒绝发布。
- 每批次追加式、原子、不可覆盖；输入漂移、未知、冲突、未来数据、超时、输出超限或越权均失败关闭。
- 资源上限和授权范围绑定批次、规则与执行器指纹；不因获得较少已证明成员而放宽任何门槛。

## 验收标准

1. 当前输入、字段映射、成员顺序、规则、授权和执行器指纹可复算；历史批次字节不变。
2. BTC、ETH分别覆盖纳入成员；每成员保留字段状态、Schema/授权指纹、证据定位、原因和撤销事实；计数守恒。
3. 只有身份字段、结构证据和授权边界完整一致的成员才可`已证明`；缺失、冲突或漂移不得默认通过。
4. 远端查询严格限于合同字段摘要；无业务正文、写入、权限变更、原始数据修改或凭据泄露证据。
5. 新批次可回放、追加、撤销；`ZS-DATA-GAP-001`仅按精确标的和成员范围部分解除或继续阻塞，其他缺口和阶段门不被改写。
6. 专项、数据、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
7. 交付PR经双只读评审、main可信复验、精确头SHA自动合并；合并后独立状态闭环将任务标记为`已完成`。

## 验证命令

```bash
python3 -m unittest tests/数据/test_来源身份字段级证据复采.py -q
python3 -m unittest tests/数据/test_来源身份输入指纹复采.py tests/数据/test_冻结数据来源身份.py tests/数据/test_复采来源身份.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m py_compile scripts/数据/复采来源身份字段级证据.py tests/数据/test_来源身份字段级证据复采.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须执行一次真实、串行、专用白名单只读复采，保存批次身份、输入/规则/授权/资源/执行器指纹、撤销事实和真实结果；未证明成员必须原样保留失败安全状态。

## 完成定义

字段级来源身份合同、只读复采入口、不可变批次、精确缺口更新和全部验证记录已提交；PR经双只读评审、main可信复验并自动合并，随后独立状态闭环将任务-000076标记为`已完成`。任务完成不等于`ZS-DATA-GAP-001`关闭；只有后续最终审计证明八个叶子全部满足硬门时，阶段2才可解除。

## 执行记录

- 执行分支：`codex/task-000076-source-identity-field-evidence-v1`；开始时间：`2026-08-07T18:05:55+08:00`。
- 真实批次：`source-identity-field-evidence-20260807T182805+0800-2fca470a6756`；执行器为`复采来源身份字段级证据.py`，探针版本`source-identity-field-evidence-probe-1.0`。
- 复采结果：候选资产315、身份成员630；已证明0、拒绝12、无法判定618；BTC和ETH各已证明0、拒绝6、无法判定309；字段级证据和授权快照均未取得，计数守恒。
- 远端与安全事实：仅通过白名单逻辑别名`ubuntu`执行固定只读stat/元数据探针；远端写入、数据库业务记录读取、价格成交订单簿读取、凭据读取和原始字段落盘均为`false`，未保存地址、用户名、凭据或业务正文。
- 当前状态：复采和文档派生已完成，尚未创建交付PR；`ZS-DATA-GAP-001`未关闭，阶段1和阶段2均未放行。
