# 任务-000117：修复任务116完成后的阶段1测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 合并时间：2026-08-14 17:55:16 +0000
- 合并提交SHA：`2eaccaaddeff87d9cdb2e568714abae6b25d2b4e`
- 开始时间：`2026-08-15T01:41:35+08:00`
- 执行分支：`codex/task-000117-stage1-fixture-repair-v1`
- Pull Request：[#328](https://github.com/xk320/zhishi/pull/328)
- 实现提交SHA：`3e5e63d9cc26dd248efce9324c4d53c67451e853`
- 类型：测试
- 阶段：研发中心可信合同治理
- 优先级：P1
- 执行方案：方案A“合成阶段1入口基线并保持状态独立”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；仅在任务-000116代码交付已进入main后执行，不修改任务-000116合同或状态

## 依赖与阻塞条件

- 唯一前序依赖：任务-000116代码交付已进入main；其状态闭环由PR #326独立处理。
- 当前阻塞原因：阶段1正向测试夹具仍假设任务-000116位于待评审看板分区，任务完成后读取不到该行。
- 解除条件：测试夹具不再依赖任务-000116当前状态或看板分区，且资格专项回归全绿。

## 背景

任务-000116合并后进入状态闭环，阶段1覆盖受限合同修订测试仍从当前看板读取任务-000116待评审行，产生确定性StopIteration。需要将测试输入改为独立、可复算的合成基线，避免测试生命周期依赖真实任务状态。

## 任务目标

1. 修复阶段1覆盖受限合同修订正向测试夹具，使其与任务-000116已完成状态兼容。
2. 保留任务-000116未完成、错状态、错路径和目标合同漂移的失败关闭断言。
3. 不改变可信资格规则、任务合同、看板事实、生产或数据边界。

## 固定执行方案

1. 在测试内确定性构造任务-000116待评审的最小基线行，或改用不依赖该行的固定看板夹具。
2. 只修改研发中心测试文件，保持正向输入与阶段1固定映射一致。
3. 运行资格、工作流、看板和范围专项测试；任一回归失败则停止。

## 默认工程决策

- 复用现有Python unittest和标准库，不新增依赖。
- 合成夹具只存在于测试内，不写入任务文件、看板、审计批次或生产系统。
- 以最小变更修复状态生命周期耦合，不放宽任何可信资格门。

## 允许停止条件

- 需要修改可信资格规则、任务合同、看板事实、服务器、数据库、权限、凭据或原始数据。
- 无法在不读取真实任务状态的情况下构造确定性测试基线。
- 修复会放宽未来数据、跨标的补偿、研究准入、交易许可或自动合并评审门。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`中的阶段1覆盖受限合同修订测试。
- 任务-000116已合并的阶段1资格规则、固定章节和路径白名单。
- 任务-000116状态闭环PR #326的失败证据。

## 输出合同

- 与任务-000116完成状态兼容的独立测试夹具。
- 资格专项回归结果、编译结果、MarkdownLint和`git diff --check`证据。
- 任务文件、看板和PR交付记录。

## 工作范围

- 仅修改研发中心测试文件及本任务执行记录和派生看板。

## 不在范围

- 不修改可信资格器、自动合并工作流、任务-000116/000115/000106合同、历史批次、服务器、数据库、原始数据、凭据、生产或交易逻辑。
- 不执行阶段1研究、不采集市场数据、不训练模型、不回测、不生成信号或订单。

## 安全边界

- 合成测试输入不得进入生产、审计批次或研究结论。
- 保留真实任务-000116未完成和错误状态的失败关闭测试，不把夹具结果解释为任务完成。
- 不使用未来数据，不跨BTC/ETH补偿，不改变真实交易关闭状态。

## 验收标准

1. 阶段1覆盖受限合同修订正向测试在任务-000116已完成状态下通过。
2. 任务-000116未完成、工作流越界、合同漂移、看板漂移和目标状态迁移负向测试继续失败关闭。
3. 资格、自动合并工作流、看板和项目范围专项测试全绿。
4. Python编译、MarkdownLint和`git diff --check`通过；无生产、数据或凭据变更。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

测试夹具修复进入main，专项测试全绿，任务-000117状态闭环为已完成；任务-000116/000115/000106合同和状态事实不被改写。

## 数据与安全影响

仅修改测试夹具和研发中心治理记录；不访问服务器、数据库、市场数据、原始数据、生产服务、凭据或真实交易。

## 已知限制

本任务只修复测试生命周期耦合，不代表阶段1覆盖证据、研究准入或交易许可通过。

## 回滚方式

回滚本任务PR即可恢复旧夹具；不触碰任务-000116完成事实、任务-000115/000106合同或历史批次。

## 执行记录

- 执行分支：`codex/task-000117-stage1-fixture-repair-v1`
- 开始时间：`2026-08-15T01:41:35+08:00`
- 实现提交SHA：`3e5e63d9cc26dd248efce9324c4d53c67451e853`
- Pull Request：[#328](https://github.com/xk320/zhishi/pull/328)
- 交付物：与任务-000116完成状态兼容的阶段1合成测试看板基线。
- 验证结果：资格、工作流、看板和项目范围专项167项通过；Python编译通过；变更Markdown 2份为0 issues；`git diff --check`通过。
- 已知限制：仅修复测试生命周期耦合，不改变可信规则、任务合同、看板事实或研究/交易边界。
- 数据与安全影响：仅修改测试文件；未访问服务器、数据库、市场数据、生产服务、凭据或真实交易。
- 回滚方式：回滚本PR即可恢复旧测试夹具。
