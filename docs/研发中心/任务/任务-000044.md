# 任务-000044：修复外部状态一致性测试的状态闭环兼容性

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 类型：测试
- 阶段：阶段1 研发中心状态一致性治理
- 优先级：P1
- 执行方案：方案A“状态迁移双态断言与最小回归修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只修改本任务授权的测试和记录
- 执行分支：`codex/000044-test-closure-compat-v1`
- 开始时间：`2026-08-04T13:13:13+0800`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000043已进入`main`但尚未完成独立状态闭环；本任务用于修复其回归测试兼容性；
- 当前阻塞原因：无；
- 解除条件：已满足。

## 背景

任务-000043的专项测试原先只断言`待评审`和对应看板行。任务完成后通过独立状态闭环迁移
到`已完成`，同一测试会因仍断言旧状态而失败。这是测试合同与合法状态迁移不一致，不应通过
保留错误状态、跳过测试或放宽状态闭环规则解决。

## 任务目标

让外部状态一致性专项测试同时覆盖任务-000043交付态和合法完成态，并继续拒绝未知状态、
错分区、任务文件与看板漂移以及历史记录丢失。

## 固定执行方案

1. 在现有专项测试中使用任务状态驱动的最小分支；
2. 对`待评审`断言分支保留分支、PR和待评审看板行；
3. 对`已完成`断言分支校验真实PR #62和合并提交`abe66037161332d350b9782492beedd8898a4f8a`；
4. 不复制状态合同，不修改可信校验器，不改变任务状态迁移规则；
5. 运行专项、研发中心、Markdown和差异验证，失败即停止并如实记录。

## 默认工程决策

- 复用现有测试读取任务文件和看板的方式，不新增依赖；
- 只接受明确的`待评审`或`已完成`合法状态，任何其他状态失败；
- 以任务文件为事实源、看板为派生视图，不用PR评论或历史摘要替代；
- 测试只读本地文件和纯函数，不访问服务器、数据库或真实市场数据。

## 允许停止条件

只有需要修改生产、服务器、数据库、原始数据、权限、凭据或可信控制面，或发现任务合同
直接冲突且无法保守修复时停止。普通测试结构、断言组织和命名由Codex自主决定。

## 输入合同

- `AGENTS.md`、任务中心规范和任务-000043当前交付文件；
- `tests/研发中心/test_外部状态一致性.py`及其依赖脚本；
- 任务-000043 PR #62的真实合并提交和看板状态映射；
- 不读取服务器、数据库、原始市场记录或敏感日志。

## 输出合同

- 更新后的`tests/研发中心/test_外部状态一致性.py`；
- 本任务执行记录、分支、提交、PR和验证结果；
- 看板与任务状态同步，不改变任务-000043合同或历史证据。

## 工作范围

- 修复任务-000043专项测试的合法状态迁移断言；
- 保留现有外部状态词汇、新鲜度、恢复路径和阻塞事实测试；
- 增加必要的负向断言，防止未知状态静默通过。

## 不在范围

- 不修改生产代码、服务器、数据库、原始数据、权限、账户或凭据；
- 不修改任务-000043状态、不执行其状态闭环、不重写历史批次；
- 不改变任务中心可信校验器、看板模式、研究范围或阶段门槛；
- 不访问真实市场数据，不训练模型，不运行回测或交易模拟。

## 安全边界

只允许研发中心测试文件和本任务执行记录变更；不得新增网络目标、凭据、原始日志或市场
数据。任何测试失败必须保留失败事实，不得删除失败用例或跳过状态分支。

## 验收标准

1. 任务-000043为`待评审`时，专项测试验证分支、PR和待评审看板行；
2. 任务-000043为`已完成`时，专项测试验证真实合并PR、合并SHA和已完成看板行；
3. 未知状态、错分区、缺失行或历史记录缺失均有失败断言；
4. 原有四态词汇、证据新鲜度、唯一恢复路径和任务-000031阻塞测试继续通过；
5. 研发中心测试、MarkdownLint、`git diff --check`和脚本编译全部通过；
6. 不引入服务器、数据、生产或凭据影响。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_外部状态一致性.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m py_compile scripts/研发中心/验证外部状态一致性.py tests/研发中心/test_外部状态一致性.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check
```

## 完成定义

测试修复通过双子智能体评审、主执行器验证和`main`可信复验后合并，再通过独立状态闭环
PR将本任务标记`已完成`。任务-000043只能在本任务合并后重新执行合法的`待评审→已完成`
闭环，不能以测试失败为由降低状态规则。
