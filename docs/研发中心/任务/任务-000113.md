# 任务-000113：关闭已取消任务的旧root兼容映射

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 开始时间：`2026-08-14T06:41:59+08:00`
- 执行分支：`codex/task-000113-close-cancelled-root-mapping-exec-v1`
- 实现提交SHA：`63b6815b5414a072c2dcfb7c0fff762db848b183`
- Pull Request：[#317](https://github.com/xk320/zhishi/pull/317)
- 类型：治理
- 阶段：研发中心自动合并可信事实治理
- 优先级：P0
- 执行方案：方案A“取消状态拒绝与合成阻塞夹具修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；按评审反馈自动修复，不再询问方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；唯一前序依赖为任务-000111；不得修改任务-000084历史合同、批次或取消状态

## 依赖与阻塞条件

- 唯一前序依赖：任务-000111已完成；任务-000084取消闭环在本任务修复后重新验证。
- 当前阻塞原因：任务-000086旧root合同修复映射仍要求任务-000084为阻塞，取消后研发中心资格测试回归。
- 解除条件：已取消目标明确失败关闭，合成阻塞夹具覆盖历史允许路径，专项验证通过。

## 背景

任务-000109替代任务-000084旧来源身份路线，任务-000084正在独立取消闭环。可信资格器的历史root兼容映射不能继续把已取消目标视为阻塞，否则会产生状态语义漂移。

## 任务目标

1. 拒绝任务-000084已取消时的任务-000086 root合同修复映射。
2. 用显式合成阻塞目标保留历史正向测试，不读取当前已取消任务作为夹具。
3. 不放宽任何状态、评审、权限或安全门。

## 固定执行方案

1. 在可信资格校验器增加取消目标失败关闭断言。
2. 将root正向测试夹具改为固定的合成阻塞合同，新增取消状态负向测试。
3. 运行相关专项测试并记录基线限制。

## 默认工程决策

- 复用Python标准库、现有映射和测试框架；不新增依赖。
- 保持任务-000086固定逻辑别名、路径、协议和资源上限不变。
- 普通取舍按安全、证据、简单、可复现、可回滚顺序执行。

## 允许停止条件

- 需要改写任务-000084合同、历史批次或取消状态。
- 需要放宽已取消目标的拒绝规则或新增权限。
- 现有合成夹具无法与真实合同结构严格区分。

## 输入合同

- `scripts/研发中心/验证自动合并资格.py`、`tests/研发中心/test_验证自动合并资格.py`。
- 任务-000084取消闭环事实、任务-000086固定root兼容合同和任务-000111治理规则。

## 输出合同

- 可信资格器取消目标失败关闭规则。
- root兼容模式正向/负向专项测试。
- 任务文件、看板和PR验证记录。

## 工作范围

- 任务-000084取消状态与任务-000086 root兼容映射的治理规则和测试夹具。

## 不在范围

- 不修改任务-000084、任务-000086历史合同正文或任何数据批次。
- 不访问服务器、数据库、网络业务数据、凭据、生产系统或真实交易。
- 不改变自动合并权限范围、评审门槛或交易状态。

## 安全边界

- 仅读Git治理文件；取消目标和未知状态失败关闭。
- 不允许状态回退、跨任务补偿、伪造合并事实或未来数据。
- 测试单进程，内存上限沿用研发中心规则。

## 验收标准

1. 任务-000084已取消时，任务-000086 root合同修复资格返回失败并说明原因。
2. 显式合成阻塞目标的既有正向测试通过，固定root合同段落和看板禁止改写仍有效。
3. 任务-000084取消PR可在新规则下通过可信资格复验。
4. 专项测试、Python编译、MarkdownLint和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_任务看板模式.py -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

取消目标旧root映射失败关闭规则与测试进入main；任务-000084取消PR重新通过双评审、可信资格复验并自动合并。

## 数据与安全影响

仅修改治理校验器、测试和任务登记文件；无服务器、数据库、原始数据、凭据、生产或交易影响。

## 已知限制

本任务只关闭过期治理映射，不证明任何来源身份或数据完整性，不改变任务-000084取消依据。

## 回滚方式

回滚本任务PR可恢复旧映射代码，但不得回滚任务-000084取消事实；恢复前必须重新建立独立治理合同。

## 执行记录

- 交付状态：待评审，已完成实现，等待Pull Request合并。
- 执行分支：`codex/task-000113-close-cancelled-root-mapping-exec-v1`
- 开始时间：`2026-08-14T06:41:59+08:00`
- 实现提交SHA：`63b6815b5414a072c2dcfb7c0fff762db848b183`
- Pull Request：[#317](https://github.com/xk320/zhishi/pull/317)
- 交付物：取消目标失败关闭规则、合成阻塞root兼容测试夹具及已取消目标负向测试。
- 验证结果：资格专项116项通过；Python编译通过；`git diff --check`通过。
- 已知限制：仅关闭已取消任务084的旧root映射，不修改任务084历史合同、批次或取消事实。
- 数据与安全影响：仅治理脚本与测试；未访问服务器、数据库、原始数据、凭据、生产服务或交易账户。
- 回滚方式：回滚本PR即可恢复旧映射规则，不回滚任务084取消事实。
