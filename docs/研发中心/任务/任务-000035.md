# 任务-000035：实施最小数据闭环试点

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：数据工程
- 阶段：阶段1 最小闭环试点
- 优先级：P0
- 执行方案：方案A“单一合格叶子最小实现、只读查询与端到端血缘验证”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领
- 执行分支：`codex/000035-minimal-data-loop-pilot-v1`
- 开始时间：`2026-08-05T04:28:22+08:00`
- Pull Request：[#117](https://github.com/xk320/zhishi/pull/117)
- 实现提交SHA：`01b9785dc51e83a2b1573307577d2f2f2eda981c`
- 完成实现时间：`2026-08-05T04:37:26+08:00`
- 合并时间：2026-08-04 20:54:43 +0000
- 合并提交SHA：`d1cdfca17c791a347887d20bf4e6be64c30c2234`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000034通过评审并合并；
- 当前阻塞原因：无；任务-000034已完成。
- 解除条件：已满足。

## 背景

现有最小数据闭环只有架构与评估器，没有经过真实数据验证的来源到只读研究快照试点。
试点必须最小、可回滚、有界，不能借机建设复杂平台。

## 任务目标

从任务-000034结果中按确定性顺序选择首个满足输入条件的BTC或ETH叶子，实施来源、
不可变原始段引用、标准化、质量、成本、血缘、冻结快照和只读查询的最小端到端试点。

## 固定执行方案

1. 结果可见前冻结叶子选择规则，禁止事后选择最好标的或时段；
2. 若无合格叶子，运行拒绝路径并形成零试点成员证据，不使用样例替代；
3. 试点仅在隔离、本地有界目录生成派生产物，原始数据始终只读；
4. 所有对象使用逻辑身份、内容指纹、三类时间、数据截止和双向血缘；
5. 重复运行验证幂等或显式拒绝覆盖，研究查询只读且不能返回范围外数据。

## 默认工程决策

优先使用仓库文件分区、CSV或JSON清单和Python标准库；不引入分布式平台、新数据库或
常驻服务；磁盘不足时缩小试点成员而不降低任何质量门。

## 允许停止条件

只有无安全隔离写入目录、磁盘不足以保存最小有界产物、需要生产权限或修改原始数据时
停止。零合格输入不属于停止条件，应完成拒绝路径并保持阶段门禁止。

## 输入合同

- 任务-000034闭环批次、叶子成员和逐门状态；
- 任务-000029至000033全部确切身份和指纹；
- 选择规则、最大记录数、最大字节、总超时、内存和输出上限；
- BTC或ETH单一叶子，不得合并标的或跨市场、合约、尺度和时间范围。

## 输出合同

- `docs/数据/最小数据闭环试点合同.md`和试点报告；
- `config/数据/最小闭环试点.json`；
- 最小闭环构建器、只读查询器和测试；
- `artifacts/数据/最小闭环试点/<批次>/`清单、派生对象、血缘和验证报告；
- 输入、规则、代码、环境、输出和报告指纹；
- 本任务、看板、提交、PR和验证记录。

## 工作范围

实现一个有界叶子的端到端试点或正式拒绝路径、不可变发布、双向血缘、只读查询、幂等、
故障回滚和自动化测试。

## 不在范围

- 不建设生产数据库、对象存储、消息队列、调度器或常驻服务；
- 不扩大白名单、不补采或修改原始数据；
- 不训练模型、回测、计算胜率收益或产生交易判断；
- 不以试点成功代表容量、恢复或阶段1最终通过。

## 安全边界

所有写入只在仓库批准的隔离产物目录；发布失败不能留下可被误认的完整批次；不保存凭据、
账户信息和未授权原始数据；资源硬上限必须在处理前校验；真实资金和生产系统不接入。

## 验收标准

1. 叶子选择规则事前冻结且确定，不事后选择最佳样本；
2. 合格叶子能从来源追溯至只读快照，零合格叶子能稳定拒绝；
3. 三类时间、数据截止、质量、成本和血缘版本完整；
4. 重复运行幂等或拒绝覆盖，失败发布可恢复且旧批次不变；
5. 查询器不能越过标的、市场、合约、尺度和时间边界；
6. 资源、磁盘、脱敏和只读边界有测试与真实运行证据；
7. 全部测试、Markdown和差异检查通过。

## 验证命令

```bash
python3 -m unittest discover -s tests/数据 -p 'test_*最小闭环*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
npx --yes markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
```

## 完成定义

最小试点或真实零成员拒绝路径、报告、实现和验证记录完成后创建引用任务-000035的PR并
更新为`待评审`。不得把零成员写为成功闭环；PR合并后标记`已完成`并解除任务-000036。

## 执行记录

- 执行分支：`codex/000035-minimal-data-loop-pilot-v1`
- 开始时间：`2026-08-05T04:28:22+08:00`
- 实际批次：`pilot-20260805T045300+0800-zero-v2`
- 来源批次：`loop-20260805T035900+0800-v4`；来源成员SHA-256为`82bf0780f74c0c1405e68ca5b6e8f8cb187158581770477144866afea95c7b5a`
- 结果：候选2520（BTC 1260、ETH 1260），合格0，拒绝2520；形成真实零成员拒绝路径，未生成市场记录或交易判断。
- 产物：`docs/数据/最小数据闭环试点合同.md`、`docs/数据/最小数据闭环试点报告.md`、`config/数据/最小闭环试点.json`、`scripts/数据/构建最小闭环试点.py`、`scripts/数据/查询最小闭环试点.py`、`tests/数据/test_最小闭环试点.py`及隔离批次目录；报告清单包含输入、规则、实现、查询、环境和输出指纹。
- 查询验证：BTC、4小时查询返回空集；越界1小时查询被拒绝。
- 数据与安全：仅读取仓库内冻结聚合证据；没有访问服务器、数据库或真实市场数据，没有修改原始数据、生产服务、权限、账户或凭据。
- 已知限制：上游叶子均不可用或无法判定，零成员不解锁阶段2；需按来源批次解除条件补证后再重跑。
