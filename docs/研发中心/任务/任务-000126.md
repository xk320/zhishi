# 任务-000126：兼容阶段1覆盖受限派生文档的冻结输入指纹

<!-- markdownlint-disable MD013 -->

- 状态：阻塞
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P1
- 执行方案：方案A“冻结输入指纹的追加式兼容映射”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；任务-000125已完成后执行，任务-000124兼容复验完成前不得迁移其状态
- 执行分支：`codex/task-000126-frozen-input-fingerprint-compat-v1`
- 开始时间：`2026-08-15T11:07:58+08:00`
- 当前阻塞原因：任务-000124最终追加文档尚未进入main，冻结输入合同无法安全更新。
- 解除条件：任务-000124先完成可信合并，或建立允许叠加兼容补丁的治理路由后复跑四个输入合同。

## 依赖与阻塞条件

- 唯一前序依赖：任务-000125已完成并进入main。
- 当前问题：任务-000124按固定V2路由追加阶段1审计报告和数据缺口说明后，历史来源身份/时间质量合同仍冻结旧输入SHA，导致审计与数据测试在加载阶段失败；这不是数据补采成功，也不是历史批次变化。
- 解除条件：为受影响的冻结输入合同建立一次性、可复算的追加式兼容映射；仅把当前V2派生文档的真实SHA写入对应输入清单，并证明历史批次、状态、分母和安全边界未变。
- 阻塞条件：若需要修改原始数据、历史批次、来源身份成员、任务-000098/000106正文、研究准入、交易许可或生产/服务器边界，立即停止。

## 背景

任务-000124的覆盖受限合同已经如实保留缺失为`无法判定`，但其派生文档追加内容触发了既有冻结输入指纹硬门。若不区分“文档追加式版本变化”和“历史证据被覆盖”，则无法验证覆盖受限合同本身。

## 任务目标

1. 识别所有因任务-000124追加式派生文档而失配的冻结输入SHA。
2. 仅更新对应JSON输入清单中的当前/兼容版本指纹，保留旧指纹、旧批次和旧合同语义可追溯。
3. 让`tests/审计`与`tests/数据`在不访问服务器、不读取数据库业务记录的条件下可复算通过；任何真正的历史内容漂移仍必须失败关闭。

## 固定执行方案

1. 从任务-000124最终候选头读取实际文档字节并计算SHA-256，建立旧SHA→新追加式SHA映射。
2. 仅修订固定的来源身份/时间质量输入清单；新增版本字段或追加式兼容字段必须保留旧值，不得覆盖历史批次指纹。
3. 增加或复用确定性校验，验证旧版本仍可追溯、新版本仅对应任务-000124固定追加段，任意其他正文改写均失败。
4. 运行全量审计/数据测试、研发中心测试和格式检查；失败即保留证据，不把缺失数据伪造为通过。

## 默认工程决策

- 只处理任务-000124实际追加的两个派生文档：`docs/审计/阶段1最终审计报告.md`和`docs/审计/数据缺口与补采清单.md`。
- 只修改以下四个输入合同：`config/数据/三类时间与可见性复采.json`、`config/数据/来源身份复采.json`、`config/数据/来源身份输入指纹复采.json`、`config/数据/数据来源与资产身份.json`中受影响的输入项。
- 不修改任务合同、历史批次、成员清单、数据源正文、测试安全边界或任何交易规则。
- 不使用未来数据；缺失、延迟、无法证明的内容继续保持`无法判定`。

## 允许停止条件

- 发现无法在保留旧SHA和历史批次的前提下建立确定性兼容映射。
- 需要访问Ubuntu、数据库、网络市场数据、原始归档、凭据或生产系统。
- 任何测试通过依赖放宽指纹检查、删除分母或忽略正文漂移。

## 输入合同

- 任务-000124 PR的最终头及其V2固定追加文本。
- 四个来源身份/时间质量JSON输入合同及其历史批次指纹。
- 当前审计与数据测试的失败堆栈和冻结字段定义。

## 输出合同

- 四个JSON输入合同的最小兼容指纹修订（如确有受影响项）。
- 旧指纹、新指纹、追加段指纹和版本关系的可复算记录。
- 全量审计/数据测试及研发中心验证结果；不生成新的市场数据或身份结论。

## 工作范围

- 仅限四个指定JSON输入合同、对应治理测试（如已有测试需要固定负向夹具）和任务-000126执行记录/看板状态元数据。

## 不在范围

- 不修改任务-000124、任务-000098、任务-000106合同正文或历史执行记录。
- 不修改原始市场数据、历史审计批次、来源身份成员、数据库、服务器、生产服务、权限、凭据或交易逻辑。
- 不补造多年成本、执行延迟、时间精度、胜率、收益、方向、仓位或订单。

## 安全边界

- 旧SHA和旧批次必须保留；新SHA只能绑定任务-000124固定追加段。
- BTC与ETH不得互相补偿，主研究尺度仍为4/8/24/48小时，15分钟和1小时仅为事后观察窗口。
- 兼容映射不能改变研究准入、交易许可或真实交易关闭状态。
- 任意非追加正文、未知路径、未来数据和真实交易放行必须失败关闭。

## 验收标准

1. 四个JSON合同中的受影响输入项可由任务-000124固定追加内容复算，旧指纹仍可追溯。
2. `tests/审计`与`tests/数据`全量通过，且至少有正文改写/旧SHA丢失/未知追加段失败关闭测试。
3. 研发中心资格、Python编译、MarkdownLint和`git diff --check`通过。
4. 任务-000124仍保持待评审，不在本任务中伪造完成或迁移目标任务状态。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m pytest -q tests/审计 tests/数据
PYTHONHASHSEED=0 python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

兼容映射进入main，历史SHA/批次未被覆盖，全量审计/数据测试和治理验证通过；任务-000124随后在其原PR上重新验证，不在本任务中标记完成。

## 已知限制

本任务只解决文档追加造成的冻结输入指纹兼容，不补齐任何缺失市场数据，也不改变阶段1证据覆盖范围。

## 回滚方式

回滚本任务PR，恢复四个JSON合同到合并前版本；保留任务-000124合同、历史批次和失败证据。

## 数据与安全影响

只读现有仓库文件并修改版本化输入清单；不访问服务器、数据库、原始市场数据、生产服务、凭据或真实账户。

## 执行记录

- 执行分支：`codex/task-000126-frozen-input-fingerprint-compat-v1`
- 开始时间：`2026-08-15T11:07:58+08:00`
- 尝试命令：`PYTHONHASHSEED=0 python3 -m pytest -q tests/审计 tests/数据`；`git show 3ebe346966f80054ffddc7ce6eb2f4543855ad70:docs/审计/阶段1最终审计报告.md | shasum -a 256`；`git show 3ebe346966f80054ffddc7ce6eb2f4543855ad70:docs/审计/数据缺口与补采清单.md | shasum -a 256`
- 结果：任务-000124候选头的两份派生文档指纹为`6f9f178d104eaeded924f27131d085df837a9bb7486b3474c054c4c427a99553`和`4765a8fc8ea3dd31db93113095e7d8ea1f4f3da0eff68824edf523809d20a209`；审计/数据测试为457通过、12失败，失败集中于冻结输入指纹。
- 交付物：尚未产生配置修改；阻塞前不提交不完整兼容映射。
- 当前阻塞原因：任务-000124追加文档仍在未合并PR中，而任务-000126的配置兼容必须以进入`main`的最终文档字节为输入。若现在将未来候选SHA写入main，当前旧文档会立即失去可复验性；若把任务-000124文档复制进本PR，又会越过其不可变合同和V2路径边界。
- 解除条件：任务-000124先完成一次经可信规则认可的合并顺序，或新增明确允许“任务-000126配置兼容补丁叠加任务-000124候选头”的治理路由；随后仅更新四个输入JSON中的受影响当前指纹，并复跑全量审计/数据测试。
- 已知限制：本次阻塞不表示数据已补齐；多年成本、主网执行生命周期和时间精度缺失仍保持`无法判定`。
- 数据与安全影响：未访问服务器、数据库、网络市场数据、原始归档、凭据或生产系统；未修改历史批次、原始数据或任务-000124合同。
- 需要人工决策：无业务决策；需要治理流程选择上述可验证的合并顺序，不能通过猜测指纹解除。
- 回滚方式：本阻塞状态PR可回滚，不影响任务-000124、任务-000125或历史冻结证据。
