# 任务-000124：建立阶段1覆盖受限完成合同

<!-- markdownlint-disable MD013 -->

- 状态：待执行
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“覆盖受限合同与精确可见集修订”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；任务-000115、任务-000123已完成后执行，本任务完成前不得执行新的阶段1合同修订

## 受控合同修订路由

- 变更类型固定为：`阶段1覆盖受限完成合同修订V2`。
- 执行任务固定为：任务-000124；目标任务固定为：任务-000098、任务-000106；未知执行者或目标必须失败关闭。
- 本任务仅允许一次性建立该路由并使用该路由；不得泛化为其他任务、其他阶段或其他状态迁移。
- 允许路径固定为：`scripts/研发中心/验证自动合并资格.py`、`tests/研发中心/test_验证自动合并资格.py`、任务-000098、任务-000106、README.md、`docs/研发中心/总体计划.md`、`docs/审计/阶段1最终审计报告.md`、`docs/审计/数据缺口与补采清单.md`及任务124执行记录/看板；超出即失败关闭。
- 目标任务仅允许修订固定章节：任务098的“依赖与阻塞条件/固定执行方案第2步/任务目标/验收标准/完成定义/已知限制”，任务106的“依赖与阻塞条件/固定执行方案第11、14步/默认工程决策/允许停止条件/覆盖受限模式/任务目标/验收标准/完成定义/已知限制”；其他合同字段、执行记录和历史批次必须逐字不变。
- 路由实现必须固定保存基线目标合同SHA-256、允许章节旧文本SHA-256和新文本SHA-256；运行时按完整文本指纹和逐段白名单复算，禁止任意追加、删除或重排。
- V2优先级固定为：仅在上述指定步骤和章节内，以新文本替代与覆盖受限语义冲突的旧硬门；其余固定执行步骤、输入、输出、安全边界和历史保护逐字优先，不能借V2扩大权限或缩小分母。任务106在覆盖受限证据完整交付后不得仅因缺少多年主网证据转为阻塞，但该缺失必须保持`无法判定`，且研究准入、交易许可和真实交易准入继续关闭。
- 必须提供负向测试：错任务、错目标、未知变更类型、额外路径、状态迁移、历史执行记录改写、分母缩小、跨标的补偿、未来数据和真实交易放行均失败关闭。

## 依赖与阻塞条件

- 唯一前序依赖：任务-000115已完成并进入main；任务-000123已完成的测试夹具事实作为本任务输入。
- 当前问题：任务-000098和任务-000106仍把无法补齐的历史数据当作阶段1硬阻塞，无法在不伪造完整性的前提下交付现有可验证覆盖窗口。
- 解除条件：任务-000098采用精确截止时刻的可见集重放并保留晚到成员；任务-000106采用覆盖受限完成定义并保留所有成本/延迟缺失；派生文档、看板和测试一致。
- 阻塞条件：若修订会删除或覆盖历史批次、缩小候选总体、跨标的补偿、把`无法判定`改为通过、生成胜率/收益/订单或开启真实交易，立即停止。

## 背景

现有数据能够证明有限窗口和确定性历史现场，但无法补齐多年成本、主网执行延迟及任务-000098最后成员的亚秒级截止边界。继续把这些不可补齐事实当作“全部阶段1不可交付”，不能反映已验证证据，也不能解决数据缺口本身。

## 任务目标

1. 将任务-000106的完成定义改为“覆盖受限证据链可审计交付”，覆盖外统一为`无法判定`，完整多年覆盖仍作为未满足能力说明。
2. 将任务-000098的重放定义改为“精确截止时刻的可见集重放”：晚于截止时刻的成员保留在候选总体和缺失计数中，不进入可见集，不修改原始批次。
3. 明确覆盖受限完成不等于研究优势、交易许可或真实交易准入；真实资金交易继续关闭。

## 固定执行方案

1. 先在可信资格器和专项测试中建立上述一次性V2路由，固定目标、路径、字段和指纹门。
2. 仅通过V2路由修订任务-000098、任务-000106及其直接派生阶段文档，保留原始合同、执行记录、批次和失败证据。
3. 为任务-000098追加精确截止时间、可见集、晚到成员和守恒字段的合同条款。
4. 为任务-000106追加覆盖受限完成定义、缺失分类、分母守恒和不可升级条款。
5. 更新README、总体计划和缺口报告中的阶段状态表述，并补充负向测试。

## 默认工程决策

- 使用现有脱敏批次和标准库；不重新扫描原始归档，不访问服务器或数据库。
- 对任务-000098保留全部5180候选身份，晚到成员只进入`无法判定/时间边界拒绝`计数。
- 对任务-000106按BTC、ETH及4/8/24/48小时分别报告覆盖窗口和缺失，不跨标的补偿。
- 任何描述性差异不得解释为因果、预测优势、胜率、收益或交易许可。

## 允许停止条件

- 需要改写历史批次字节、删除缺失成员、修改原始数据或放宽真实交易关闭。
- 现有可信规则无法表达“覆盖受限但不可升级”的状态，且必须修改生产或自动合并安全门。

## 输入合同

- 任务-000098、任务-000106当前合同、执行记录、历史批次与看板阻塞事实。
- 任务-000115覆盖受限规则和任务-000123生命周期测试夹具。
- README、总体计划、阶段1审计报告及数据缺口报告。

## 输出合同

- 修订后的任务-000098和任务-000106合同及执行记录边界。
- 覆盖受限阶段1状态、精确可见集重放语义和负向测试。
- 更新后的README、总体计划、阶段1审计报告、数据缺口报告和看板。

## 工作范围

- 任务合同、研究治理文档、审计报告、研发中心测试和阶段状态派生视图。

## 不在范围

- 不获取新市场数据，不访问Ubuntu/数据库，不修改原始数据或历史批次。
- 不实现运行时、模型、回测、交易信号、仓位、订单或真实资金交易。
- 不把覆盖受限结果标记为完整多年证据或交易许可。

## 安全边界

- `无法判定`、拒绝、失败、未成熟和失效必须完整保留。
- 任务-000098晚到成员不得删除或从候选总体分母中隐去。
- 任务-000106的覆盖外窗口不得外推、填充或跨BTC/ETH补偿。
- 任何未来数据、凭据、生产权限或真实交易路径均失败关闭。

## 验收标准

1. 任务-000098合同明确精确截止时刻、可见集和晚到成员守恒，原始批次不变。
2. 任务-000106合同允许覆盖受限证据交付，但完整多年缺失仍为`无法判定`且不升级交易许可。
3. README、总体计划、审计报告、缺口报告、任务文件和看板状态一致。
4. 负向测试证明删除晚到成员、缩小分母、跨标的补偿、未来数据和交易许可升级均失败关闭。
5. 研发中心、审计和治理测试，MarkdownLint、Python编译和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
PYTHONHASHSEED=0 python3 -m pytest -q tests/审计 tests/数据
python3 -m py_compile scripts/研发中心/验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

合同修订和派生文档进入main，负向测试与全量验证通过，双只读评审、可信复验和自动合并完成；任务-000098与任务-000106随后按新合同独立执行并形成覆盖受限证据。完成本任务不宣称完整多年数据或交易许可。

## 数据与安全影响

仅修改治理合同、报告和测试；不访问或写入服务器、数据库、市场数据、原始归档、生产服务、凭据或真实账户。

## 已知限制

覆盖受限完成只能说明已有窗口可审计，不能补齐不存在的多年成本、主网执行延迟或缺失时间精度。

## 回滚方式

回滚本任务PR的合同和派生文档修订；保留原任务合同、历史批次、失败证据和任务115/123实现。
