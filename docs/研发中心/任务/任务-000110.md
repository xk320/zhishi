# 任务-000110：修复任务-000109缺失审计分母与执行器指纹

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“追加式分母闭环与执行器可复现修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；按任务-000109评审反馈自动修复，不再询问方案
- 并行规则：禁止并行；唯一前序依赖为任务-000108；不修改任务-000109首批历史批次

## 依赖与阻塞条件

- 唯一前序依赖：任务-000108已完成；任务-000109已进入main但保留待评审，不作为本任务的完成依据。
- 当前阻塞原因：任务-000109首批叶子未携带207个来源拒绝的标的/对象分母，且执行器提交不含脚本。
- 解除条件：新增修复批次按BTC/ETH和八个尺度闭合分母，执行器提交与脚本blob可复现，双只读评审通过。

## 背景

任务-000109将任务-000084路线收敛为缺失审计，但首批交付复评发现来源拒绝候选仍停留在顶层清单，无法从各标的和叶子独立复算；运行批次时执行器Git头也早于脚本提交。本任务只修复证据可审计性，不扩大研究范围。

## 任务目标

1. 按原始group把BTC 8个、ETH 199个来源拒绝候选映射到对象和八个主研究尺度叶子。
2. 在groups和leaves中分别保存正式成员、来源拒绝、合计分母、正式状态、来源拒绝状态、合计状态、连续段数量和覆盖日期。
3. 重新运行追加式批次，保存包含脚本的Git提交和脚本blob SHA。

## 固定执行方案

1. 只读取任务-000109已合并的固定SHA脱敏批次。
2. 不覆盖或删除`stage1-missing-data-audit-20260814T050000Z-6487a1f0cd7a`及其历史输入。
3. 通过确定性group映射和BTC/ETH独立聚合闭合分母；任何不守恒即失败安全。
4. 以包含执行器脚本的Git提交和blob指纹绑定新批次。

## 默认工程决策

- 复用Python标准库和任务-000109执行器；不新增依赖、数据库或服务。
- 使用自然日、已证明集合和已冻结连续段计算缺失；不使用结果可见后的筛选。
- 普通技术取舍按安全、证据、简单、可复现和可回滚顺序自主决定。

## 允许停止条件

- 输入指纹、成员顺序或来源拒绝group漂移。
- 无法从Git提交复现执行器源码。
- 需要访问服务器、数据库、网络、原始数据、凭据或真实交易能力。
- 计数不守恒、跨标的补偿或覆盖历史批次。

## 输入合同

- 任务-000109首批批次、任务文件、配置和审计报告。
- `summary.json`、`members-001.json`、`segments.json`、`leaves.json`、`source-rejected.json`及其固定SHA。
- 最新`AGENTS.md`、README、任务规范、看板和总体计划。

## 输出合同

- `scripts/审计/审计阶段1数据缺失.py`及专项测试修复。
- `artifacts/审计/阶段1数据缺失/<新批次>/`追加式摘要、groups、leaves、规则、执行器和manifest。
- 缺口清单、阶段1最终审计报告、任务文件和看板中的修复记录。

## 工作范围

- 来源拒绝的BTC/ETH对象映射、八叶子分母守恒和执行器指纹修复。
- 连续段数量、覆盖日期和缺失日期的结构化复算。

## 不在范围

- 不访问Ubuntu、数据库、网络、真实市场数据或原始业务行。
- 不重新证明来源身份，不修改任务-000084历史合同和批次。
- 不训练模型、回测、生成胜率、收益、方向、仓位、订单、研究准入或交易许可。

## 安全边界

- 单进程、只读输入、追加式输出、固定资源上限；异常时失败安全。
- 不写入生产系统、数据库、服务器、账户或权限，不记录凭据。
- BTC与ETH不得互相补偿，15分钟和1小时不得升级为研究尺度。

## 验收标准

1. BTC来源拒绝8、ETH来源拒绝199分别进入对象和八个叶子分母。
2. 每个叶子可独立复算正式成员、来源拒绝、合计候选、状态、缺失日期、连续段和覆盖日期。
3. 新批次executor的Git提交包含脚本，blob SHA与工作树脚本一致；旧批次字节不变。
4. 专项、审计、编译、MarkdownLint、敏感扫描和`git diff --check`通过。
5. PR经双只读评审、可信资格复验和自动合并；任务-000109后续仅按独立状态闭环处理。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/审计/test_审计阶段1数据缺失.py -q
PYTHONHASHSEED=0 python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/审计阶段1数据缺失.py tests/审计/test_审计阶段1数据缺失.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

修复批次可从包含脚本的Git提交复现，BTC/ETH和八叶子分母、状态、缺失日期、连续段和覆盖日期守恒；所有验证和双只读评审通过并自动合并。

## 数据与安全影响

仅读取仓库固定SHA脱敏证据，新增小型追加式摘要；无服务器、数据库、网络、凭据、原始数据或交易影响。

## 已知限制

本任务只修复审计证据结构和可复现性，不关闭数据缺口，不改变阶段1/阶段2门禁，不产生研究准入或交易许可。

## 回滚方式

回滚本任务新增修复代码、测试、批次和报告追加；保留任务-000109首批批次与任务-000084历史合同不变。

## 执行记录

- 交付状态：待评审，已创建Pull Request，尚未合并。
- 执行分支：`codex/task-000110-missing-audit-repair-v1`
- 开始时间：`2026-08-13T21:14:48+00:00`
- 实现提交SHA：`04f7109a6ebcb65bfd71ac767e48a55d428c7ffd`
- Pull Request：[#304](https://github.com/xk320/zhishi/pull/304)
- 交付物：修复后的缺失审计执行器、专项测试、追加批次`stage1-missing-data-audit-20260814T051500Z-v3`、缺口清单与阶段1最终审计报告。
- 验证结果：专项审计3项通过；审计测试250项通过；Python编译通过；5份变更Markdown检查通过；`git diff --check`通过。
- 已知限制：仅审计固定脱敏批次，不补采、不修改原始数据，不产生研究准入或交易许可；研发中心跨载体全量测试的既有基线失败未由本任务引入。
- 数据与安全影响：仅读取仓库固定脱敏输入并追加结构化证据；未访问服务器、数据库、网络、凭据、生产系统、原始数据或交易账户。
- 回滚方式：回滚本PR提交即可移除修复代码、测试、v3追加批次及报告追加；任务-000109首批批次与任务-000084历史合同不变。
