# 任务-000125：补齐阶段1覆盖受限V2跨载体类型识别

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 合并时间：2026-08-15 02:45:43 +0000
- 合并提交SHA：`86a3bbfacedab637e47b36a1968a791bb08360d7`
- 类型：治理
- 阶段：研发中心自动合并治理
- 优先级：P1
- 执行方案：方案A“跨载体变更类型最小注册”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 执行分支：`codex/task-000125-cross-carrier-v2-v1`
- 开始时间：`2026-08-15T10:26:41+08:00`
- Pull Request：[#361](https://github.com/xk320/zhishi/pull/361)
- 实现提交SHA：`84c29af277e75198978badb226d4d9ba493f3dfe`
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务仅注册固定类型识别，不执行任务-000124目标合同

## 依赖与阻塞条件

- 唯一前序依赖：任务-000123已完成并进入main。
- 当前问题：任务-000124使用的`阶段1覆盖受限完成合同修订V2`已进入可信资格器，但跨载体检查器和自动合并工作流仍只识别旧类型，导致同一PR的任务合同、跨载体正文和评审证据无法形成一致事实。
- 解除条件：跨载体解析器和专项负向测试在main中精确识别V2类型，未知类型、错任务和额外路径继续失败关闭。
- 阻塞条件：需要修改任务-000124合同、目标任务-000098/000106、看板状态、历史批次或生产边界时立即停止。

## 背景

任务-000124已登记一次性V2路由。当前跨载体检查器的类型正则尚未同步该固定类型，若直接执行任务-000124，可信资格与跨载体资格会出现分歧。

## 任务目标

1. 让跨载体检查器和自动合并工作流精确识别`阶段1覆盖受限完成合同修订V2`。
2. 将该类型仅绑定任务-000124的固定执行事实，不改变任务-000124目标合同或状态。
3. 为未知类型、错任务、错目标和额外路径保留失败关闭测试。

## 固定执行方案

1. 在跨载体检查器和自动合并工作流中增加一个固定类型字面量和最小解析分支。
2. 仅允许任务-000124作为该类型执行者；不迁移任何既有任务状态。
3. 增加正向解析和负向越权测试，运行研发中心治理验证。

## 默认工程决策

- 复用现有正则、任务引用和变更路径校验，不新增依赖。
- 不复制可信资格器的目标合同逻辑；跨载体层只负责识别类型并保持任务引用约束。
- 所有未知类型、缺失类型、错任务、错目标和额外路径均失败关闭。

## 允许停止条件

- 需要修改任务-000124或任务-000098/000106合同正文。
- 需要修改真实看板、历史批次、数据、服务器、数据库、生产服务、凭据或交易边界。
- 跨载体检查器无法在不放宽既有硬门的情况下加入固定类型。

## 输入合同

- `scripts/研发中心/验证跨载体冲突.py`当前类型解析和任务引用规则。
- `tests/研发中心/test_验证跨载体冲突.py`现有类型测试。
- 任务-000124已登记的V2类型、执行者和目标说明。

## 输出合同

- 跨载体检查器识别V2类型的最小实现。
- 正向解析、错任务、未知类型和额外路径失败关闭测试。
- 不修改任务-000124、任务-000098、任务-000106及其历史看板事实；任务-000125自身状态迁移所需的看板派生行除外。

## 工作范围

- `scripts/研发中心/验证跨载体冲突.py`、`.github/workflows/pr-auto-merge.yml`及其对应研发中心测试。

## 不在范围

- 不执行任务-000124合同修订，不修改既有任务状态、任务看板事实或研究文档；仅按生命周期协议更新任务-000125自身状态元数据和派生看板行。
- 不访问服务器、数据库、市场数据、生产服务、凭据或真实交易账户。
- 不实现运行时、模型、回测、交易信号、仓位、订单或真实交易。

## 安全边界

- 只增加一个固定字符串解析分支，未知类型和越权引用仍失败关闭。
- 不放宽任务合同、路径、状态、数据完整性或交易许可门槛。
- 不使用未来数据，不删除或覆盖原始数据和历史证据。

## 验收标准

1. `_change_type_from_body`能精确返回V2类型，旧类型行为不变。
2. V2类型错任务、错目标、未知类型和额外路径均失败关闭。
3. 任务-000124、任务-000098、任务-000106及既有看板历史证据无差异；任务-000125自身状态元数据按协议闭环。
4. 研发中心相关测试、Python编译、MarkdownLint和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证跨载体冲突.py tests/研发中心/test_验证自动合并资格.py -q
python3 -m py_compile scripts/研发中心/验证跨载体冲突.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

跨载体解析与负向测试进入main，经双只读评审、可信复验和自动合并；任务-000124仍保持其自身待执行/待评审生命周期，不在本任务中迁移。

## 数据与安全影响

仅修改治理脚本和测试，不访问或写入服务器、数据库、市场数据、原始归档、生产服务、凭据或真实账户。

## 已知限制

本任务只同步变更类型识别，不证明任务-000124目标合同、数据完整性或研究准入。

## 回滚方式

回滚本任务PR即可移除V2跨载体类型识别，保留任务-000124登记合同和历史证据不变。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双只读评审与可信合并。
- 执行分支：`codex/task-000125-cross-carrier-v2-v1`
- 开始时间：`2026-08-15T10:26:41+08:00`
- 实现提交SHA：`84c29af277e75198978badb226d4d9ba493f3dfe`
- Pull Request：[#361](https://github.com/xk320/zhishi/pull/361)
- 交付物：跨载体V2类型解析、自动合并工作流类型同步、V2越权失败关闭测试。
- 验证结果：跨载体/自动合并工作流专项测试通过；Python编译和`git diff --check`通过；变更Markdown默认Lint 0 issues。
- 已知限制：只同步类型识别，不执行任务-000124目标合同，不提供数据完整性、研究准入或交易许可证据。
- 数据与安全影响：未访问服务器、数据库、市场数据、生产服务、凭据或真实交易账户；既有任务合同和历史看板事实未改。
- 回滚方式：回滚PR #361即可移除V2类型识别与工作流同步。
