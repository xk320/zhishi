# 任务-000067：建立任务合同冲突修复的可信引导路径

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：治理
- 阶段：阶段1研发中心可信交付治理
- 优先级：P0
- 执行方案：方案A“先建立可信入口、再由下一任务执行单目标双字段修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 自动合并范围：治理自动化
- 执行分支：`codex/000067-contract-repair-bootstrap-v1`
- 开始时间：`2026-08-06T13:40:07+08:00`
- 实现提交SHA：`a04636fb195dd15034c50e007730234fc4666e73`
- Pull Request：[#168](https://github.com/xk320/zhishi/pull/168)
- 合并时间：2026-08-06 06:22:43 +0000
- 合并提交SHA：`a3c36786b75368da6280d1450b840cd3887d332b`
- 并行规则：禁止并行；必须先完成本任务并闭环，再登记任务-000068执行实际合同修复；不得与其他研发中心可信治理任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000065（已完成；提供自定义主题合并事实复算能力）
- 独立前置事实：任务-000066交付PR #165已进入main；其状态闭环冲突证据已固定，但本任务不修改任务-000066。该事实必须由main中的真实双父合并提交复算，不把任务-000066的未完成状态当作已满足依赖。
- 当前阻塞原因：无；本任务只建立可信治理入口、测试和说明，不访问服务器、数据库、真实市场数据或生产系统。
- 解除条件：新变更类型、下一任务-000068→任务-000066的单目标映射、双字段白名单和失败关闭测试进入main，并通过双只读评审、主执行器验证和精确SHA可信复验；随后才能登记任务-000068。

## 背景

任务-000066的正文只读质量复验交付PR #165已经合并到main，双只读评审和全部验证均通过；但任务合同的
“完成定义”仍保留了早期登记阶段“本登记PR合并后任务保持阻塞”的表述，导致独立状态闭环PR #166
被可信冲突检查拒绝。任务-000066同时只记录了“实现提交SHA”，而自定义合并主题的可信事实复算要求
明确的“交付提交SHA”，导致交付事实无法在状态闭环中被完整绑定。直接在同一PR中首次引入新变更类型并
修改既有任务会被基线可信规则拒绝，不能通过直接改状态、扩大普通状态闭环可修改字段或删除历史证据解决。

本任务只建立一次性的、严格预绑定到下一编号任务-000068→任务-000066的治理修复入口，并用固定夹具证明
其字段白名单和失败关闭语义；本任务不修改任务-000066。入口本身不得成为通用绕过机制，也不得改变
任务-000066的审计结果、数据门槛或阶段状态。

## 任务目标

1. 新增变更类型“任务合同冲突修复”，仅允许下一编号任务-000068绑定目标任务-000066；本任务交付PR不使用该新类型。
2. 以固定夹具验证目标任务在基线和头部都保持“待评审”，看板中的目标行保持原样，不得把合同修复伪装成状态闭环。
3. 固定目标未来仅可进行两项可复算合同修复：补充与已合并交付事实一致的“交付提交SHA”字段；将完成定义中与已交付事实冲突的登记阶段阻塞表述替换为可验证的状态闭环定义。
4. 保持普通任务交付、任务登记、阻塞任务合同修复和合并后状态闭环的既有硬门；错目标、多字段、状态迁移、未知路径和生产/数据夹带必须失败关闭。
5. 为后续任务-000068从最新main执行实际修复提供可验证的两步引导，不在本任务中标记任务-000066完成。

## 固定执行方案

1. 在可信资格器、跨载体冲突检查器、自动合并工作流和治理策略中登记“任务合同冲突修复”变更类型，但不在本PR使用该类型。
2. 将新类型固定预绑定到下一编号任务-000068→任务-000066，并要求目标基线状态为“待评审”、目标状态和看板行在头部保持不变。
3. 由固定夹具证明未来规则只能从目标基线的“实现提交SHA”和已合并PR #165的双父提交事实复算允许写入的“交付提交SHA”；不信任PR正文、Issue或聊天提供的SHA。
4. 由固定夹具按完整段落指纹校验“完成定义”只能从登记阶段阻塞表述变为交付后独立状态闭环表述，禁止修改其他章节、字段、执行记录或审计结果。
5. 增加合法引导、错目标、缺SHA、多字段、状态迁移、看板漂移、未授权路径和高风险路径的失败关闭测试；同步规范文档后运行研发中心全量验证并创建本任务交付PR。

## 默认工程决策

- 复用现有Python标准库、任务状态解析、Git双父合并事实和跨载体冲突检查器，不引入依赖、服务、数据库或常驻进程。
- 采用精确目标映射和完整文本指纹；宁可拒绝无法复算的修复，不接受模糊替换、正则放宽或聊天补齐字段。
- 变更类型只允许任务-000068→任务-000066这一条映射；后续需求必须重新登记任务，不得复用本入口。
- 评审最多两个独立只读子智能体，测试单进程，Node堆上限256 MiB；不创建额外工作树，不访问外部服务器。
- 不改变BTC、ETH、4小时、8小时、24小时、48小时研究范围，不解除阶段1数据证据硬门。

## 允许停止条件

- 无法在固定夹具中从main中的Git提交、任务-000066现有字段和PR #165合并事实确定唯一交付SHA。
- 需要在本任务中修改任务-000066，或需要让任务-000068修改除“交付提交SHA”和“完成定义”以外的任何合同内容、状态、看板行或审计结果。
- 需要放宽普通状态闭环、阻塞合同修复、精确头SHA、双评审、来源仓库或失败关闭硬门。
- 需要访问服务器、数据库、真实市场数据、生产系统、权限、凭据、原始数据或执行不可逆操作。
- 发现任务-000066交付事实、双父关系或PR #165合并证据与main不一致。

## 输入合同

- 最新main上的AGENTS.md、README、研发中心入口、任务规范、任务看板模式、PR自动合并策略和全部任务文件。
- 任务-000065的自定义合并事实复算规则、可信资格器、跨载体冲突检查器、自动合并工作流及研发中心测试。
- main中任务-000066的完整合同、任务-000066交付提交“实现提交SHA”、PR #165的真实合并提交和双父Git对象。
- PR #166失败关闭的结构化校验结果，仅作为可复算冲突证据，不作为任务合同来源。

## 输出合同

- `scripts/研发中心/验证自动合并资格.py`：新增并严格验证“任务合同冲突修复”单目标路径。
- `scripts/研发中心/验证跨载体冲突.py`：按相同目标和字段白名单允许一次性合同修复，其他任务继续失败关闭。
- `.github/workflows/pr-auto-merge.yml`及必要的治理策略文档：登记新变更类型和失败关闭语义。
- `AGENTS.md`、`docs/研发中心/任务规范.md`和`docs/治理/PR自动合并策略.md`：同步新变更类型、两步引导顺序和固定映射，避免机器规则与用户可读规范漂移。
- `tests/研发中心/`：合法路径及全部越权、漂移和高风险反例测试。
- 任务-000067执行记录、任务-000068登记所需的引导说明、可信规则、研发中心看板、PR和结构化评审证据。

## 工作范围

- 可信资格器、跨载体冲突检查器、自动合并工作流解析、治理策略和研发中心测试。
- 任务-000068→任务-000066唯一目标映射、目标状态保持“待评审”和看板保持不变的机器校验。
- “交付提交SHA”与“完成定义”两项精确合同修复的Git事实绑定规则和固定夹具。

## 不在范围

- 不执行任务-000066的数据库或日志审计，不重新访问Ubuntu，不读取或修改原始数据。
- 不修改任务-000066或未来任务-000068的状态、看板分区、依赖、范围、安全边界、输入输出、审计批次、失败计数或研究结论。
- 不修改普通任务交付、任务登记、任务-000056→任务-000055阻塞合同修复和合并后状态闭环的通用授权。
- 不实现运行时、模型、回测、真实交易、订单、方向、仓位或交易许可。

## 安全边界

- 仅读取本地Git对象和仓库文件；禁止访问服务器、数据库、真实市场数据、账户或生产服务。
- 新变更类型必须绑定单一执行任务、单一目标、目标基线状态和完整字段指纹；任一事实缺失即失败关闭。
- 不提交IP、域名、用户名、密钥、令牌、密码、原始日志、业务正文或账户凭据。
- 合同修复完成后仍须由任务-000068交付PR和独立状态闭环PR处理任务-000066；本任务不得修改或代替任何状态迁移。

## 验收标准

1. 任务登记PR只新增任务-000067、对应设计文档和看板唯一映射；编号为基线最大编号加一，状态为“待执行”。
2. “任务合同冲突修复”只能引用下一编号任务-000068，并只能修改目标任务-000066的“交付提交SHA”和“完成定义”两项内容；目标状态、看板行和其他合同逐字不变。
3. “交付提交SHA”必须等于main真实PR #165交付头的40位SHA，并通过双父合并事实和祖先关系复算；伪造、缺失、非祖先和错PR全部拒绝。
4. 完成定义替换必须通过完整旧/新段落指纹校验；部分替换、同义改写、夹带其他章节和审计结果修改全部拒绝。
5. 普通任务交付、任务登记、阻塞任务合同修复和状态闭环的既有正反测试全部保持通过；新路径的越权测试全部失败关闭。
6. Python编译、研发中心全量测试、MarkdownLint、敏感信息扫描和`git diff --check`通过；规范文档与机器规则一致；不访问服务器、数据库或真实市场数据；本任务交付PR使用已有“任务交付”类型并通过基线可信规则。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_验证跨载体冲突.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/*.py tests/研发中心/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须使用固定Git夹具验证任务-000068→任务-000066唯一映射、双字段白名单、目标状态不变和真实合并事实绑定。

## 完成定义

新变更类型、单目标字段指纹、可信资格器、跨载体检查器、工作流解析、回归测试和治理文档通过双只读评审、
主执行器验证及main可信复验后，以现有“任务交付”类型合并；随后通过独立状态闭环PR将任务-000067标记为“已完成”。
任务-000068必须在本任务合并并闭环后另行登记，随后才可使用新类型修复任务-000066；本任务不直接标记任务-000066完成。

## 数据与安全影响

只修改可信治理脚本、工作流解析、文档和测试；不访问或修改服务器、数据库、真实市场数据、
原始数据、生产服务、订单簿逻辑、账户权限或凭据。不产生胜率、收益、模型、回测、交易信号或交易许可。

## 已知限制

本任务只建立任务合同冲突修复的可信引导，不证明正文审计结果通过，不解除无法判定、失败或未成熟计数，
不放行阶段2，也不改变任务-000066的任何数据结论；实际两字段修复必须由后续任务-000068执行。

## 回滚方式

回滚本任务交付PR的合并提交即可移除“任务合同冲突修复”入口、测试和引导规则；随后任务-000068不得执行，
任务-000066恢复为原始待评审合同并继续保持状态闭环阻塞。不得重写历史审计批次、PR #165或Git历史。

## 执行记录

- 执行分支：`codex/000067-contract-repair-bootstrap-v1`
- 开始时间：`2026-08-06T13:40:07+08:00`
- 尝试命令：`python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q`；`python3 -m py_compile scripts/研发中心/*.py tests/研发中心/test_*.py`；`NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'`；`git diff --check`
- 结果：194项研发中心测试通过；Python编译通过；MarkdownLint 161个文件0问题；`git diff --check`通过；固定夹具覆盖任务-000068→任务-000066两字段白名单、执行记录不可变、CLI执行任务分离及越权反例。
- 外部证据：仅复算本地Git对象中的PR #165双父合并事实；未访问服务器、数据库、真实市场数据或生产系统。
- 阻塞原因：无；任务-000067交付等待双只读评审和main可信复验。
- 解除条件：交付PR合并并通过独立状态闭环后，才能登记任务-000068。
- 数据与安全：只修改可信治理脚本、工作流、治理文档和测试；未修改任务-000066、原始数据、数据库、订单簿逻辑、权限、凭据或生产服务。
