# 任务-000120：补齐任务115交付期看板合成测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 执行分支：`codex/task-000120-board-fixture-v1`
- 开始时间：`2026-08-15T03:14:50+08:00`
- Pull Request：[#342](https://github.com/xk320/zhishi/pull/342)
- 实现提交SHA：`db03dab2ea4fc6b53a5aa7c5243739165392c57f`
- 合并时间：2026-08-14 19:18:47 +0000
- 合并提交SHA：`24a4e52cc55a0c09875c898695ac3d8a8155b34d`
- 类型：测试
- 阶段：研发中心可信合同治理
- 优先级：P1
- 执行方案：方案A“固定任务115交付期看板基线并保持阶段1白名单边界”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；只修改研发中心测试夹具，不修改任务-000115/000116/000106合同、状态或阶段1可信规则

## 依赖与阻塞条件

- 唯一前序依赖：任务-000119已完成并进入main。
- 当前阻塞原因：任务115交付期必须把任务115看板行从待执行迁移到待评审；现有阶段1正向夹具直接读取真实看板，无法同时表达任务115执行前基线和交付后状态。
- 解除条件：测试内合成任务115待执行看板基线，且任务115交付后阶段1专项测试在不修改阶段1可信规则的情况下全绿。

## 背景

任务119已经固定了任务115和任务106的合同基线，但任务115开始执行后看板行会迁移到执行中/待评审。阶段1正向测试仍直接读取真实任务看板，导致任务115合同修订PR会因生命周期状态而自失效。本任务只补齐该测试夹具，不扩大阶段1合同修订白名单。

## 任务目标

1. 在阶段1正向测试内合成任务115待执行看板基线。
2. 保留任务115从待执行迁移到待评审、任务106阻塞和任务116已完成的正向约束。
3. 保留看板分支、PR、任务标题和依赖不一致时的失败关闭断言。

## 固定执行方案

1. 仅修改`tests/研发中心/test_验证自动合并资格.py`，在内存中移除真实任务115看板行并插入固定待执行行。
2. 不修改可信资格器、自动合并工作流、任务合同、看板事实或任何数据。
3. 运行研发中心专项测试；任一失败均停止，不跳过测试。

## 默认工程决策

- 复用Python标准库、现有unittest和任务119已建立的合成合同基线辅助函数。
- 合成看板只存在于测试内，不进入任务事实、审计批次或研究输出。
- 仅固定任务115一行，不泛化为任意任务或放宽看板复算规则。

## 允许停止条件

- 需要修改可信资格器、自动合并工作流、任务合同、真实看板、服务器、数据库、权限、原始数据或生产路径。
- 无法在内存中确定性构造任务115待执行看板行。
- 修复会放宽未来数据、跨标的补偿、研究准入、交易许可或失败关闭门槛。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`中阶段1覆盖受限合同修订正向测试。
- 任务115当前任务标题、P0优先级和唯一前序依赖字段。

## 输出合同

- 与真实任务生命周期解耦的任务115看板测试夹具。
- 研发中心资格、工作流、看板和范围专项验证结果。
- 任务120执行记录及看板派生状态。

## 工作范围

- 仅修改研发中心资格测试、任务120执行记录和看板状态。

## 不在范围

- 不修改任务-000115/000116/000106合同或状态、可信资格器、自动合并工作流、历史批次、服务器、数据库、市场数据、凭据、生产或交易逻辑。

## 安全边界

- 合成看板不进入生产、审计或研究结论。
- 不使用未来数据，不跨BTC/ETH补偿，真实资金交易保持关闭。

## 验收标准

1. 阶段1覆盖受限合同修订正向测试在任务115交付期看板迁移后仍通过。
2. 任务115看板标题、优先级、依赖、分支和PR不一致的越权变体仍失败关闭。
3. 研发中心资格、工作流、看板和范围专项测试全绿。
4. Python编译、MarkdownLint和`git diff --check`通过，无生产、数据或凭据变更。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

测试夹具进入main，专项测试全绿，任务-000120状态闭环为已完成；任务115/116/106合同、状态和历史证据不被改写。

## 数据与安全影响

仅修改测试夹具和研发中心治理记录；未访问服务器、数据库、市场数据、原始数据、生产服务、凭据或真实交易。

## 已知限制

本任务只补齐任务115交付期看板测试基线，不提供成本证据，不改变研究准入或交易许可状态。

## 回滚方式

回滚本任务PR即可恢复旧看板测试夹具，不触碰任务115/116/106合同、状态或历史批次。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000120-board-fixture-v1`
- 开始时间：`2026-08-15T03:14:50+08:00`
- 实现提交SHA：`db03dab2ea4fc6b53a5aa7c5243739165392c57f`
- Pull Request：[#342](https://github.com/xk320/zhishi/pull/342)
- 交付物：任务115待执行看板合成基线、任务120状态记录、研发中心看板迁移。
- 验证结果：研发中心专项167项通过；Python编译通过；变更Markdown 2份为0 issues；`git diff --check`通过。
- 已知限制：不改变任务115/106阻塞、研究准入或交易许可；不提供成本或延迟证据。
- 数据与安全影响：仅内存合成测试文本和治理文档，无服务器、数据库、原始数据、凭据、生产或真实交易访问。
- 回滚方式：回滚PR #342，恢复旧测试夹具，不触碰任务115/116/106合同及历史证据。
