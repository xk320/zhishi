# 任务-000094：完成新正式输入逐行时间与质量审计

<!-- markdownlint-disable MD013 -->

- 状态：阻塞
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“公开可见时间冻结与单次全量逐行质量扫描”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；只读扫描任务-000093正式输入并生成追加式时间质量批次；不得再次要求用户选择方案
- 执行分支：`codex/task-000094-time-quality-exec-v1`
- 开始时间：`2026-08-12T08:17:51+08:00`
- 并行规则：禁止并行；任务-000093是唯一前序依赖；本任务完成前不得登记或执行其他阶段1门禁任务

## 依赖与阻塞条件

- 唯一前序依赖：任务-000093已完成，正式批次为`stage1-candidate-recompute-20260811T145500Z-22191bf6b82a`，正式输入指纹为`a1c69d6e2446c364578edb08e6b6fa30912f2e5577d7e51377ef8ab4c648952d`。
- 当前阻塞原因：PR #252双只读评审发现既有执行拓扑与“单进程”资源事实不一致，且最终交付执行器、任务合同指纹与真实批次未绑定同一版本；当前受控交付白名单又不允许审计热路径的`.c`源码，不能合并或把旧批次作为完成证据。
- 解除条件：独立治理任务明确最小`.c`审计源码白名单、固定进程拓扑和整个进程组资源计量合同并合并后，将本任务恢复为`需修复`，用最终交付代码重跑5180成员批次并通过双只读评审。
- 失败安全：任务-000092/000093批次、成员、内容SHA、Schema、官方对象时间或源目录发生漂移时不得发布部分通过结果；对应运行失败关闭，不回退到任务-000084旧路线。

## 背景

任务-000093已经关闭新路线的来源身份与血缘门，但只解析每个成员的首末有效记录。当前5180个正式成员包含约161.22亿行、约796GiB解压数据；三类时间门仍缺公开可见与采集边界，质量门仍缺逐行业务键、重复、乱序、日期和数值合法性证明。BTC逐笔缺8日、ETH逐笔缺199日必须形成明确连续段，不能通过删除分母或跨标的补偿消失。

任务-000092成员已经保存官方ZIP和CHECKSUM对象的`LastModified`，可以作为归档研究的保守最早公开可见时间。本任务在结果可见前冻结时间、业务键和质量规则，用一次全量流式读取同时关闭`ZS-DATA-GAP-002`与`ZS-DATA-GAP-003`中能够由当前正式输入证明的范围。

## 任务目标

1. 冻结事件时间、来源可见时间、采集时间、UTC时区、精度、历史可见性和截止规则；禁止使用本地文件修改时间或复制事件时间补齐。
2. 为`trades`和`aggTrades`冻结业务键、排序、重复、乱序、文件日期、数值和结构规则。
3. 对5180个正式成员执行单进程逐ZIP逐行扫描，保存成员级状态、原因码、计数、时间边界、连续段和指纹，不保存原始业务值。
4. 保留207个来源拒绝成员和2个观察项；BTC、ETH和数据对象分别统计，禁止互相补偿。
5. 重算八个研究叶子的三类时间与质量门；证据不足的叶子保持阻塞，不改变重放、成本、容量、恢复门。
6. 同步阶段1审计报告、数据缺口清单、README和总体计划中的真实最新结论。

## 固定执行方案

1. 从最新`main`冻结任务-000092、任务-000093、正式输入合同、正式批次全部文件、任务合同、配置和执行器指纹。
2. 逐成员从任务-000092来源成员读取官方ZIP与CHECKSUM对象`LastModified`，取较晚值作为`source_visible_at`；缺失、格式错误、早于事件日期结束或对象身份不一致时成员拒绝。
3. 采集时间固定为本次批次开始后、成员内容与身份实际验证完成时的UTC时间；它只证明本项目观察时间，不倒推历史实时到达。
4. `trades`业务键固定为`精确合约 + id`；`aggTrades`业务键固定为`精确合约 + agg_trade_id`。物理顺序必须满足键严格递增且事件时间不回退；跨成员边界按确定性日期顺序继续校验。
5. 每行严格校验列数、整数、有限十进制、正价格、正数量、非负计价数量、布尔枚举、聚合首末编号关系、事件UTC日期和CSV结构；空行、额外列、解析异常和ZIP错误均有固定原因码。
6. 不在内存保存业务键全集；利用已冻结物理顺序和相邻边界检测重复/回退，按成员保存常量大小状态。若发现非单调输入使相邻检测不足以证明全局唯一，成员直接拒绝，不尝试无界集合去重。
7. 按标的和数据对象生成已通过成员连续日期段；来源拒绝、质量拒绝、失败、无法判定、未成熟和失效均保留在原分母，不插值、不补采、不删除。
8. 只依据本任务新批次更新八叶子的时间与质量门；其他六门沿用任务-000093同版本事实。只有证据完整的门可标记通过。

## 默认工程决策

- 使用Python标准库和已有任务-000093列式JSON工具；单进程、逐文件、逐行、常量内存，不新增依赖、数据库或常驻服务。
- 先运行不超过三个冻结小成员的基准预检，只用于确认解析器正确性和估算时间，不据此改变规则、阈值或成员；正式批次仍须覆盖全部5180成员。
- 输出按成员分片，单文件不超过5MiB、总量不超过25MiB；只保存统计、状态、原因码、时间边界、连续段和指纹。
- 可用内存低于20%、进程RSS超过512MiB、输出盘低于5GiB、源盘低于20GiB或总运行超过28800秒时失败关闭。
- 任何质量异常都按执行前固定规则裁决；不得修复原始数据或为了通过而修改阈值。

## 允许停止条件

- 需要修改、移动、删除、修复、覆盖、重新下载或解压落盘原始归档。
- 需要新增真实账号、凭据、付费服务、生产权限、数据库写入或Ubuntu/订单簿系统修改。
- 任务-000092官方对象时间不足以建立归档最早可见边界，且现有合同没有保守失败语义。
- 全量单进程扫描触发28800秒、512MiB、磁盘或25MiB输出硬门，且无法在不改变合同语义的情况下继续。
- 发现必须改变第一阶段研究范围、交易尺度或风险门槛才能宣称通过。

普通实现、文件组织、测试策略、批次命名、异常导致拒绝或无法判定不属于暂停条件，由Codex自动处理。

## 输入合同

- 最新`main`上的`AGENTS.md`、`README.md`、研发中心入口、任务规范、看板、总体计划和全部任务文件。
- 任务-000092最终批次及全部来源成员，精确批次`binance-archive-provenance-20260811T063739Z-7a6da0087493`。
- 任务-000093正式输入合同、配置和批次`stage1-candidate-recompute-20260811T145500Z-22191bf6b82a`的正式输入、成员观察、覆盖、叶子和摘要。
- 固定只读根`/Volumes/data/data/binance/futures/um`中的`trades/BTCUSDT`、`trades/ETHUSDT`、`aggTrades/BTCUSDT`正式成员；禁止扫描白名单外目录。
- 研究标的仅BTC、ETH；主研究尺度仅4小时、8小时、24小时、48小时；15分钟、1小时只作事后观察窗口。

## 输出合同

- `docs/数据/阶段1新正式输入时间与质量合同.md`：三类时间、可见性、业务键、排序、异常、缺日和连续段合同。
- `config/审计/任务-000094逐行时间质量审计.json`：输入批次、固定路径、Schema、规则、状态、资源和安全配置。
- `scripts/审计/审计阶段1新正式输入时间质量.py`及专项测试：确定性解码、逐行验证、成员裁决、连续段和原子发布。
- `artifacts/审计/阶段1逐行时间质量/<唯一批次>/`：摘要、成员分片、连续段、八叶子、异常计数、输入/规则/执行器/输出指纹和资源事实。
- 更新`docs/审计/阶段1最终审计报告.md`、`docs/审计/数据缺口与补采清单.md`、`README.md`和`docs/研发中心/总体计划.md`。
- 本任务文件、看板、PR、双只读评审证据、验证结果、已知限制、数据与安全影响和回滚记录。

## 工作范围

- 只读访问任务-000092/000093冻结证据和本地正式归档。
- 冻结归档历史研究的时间可见性与逐行业务质量规则。
- 完成全量逐行扫描并更新八叶子时间、质量门裁决。

## 不在范围

- 不访问Ubuntu、数据库、订单簿、账户、凭据、生产服务或白名单外网络。
- 不补采、清洗、去重、修复、删除、迁移、覆盖或解压落盘原始数据。
- 不实现历史决策重放、成本执行数据、研究模型、回测、模拟交易或真实交易。
- 不生成因果、预测优势、胜率、收益、方向、仓位、订单或交易许可。
- 不关闭历史重放、成本与执行、容量或恢复门，不宣称阶段1完成。

## 安全边界

- 固定输入全程只读；拒绝符号链接、特殊文件、多成员ZIP、路径越界、内容漂移和执行中变化。
- 单进程、流式、常量内存；不保存或输出价格、数量、成交编号、逐行业务正文或敏感信息。
- 输出只写仓库内新的追加式临时目录并原子发布；不得覆盖既有批次。
- 源目录执行前后清单指纹必须一致；异常、超时或资源硬门触发时不得发布部分成功批次。
- 不执行网络请求、远端命令、DDL、DML、权限变更、账户操作或交易动作。

## 验收标准

1. 5180个正式成员全部进入逐行审计分母，207个拒绝成员和2个观察项完整保留；所有状态计数守恒。
2. 每个成员的事件时间、来源可见时间和采集时间均有带时区、精度、来源、证据定位和内容指纹；本地文件时间未被使用。
3. 每行列数、类型、业务键、排序、重复、事件时间、UTC日期、数值和枚举规则全量执行；未扫描、抽样或有界部分不得记为通过。
4. BTC、ETH、`trades`、`aggTrades`分别生成通过、拒绝、失败和连续段证据；缺8日与199日保留且禁止跨标的/对象补偿。
5. 八叶子的时间与质量门按同版本证据重算；其他硬门和阶段2状态不被提前改变。
6. 真实单进程运行完成，保存扫描行数、解压字节、耗时、RSS、磁盘和源目录前后指纹；资源硬门均满足。
7. 专项、审计、数据、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`均通过。
8. PR经双只读评审、main可信复验和精确头SHA自动合并；随后独立状态闭环将任务标记为`已完成`。

## 验证命令

```bash
python3 -m unittest tests/审计/test_审计阶段1新正式输入时间质量.py -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/审计阶段1新正式输入时间质量.py tests/审计/test_审计阶段1新正式输入时间质量.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须运行一次真实、串行、全量、只读的5180成员逐行审计，保存批次身份、成员与连续段状态、八叶子裁决、规则/执行器/输入/输出指纹、资源事实和源目录前后不变证明。

## 完成定义

时间质量合同、配置、执行器、专项测试和真实全量不可变批次已在独立PR交付；5180个正式成员逐行审计状态与207个拒绝成员完整可复算；时间与质量门只在证据充分范围内更新，其他硬门保持保守；双只读评审、main可信复验和自动合并完成，随后独立状态闭环将本任务标记为`已完成`。

## 数据与安全影响

本任务预计顺序读取约154GiB压缩归档和约796GiB解压流，不复制源数据、不解压落盘；仅新增不含业务行的脱敏批次，预计小于25MiB。未访问Ubuntu、数据库、订单簿、账户、凭据、生产系统或网络，未修改原始数据和权限。

## 已知限制

官方对象`LastModified`证明公开归档的最早可见边界，不证明实时推送到达延迟；本任务只能放行遵守公开时间截止的归档历史研究。缺日和质量拒绝成员会缩小预先规则确定的连续可用段，但不得从原始分母删除。历史重放、成本、容量和恢复仍需后继任务。

## 回滚方式

回滚本任务PR新增的合同、配置、执行器、测试、批次和派生文档更新即可；不得删除或覆盖任务-000092、任务-000093、本地归档、历史批次、Ubuntu或数据库。

## 执行记录

- 执行分支：`codex/task-000094-time-quality-exec-v1`
- 开始时间：`2026-08-12T08:17:51+08:00`
- 尝试命令：`python3 scripts/研发中心/验证自动合并资格.py`及任务合同全部串行验证命令。
- 结果：5180成员全量扫描形成候选批次，但PR #252两份独立评审均为REQUEST_CHANGES，合计发现进程资源事实、扩展名边界、合同指纹和执行器指纹四项P1；PR已关闭且未合并。
- 外部证据：PR #252及绑定精确头提交的两份独立只读评审结论。
- 阻塞原因：旧批次没有把最终交付代码、冻结合同和完整进程组资源事实绑定到同一版本，且当前白名单不能诚实交付实际C热路径。
- 解除条件：最小治理修复进入main后恢复任务并用最终代码重跑、复验和评审。
- 数据与安全：未访问Ubuntu、数据库、订单簿、账户或生产系统；未修改、删除、迁移或修复原始数据，旧批次不作为完成证据。
