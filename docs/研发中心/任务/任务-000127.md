# 任务-000127：修复任务126首次阻塞状态闭环兼容

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：治理
- 阶段：研发中心状态闭环治理
- 优先级：P1
- 执行分支：`codex/task-000127-first-blocking-layout-compat-v1`
- 开始时间：`2026-08-15T11:23:37+08:00`
- 实现提交SHA：`0cab5602d805b051fe6a0acb79c759f33816b594`
- Pull Request：[#366](https://github.com/xk320/zhishi/pull/366)
- 合并时间：2026-08-15 11:35:46 +0800
- 合并提交SHA：`4af4d1b4e2a54fba5871b15051e76058da49f10f`
- 执行方案：方案A“单任务首次阻塞布局兼容”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；任务-000125已完成后执行，仅服务任务-000126首次阻塞，不迁移其他任务状态

## 依赖与阻塞条件

- 唯一前序依赖：任务-000125已完成并进入main。
- 当前问题：任务-000126登记合同未预置`当前阻塞原因`字段，首次`待执行→阻塞`状态闭环无法通过可信布局解析。
- 解除条件：可信规则仅对任务-000126首次阻塞合成缺失字段布局，且八字段阻塞记录、看板派生和合同不可变检查全部通过。
- 阻塞条件：需要放宽其他任务、恢复流程、研究准入、交易许可、数据指纹或生产安全门时立即停止。

## 背景

任务-000126因任务-000124候选文档未进入main而需要如实阻塞，但通用首次阻塞校验要求基线已有阻塞字段，和该登记任务的最小合同发生兼容缺口。

## 任务目标

1. 为可信资格器增加仅绑定任务-000126的首次阻塞布局兼容。
2. 保留八字段结构化阻塞记录、旧合同正文不可变、看板唯一映射和失败关闭。
3. 增加正向与越权负向测试，确保其他任务不能复用该例外。

## 固定执行方案

1. 在可信资格器中固定任务127→任务126映射和一次性目标编号。
2. 仅当基线任务126为`待执行`且无执行元数据、头部新增合法分支/开始时间/阻塞原因/解除条件及八字段记录时允许验证。
3. 错任务、状态迁移、合同改写、额外路径、缺字段、非八字段记录和其他任务复用均失败关闭。

## 默认工程决策

- 复用现有状态闭环函数和资源限制，不新增依赖。
- 只改`验证自动合并资格.py`及其研发中心测试；不改任务126合同、四个输入JSON或数据文件。
- 只读验证，不访问服务器、数据库、市场数据、凭据、生产或交易系统。

## 允许停止条件

- 需要修改任务126合同正文或历史批次。
- 需要允许其他任务复用首次阻塞例外。
- 任何测试只能通过删除阻塞字段、放宽看板或绕过可信资格器。

## 输入合同

- 任务-000126基线合同、阻塞状态PR和现有可信状态闭环规则。
- `tests/研发中心/test_验证自动合并资格.py`中的状态迁移夹具。

## 输出合同

- 一次性任务126首次阻塞兼容规则及正负向测试。
- 任务127执行记录、验证结果和看板状态元数据。

## 工作范围

- `scripts/研发中心/验证自动合并资格.py`、对应研发中心测试、任务127执行记录和看板状态元数据。

## 不在范围

- 不修改任务126、任务124、任务125合同或四个冻结输入JSON。
- 不修改历史批次、原始数据、服务器、数据库、生产服务、凭据或交易逻辑。

## 安全边界

- 例外只绑定任务-000126首次`待执行→阻塞`，不允许恢复、完成、取消或合同修订。
- 阻塞记录必须保留执行分支、开始时间、命令、结果、外部证据、阻塞原因、解除条件和数据安全八字段。
- 不使用未来数据，不改变研究准入、交易许可或真实交易关闭。

## 验收标准

1. 任务126首次阻塞状态闭环可被可信规则复算。
2. 其他任务、缺字段、额外路径、合同改写和非八字段记录继续失败关闭。
3. 研发中心资格/工作流/看板测试、Python编译、MarkdownLint和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_任务看板模式.py -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

一次性规则进入main并经可信复验；任务126阻塞PR随后重新验证，不在本任务中伪造解除或完成。

## 已知限制

本任务不补齐数据，不解决任务124的冻结输入指纹本身，只修复首次阻塞状态解析兼容。

## 回滚方式

回滚本任务PR即可移除任务126专用兼容规则，保留任务合同和历史证据。

## 数据与安全影响

仅修改研发中心治理脚本、测试和任务状态元数据；不访问服务器、数据库、原始市场数据、凭据、生产或真实账户。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双只读评审与可信合并。
- 执行分支：`codex/task-000127-first-blocking-layout-compat-v1`
- 开始时间：`2026-08-15T11:23:37+08:00`
- 实现提交SHA：`0cab5602d805b051fe6a0acb79c759f33816b594`
- Pull Request：[#366](https://github.com/xk320/zhishi/pull/366)
- 交付物：任务-000126首次阻塞依赖章节布局兼容、固定目标分支硬绑定及正负向测试。
- 验证结果：资格测试123项通过；Python编译、MarkdownLint（任务127与看板0 issues）和`git diff --check`通过。
- 已知限制：不更新任务-000126冻结输入JSON；任务-000124审计/数据12项失败仍保持独立阻塞。
- 数据与安全影响：未访问服务器、数据库、原始市场数据、凭据、生产或真实交易系统。
- 回滚方式：回滚PR #366即可移除任务-000126专用兼容入口，保留任务合同和历史证据。
