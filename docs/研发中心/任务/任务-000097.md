# 任务-000097：修复合同修复源任务历史元数据误绑定

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“已完成源任务历史元数据窄豁免与真实PR回归”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；用户已批准以任务-000092新候选集替代任务-000084旧路线并要求自动推进，不得再次要求选择方案
- 自动合并范围：治理自动化
- 执行分支：`codex/task-000097-source-metadata-fix-v1`
- 开始时间：`2026-08-12T13:45:42+08:00`
- 实现提交SHA：`81cfdf77e8d0c8aca0a7a7113e1e46bc94d12a9c`
- Pull Request：[#262](https://github.com/xk320/zhishi/pull/262)
- 合并提交SHA：`17361aa768cd386b871f27032e79ef863ce60d69`
- 合并时间：2026-08-12 05:54:39 +0000
- 并行规则：禁止并行；本任务是任务-000094合同修复PR进入可信合并流程前的确定性治理阻塞，完成前不得合并或执行任务-000094
- 一次性领取顺序：依据PR #260真实可信运行暴露的确定性硬阻断，本任务仅本次先于任务-000094领取；完成后立即恢复全局普通优先级和编号排序，不得复用

## 依赖与阻塞条件

- 唯一前序依赖：任务-000096已完成；任务-000095已完成并在main建立任务-000095→任务-000094固定合同修复映射。
- 当前阻塞原因：无。PR #260、可信运行`31566514106`、任务-000095历史合同和现有任务-000086窄豁免均可复验；本任务不依赖服务器、数据库、市场数据、凭据或生产权限。
- 目标任务阻塞证据：PR #260只修改任务-000094且固定八段转换、208项研发中心测试和文档检查均通过，但main可信运行把已完成任务-000095的历史执行分支与PR #255错误绑定到当前PR #260，确定性返回`PR_BASELINE_DRIFT`。
- 失败安全：若修复需要放宽到未知任务、未完成源任务、普通任务交付、任意分支或PR编号，或需要改变任务-000094合同与看板状态，则停止；只允许固定任务-000095→任务-000094入口使用历史元数据语义。

## 背景

任务-000095已完成，其执行分支和Pull Request是不可改写的历史交付证据。后续PR #260以任务-000095作为固定治理授权，只修改任务-000094合同。自动合并资格层已正确验证源任务逐字不变和已完成状态，但跨载体冲突层仍把源任务的历史分支与PR绑定到当前修复PR，导致正确交付被错误拒绝。任务-000086→任务-000084入口已有同类窄豁免，任务-000095映射缺少等价实现与真实GitHub元数据回归测试。

## 任务目标

1. 为固定`任务-000095→任务-000094`合同修复入口增加已完成源任务历史执行元数据窄豁免。
2. 继续要求任务-000095状态为`已完成`，且历史执行分支、开始时间和Pull Request字段完整。
3. 不把任务-000095历史分支或PR #255绑定到当前任务-000094修复PR的头分支和PR编号。
4. 新增携带真实`head_ref`与`pr_number`的端到端正向测试，并覆盖错误状态、缺失历史元数据、未知映射和普通任务仍失败关闭。
5. 修复合并并闭环后，回到PR #260重新基于最新main验证和双评审。

## 固定执行方案

1. 复用任务-000086已完成源任务的历史元数据校验模式，不创建通用豁免框架。
2. 将窄豁免同时绑定变更类型`任务合同冲突修复`和执行任务编号`000095`；其他变更类型与任务继续使用当前严格绑定。
3. 只豁免“历史分支/PR必须等于当前PR”这一不成立的要求；已完成状态及分支、开始时间、PR字段非空继续硬校验。
4. 补充跨载体单元测试与自动合并资格真实Git仓库CLI测试，元数据必须包含当前目标分支和PR编号。
5. 运行研发中心全量单进程测试、Python编译、MarkdownLint和`git diff --check`，经双只读评审与main可信复验后自动合并。

## 默认工程决策

- 采用最小条件分支和现有错误码，不抽象通用授权语言，不新增依赖或工作流。
- 测试夹具复用main中的任务-000094合同双态辅助函数和固定映射常量。
- 当前PR元数据使用与历史任务元数据不同的分支和PR编号，确保回归测试覆盖真实故障。
- 普通命名、断言拆分和测试位置由Codex按最小改动自主决定。

## 允许停止条件

- main中任务-000095不是已完成或其历史执行分支、开始时间、PR字段不完整。
- 必须扩大到任务-000095→任务-000094之外的任务、变更类型或元数据字段。
- 必须修改任务-000094合同、看板状态、生产系统、服务器、数据库、数据或权限。
- 修复无法同时证明正向PR #260场景通过以及未知/普通场景继续失败关闭。

除上述条件外，不得因普通测试组织、命名或实现细节暂停询问。

## 输入合同

- 最新`main`中的任务-000094、任务-000095、任务-000096、任务规范、看板和自动合并治理文档。
- `scripts/研发中心/验证跨载体冲突.py`、`scripts/研发中心/验证自动合并资格.py`及对应研发中心测试。
- PR #260脱敏GitHub元数据与可信运行`31566514106`失败代码；不读取凭据或外部数据正文。

## 输出合同

- 更新`scripts/研发中心/验证跨载体冲突.py`，实现固定源任务历史元数据窄豁免。
- 更新`tests/研发中心/test_验证跨载体冲突.py`和必要的自动合并资格集成测试，覆盖真实`head_ref`与`pr_number`。
- 必要时更新`docs/治理/PR自动合并策略.md`，只说明固定映射的历史元数据语义。
- 更新本任务文件和`docs/研发中心/看板.md`的执行与交付元数据。

## 工作范围

- 固定任务-000095→任务-000094合同修复入口的跨载体元数据校验和回归测试。
- 与上述窄修复直接相关的治理文档、任务和看板记录。

## 不在范围

- 不修改任务-000094、任务-000095或其他任务合同。
- 不修改质量审计执行器、扫描器、配置、批次证据或研究结论。
- 不访问Ubuntu、数据库、市场接口、原始数据、账户、凭据或生产系统。
- 不实现模型、回测、模拟交易或真实交易。

## 安全边界

- 可信控制面仍从main执行；PR头内容仅作为不可信Git对象验证。
- 窄豁免只接受固定变更类型和执行任务编号，并继续复算源任务逐字不变及已完成状态。
- 不降低任务-000094的八段逐字转换、目标状态、看板不变、路径、资源、评审或精确SHA硬门。
- 全部测试单进程，Node堆不超过256MiB，不创建评审工作树，不提交敏感信息。

## 验收标准

1. PR #260等价元数据场景不再把任务-000095历史分支和PR绑定到当前目标PR。
2. 任务-000095非已完成，或缺少历史分支、开始时间、PR任一字段时失败关闭。
3. 未知任务、普通任务交付、错误变更类型和任务-000094额外改动继续失败关闭。
4. 真实Git基线/头CLI测试携带`head_ref`和`pr_number`并证明正向通过、负向拒绝。
5. 研发中心全部测试、Python编译、MarkdownLint与`git diff --check`通过。
6. PR经两个独立只读评审、main可信复验和精确头SHA自动合并；独立状态闭环后任务标记`已完成`。

## 验证命令

```bash
python3 -m unittest tests.研发中心.test_验证跨载体冲突 -q
python3 -m unittest tests.研发中心.test_验证自动合并资格 -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证跨载体冲突.py tests/研发中心/test_验证跨载体冲突.py tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 'docs/研发中心/任务/任务-000097.md' 'docs/superpowers/specs/2026-08-12-task-000097-source-metadata-binding-design.md' 'docs/研发中心/看板.md'
git diff --check
```

## 完成定义

固定任务-000095已完成源任务历史元数据窄豁免及真实PR回归测试进入main；双评审、可信复验、自动合并和独立状态闭环完成；随后PR #260在最新main上重新验证并通过可信合并，不得把本治理任务解释为任务-000094数据审计完成。

## 数据与安全影响

仅修改仓库治理校验与测试，不读取或修改真实数据，不访问Ubuntu、数据库、订单簿、账户、凭据或生产系统，不生成任何交易结论。

## 已知限制

本任务只修复任务-000095→任务-000094固定入口，不建立通用历史元数据豁免。任务-000094仍须在合同修复合并后使用最终代码重跑真实全量批次。

## 回滚方式

回滚本任务交付PR即可恢复旧严格绑定；PR #260会再次被错误拒绝，但不会修改数据、服务器、生产系统或任务-000094合同。

## 执行记录

- 交付物：任务-000095已完成源任务历史元数据窄豁免；三组跨载体正负测试；携带真实当前分支和PR编号的Git CLI回归；治理策略说明。
- 验证结果：跨载体与自动合并资格定向测试通过；研发中心211项测试通过；Python编译、MarkdownLint和`git diff --check`通过。
- 已知限制：只修复固定任务-000095→任务-000094入口；任务-000094仍须重跑最终全量批次。
- 数据与安全：未访问服务器、数据库、市场数据、账户、凭据或生产系统，未修改任务-000094合同或任何研究结论。
- 需要人工决策：无。
