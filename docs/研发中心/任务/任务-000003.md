# 任务-000003：发现运行环境与数据资产，建立可验证数据源清单

- 状态：已完成
- 优先级：P0
- 讨论关联：GitHub Issue #4（非任务事实来源）
- 建议分支：`agent/000003-data-discovery`
- 执行分支：`codex/000003-access-blocker-20260727`
- 开始时间：2026-07-28 09:03:47 +0800
- 完成实现时间：2026-07-28 09:37:41 +0800
- 安全复验完成时间：2026-07-28 19:40:22 +0800
- 合并完成时间：2026-07-28 21:34:10 +0800
- 最新实现提交 SHA：`6c996aa5c5e641a49eb6b0a2f977a3780a41727e`
- Pull Request：[#23](https://github.com/xk320/zhishi/pull/23)
- 合并提交 SHA：`3b80179191c0ec20fa5df5cb865753d8c215add0`
- 解除阻塞证据：SSH别名`ubuntu`可使用本机既有身份完成无交互登录；目标环境返回
  Linux系统信息和`+0800`时区，未修改远端状态。

## 任务目标

只读发现BTC、ETH、SOL相关服务器、数据库、目录、接口及存储资源，形成可重复执行的数据源清单。

## 主要交付物

- `docs/审计/数据源清单.md`
- `artifacts/审计/数据源清单.csv`
- `scripts/审计/发现数据资产.py`
- `tests/审计/test_发现数据资产.py`

## 解除阻塞条件

服务器和SSH服务正常，且Codex能够使用本机既有只读身份完成SSH握手并进入目标环境。
不得为了解除阻塞自动修改权限、防火墙、SSH配置或生产配置。

## 本次执行边界

- 只允许通过SSH别名`ubuntu`执行固定只读探针。
- 不读取候选数据文件内容、数据库业务记录、配置凭据或环境变量。
- 不修改远端文件、服务、权限、防火墙、数据库或生产配置。

## 执行记录

### 交付物

- `scripts/审计/发现数据资产.py`
- `tests/审计/test_发现数据资产.py`
- `artifacts/审计/数据源清单.csv`
- `docs/审计/数据源清单.md`
- `docs/superpowers/specs/2026-07-28-data-asset-discovery-design.md`
- `docs/superpowers/plans/2026-07-28-data-asset-discovery.md`

### 验收与验证

- 真实只读发现：通过。批次`discovery-20260728T194022+0800`生成388条资产记录，
  包含223个候选数据文件、92项数据库元数据、36个挂载、20个监听接口、9个相关
  运行服务、6个白名单根目录状态、1项运行环境及1项探针限制。
- 输出合同：通过。CSV共16列，388个资产编号唯一且连续；CSV与Markdown使用同一
  发现批次，所有时间范围保持`未知`，未从文件名或资源存在猜测数据质量。
- 标的映射：发现53项明确BTC资产及2项BTC、ETH共同资产；没有发现名称或路径明确
  映射SOL的资产。该结果不证明SOL数据不存在，只登记本次白名单元数据发现范围。
- 数据库边界：通过。所有远端命令使用显式清洁环境；MySQL关闭默认配置和登录路径
  文件，固定本地Unix socket，只查询`information_schema.TABLES`，未读取业务记录。
- 远端只读边界：通过。挂载、Docker与LXD只请求显式安全列；目录遍历不跟随符号
  链接；未读取候选文件内容、容器环境变量、完整配置或凭据。
- 失败安全：通过。SSH失败、非法JSON、目录权限不足、命令缺失、第二产物发布失败和
  回滚自身失败均有测试；回滚失败时保留两个旧产物备份，不删除恢复证据。
- 可信边界：通过。部署副本和明确样例文件被排除；冲突重复项不依赖输入顺序且不
  选择任一值；CSV公式前缀被安全转义；目录、符号链接和错误扩展名不能作为输出目标。
- 自动化回归：研发中心22项测试通过，审计发现21项测试通过，0失败、0错误。
- Markdown检查：设计、计划、数据源清单、任务文件和看板共5个文件0 issues。
- 任务中心一致性：20个任务编号唯一、状态合法，且各自在看板中唯一映射。
- IPv4、私钥、令牌、云密钥和明文凭据模式扫描：0命中；`git diff --check`通过。
- 独立复核发现的失败回滚、确定性、样例排除、CSV安全和文档事实问题均已补充回归
  测试或修正文档；最终复验以本节命令和Pull Request最新头提交为准。

### 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m py_compile \
  scripts/审计/发现数据资产.py tests/审计/test_发现数据资产.py
python3 scripts/审计/发现数据资产.py --target ubuntu \
  --csv-output artifacts/审计/数据源清单.csv \
  --markdown-output docs/审计/数据源清单.md
npx --yes markdownlint-cli2 \
  docs/superpowers/specs/2026-07-28-data-asset-discovery-design.md \
  docs/superpowers/plans/2026-07-28-data-asset-discovery.md \
  docs/审计/数据源清单.md \
  docs/研发中心/任务/任务-000003.md \
  docs/研发中心/看板.md
git diff --check origin/main...HEAD
```

另执行CSV/Markdown批次、列、编号、状态和任务中心映射的只读合同检查，以及IPv4、
私钥、令牌、云密钥、明文凭据和敏感路径的PCRE2扫描。

### 已知限制

- 本任务只发现候选资源及元数据，不验证缺失、重复、乱序、断档、异常、三类时间、
  数据完整性、研究可用性或历史可重放性；这些结论由任务-000004至000006继续验证。
- LXD客户端在目标环境无法使用，清单明确记录为`无法判定`；未提升权限或扩大访问。
- 当前名称与路径未明确映射SOL资产，不得用BTC、ETH结果外推SOL，也不得据此断言
  SOL数据不存在。
- 数据源清单是批次快照；服务、文件和数据库元数据变化后必须重新运行发现器。
- CSV和Markdown位于不同目录，无法形成跨两个文件的单次文件系统原子提交。实现可在
  可捕获异常下回滚并在回滚失败时保留备份；`SIGKILL`或断电后仍须核对两者批次一致。

### 数据与安全影响

- 远端仅执行固定只读命令和白名单`stat`扫描，未写入服务器、数据库或原始数据。
- 未读取候选文件内容、数据库业务记录、环境变量、完整容器配置、配置凭据或密钥。
- 未修改生产服务、权限、防火墙、SSH配置或数据库认证，未启停或重启服务。
- 未访问真实交易账户，未执行交易、模型训练或回测，未使用未来数据。
- 未生成或声明数据质量、胜率、收益、研究准入或交易许可结论。

### 合并结果

- 仓库所有者明确要求人工合并Pull Request #23；合并前复核当前头提交、检查结果和
  无冲突状态，已通过普通merge commit合入`main`，任务状态据此更新为`已完成`。

### 自动合并资格

- 不符合。基线任务缺少允许类型，且本PR包含Python脚本、测试和CSV产物，必须人工
  合并。
