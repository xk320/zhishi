# 任务-000003：发现运行环境与数据资产，建立可验证数据源清单

- 状态：阻塞
- 优先级：P0
- 讨论关联：GitHub Issue #4（非任务事实来源）
- 建议分支：`agent/000003-data-discovery`
- 最近阻塞复核分支：`codex/000003-access-blocker-20260727`
- 最近阻塞复核时间：2026-07-27 23:45:04 +0800
- 阻塞原因：目标主机网络端口有响应，但SSH连接在密钥交换前由远端关闭，
  Codex无法通过已配置方式进入目标环境。

## 任务目标

只读发现BTC、ETH、SOL相关服务器、数据库、目录、接口及存储资源，形成可重复执行的数据源清单。

## 主要交付物

- `docs/审计/数据源清单.md`
- `artifacts/审计/数据源清单.csv`
- `scripts/审计/发现数据资产.py`
- `tests/审计/test_发现数据资产.py`

## 解除阻塞条件

服务器和SSH服务正常，且Codex能够使用本机既有只读身份完成SSH握手并进入目标环境。
不得为了解除阻塞自动修改权限、防火墙、SSH配置或生产配置。

## 最近阻塞复核

2026-07-27只读复核：

- 本机存在`btc-prod`与`btc-event-prod`连接配置，所引用身份文件存在；
- 两个目标的22端口均建立了网络连接；
- 两个目标均在SSH密钥交换前关闭连接，退出码为255，未进入认证或远端命令阶段；
- 未列出远端目录，未读取服务器、数据库、市场数据或账户数据；
- 未生成数据源清单、CSV、发现脚本或审计结论。

解除时应由环境维护者恢复SSH服务或允许既有只读访问路径。恢复后先以无交互只读握手
复验，再将任务从`阻塞`转为`待执行`或`执行中`。
