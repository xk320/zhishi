# 任务-000102：授权任务-000100唯一输出合同修复

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“固定目标合同修复授权清单”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；只生成仓库内治理授权清单，不得再次要求用户选择方案
- 并行规则：禁止并行；本任务完成并进入main前不得创建任务-000100目标合同修复PR
- 执行分支：`codex/task-000102-contract-repair-authorization-v1`
- 开始时间：`2026-08-12T23:46:11+08:00`
- 合并时间：2026-08-12 23:57:11 +0800
- 合并提交SHA：`dabb2452ce86e055a298a81f155a0f79906cd360`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000101已完成；
- 当前阻塞原因：无。任务-000101已通过PR #275交付并通过独立状态闭环进入main。
- 失败安全：若固定旧/新条目、任务编号、目标状态或禁止路径与main可信规则不一致，必须停止，不得近似匹配或扩大授权。

## 背景

任务-000101已在main建立一次性可信映射。为保持治理授权源与目标修复PR分离，本任务只生成
可复算的固定授权清单并先完成状态闭环；后续独立PR才可逐字修复任务-000100唯一输出条目。

## 任务目标

1. 固定执行任务-000102、目标任务-000100和治理任务-000101的身份与完成顺序。
2. 固定唯一旧/新输出条目及其SHA-256，不复制或改写目标任务其他合同。
3. 固定目标基线/头部状态均为待执行、看板不变和只允许单一目标任务文件路径。
4. 固定历史审计、旧配置、旧批次、原始数据、服务器和生产路径禁止夹带。

## 固定执行方案

1. 从最新main读取任务-000101、任务-000100、AGENTS、自动合并策略和两个可信校验器。
2. 新增`docs/治理/任务-000100输出合同修复授权.md`，记录固定身份、旧/新全文、内容指纹、顺序和失败安全边界。
3. 运行研发中心定向测试、MarkdownLint、敏感扫描和`git diff --check`。
4. 双评审、可信资格、自动合并及独立状态闭环通过后，才创建目标合同修复PR。

## 默认工程决策

- 授权清单为普通UTF-8 Markdown，不新增脚本、依赖、工作流或通用合同编辑器。
- 旧/新条目必须与main可信常量逐字相等，SHA-256使用UTF-8字节计算。
- 普通格式和验证命令由Codex自主决定。

## 允许停止条件

- 任务-000101未完成或可信规则没有固定任务-000102到任务-000100映射。
- 任务-000100已不再处于待执行，或固定旧条目不唯一。
- 需要修改任务-000100、看板、历史证据、服务器、数据、生产系统或权限。

## 输入合同

- 最新main中的任务-000101、任务-000100、AGENTS.md和`docs/治理/PR自动合并策略.md`。
- `scripts/研发中心/验证自动合并资格.py`和`scripts/研发中心/验证跨载体冲突.py`中的固定常量与规则指纹。

## 输出合同

- 新增`docs/治理/任务-000100输出合同修复授权.md`。
- 更新本任务文件、看板、PR证据和验证记录。
- 不修改任务-000100或任何可信脚本；目标修复留给后续独立PR。

## 工作范围

仅限固定治理授权清单、本任务文件和派生看板。

## 不在范围

- 不在本任务PR修改任务-000100。
- 不访问Ubuntu、数据库、网络、账户、凭据、市场数据或生产系统。
- 不改写历史审计文件、旧配置、旧批次或原始数据。
- 不训练、回测、模拟下单或产生交易结论。

## 安全边界

全部输入来自main仓库；所有固定值须逐字复算。授权清单不授予服务器、数据、生产、交易或
凭据能力，也不能替代目标合同修复PR的双评审和可信资格。

## 验收标准

1. 任务-000101已完成；任务-000102唯一依赖为任务-000101。
2. 授权清单的三个任务身份、固定旧/新条目及SHA-256与main可信规则一致。
3. 清单明确目标双端待执行、看板不变、单文件路径和源任务先完成边界。
4. 历史证据、旧批次、数据、服务器、生产与权限路径均无变更。
5. 定向测试、MarkdownLint、敏感扫描和`git diff --check`通过。
6. 双评审、自动合并和独立状态闭环完成。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_验证自动合并资格.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_验证跨载体冲突.py' -q
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 docs/治理/任务-000100输出合同修复授权.md docs/研发中心/任务/任务-000102.md docs/研发中心/看板.md
git diff --check
```

## 完成定义

固定授权清单已进入main，双评审和状态闭环完成；只表示目标修复获得一次性可信授权，不表示
任务-000100已修复、PR #273已恢复或阶段1完成。

## 数据与安全影响

仅仓库治理文档；不访问或修改服务器、数据库、市场数据、历史证据、账户、凭据或生产系统。

## 已知限制

本任务不修改目标合同。任务-000100修复后，PR #273仍须同步最新main并重新生成与验证最终证据。

## 回滚方式

回滚本任务交付和状态闭环PR即可移除授权清单；若目标修复已使用授权，须另行回滚目标修复，
禁止改写Git历史。

## 执行记录

- 实现提交SHA：`5d7f549b9abdfddef573524ce76cb3fb1f19abc5`
- Pull Request：[#278](https://github.com/xk320/zhishi/pull/278)
- 交付物：固定任务身份、唯一旧/新条目、SHA-256、四步顺序和失败安全的治理授权清单；目标修复时现场复验main可信规则。
- 验证结果：资格定向110项、跨载体定向29项、授权身份与SHA复算通过；MarkdownLint、敏感扫描和`git diff --check`通过。
- 已知限制：任务-000100合同和PR #273尚未恢复，阶段1尚未完成。
- 数据与安全：仅治理文档；未访问Ubuntu、数据库、网络、市场数据、账户、凭据或生产系统。
- 需要人工决策：无。
