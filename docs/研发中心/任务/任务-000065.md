# 任务-000065：修复自定义合并主题的可信合并事实复算

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 合并时间：2026-08-05 16:11:36 +0800
- 合并提交SHA：`d664423872ff203aa30eb605754a520aaa5ebdd7`
- 执行分支：`codex/000065-merge-fact-verification-v1`
- 开始时间：`2026-08-05T15:56:11+08:00`
- 类型：治理
- 阶段：阶段1 研发中心可信交付治理
- 优先级：P0
- 自动合并范围：治理自动化
- 执行方案：方案A“保留精确SHA并增加双父提交与交付头祖先校验”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000013完成后执行；不得与其他可信自动合并治理任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000013
- 依赖事实：PR自动合并策略已进入`main`；任务-000064的PR #154留下了可复现的自定义合并主题兼容性阻断。
- 当前阻塞原因：无；本任务只修改仓库内可信治理脚本和测试，不访问服务器、数据库或真实市场数据。
- 解除条件：必须保留来源仓库、目标`main`、非草稿、精确头SHA、双只读评审和CI检查等既有硬门；任一结构事实缺失即失败关闭。

## 背景

PR #154的合并提交是双父提交，且已进入`main`，但提交主题由自动化调用显式覆盖，未采用
`Merge pull request #N from ...`标准主题。现行可信校验器因此无法从`main`推导任务-000064的
合并事实。不能通过改写任务记录或猜测补齐。需要补充一种可复算、失败安全且不降低来源和头SHA
约束的结构化推导方式，随后重新验证任务-000064状态闭环。

## 任务目标

1. 保留标准`Merge pull request #N from ...`主题的既有推导路径和严格拒绝行为。
2. 对自定义主题只在合并提交恰有两个父提交、精确合并SHA已进入基线、任务记录中的交付提交SHA是第二父提交祖先、且提交内容与任务PR范围一致时推导合并事实。
3. 明确拒绝单父提交、父提交缺失、交付头不在第二父链、SHA不在基线、PR号缺失或任务合同不完整的情况。
4. 增加回归测试和设计说明，使任务-000064的真实合并提交可验证，不把结构校验扩展为信任聊天或自填字段。

## 固定执行方案

1. 在`main`可信脚本中增加最小结构校验函数，读取Git提交父列表和任务执行记录中的交付提交SHA。
2. 仅在标准主题无法匹配时进入兼容路径；兼容路径不得跳过精确SHA祖先、双父和交付头祖先校验。
3. 用临时Git仓库测试标准主题、合法自定义主题和每个失败分支；不访问网络、服务器或生产环境。
4. 更新治理设计文档和测试，运行全部研发中心验证后提交PR；不直接修改`main`可信脚本。

## 默认工程决策

- 复用Python标准库和既有`MergeFact`结构，不引入依赖、GitHub API调用、常驻服务或数据库。
- 结构事实优先于提交文案；文案不匹配不能自动放宽为通过，必须同时满足父链和交付头证据。
- 任务合同仍是唯一事实来源；Issue、评论、聊天和PR正文不能替代任务文件中的交付SHA或PR号。
- 不改变数据研究范围：BTC、ETH及4小时、8小时、24小时、48小时主研究尺度保持不变。

## 允许停止条件

- 需要访问服务器、数据库、真实市场数据、生产系统、权限或账户；
- 需要凭据、外部网络查询、修改原始数据、重写`main`历史或降低精确头SHA/来源硬门；
- 无法从任务合同和Git对象确定交付提交SHA、父提交关系或PR号；
- 修复会把任意单父提交、非祖先提交或无任务合同的提交解释为已合并。

## 输入合同

- `AGENTS.md`、README、研发中心入口、任务规范、全部任务文件和看板；
- `scripts/研发中心/验证自动合并资格.py`及其测试；
- 任务-000013自动合并策略、任务-000064合同、PR #154合并提交和PR #155失败校验证据；
- 本地Git对象中的基线、交付头、父提交列表和提交时间。

## 输出合同

- 更新后的`scripts/研发中心/验证自动合并资格.py`及最小回归测试；
- `docs/superpowers/specs/merge-fact-verification-v1-design.md`；
- 更新后的任务-000065、看板、PR、双只读评审记录和验证结果。

## 工作范围

- 设计并实现标准主题与自定义主题的双路径合并事实复算；
- 校验双父提交、交付提交祖先、精确SHA、任务PR号和基线祖先关系；
- 补充拒绝分支测试、治理文档和研发中心验证。

## 不在范围

- 不修改Git历史、不重写或强推`main`；
- 不访问服务器、数据库、真实市场数据、生产系统或账户；
- 不改变任务状态流转、数据研究合同、风险预算或阶段2门禁；
- 不以任务-000064的自定义主题阻断为理由伪造合并事实，不修改原始证据。

## 安全边界

- 只读本地Git对象和仓库文件；输出不得包含凭据、IP、域名、原始数据或敏感日志。
- 兼容路径必须失败安全：父关系、祖先关系、SHA、PR号和任务合同任一不满足即拒绝。
- 只有任务合同明确`自动合并范围：治理自动化`时，才允许修改可信自动合并脚本；本任务不修改其他工作流权限。

## 验收标准

1. 标准主题路径行为不变；合法双父自定义主题且交付头祖先关系完整时可推导唯一合并事实。
2. 单父、三父、交付头不在第二父链、SHA不在`main`、缺PR号和缺任务交付SHA的样例全部拒绝。
3. 任务-000064的PR #154合并提交可由Git对象和任务合同复算，不修改其原始合并提交或任务历史。
4. 任务中心可信校验、回归测试、MarkdownLint、敏感扫描、Python编译和`git diff --check`通过。
5. 仍不授予服务器、数据库、市场数据或生产权限，不改变阶段1数据硬门。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/*.py tests/研发中心/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

可信脚本、回归测试和设计文档通过双只读评审、主执行器验证和`main`可信复验后合并；随后以独立
状态闭环PR标记任务-000065为`已完成`。本任务只修复合并事实复算，不自动标记任务-000064完成；
任务-000064必须在修复进入`main`后重新创建状态闭环PR并重新评审。

## 执行记录

- 执行分支：`codex/000065-merge-fact-verification-v1`
- 开始时间：`2026-08-05T15:56:11+08:00`
- 交付提交SHA：`60900d5f5247835097c4351bc8a59107ced57657`
- Pull Request：[#157](https://github.com/xk320/zhishi/pull/157)
- 交付物：可信合并事实复算兼容路径、回归测试、设计文档和任务中心状态记录。
- 验证命令与结果：研发中心190项测试通过；Python编译通过；MarkdownLint 156个文件0 issues；`git diff --check`通过。
- 资源与安全事实：仅读取本地Git对象和仓库文件；未访问服务器、数据库、真实市场数据或任何正文，未修改Git历史、原始数据、生产系统、权限、凭据或账户。
- 已知限制：任务-000064尚未重新闭环；兼容路径只接受双父和交付头祖先证据，不改变其他自动合并硬门。
- 需要人工决策：无。
- 回滚方式：回滚PR #157合并提交即可恢复旧复算规则和任务-000065执行中状态。
