# 任务-000036：完成容量试采与隔离恢复演练

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：基础设施验证
- 阶段：阶段1 容量与恢复
- 优先级：P1
- 执行方案：方案A“有界试采测量、双标的容量重算与隔离恢复校验”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领
- 执行分支：`codex/000036-capacity-recovery-v1`
- 开始时间：`2026-08-05T05:00:47+08:00`
- Pull Request：[#119](https://github.com/xk320/zhishi/pull/119)
- 合并时间：2026-08-04 21:38:21 +0000
- 合并提交SHA：`fa062eb0fd0c4b9726a345248921f1b314873d1b`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000035通过评审并合并；
- 当前阻塞原因：无；任务-000035已完成。
- 解除条件：已满足。

## 背景

现有3个月、6个月和12个月容量只是历史三标的规划假设，备份介质、恢复时间、记录数和
内容指纹也未演练。现行BTC、ETH范围必须用真实有界样本重算，不能凭计划宣称磁盘充足。

## 任务目标

对任务-000035试点执行有界只读试采测量，按BTC、ETH现行范围重算容量，并在本地隔离
目录完成可删除副本的恢复演练和指纹核对，不修改服务器、生产数据或原始资产。

## 固定执行方案

1. 冻结试点版本、采样窗口、成员顺序、资源上限和容量公式；
2. 记录原始增长、压缩、索引、质量血缘、日志、峰值和副本开销；
3. 分别计算3个月、6个月、12个月区间及假设敏感性，不给单点伪精度；
4. 在`mktemp`创建的本地隔离目录复制获批试点产物并执行恢复；
5. 核对文件清单、记录数和SHA-256，演练结束后只清理自身临时副本。

## 默认工程决策

优先小样本、串行、流式和硬资源上限；磁盘余量不足时先减少样本并保留不确定区间；
不使用历史三标的配置直接作为现行结果；备份存在但未恢复一律为无法判定。

## 允许停止条件

本机可用空间低于安全余量、无法建立隔离临时目录、需要访问生产备份介质或修改服务器时
停止。试点零成员时完成容量无法判定报告，不虚构增长率或恢复成功。

## 输入合同

- 任务-000035确切试点批次、清单和指纹；
- 当前本机磁盘和内存只读状态；
- 事前冻结的采样窗口、公式、压缩和副本假设；
- BTC与ETH现行范围，3个月、6个月、12个月规划区间；
- 临时目录必须由系统安全创建且只包含本任务副本。

## 输出合同

- `docs/审计/容量试采与隔离恢复报告.md`；
- 更新后的BTC/ETH容量配置、评估器和测试；
- `artifacts/审计/容量恢复/<批次>/`测量、清单、恢复结果和验证报告；
- 输入、公式、环境、成员、输出和恢复指纹；
- 本任务、看板、提交、PR和验证记录。

## 工作范围

执行有界试采、容量区间计算、敏感性分析、本地隔离复制与恢复、清单和指纹复核、资源
监控、失败清理和测试。

## 不在范围

- 不访问或修改生产备份、服务器磁盘、数据库或原始数据；
- 不购买硬件、扩容磁盘或修改系统权限；
- 不把规划容量当作生产容量承诺；
- 不训练模型、回测或生成交易结论。

## 安全边界

开始前检查磁盘和内存；串行执行并设置输入、临时副本、输出、内存和总时长上限；仅删除
任务自身明确验证的临时目录；仓库不保存大体积原始副本、网络地址或凭据。

## 验收标准

1. 真实测量与规划假设分开，公式、单位、区间和版本可复算；
2. BTC与ETH现行范围的3、6、12个月结果不沿用旧三标的总量；
3. 原始、压缩、索引、质量血缘、日志、峰值和副本开销均有证据或无法判定；
4. 隔离恢复核对清单、记录数和SHA-256，失败不能写为通过；
5. 不修改服务器、生产数据和历史批次，临时副本清理范围可证明；
6. 资源上限、低磁盘和中断路径有测试；
7. 测试、Markdown和差异检查全部通过。

## 验证命令

```bash
python3 -m unittest tests/审计/test_评估最小数据闭环.py -v
python3 -m unittest discover -s tests/审计 -p 'test_*容量*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
npx --yes markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
```

## 完成定义

真实试采或零成员无法判定证据、容量报告、隔离恢复结果和验证记录完成后创建引用
任务-000036的PR并更新为`待评审`。只有验证过的恢复才可标记通过；PR合并后标记
`已完成`并解除任务-000037。

## 执行记录

- 执行分支：`codex/000036-capacity-recovery-v1`
- 开始时间：`2026-08-05T05:00:47+08:00`
- 实际批次：`capacity-20260804T212023Z`
- 输入试点：`pilot-20260805T045300+0800-zero-v2`；候选2520（BTC 1260、ETH 1260），合格0，拒绝2520。
- 结果：试点元数据原始1516016字节、gzip-6压缩117358字节；索引和生产日志无法判定；市场记录增长及生产副本容量因零成员无法判定。
- 恢复：本地`mktemp`隔离目录复制五个试点文件，源、备份和恢复SHA-256全部匹配；临时目录已清理；市场记录恢复仍为无法判定。
- 产物：`docs/审计/容量试采与隔离恢复报告.md`、`config/审计/双标的容量恢复.json`、`scripts/审计/执行容量恢复演练.py`、`tests/审计/test_容量恢复.py`及`artifacts/审计/容量恢复/capacity-20260804T212023Z/`。
- 实现提交：`234077e022ee5bcc2e2bb557ee54e9fcf56ef7f9`
- 修复提交链（当前复审基线）：`3074585c9922903bd710178c60a00bcd0f04a0f2` → `7b99409083a00f91be4b526da76e462781e34514` → `7527fe040e7e37186a5d5f662674606b7c9d6ad0` → `a0b45e61ce95df53664f75b697bb45bb6ed98c89` → `f8382f063b74832197a9f62bfafcb8d05179825a` → `87d95e46c896d4e1c2b78079d1aebb0ac85c2d3f` → `f985f518c002f406a9f1485acfa15364afda3ef9`
- 评审修复轮次：3；治理/架构与范围/安全最终评审均APPROVE，P0/P1/P2/P3均为0。
- 验证结果：容量恢复合同测试7项、最小闭环回归6项、研发中心回归177项、Markdown lint 132个文件、`git diff --check`均通过。
- 数据与安全：未访问Ubuntu、服务器、数据库、生产备份、原始市场数据或账户权限；没有修改原始数据、生产服务、权限、账户或凭据。
- 已知限制：所有容量区间均标记为BTC/ETH分别计算的规划假设，不能替代非零成员试采或生产容量结论；任务-000037已解除阻塞但阶段2仍未解锁。
