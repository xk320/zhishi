# 任务-000024：历史事件回放与结果统计体系

- 状态：已完成
- 类型：研究基础设施 / 历史验证
- 阶段：阶段3 市场事件验证阶段
- 优先级：P0
- 开始时间：2026-08-01 23:03:59 +0800
- 完成实现时间：2026-08-01 23:12:14 +0800
- 执行分支：`codex/000024-historical-event-replay-contract-v1`
- 实现提交 SHA：`ab4cf7014f01f6b0a9661ac9442870a6fe0564d1`
- Pull Request：[#35](https://github.com/xk320/zhishi/pull/35)
- 架构评审时间：2026-08-01 23:29:03 +0800
- 架构评审结论：通过；仓库所有者批准当前头提交
  `fae5d1277d0318f4842880bee4ac88b2cdbed5f3`
- 合并完成时间：2026-08-01 23:33:20 +0800
- 合并提交 SHA：`1298e6bbb6c8dc09c864eead181dc0558123de77`
- 执行方案：方案A“不可变事件事实、结果标签分离与历史窗口统计合同”
- 方案状态：已批准执行
- 执行授权：任务合同进入`main`后由Codex直接执行，不得再次要求用户选择方案
- 并行规则：不允许与其他任务并行执行

## 依赖与阻塞条件

- 任务-000006：最小数据闭环
- 任务-000021：Market Regime 市场状态架构
- 任务-000023：市场事件层架构设计

三项依赖均必须保持`已完成`，且本任务文件与看板必须先通过治理变更进入`main`；
进入`main`前禁止开始架构或实现交付。

## 背景

任务-000023已经将事件事实、确认记录和结果记录分离，但尚未定义后续任务应如何建立
可重复的历史事件回放和结果统计体系。若短期结果窗口被误写为新的研究尺度，或者用
成熟后的结果反向改写事件事实，会突破第一阶段仅研究BTC、ETH、SOL以及4小时、8小时、
24小时、48小时时间尺度的范围，并形成未来数据污染。

## 任务目标

建立历史事件回放与结果统计体系，将市场事件事实、市场状态和未来结果进行可重复验证。

## 固定执行方案

采用方案A“不可变事件事实、结果标签分离与历史窗口统计合同”。事件事实、事件发生时
引用的市场状态和事后结果记录分别保持不可变版本；结果统计只能引用原事件，不得覆盖
或重算原事件事实。15分钟和1小时只作为事后结果标签的辅助观察窗口，4小时、8小时、
24小时、48小时继续作为项目唯一主研究尺度。

默认工程决策依次选择资金与数据风险最低、证据最完整、失败语义最保守、依赖最少、
最容易复现和回滚的方式。合同范围内的文档结构、字段命名、枚举组织、文件组织和验证
方式由Codex自主决定，不得再次提供A/B/C选项或等待普通工程取舍。

只有需要新增真实账号、凭据、生产权限或外部付费能力，修改生产服务、数据库、权限、
防火墙或原始数据，执行不可逆操作，现有权威合同存在无法保守解决的直接冲突，缺少会
改变业务目标、风险门槛或正式输出含义的外部事实，或者继续执行必然超出本任务范围或
安全边界时，才允许停止并请求人工决策。数据证据不足、结果为`无法判定`以及普通可逆
工程细节不属于停止条件。

## 输入合同

输入仅允许使用事件发生时已经存在的信息：

- 市场事件事实；
- Market Regime 状态；
- 事件时间戳；
- 当时可获得的市场数据快照。

禁止使用事件发生后的信息生成事件事实。

## 输出合同

输出：

- 事件结果记录；
- 结果窗口统计；
- 无法判定原因；
- 拒绝原因。

结果观察窗口：

辅助短期结果观察窗口：

- 15分钟；
- 1小时；

15分钟和1小时仅用于生成事后结果标签和统计短期反应，不得作为事件探测、市场状态、
研究结论、回测策略或交易许可的新增主时间尺度，也不得突破第一阶段研究范围。

主研究尺度对应的结果观察窗口：

- 4小时；
- 8小时；
- 24小时；
- 48小时。

项目主研究尺度保持为4小时、8小时、24小时、48小时，不因增加15分钟和1小时的辅助
结果观察而改变。

## 工作范围

1. 定义历史事件回放的输入身份、时间边界、版本引用和可重复性要求。
2. 定义事件事实、市场状态引用和结果标签之间的不可变关联与防泄漏隔离。
3. 定义15分钟、1小时、4小时、8小时、24小时、48小时结果观察窗口的边界、成熟状态、
   缺失、拒绝和无法判定语义。
4. 定义按事件类型、标的、交易场所、主研究尺度和市场状态版本统计结果的合同。
5. 定义失败样本、拒绝记录、无法判定记录和未来数据污染拒绝证据的保留要求。
6. 交付历史事件回放架构、结果统计合同及相应任务中心记录，并完成一致性验证。

## 不在范围

- 不把15分钟或1小时新增为项目主研究尺度，不修改4小时、8小时、24小时、48小时的
  既有研究范围。
- 不实现事件探测器、回放运行时、数据库、服务、队列、调度器、模型或交易代码。
- 不设置生产事件阈值、策略参数、收益目标或交易许可门槛。
- 不采集、补采、清洗、迁移、删除、覆盖或修改原始市场数据。
- 不运行正式回测，不计算或宣称胜率、收益、赔率或净期望。
- 不生成交易信号、买卖方向、仓位、订单、自动下单或真实交易执行。

## 核心链路

事件事实
→ 市场状态
→ 结果窗口
→ 统计分析

## 安全边界

禁止：

- 交易信号生成；
- 收益优化；
- 模型训练；
- 自动下单；
- 真实交易执行。

## 数据原则

- 禁止未来数据污染；
- 事件事实与结果标签必须分离；
- 历史状态只能使用当时可获得信息；
- 不允许使用成熟后K线替代历史现场。

## 验收标准

- 完成历史事件回放架构；
- 定义事件结果统计合同；
- 支持15分钟、1小时、4小时、8小时、24小时、48小时结果观察统计；
- 明确15分钟和1小时仅为辅助结果观察窗口，项目主研究尺度仍为4小时、8小时、24小时、
  48小时；
- 保留无法判定和拒绝记录；
- 通过研发中心测试；
- 通过Markdown检查与diff检查。

## 验证命令

```bash
npx --yes markdownlint-cli2 docs/研发中心/任务/任务-000024.md docs/研发中心/看板.md
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
git diff --check
```

## 完成定义

- 提交PR；
- 架构评审通过；
- 合并后更新任务状态。

## 执行记录

### 交付物

- `docs/架构/历史事件回放与结果统计体系.md`
- `README.md`中的架构入口
- `docs/方法论/知势词典.md`中的历史事件回放、结果窗口合同和统计快照术语
- 本任务文件与`docs/研发中心/看板.md`的状态记录

### 验收与验证

- 历史事件回放架构：通过。回放方案、输入清单、运行、比较、原版本、截止时间、稳定
  顺序、幂等和失效均有明确合同；结论只允许`完全一致`、`不一致`或`无法重放`。
- 结果统计合同：通过。统计方案在结果可见前冻结，按事件类型、标的、交易场所、主研究
  尺度、市场状态确切版本和结果窗口精确分组，并保留全状态分母与版本化输入清单。
- 结果窗口：通过。15分钟、1小时、4小时、8小时、24小时、48小时均定义固定经过时长、
  半开边界、锚点、成熟、资格、缺失、拒绝、无法判定和失败语义。
- 研究尺度：通过。15分钟和1小时仅为辅助事后结果标签，不进入事件探测、市场状态、
  主研究尺度、回测策略或交易许可；主研究尺度仍为4小时、8小时、24小时、48小时。
- 防泄漏：通过。历史现场域与事后评价域分离，确认、结果、成熟标签、晚到、补采、
  事后修订和当前版本不能进入原事件事实回放。
- 失败样本：通过。原事件和窗口的拒绝、无法判定、失败、未成熟及失效均进入正式记录、
  统计输入清单和状态计数，不允许静默删除。
- 任务中心：通过。24个任务文件对应看板24行唯一任务记录，任务状态与看板分区一致。
- Markdown：5个变更文档0项问题；研发中心测试：22项全部通过；`git diff --check`通过。

### 实际验证命令

```bash
npx --yes markdownlint-cli2 \
  docs/架构/历史事件回放与结果统计体系.md \
  README.md \
  docs/方法论/知势词典.md \
  docs/研发中心/任务/任务-000024.md \
  docs/研发中心/看板.md
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
git diff --check origin/main...HEAD
```

另执行任务与看板映射、六个窗口合同、未来数据拒绝、统计分母漂移、未完成标记和常见敏感
信息模式检查。

### 已知限制

- 当前315个质量验证单元均为`无法判定`，历史重放为4个拒绝、311个无法判定、0个
  通过，简单基准模型阶段仍为`禁止`。
- 本任务只交付技术中立Markdown合同，未实现回放器、标签程序、统计服务、数据库、
  模型、回测或交易代码，也未选择生产锚点、标签阈值、存储或部署技术。
- 本任务未读取真实市场数据，不产生真实事件结果、统计数字、胜率、收益、赔率或净期望。
- 后续运行时实现、标签定义和真实数据验证必须由独立任务文件授权并验收。

### 数据与安全影响

- 未访问服务器、数据库、市场接口、生产服务、真实账户或交易权限。
- 未删除、覆盖、迁移或修改原始数据，未提交凭据、密钥、令牌或生产配置。
- 未使用未来数据，未训练模型、运行正式回测、生成交易信号或执行真实交易。
- 合同明确原事件与结果标签隔离，异常时安全停止，没有降低研究和风险门槛。

### 需要人工决策

- 无。仓库所有者已批准并合并Pull Request #35，任务验收闭环完成。

### 自动合并资格

- 不符合。基线任务类型为`研究基础设施 / 历史验证`，不属于当前策略精确允许的`文档`、
  `治理`或`研究规范`；本次已按规则完成人工评审和合并。
