# 任务-000118：兼容任务116完成看板格式的阶段1测试夹具

<!-- markdownlint-disable MD013 -->

- 状态：待执行
- 类型：测试
- 阶段：研发中心可信合同治理
- 优先级：P1
- 执行方案：方案A“固定兼容行解析并保持合成基线”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；只修改研发中心测试夹具，不修改任务-000116/000117合同或状态

## 依赖与阻塞条件

- 唯一前序依赖：任务-000117已完成并进入main。
- 当前阻塞原因：任务116完成后看板行采用已完成格式，任务117测试夹具的旧行删除正则无法识别该格式，导致阶段1资格测试结构失败。
- 解除条件：测试夹具同时兼容待评审和已完成看板行，且全套研发中心专项测试通过。

## 背景

任务-000117已将阶段1正向测试改为合成任务116待评审行，但行清理正则只覆盖带优先级的待评审行。任务116完成后，其已完成行不带优先级，形成重复任务行和看板结构失败。需要在测试内固定兼容两种合法历史格式。

## 任务目标

1. 让阶段1覆盖受限合同修订测试稳定处理任务116待评审和已完成两种看板行格式。
2. 保持合成基线、任务状态负向、路径越界和合同漂移失败关闭断言。
3. 不改变可信资格规则、任务合同、看板事实、生产或数据边界。

## 固定执行方案

1. 将测试内任务116行清理规则限制为两种已知合法表格格式。
2. 使用固定任务116标题构造合成行，避免借用其他任务标题。
3. 运行研发中心全套专项测试并记录结果；失败则停止。

## 默认工程决策

- 复用Python标准库和现有unittest，不新增依赖。
- 兼容逻辑只在测试夹具中存在，不放宽生产或可信规则。
- 不读取服务器、数据库、市场数据或凭据。

## 允许停止条件

- 需要修改可信资格器、自动合并工作流、任务合同、看板事实、服务器、数据库、权限或原始数据。
- 无法将兼容范围限制为待评审和已完成两种固定行格式。
- 修复会放宽未来数据、跨标的补偿、研究准入、交易许可或自动合并评审门。

## 输入合同

- `tests/研发中心/test_验证自动合并资格.py`中的阶段1合成看板夹具。
- 任务-000116完成后的看板已完成行格式及任务-000117已合并测试修复。

## 输出合同

- 兼容两种固定看板行格式的测试夹具。
- 全套研发中心专项测试、编译、MarkdownLint和`git diff --check`证据。
- 任务118执行记录和看板派生状态。

## 工作范围

- 仅修改研发中心测试文件、任务118执行记录和派生看板。

## 不在范围

- 不修改可信资格器、工作流、任务-000116/000117/000115/000106合同、历史批次、服务器、数据库、原始数据、凭据、生产或交易逻辑。

## 安全边界

- 合成测试输入不进入生产、审计批次或研究结论。
- 保留错误状态、额外路径、合同漂移和看板漂移的失败关闭测试。
- 不使用未来数据，不跨BTC/ETH补偿，真实资金交易保持关闭。

## 验收标准

1. 任务116待评审和已完成看板格式下阶段1正向测试均通过。
2. 任务状态、目标合同、工作流路径和看板漂移负向测试继续失败关闭。
3. 研发中心资格、工作流、看板和范围专项测试全绿。
4. Python编译、MarkdownLint和`git diff --check`通过；无生产、数据或凭据变更。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

兼容夹具进入main，研发中心专项测试全绿，任务-000118状态闭环为已完成；不改写任务-000116/000117及历史证据。

## 数据与安全影响

仅修改测试夹具和研发中心治理记录；不访问服务器、数据库、市场数据、原始数据、生产服务、凭据或真实交易。

## 已知限制

本任务仅修复看板行格式兼容，不代表阶段1覆盖证据、研究准入或交易许可通过。

## 回滚方式

回滚本任务PR即可恢复旧兼容规则，不触碰任务-000116/000117合同、状态或历史批次。
