# 任务-000093：采纳 Binance 已证明归档并重算阶段1门禁

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“新候选集内容寻址采纳、旧路线去门禁与八叶子保守重算”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；只读使用任务-000092最终批次和其已证明本地归档，建立新的阶段1正式输入并重算门禁；不得再次要求用户选择方案
- 执行分支：`codex/task-000093-stage1-candidate-recompute-v1`
- 开始时间：`2026-08-11T17:53:59+08:00`
- 实现提交SHA：`089f590e728d9f86bc5cd0ba9b8a93c439c9f739`
- Pull Request：[#249](https://github.com/xk320/zhishi/pull/249)
- 完成实现时间：`2026-08-11T19:14:24+08:00`
- 并行规则：禁止并行；任务-000092是唯一前序依赖；本任务完成前不得并行登记或执行其他阶段1数据门禁任务

## 依赖与阻塞条件

- 唯一前序依赖：任务-000092已完成，最终来源身份批次为`binance-archive-provenance-20260811T063739Z-7a6da0087493`。
- 项目路线决策：自本任务进入`main`起，任务-000092最终批次中的新候选集取代任务-000084旧630成员路线，成为阶段1当前正式输入候选；任务-000084及其历史批次保持原样，仅作为旧路线历史记录，不再阻塞当前阶段门禁。
- 当前阻塞原因：无。任务-000092已为5180个归档成员闭合来源身份，任务合同允许只读访问固定本地归档并生成新的追加式审计批次。
- 失败安全：若任务-000092批次、成员内容SHA、源清单指纹、Schema或固定本地文件发生漂移，则不得采纳漂移成员；对应成员保持拒绝、失败或无法判定，不得回退到任务-000084旧成员补偿。

## 背景

任务-000084要求为Ubuntu旧候选资产的630个身份成员取得外部`source-identity-evidence-1.0`清单；该路线持续得到声明入口空集，无法通过重复扫描自行产生来源证明。任务-000092已经通过本地ZIP与CHECKSUM全量哈希、Binance公开对象键、对象大小、CHECKSUM对象ETag、ZIP结构和Schema指纹，形成新的5387成员候选集，其中5180个成员来源身份已证明、207个成员被拒绝。

此前任务-000092合同明确禁止自行替代任务-000084或改变阶段门禁。现在项目已批准以该新候选集替代旧路线。本任务负责把这项路线决策落实为版本化正式输入、保留旧路线历史边界，并基于新输入重新计算BTC、ETH在4小时、8小时、24小时、48小时八个研究叶子的阶段1硬门。来源身份通过不等于质量、重放、成本或阶段2通过。

## 任务目标

1. 将任务-000092最终批次中5180个`已证明`成员冻结为新的阶段1正式输入候选版本，207个拒绝成员和2个观察项继续保留在原分母或排除清单中。
2. 以内容SHA、任务-000092批次、规则、执行器、Schema、授权和源清单指纹绑定正式输入；成员顺序必须确定且可复算。
3. 明确任务-000084旧630成员路线为历史非门禁路线，不改写其任务文件、状态、批次或历史结论，不用旧成员补偿新候选集缺口。
4. 对新候选集执行只读、单进程、有界审计，至少复验成员定位与内容绑定、交易事件时间Schema、按日覆盖、ZIP/CSV结构和基础可解析性；不把有界检查描述为未执行的全量业务质量证明。
5. 对BTC、ETH分别按4小时、8小时、24小时、48小时建立八个叶子，重算来源身份、三类时间、质量、历史重放、成本与执行、血缘、容量和恢复硬门。
6. 输出最新阶段1裁决、允许研究范围和精确剩余缺口；只有同一叶子的全部硬门均有可复算证据时才可放行，任一门拒绝或无法判定即保持阻塞。

## 固定执行方案

1. 冻结最新`main`、本任务合同、任务-000092合同与最终批次文件SHA；只接受批次`binance-archive-provenance-20260811T063739Z-7a6da0087493`及其中状态为`已证明`的成员。
2. 从任务-000092成员分片重建确定性正式输入清单，排序键固定为`标的、精确合约、数据对象、事件日期、相对路径、内容SHA-256`；重复键、未知标的、未知合约、未知对象、缺失文件、大小或内容SHA漂移一律失败安全。
3. BTC和ETH分别统计；BTC的聚合成交或逐笔成交不得补偿ETH缺失，ETH不得补偿BTC，聚合成交与逐笔成交不得互相冒充。
4. 对每个采纳成员只读验证普通ZIP/单CSV结构和首末有效记录的事件时间；以中央目录、压缩成员大小、CSV字段结构、首末事件时间、日期边界和解析状态形成紧凑证据。不解压落盘，不保存价格、数量或成交业务行。
5. 按日成员构造覆盖证据并记录可观察起止、缺日、拒绝、无法判定、失败、未成熟和失效；成员正文未全量逐行扫描的质量维度必须明确标记`无法判定`，不得用首末记录替代重复、乱序或异常值全量证明。
6. 将任务-000029至任务-000037、任务-000074至任务-000092的历史批次视为只读证据。只有身份、对象、内容、时间范围和版本与新正式输入精确兼容的事实才可复用；旧630成员上的事实不得迁移。
7. 为八个研究叶子逐门裁决并生成最新追加式审计批次。`15分钟`和`1小时`仅可登记为事后结果观察窗口，不得升级为市场状态、事件探测、正式研究或交易许可尺度。
8. 同步最新阶段1审计报告、数据缺口清单、README、总体计划和阶段路线派生视图；明确旧路线已退役、当前硬门和下一解除条件，不删除或覆盖历史报告与批次。

## 默认工程决策

- 使用Python标准库、单进程和流式读取，不新增依赖、数据库、常驻服务或并行解压进程。
- 优先生成按成员和按叶子分片的紧凑JSON；单文件不得超过5MiB，仓库交付总量不得超过25MiB。
- 正式输入只包含任务-000092的5180个已证明成员；207个拒绝成员不得因路线替代被重新分类为通过。
- 来源身份门可由任务-000092的精确证据闭合；到达时间、采集时间、全量质量、重放、成本、容量或恢复没有兼容证据时保守记为`无法判定`，不猜测、不外推。
- 阶段2是否放行完全由本任务新批次的八叶子硬门决定，不预设通过，也不因旧路线退役而自动通过。
- 可用内存低于20%或输出盘可用空间低于5GiB时停止；Python进程最大RSS目标不超过256MiB。

## 允许停止条件

- 任务-000092最终批次或固定本地数据根不可读，或者需要修改、移动、删除、修复、覆盖、重新下载原始归档。
- 需要把任务-000092拒绝成员、观察项或任务-000084旧成员伪装为正式已证明输入。
- 需要连接真实交易账户、写数据库、修改Ubuntu、生产服务、权限、防火墙或订单簿系统。
- 任务合同与项目已批准的“新候选集替代旧路线”决策发生直接冲突，且无法通过保守失败语义继续。
- 单进程执行超过14400秒、任一输入文件超过16GiB、成员超过8192、输出超过25MiB或资源硬门触发。

普通文件组织、测试策略、字段命名、批次命名和对缺失证据作`无法判定`不属于人工决策条件，由Codex自动处理。

## 输入合同

- 最新`main`上的`AGENTS.md`、`README.md`、研发中心入口、任务规范、看板、总体计划和全部任务文件。
- 任务-000092最终批次`artifacts/数据/Binance历史归档来源身份/binance-archive-provenance-20260811T063739Z-7a6da0087493/`的`summary.json`、`schema-catalog.json`、`exclusions.json`和全部成员分片。
- 任务-000092固定只读根目录`/Volumes/data/data/binance/futures/um`中的`trades/BTCUSDT`、`trades/ETHUSDT`和`aggTrades/BTCUSDT`已证明ZIP；不得扫描或吸收白名单外目录。
- 任务-000029至任务-000037、任务-000074至任务-000092的合同、配置、不可变批次、最终审计报告和数据缺口清单，仅按精确版本兼容规则复用。
- 研究范围固定为BTC、ETH及4小时、8小时、24小时、48小时；15分钟、1小时只作事后结果观察窗口。

## 输出合同

- `docs/superpowers/specs/2026-08-11-stage1-candidate-route-replacement-design.md`：替代决策、正式输入身份、兼容规则、硬门和回滚边界。
- `docs/数据/阶段1新候选集采纳合同.md`：正式输入版本、成员选择、排序、身份、时间、状态和失败安全合同。
- `config/审计/任务-000093阶段1新候选集重算.json`：输入批次、固定路径、成员规则、叶子、硬门、资源和安全配置。
- `scripts/审计/重算阶段1新候选集门禁.py`及专项测试：确定性采纳、只读结构/时间复验、兼容证据裁决、八叶子重算和追加式发布。
- `artifacts/审计/阶段1新候选集重算/<唯一批次>/`：正式输入清单、成员/覆盖/异常分片、八叶子、结果摘要、输入/规则/执行器/来源指纹和资源事实。
- 更新`docs/审计/阶段1最终审计报告.md`、`docs/审计/数据缺口与补采清单.md`、`README.md`、`docs/研发中心/总体计划.md`及阶段1路线派生文档中的最新裁决。
- 本任务文件、看板、PR、双只读评审证据、验证结果、已知限制、数据与安全影响和回滚记录。

## 工作范围

- 采纳任务-000092已证明的新候选集，建立阶段1正式输入版本。
- 只读复验本地已证明归档的定位、内容绑定、结构、事件时间边界和按日覆盖。
- 基于新输入重算BTC/ETH八叶子的阶段1硬门，退役旧路线的当前门禁地位并同步派生文档。

## 不在范围

- 不修改任务-000084任务文件、状态、旧630成员、Ubuntu历史批次或任何历史审计批次。
- 不修复、删除、覆盖、迁移、重新下载或补采原始市场数据，不访问Ubuntu或数据库。
- 不把首末行、有界Schema或来源证明解释为全量质量、可重放、成本、流动性、研究优势或交易许可。
- 不训练模型、不回测、不优化参数、不生成胜率、收益、因果、预测优势、方向、仓位、订单或真实交易信号。
- 不修改订单簿系统、生产服务、权限、防火墙、账户或凭据。

## 安全边界

- 数据根全程只读；拒绝符号链接、目录越界、特殊文件、多成员ZIP、路径漂移和执行中变化。
- ZIP只在内存中有界读取首末记录，不解压落盘；输出不得保存价格、数量、成交ID或任何业务行。
- 单进程、最大RSS目标256MiB、仓库输出不超过25MiB；执行前后复算源清单指纹并要求一致。
- 不访问网络、Ubuntu、数据库、账户、环境变量或凭据；不产生远端写入、DDL、DML、权限变更或交易动作。
- 所有未知、缺失、漂移、解析失败和不兼容证据均失败安全，不缩小分母、不跨标的或对象补偿。

## 验收标准

1. 任务-000092的5180个已证明成员以确定顺序形成新正式输入；207个拒绝成员和2个观察项完整保留，计数可复算且不缩小分母。
2. 正式输入绑定任务-000092批次、成员内容SHA、规则、执行器、Schema、授权和源清单指纹；源目录执行前后指纹不变。
3. 任务-000084旧路线在最新阶段文档中明确为历史非门禁路线，其任务文件、状态和历史证据字节不变。
4. BTC、ETH分别形成4小时、8小时、24小时、48小时八个叶子；每叶子完整保存八类硬门、状态、证据定位、原因码和解除条件。
5. 来源身份、时间、质量、重放、成本与执行、血缘、容量和恢复均按新输入兼容性裁决；没有证据的门保持拒绝或无法判定，阶段2不因路线替代自动放行。
6. 真实只读批次完成成员定位、结构、事件时间边界和按日覆盖复验，资源、异常、状态计数和执行限制有可复算记录；未执行的全量扫描明确披露。
7. 最新审计报告、缺口清单、README、总体计划和阶段路线对正式输入、旧路线地位、八叶子结论和下一解除条件表述一致。
8. 专项、审计、数据、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`有真实记录；PR经双只读评审、main可信复验和自动合并。

## 验证命令

```bash
python3 -m unittest tests/审计/test_重算阶段1新候选集门禁.py -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/重算阶段1新候选集门禁.py tests/审计/test_重算阶段1新候选集门禁.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须运行一次真实、串行、只读的新候选集采纳与门禁重算，保存精确批次身份、正式输入、八叶子裁决、规则/执行器/输入指纹、资源事实和源目录前后不变证明。

## 完成定义

新正式输入合同、配置、执行器、专项测试和真实不可变批次已在独立PR交付；5180个已证明成员被确定性采纳，207个拒绝成员未被漂白；旧任务-000084路线不再作为当前门禁，八个新输入叶子得到可复算裁决，最新阶段文档明确真实剩余缺口；双只读评审、main可信复验和自动合并完成，随后独立状态闭环将本任务标记为`已完成`。只有八叶子全部硬门通过时才可宣布阶段1完成并放行阶段2，否则必须明确新的、可执行的阻塞条件。

## 数据与安全影响

本任务只读访问任务-000092固定本地归档和仓库证据，新增不含业务值的脱敏正式输入与审计批次。预计仓库新增小于25MiB，不复制154GiB源数据，不访问Ubuntu、数据库、订单簿、账户或生产系统，不修改原始数据或权限，不产生交易结论。

## 已知限制

任务-000092证明的是Binance归档对象身份和字节完整性。公开逐笔成交归档通常只提供事件时间，不天然提供本项目采集到达时间和采集时间；逐成员首末记录检查也不等于全量逐行质量证明。若这些门在兼容证据中仍缺失，本任务将把阻塞从不可获得的旧630身份声明收敛为新候选集上的具体时间、质量、重放、成本、容量或恢复缺口，而不会虚构阶段1通过。

## 回滚方式

回滚本任务PR新增的正式输入合同、配置、执行器、测试、批次和派生文档更新，即恢复任务-000092只作为替代候选集、任务-000084仍为当前门禁的上一版本状态。不得删除、覆盖或改写任务-000084、任务-000092、历史审计批次、本地Binance归档、Ubuntu或数据库。

## 执行记录

- 最终批次：`stage1-candidate-recompute-20260811T120000Z-c88fa0502d54`；候选5387、正式输入5180、拒绝207、观察项2、运行时失败0。正式输入指纹为`a1c69d6e2446c364578edb08e6b6fa30912f2e5577d7e51377ef8ab4c648952d`。
- 分标的结果：BTC正式成员2937，其中聚合成交423、逐笔成交2514；ETH逐笔成交正式成员2243。BTC与ETH分别形成4小时、8小时、24小时、48小时八个叶子，未跨标的或数据对象补偿缺失。
- 门禁结果：八叶子的来源身份和血缘通过；到达/采集时间、逐行质量、历史重放、成本与执行、研究容量和隔离恢复均因缺少同版本完整证据保持`无法判定`。每门完整保存状态、原因码、稳定证据定位和解除条件；八叶子全部阻塞，允许研究范围为空，阶段1未完成，阶段2继续禁止。
- 覆盖结果：BTC聚合成交覆盖2025-05-14至2026-07-10且缺日0；BTC逐笔成交覆盖2019-09-08至2026-08-03且缺日8；ETH逐笔成交覆盖2019-11-27至2026-08-03且缺日199。累计记录数16121999478，完整流式读取解压字节854743871570；仅解析首末有效记录，未冒充逐行业务质量证明。
- 来源与资源：源清单执行前后指纹均为`03897f148c771096f4567575c0fb270a30cb0e60d696865dab079bd19583dfe5`；最终真实批次单进程耗时1977.605秒，最大RSS 236339200字节，仓库批次输出5662332字节，配置、来源全文件集合、单源文件、RSS、磁盘和发布前资源均受失败安全硬门约束。
- 失败安全实证：修复轮首个全量进程观察5180/5180且运行时失败0，但在发布前因RSS超过256MiB被拒绝，未生成批次目录；改为增量分片并提前释放对象后从全新进程复跑成功，未提高资源上限或缩小分母。
- 验证：修复后专项17项和审计188项已通过；数据、研发中心、MarkdownLint、Python编译、敏感扫描和`git diff --check`将在精确修复头统一复验并绑定第二轮双只读评审。PR #249不得复用旧头评审结论。
- 安全：只读使用任务-000092固定本地归档；未访问Ubuntu、数据库、订单簿、账户或生产系统，未修改、迁移、删除或补采原始数据，未训练模型、回测或产生交易信号。
- 已知限制：公开归档没有本项目采集到达时间和采集时间；本批次未执行逐行业务质量、可信历史重放、成本执行、研究容量或隔离恢复验证。上述六门须由后继任务在同一正式输入版本上闭合。
