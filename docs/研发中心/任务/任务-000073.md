# 任务-000073：三类时间与可见性合同复采

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000073-time-visibility-recapture-v1`
- 开始时间：`2026-08-07T14:15:33+08:00`
- 实现提交SHA：`f08a30a6c0d004332684f797085c344c95a2e50f`
- Pull Request：[#189](https://github.com/xk320/zhishi/pull/189)
- 类型：数据治理
- 阶段：阶段1 时间语义与可见性证据修复
- 优先级：P0
- 执行方案：方案A“三类时间语义与可见性冻结”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅本地仓库证据和Ubuntu白名单只读元数据；不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；唯一前序依赖任务-000072；不得与其他时间、质量、重放或数据复采任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000072。
- 输入依赖：任务-000072来源身份复采合同与批次、任务-000037最终审计、`ZS-DATA-GAP-002`记录、资产清单和最新规则指纹。
- 当前阻塞原因：无；时间字段缺失、时区未知、截止点未知或探针失败必须形成`无法判定`，不是请求人工选择的理由。
- 解除条件：每个候选成员均有当前版本事件时间、到达时间、采集时间、时区、时钟漂移、数据截止点、迟到和修订规则证据；否则保持缺口阻塞。

## 背景

阶段1最终审计显示BTC、ETH的315个质量单元三类时间均无法判定，无法证明历史可见性。任务-000072只补来源身份，
没有把来源身份或文件修改时间升级为时间语义。本任务只建立三类时间和可见性合同，不能绕过来源、质量、重放或成本硬门。

## 任务目标

1. 固定事件时间、到达时间、采集时间的中文语义、时区、精度、空值、截止点和可见性规则。
2. 固定时钟漂移、迟到、补录、撤销、修订、乱序和数据截止的证据字段及失败安全状态。
3. 对BTC、ETH分别按资产、市场、合约、数据对象和主研究尺度登记候选成员；禁止跨标的补偿。
4. 生成可复算、不可覆盖的合同批次，并只对`ZS-DATA-GAP-002`写入精确部分解除或继续阻塞范围。

## 固定执行方案

1. 从最新`main`冻结任务-000072输出、资产清单、时间相关合同和缺口清单输入指纹。
2. 新增本任务专用合同、字段映射、只读复采入口和专项测试；不改写任务-000030历史合同和批次。
3. 仅通过逻辑别名`ubuntu`执行合同允许的白名单元数据探针；不能读取业务正文时必须保留`无法判定`。
4. 采用串行、资源有界、追加式和无覆盖批次，保存输入/规则/执行器/成员/结果指纹及BTC/ETH分组计数。

## 默认工程决策

- 事件时间表示市场事件发生时间；到达时间表示数据进入指定来源边界的时间；采集时间表示本系统取得数据的时间，三者不得互换。
- 所有时间必须带时区、精度和截止语义；文件修改时间不替代到达或采集时间，未知值不填默认值。
- 主研究尺度固定为4小时、8小时、24小时、48小时；15分钟和1小时只能作为事后结果观察窗口。
- 冲突、缺失、迟到、修订或时钟漂移无法裁决时，状态为`无法判定`，不得产生研究准入或交易许可。

## 允许停止条件

- 需要新增账号、长期凭据、非白名单主机、数据库写入、原始数据读取、生产权限或不可逆操作时停止并记录阻塞。
- 普通数据不足、时间未知、探针失败、成员拒绝或结果为空不得请求用户选方案，必须形成失败安全批次。

## 输入合同

- `config/数据/来源身份复采.json`、任务-000072真实来源身份批次和`docs/数据/来源身份复采与标的范围证据合同.md`；只读。
- `docs/审计/阶段1最终审计报告.md`、`docs/审计/数据缺口与补采清单.md`和资产清单；只读并冻结指纹。
- 最新`main`上的时间质量规则、任务文件、发现规则和执行器内容指纹；缺失时失败关闭。
- 远端仅允许逻辑别名`ubuntu`和合同列明的只读元数据范围。

## 输出合同

- `docs/数据/三类时间与可见性合同.md`；
- `config/数据/三类时间与可见性复采.json`；
- `scripts/数据/复采三类时间.py`及`tests/数据/test_复采三类时间.py`；
- `artifacts/数据/三类时间与可见性/<批次>/`不可变脱敏批次；
- `docs/审计/数据缺口与补采清单.md`仅追加本批次和精确解除范围；
- 任务文件、看板、PR、提交SHA、验证命令、真实结果、限制和安全记录。

## 工作范围

- 复算三类时间字段、时区、精度、截止点、时钟漂移、迟到、补录、修订和可见性成员合同。
- 按BTC/ETH独立生成状态、证据定位、限制、解除条件和状态守恒批次。
- 运行专项、研发中心、审计、数据测试、Python编译、MarkdownLint、敏感信息扫描和`git diff --check`。

## 不在范围

- 不读取、采集、迁移或修改市场业务记录，不修改数据库、表、索引、用户、权限、原始数据或生产服务。
- 不实现运行时、模型、回测、模拟交易、方向、仓位、订单、胜率、收益或交易许可。
- 不把时间字段存在性解释为数据质量通过、历史可重放、预测优势、研究准入或阶段2放行。

## 安全边界

- SSH仅使用无交互、密码关闭、转发关闭的逻辑别名`ubuntu`；不输出地址、用户名、凭据、令牌、环境变量或原始响应。
- 远端探针只允许合同固定的只读元数据；批次串行并受总超时、成员超时、输出、日志和内存预算约束。
- 批次追加式原子无覆盖；任何漂移、越权、未来数据、超时或敏感信息均失败关闭。

## 验收标准

1. 输入和规则SHA-256可复算，成员确定性排序，BTC/ETH完整覆盖且无静默遗漏。
2. 每个成员均有事件/到达/采集时间状态、时区、精度、截止点、迟到/修订限制、证据定位和输入成员指纹。
3. 只有三类时间语义和可见性字段均有当前结构化证据时才可标记为已证明；未知必须保留无法判定。
4. 远端严格只读、无业务正文、无数据库写入；历史任务-000030合同和批次字节保持不变。
5. `ZS-DATA-GAP-002`只按精确范围部分解除或继续阻塞，其他P0缺口和阶段2门禁保持原结论。
6. 所有任务验证命令、真实只读批次、敏感扫描和差异检查均通过并如实记录。

## 验证命令

```bash
python3 -m unittest tests/数据/test_三类时间与可见性.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m py_compile scripts/数据/复采三类时间.py tests/数据/test_三类时间与可见性.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另需执行一次真实、串行、专用只读复采，并记录批次身份、输入/规则/授权/资源指纹和安全事实。

## 完成定义

三类时间与可见性合同、入口、测试、真实只读批次、精确缺口更新和全部验证记录已提交；PR经双只读评审、
main可信复验并自动合并，随后独立状态闭环将任务-000073标记为`已完成`。任务完成不等于阶段1通过；
只有后续最终审计证明全部8个叶子硬门满足后，阶段2才可解除。

## 执行记录

- 真实批次：`time-visibility-recapture-20260807T144822+0800-114ed0567118`
- 批次范围：任务-000072来源身份批次的630个成员（BTC 315、ETH 315），每个成员固定绑定四个主研究尺度并生成确定性分组成员；15分钟和1小时仅为事后观察窗口。
- 批次结果：候选总体630、分母630、已观察630、拒绝12、失败12、无法判定618、未成熟0、失效0；BTC和ETH分别为候选315、拒绝6、失败6、无法判定309；四个主研究尺度各复用同一630个候选分组并独立输出统计。
- 时间结论：事件时间、到达时间、采集时间及其时区、精度、数据截止、时钟漂移和质量规则均未取得当前结构化证据；所有未知保持无法判定，来源身份拒绝保持失败安全状态。
- 远端与安全事实：通过逻辑别名`ubuntu`执行固定版本只读可达性元数据探针；远端写入、业务正文读取、数据库业务记录读取、凭据读取均为`false`，未修改原始数据、数据库或生产服务，未使用未来数据。
- 缺口裁决：`ZS-DATA-GAP-002`未关闭且没有部分解除范围，其他P0/P1缺口和阶段2门禁保持原结论。
- 兼容维护事实：批次冻结的是缺口台账追加前SHA；发布后仅刷新`config/数据/三类时间与可见性复采.json`及历史来源身份配置中的缺口台账输入SHA，未改写批次、任务-000030历史合同或原始数据。

### 已执行验证

- `python3 -m unittest tests/数据/test_三类时间与可见性.py -q`：5通过。
- `python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q`：通过。
- `python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q`：168通过。
- `python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q`：64通过。
- `python3 -m py_compile scripts/数据/复采三类时间.py tests/数据/test_三类时间与可见性.py`：通过。
- `NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'`：0 issues。
- `git diff --check`：通过。
- `validate_manifest(artifacts/数据/三类时间与可见性/time-visibility-recapture-20260807T144822+0800-114ed0567118/三类时间与可见性清单.json)`：通过。
