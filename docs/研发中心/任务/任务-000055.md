# 任务-000055：修复现行外部状态与双标的范围文档漂移

<!-- markdownlint-disable MD013 -->

- 状态：阻塞
- 类型：治理
- 阶段：阶段1 数据闭环证据修复与范围治理
- 优先级：P0
- 执行方案：方案A“最新脱敏证据绑定与历史范围隔离”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；任务-000033已完成后认领；不得与任务-000034至任务-000037并行
- 执行分支：`codex/task-000055-blocked-auto-merge-scope-v1`
- 开始时间：`2026-08-05T02:55:00+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000033通过评审并合并；
- 当前阻塞原因：任务合同要求机器负向测试；候选交付头包含测试路径时，main可信资格返回“变更路径tests/研发中心/test_项目范围与阶段状态.py不允许自动合并”，需先完成治理修复任务并从最新main重新认领。
- 解除条件：治理修复任务合并后，任务-000055的自动合并范围与测试交付路径通过main可信资格复验。

## 背景

任务-000031、任务-000032和任务-000033已经形成新的脱敏只读可达性与运行时预检证据，
但根目录 `AGENTS.md`、`README.md`、研发总体计划和自动执行提示词仍引用旧的“ubuntu不可达、
后续任务阻塞”声明，造成现行入口与任务文件事实漂移。现行研究范围已经收缩为BTC、ETH；
历史审计、历史任务和测试夹具中的SOL事实必须保留，不能通过机械替换改写历史证据。

## 任务目标

1. 让现行入口绑定任务-000031及后续真实脱敏预检的最新状态：环境可达仅表示可执行白名单
   只读探针，不等于数据闭环、研究准入或阶段2放行；
2. 统一现行入口的阶段、依赖和阻塞描述，不再声称任务-000031至任务-000037因旧的
   “ubuntu不可达”事实整体阻塞；
3. 明确现行前向研究范围只包含BTC、ETH，并把SOL限定在历史证据、历史任务和负向测试上下文；
4. 保留历史审计产物、历史任务执行记录、批次指纹和测试夹具原文，不产生范围外推。

## 固定执行方案

1. 冻结任务-000031、任务-000032和任务-000033的最新合并事实与脱敏预检记录；
2. 只修改现行治理入口和对应一致性测试，不修改历史审计证据、历史批次或任务执行记录；
3. 为现行外部状态使用“可达（仅只读预检）”表述，明确数据质量、重放、成本和双标的闭环
   仍由任务-000034至任务-000037逐项裁决；
4. 为现行范围使用BTC、ETH双标的表述，历史SOL只在明确的历史/不可变/未纳入当前语境中出现；
5. 运行研发中心、审计回归、Markdown、敏感信息和差异检查，形成可回滚PR。

## 默认工程决策

- 任务文件是状态事实源；现行入口只引用最新任务事实，不依据聊天或旧历史批次推断；
- 可达性不自动授予数据读取、研究准入或交易许可；数据不足保持无法判定；
- 历史证据优先保持字节不变；采用最小文档和负向测试修复；
- 使用Python标准库和既有测试，不引入依赖、服务器访问或复杂系统。

## 允许停止条件

- 需要修改历史审计产物、原始数据、服务器、数据库、生产服务、权限或凭据；
- 权威任务事实与现行合同直接冲突且无法从严收敛；
- 发现无法证明最新外部状态的真实事实。

普通文档措辞、测试组织和历史SOL保留不属于停止条件，应按失败安全原则继续。

## 输入合同

- `AGENTS.md`、`README.md`、`docs/研发中心/README.md`、总体计划、看板、任务规范和全部任务文件；
- 任务-000031、任务-000032、任务-000033在`main`中的合并事实和脱敏预检记录；
- `tests/研发中心/test_项目范围与阶段状态.py`及相关现行范围/状态检查；
- 现行规范清单与历史证据不可变路径清单。

## 输出合同

- 修正后的`AGENTS.md`、`README.md`、`docs/研发中心/总体计划.md`和
  `docs/研发中心/Codex自动执行提示词.md`；
- 现行外部状态与双标的范围一致性测试；
- `docs/superpowers/specs/task-000055-current-state-scope-remediation-design.md`、本任务记录、看板、提交和PR证据。

## 工作范围

- 修复现行入口中的Ubuntu外部状态、阶段依赖和阻塞描述；
- 复核并补充BTC、ETH现行范围与SOL历史边界；
- 增加或修正机器可验证的现行状态/范围负向测试；
- 验证历史不可变路径和任务文件—看板映射保持不变。

## 不在范围

- 不访问服务器、数据库业务正文、真实市场数据或账户；
- 不执行任务-000034及其后继数据闭环、模型、回测或交易判断；
- 不修改历史审计报告、历史CSV/JSON、批次索引、历史任务执行记录或测试夹具事实；
- 不删除SOL历史事实，不创建SOL替代研究入口；
- 不改变研究准入、风险门、交易许可或阶段2硬门。

## 安全边界

- 仅允许文档、治理测试和任务中心文件变更；
- 不提交IP、域名、用户名、密钥、密码、令牌、原始日志或市场记录；
- 任何可达性结论必须绑定脱敏时间和逻辑别名`ubuntu`，不扩大为数据可用性；
- 历史不可变路径必须与基线字节一致。

## 验收标准

1. 四个现行入口不再包含“ubuntu不可达导致任务-000031至任务-000037整体阻塞”的失效声明；
2. 现行入口统一说明Ubuntu仅为只读预检可达，数据闭环仍由任务-000034至任务-000037裁决；
3. 现行规范清单只声明BTC、ETH，SOL仅出现在明确历史/不可变/未纳入当前语境；
4. 任务文件与看板逐项唯一映射，任务-000034仍为下一可执行任务；
5. 历史审计路径、历史任务执行记录和指纹保持不变；
6. 研发中心、审计、Markdown、敏感信息和`git diff --check`全部通过。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_项目范围与阶段状态.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
npx --yes markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check origin/main...HEAD
```

## 完成定义

现行入口、状态/范围测试和历史保护检查完成后，创建引用任务-000055的PR并更新为`待评审`；
双只读评审通过、精确SHA自动合并并完成独立状态闭环后，任务-000055才能标记为`已完成`。

## 执行记录

- 执行分支：`codex/task-000055-blocked-auto-merge-scope-v1`
- 开始时间：`2026-08-05T02:55:00+08:00`
- 尝试命令：`python3 scripts/研发中心/验证自动合并资格.py --repo-root . --base-ref 99ed6f5f2b3414f12a8317729dd73bd9048a824d --head-ref 7325cb793635b59e8d0f0b620db35312a3fd94ae --metadata /tmp/zhishi-pr111-candidate-meta`
- 结果：可信资格检查返回`eligible=false`；原因包含“任务交付看板不是唯一可复算映射”和“变更路径tests/研发中心/test_项目范围与阶段状态.py不允许自动合并”。
- 外部证据：候选头`7325cb793635b59e8d0f0b620db35312a3fd94ae`包含任务合同要求的机器负向测试；可信资格命令以脱敏元数据文件复验并返回上述拒绝。PR #111 双只读评审中治理评审报告P1=1，PR #111已关闭，未合并任何交付。
- 阻塞原因：任务合同与当前治理自动合并白名单对测试路径的授权不一致。
- 解除条件：治理修复任务完成并进入main，且任务-000055从最新main重新执行、恢复测试交付并通过可信资格复验。
- 数据与安全：未访问服务器、数据库或真实市场数据；未修改原始数据、生产服务、权限、账户或凭据；未生成交易或研究结论。
