# 任务-000098：重放阶段1同版本数据资格决策

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：数据审计
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“不可变门禁决策快照与同版本双重放”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅读重放任务-000094已合并的脱敏证据，不得再次要求用户选择方案
- 执行分支：`codex/task-000098-versioned-replay-exec-v1`
- 开始时间：`2026-08-12T19:08:00+08:00`
- 实现提交SHA：`82250cf55149b43a8f5b7e872f140bd7bfebb697`
- Pull Request：`#267`
- 并行规则：禁止并行；任务-000094是唯一前序依赖；本任务完成前不得登记或执行成本、容量或恢复后继任务

## 依赖与阻塞条件

- 唯一前序依赖：任务-000094已完成；PR #264合并提交为`a2e50bf6ad86ddb1eecb0bff071de1a429347bd2`，最终批次为`stage1-time-quality-20260812T091000Z-6968246516ef`。
- 当前阻塞原因：无。任务-000094已保存5180个正式成员、207个来源拒绝、175个连续段、8个叶子及完整时间/身份/指纹证据；本任务不需要Ubuntu、数据库、网络、凭据或原始业务行。
- 解除条件：已满足。
- 失败安全：任一上游文件、指纹、版本、时间、成员顺序或计数漂移时整批停止，不发布部分通过结果，不回退到任务-000032或任务-000084旧路线。

## 背景

任务-000094已关闭新正式输入的来源身份、三类时间、质量和血缘门，但旧历史重放批次基于任务-000031的630成员旧路线，且因历史策略决策登记不存在而零通过，不能用于当前正式输入。

阶段1未启动策略研究，不得为闭门而虚构历史交易决策。任务-000094本身已产生一次真实、带时间和确切版本的数据资格门禁决策，可用于证明历史现场重建、无未来数据和确定性。策略、模型和交易决策开始生成后，仍必须另行满足原决策重放合同。

## 任务目标

1. 冻结任务-000094数据资格决策的唯一身份、决策时间、输入版本、规则版本、执行器版本和内容指纹。
2. 从已审计的脱敏成员证据复算5180个正式成员、391个质量拒绝、207个来源拒绝、2个观察项、175个连续段和8个叶子，不读原始业务行。
3. 依据`source_visible_at <= decision_at`与`collected_at <= decision_at`重建当时可见集，证明未来或修订数据不能进入旧决策。
4. 使用两个完全独立的派生过程连续重放，规范输出必须逐字节相等。
5. 只对8个叶子的历史重放门进行有证据的更新；成本与执行、容量和恢复保持无法判定。

## 固定执行方案

1. 固定加载任务-000094最终批次的7个文件和合并提交，从配置中复核精确字节SHA、输出载荷指纹、完整批次指纹和任务合同指纹。
2. 以批次`completed_at`所标识UTC秒的闭区间末端为决策时间边界，同时保存原始秒级值；任一成员的来源可见或采集时间晚于该秒末时失败关闭。
3. 可逆解码成员和连续段，按`标的 + 精确合约 + 数据对象 + 日期/尺度`稳定排序；逐组守恒，禁止跨组补偿。
4. 第一次和第二次重放分别从不可变输入字节重新解码，不复用派生对象；对比计数、成员顺序、分组、叶子和规范JSON SHA-256。
5. 生成追加式批次；发布前第三次复核上游文件，逐临时文件回读SHA，仅在全部一致时原子更名。
6. 用临时负向样例验证未来可见、版本漂移、重复、结构异常和跨对象补偿必须拒绝；负向样例不进入正式批次。

## 默认工程决策

使用Python标准库、规范JSON、内容寻址、单进程与临时目录原子发布；复用任务-000094列式JSON解码及身份检查逻辑，但重放输出不调用任务-000094的第一次派生结果。普通取舍依次选择失败最保守、证据最完整、依赖最少和运行成本最低的实现，不再向用户提供备选方案。

## 允许停止条件

只有需要新增凭据、付费能力、生产权限、访问仓库外业务正文、修改原始数据或不可逆操作，以及现行任务合同发生直接冲突时才停止。数据证据拒绝、普通工程选择或测试失败不属于人工决策条件；必须保守记录并自动修复或进入有证据的阻塞。

## 输入合同

- 任务-000094已合并任务文件、合并提交与最终批次全部文件；
- 正式输入指纹`a1c69d6e2446c364578edb08e6b6fa30912f2e5577d7e51377ef8ab4c648952d`；
- 任务-000094输出载荷指纹`c9cef13395b3f777854d29736e8f9755af6dcaf40ec640a197bc77afdd79a98c`与完整批次文件集合指纹`7d01f26fe70874afd01d979fa3642bcf3de24c484ba5830b1d75f0a8d92325d0`；
- 5180个正式成员、207个来源拒绝、175个连续段、8个叶子和2个观察项；
- 标的只允许BTC、ETH，主研究尺度只允4小时、8小时、24小时、48小时；15分钟、1小时只作事后结果观察窗口。

## 输出合同

- `docs/数据/阶段1同版本历史重放合同.md`；
- `config/审计/任务-000098阶段1同版本历史重放.json`；
- `scripts/审计/重放阶段1同版本数据资格决策.py`与专项测试；
- `artifacts/审计/阶段1同版本历史重放/<批次>/`不可变验证清单、决策身份、两次重放摘要、8叶子和资源/安全事实；
- 更新阶段1审计报告、数据缺口清单、README、总体计划、任务文件与看板。

## 工作范围

实现固定输入加载、全部指纹复算、决策时间可见集、成员/分组/连续段/叶子守恒、两次独立确定性重放、未来数据负向验证、追加式原子发布和派生文档同步。

## 不在范围

- 不访问Ubuntu、数据库、网络、账户、凭据或生产系统；
- 不再次读取、下载、扫描或复制原始市场数据；
- 不修改、删除、覆盖、迁移或修复原始数据和历史批次；
- 不生成或补写历史策略/交易决策，不训练模型、回测或估计胜率收益；
- 不修改成本与执行、容量、恢复门或阶段2状态。

## 安全边界

只读仓库内脱敏证据；输出不含价格、数量、成交编号、业务正文、主机信息或凭据。执行单进程，最大RSS 512MiB，总超时120秒，最多6000个成员，单文件5MiB，输出合计25MiB。任一输入漂移、资源超限或发布冲突都失败关闭。

## 验收标准

1. 决策身份精确绑定任务-000094合并提交、最终批次、决策时间、输入、规则、配置、合同、执行器和输出指纹；
2. 5180个正式成员、207个来源拒绝、391个质量拒绝、2个观察项、175个连续段和8个叶子均守恒；
3. 每个正式成员的来源可见与采集时间不晚于来源`completed_at`所标识UTC秒末；下一秒及更晚时间的负向测试失败关闭；
4. 两次独立重放的计数、排序、分组、叶子和规范输出SHA-256逐项一致；
5. BTC、ETH、合约、数据对象和尺度分别裁决，不得交叉补偿；
6. 仅8个叶子的历史重放门可更新为通过；成本与执行、容量和恢复保持无法判定，阶段1/阶段2不提前放行；
7. 正式产物追加式、指纹可复算、不含业务正文或敏感信息，原始数据和历史批次不变；
8. 任务专项、审计、数据和研发中心测试，Python编译、变更Markdown、敏感扫描与`git diff --check`全部通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m pytest -q tests/审计/test_重放阶段1同版本数据资格决策.py
PYTHONHASHSEED=0 python3 -m pytest -q tests/审计
PYTHONHASHSEED=0 python3 -m pytest -q tests/数据
PYTHONHASHSEED=0 python3 -m pytest -q tests/研发中心
python3 -m py_compile scripts/审计/重放阶段1同版本数据资格决策.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

另须运行一次真实、只读、双重放批次，保存决策身份、两次输出指纹、成员/分组/叶子守恒、资源和源证据不变事实。

## 完成定义

合同、配置、执行器、专项测试和真实不可变批次全部交付；任务-000094数据资格决策以同一历史可见集连续两次字节一致重建，8个叶子历史重放门有证据通过，其他三门保守阻塞；双只读评审、main可信复验和自动合并完成，随后独立状态闭环将本任务标记为`已完成`。

## 数据与安全影响

仅读取仓库内任务-000094脱敏证据，不读原始业务行，不访问Ubuntu、数据库、网络、账户或凭据；不修改原始数据、生产系统、权限、防火墙或任何历史批次。

## 已知限制

本任务证明数据资格门禁决策可按历史可见输入重放，不证明尚未产生的策略、模型或交易决策可重放；这些决策一旦开始生成，仍必须继续遵守任务-000022和《可信重放来源与历史决策现场合同》。

## 回滚方式

回滚本任务PR新增的合同、配置、执行器、测试、批次和派生文档更新；不删除或改写任务-000022、任务-000032、任务-000092至000094、原始归档或其他历史证据。

## 执行记录

- 真实批次：`stage1-versioned-replay-20260812T111640Z-ef65e3cfc47d`；候选5387、正式成员5180、质量已证明4789、质量拒绝391、来源拒绝207、观察项2、连续段175、叶子8全部守恒。
- 双重放：两个派生过程分别从任务-000094七个冻结文件重新加载，规范输出SHA-256均为`5d562dca473dd0bb8a8bc673490818920fc853e2d546c609b7d8de1b409f029f`；输出载荷指纹为`207014ce33146cdbc5821ca0b83f4c662f25b686ff22101b506cdf4723034993`。
- 时间边界：任务-000094的来源`completed_at`为秒级，固定决策边界为该UTC秒末`2026-08-12T10:19:32.999999Z`；所有正式成员可见和采集时间均不晚于边界，下一秒及更晚负向样例失败关闭。
- 门禁：仅历史重放门更新为通过；成本与执行、容量、恢复继续无法判定。八叶子仍阻塞，阶段1和阶段2不放行。
- 资源与安全：单进程耗时0.053520秒、最大RSS 38,993,920字节、输出64,653字节；只读约1.3MB脱敏证据，没有重扫854GB原始归档，未访问Ubuntu、数据库、网络、凭据或原始业务行，未修改历史批次或原始数据。
- 验证：专项6项、审计223项、数据185项、研发中心211项通过；Python编译、变更Markdown、敏感扫描及`git diff --check`通过。
- 已知限制：本批次只证明任务-000094数据资格决策可重放；真实策略、模型或交易决策产生后仍须独立登记和重放。阶段1尚缺成本与执行、研究容量、隔离恢复三个硬门。
