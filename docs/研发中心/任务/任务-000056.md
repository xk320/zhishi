# 任务-000056：修复治理任务测试路径授权冲突

<!-- markdownlint-disable MD013 -->

- 状态：待执行
- 类型：治理
- 阶段：阶段1 研发自动化治理
- 优先级：P0
- 执行方案：方案A“最小自动化范围授权与任务合同对齐”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；任务-000033已完成后执行；不得与任务-000055、任务-000034至任务-000037并行
- 执行分支：`codex/task-000056-blocked-governance-repair-v1`
- 开始时间：`2026-08-05T03:20:00+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000033已完成；本任务是任务-000055阻塞后的治理修复，不依赖数据闭环。
- 当前阻塞原因：治理规则修复任务-000058已进入main，已建立严格绑定现有任务-000056合同语义的受控资格路径；可从最新main重新执行本任务。
- 解除条件：从最新main创建执行分支并按照本任务唯一方案验证任务-000055合同授权；在PR合并前保持任务-000055阻塞。

## 背景

任务-000055要求交付现行状态与范围的机器可验证负向测试，但其登记合同为普通治理任务，
未声明治理自动化范围，导致包含`tests/研发中心/*.py`的候选交付头被main可信资格拒绝。
该冲突必须通过明确、可审计的任务合同授权修复，不能删除测试、绕过可信检查器或降低研究安全门。

## 任务目标

1. 为任务-000055补齐仅限治理自动化的受控授权，使其机器测试交付路径可由可信规则复验；
2. 保持任务-000055的文档、双标的范围、历史证据和安全边界不变；
3. 明确授权只覆盖研发中心治理脚本/测试与文档，不扩大到生产、服务器、数据或交易路径；
4. 让任务-000055可以从阻塞状态通过独立状态闭环恢复后重新执行。

## 固定执行方案

1. 只在任务-000055任务文件中增加唯一的`自动合并范围：治理自动化`字段，并同步说明其最小边界；
2. 若治理策略文档需要澄清，限于`docs/治理/PR自动合并策略.md`的文字对齐，不修改可信脚本；
3. 不修改任务-000055的目标、输入、输出、研究范围、验收标准、安全边界或历史执行记录；
4. 使用现有可信资格检查器验证：带任务-000055机器测试的候选头仅因授权字段变化而可进入受控路径；
5. 通过独立PR合并后，再由任务-000055状态闭环恢复并重新执行。

## 默认工程决策

- 采用最小字段授权，不扩大全局白名单，不修改路径检查代码，不引入依赖。
- 授权只对明确写入该任务合同的治理自动化范围生效；其他治理任务仍保持文档低风险路径。
- 任何生产、服务器、原始数据、真实市场、账户、凭据或交易逻辑变更都失败关闭。
- 验证使用固定基线、候选头、任务正文和脱敏元数据，资源保持两个评审者、单进程、Node堆256 MiB。

## 允许停止条件

- 需要修改可信脚本、工作流、生产服务、服务器、数据库、原始数据、权限或凭据；
- 无法在不改写任务-000055目标和安全边界的情况下完成最小授权；
- 发现授权会让非治理自动化路径或其他任务获得额外资格。

## 输入合同

- 任务-000055最新阻塞状态、PR #112合并事实和候选头拒绝证据；
- `docs/治理/PR自动合并策略.md`、`scripts/研发中心/验证自动合并资格.py`及现有治理测试；
- 任务文件—看板唯一事实和自动合并范围字段规则。

## 输出合同

- 更新后的`docs/研发中心/任务/任务-000055.md`，仅增加受控治理自动化授权字段；
- 必要时更新`docs/治理/PR自动合并策略.md`的对应文字；
- 本任务文件、看板、设计文档、提交、PR与验证证据。

## 工作范围

- 对齐任务-000055的自动合并范围合同与其机器测试交付要求；
- 增加或更新授权字段的治理回归测试（仅研发中心路径）；
- 验证任务映射、阻塞恢复前置条件和受控路径边界。

## 不在范围

- 不执行任务-000055的文档修复或测试交付；
- 不修改任务-000055状态；恢复由独立状态闭环完成；
- 不修改生产代码、服务器、数据库、真实市场数据、原始数据、账户权限或交易逻辑；
- 不扩大到`src`交易策略、部署、工作流或生产路径。

## 安全边界

- 仅允许任务合同、治理文档、研发中心测试和看板变更；
- 不提交IP、域名、用户名、密钥、密码、令牌、原始日志或市场记录；
- 授权字段必须唯一、可复算、可回滚，不能替代数据、研究或交易质量门。

## 验收标准

1. 任务-000055保留原目标、范围、输入、输出、安全边界和研究尺度，仅新增唯一授权字段；
2. main可信资格能区分：无授权的普通治理任务仍拒绝测试路径，有授权的任务-000055候选头可进入受控路径；
3. 任务文件与看板唯一映射保持一致，任务-000055仍为阻塞，任务-000034仍为下一数据任务；
4. 未修改可信脚本、工作流、生产代码、数据和历史证据；
5. 研发中心测试、审计测试、Markdown、敏感信息、跨载体和`git diff --check`通过。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/*.py tests/研发中心/*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
python3 scripts/研发中心/验证自动合并资格.py --repo-root . --base-ref <base-sha> --head-ref <head-sha> --metadata <metadata-path>
git diff --check
```

## 完成定义

最小授权字段、边界测试和文档对齐通过双只读评审、main可信资格复验并合并；任务-000055仍保持阻塞，
随后由独立状态闭环恢复，不能在本任务中伪造其完成或交付。

## 执行记录

- 执行分支：`codex/task-000056-blocked-governance-repair-v1`
- 开始时间：`2026-08-05T03:20:00+08:00`
- 尝试命令：`python3 scripts/研发中心/验证自动合并资格.py --repo-root . --base-ref ff87de50c3a45ef22e66b353322099ef9f67fab0 --head-ref c8c4f8f6b9e53bbaa203d8aa8c5bcbd89d191439 --metadata /tmp/zhishi-pr113-metadata`
- 结果：任务-000056登记已合并，但其目标需要修改处于阻塞状态的任务-000055合同；现有可信状态/交付闭环不允许该跨任务合同修复，不能通过猜测或绕过规则继续。
- 外部证据：任务-000055已由PR #112如实标记阻塞；任务-000056登记PR #113已合并；当前规则未提供阻塞任务合同修复的可审计变更类型。
- 阻塞原因：状态闭环和任务交付资格之间缺少有证据的阻塞任务合同修复路径。
- 解除条件：新增治理规则任务进入main并通过双评审、可信复验；恢复后从最新main重建任务-000056执行分支。
- 数据与安全：未访问服务器、数据库或真实市场数据；未修改原始数据、生产服务、权限、账户或凭据；未生成交易、模型、回测、胜率或收益。
