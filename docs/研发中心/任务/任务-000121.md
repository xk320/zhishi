# 任务-000121：补齐阶段1合同修订跨载体类型识别

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000121-stage1-conflict-type-v1`
- 开始时间：`2026-08-15T03:41:00+08:00`
- 类型：治理
- 阶段：研发中心可信合同治理
- 优先级：P0
- 执行方案：方案A“固定类型与目标映射的最小冲突检查修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务完成前不得执行任务-000115的覆盖受限合同修订PR

## 依赖与阻塞条件

- 唯一前序依赖：任务-000116已完成并进入main。
- 当前阻塞原因：跨载体冲突检查器的变更类型解析未登记`阶段1覆盖受限合同修订`，导致合法目标任务-000106修订被通用合同漂移门失败关闭。
- 解除条件：固定类型、任务映射和失败关闭测试进入main，并通过可信资格复验；任务-000115随后必须基于最新main重新执行。

## 背景

任务-000116已经在可信资格器中建立阶段1覆盖受限合同修订入口，但跨载体冲突检查器仍只解析旧变更类型。两个可信载体不一致，造成任务-000115无法在不绕过硬门的情况下推进。

## 任务目标

1. 在跨载体冲突检查器登记固定变更类型`阶段1覆盖受限合同修订`。
2. 将该类型绑定到唯一执行任务-000115和目标任务-000106，避免通用合同漂移检查误报，同时保留任务资格器的完整合同指纹与路径校验。
3. 增加正向类型识别、错误类型、错任务、额外路径和元数据漂移的失败关闭测试。

## 固定执行方案

1. 仅修改跨载体冲突检查器、对应研发中心测试和本任务交付记录。
2. 复用任务-000116已经进入main的固定常量、任务编号和失败安全原则，不新增通用合同编辑能力。
3. 让未知变更类型、缺失任务编号、非`main`目标、外部仓库、分支或PR元数据漂移继续失败关闭。

## 默认工程决策

- 使用Python标准库和现有测试夹具，不新增依赖、服务、数据库或网络访问。
- 由主可信资格器负责目标合同全文指纹、路径和状态验证；跨载体检查器只负责识别同一固定类型并传递目标豁免参数。
- 测试单进程运行，内存上限沿用研发中心现有约束。

## 允许停止条件

- 需要修改任务-000115、任务-000106、任务-000116合同或其状态、看板、批次和历史证据。
- 需要扩大阶段1路径白名单、放宽真实交易、未来数据、跨标的补偿、研究准入或交易许可。
- 可信资格器无法同时保持目标任务、执行任务、路径和PR元数据的固定绑定。

## 输入合同

- `scripts/研发中心/验证跨载体冲突.py`当前类型解析、合同漂移和元数据检查。
- `scripts/研发中心/验证自动合并资格.py`中任务-000116建立的阶段1固定入口。
- `docs/研发中心/任务/任务-000116.md`、任务-000115和任务-000106的现行合同事实。

## 输出合同

- 跨载体检查器对固定阶段1变更类型的识别和最小目标豁免传递。
- 正向与失败关闭专项测试。
- 任务-000121执行记录、PR和验证结果；不交付研究、数据或交易能力。

## 工作范围

- 跨载体冲突检查器的固定类型解析与任务-000106目标参数传递。
- 对应研发中心测试、任务文件状态记录和看板派生状态。

## 不在范围

- 不修改任务-000115、任务-000106或任务-000116合同、状态、看板、历史批次和失败证据。
- 不修改市场数据、服务器、数据库、原始文件、生产服务、账户权限或凭据。
- 不执行覆盖受限研究、模型、回测、运行时、模拟订单或真实交易。

## 安全边界

- 类型识别不等于自动批准；任务-000115的完整可信资格、双评审和main复验仍是后续硬门。
- 不使用未来数据，不跨BTC/ETH补偿，不生成胜率、收益、方向、仓位、订单或交易许可。
- 任何未知类型、错目标、额外路径或证据缺失均失败关闭。

## 验收标准

1. 跨载体检查器识别`阶段1覆盖受限合同修订`并绑定任务-000115→任务-000106。
2. 任务-000115、任务-000106、任务-000116及看板在本任务中逐字不变。
3. 错任务、错类型、额外路径、非main、外部仓库和元数据漂移测试失败关闭。
4. 研发中心专项测试、Python编译、MarkdownLint和`git diff --check`通过。
5. 变更仅治理脚本、测试和任务记录，无服务器、数据库、生产、数据或交易越界。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证跨载体冲突.py tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile scripts/研发中心/验证跨载体冲突.py tests/研发中心/test_验证跨载体冲突.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 docs/研发中心/任务/任务-000121.md docs/研发中心/看板.md docs/superpowers/specs/task-000121-stage1-conflict-type-registration-design.md
git diff --check
```

## 完成定义

修复进入main并通过双只读评审、可信资格复验和自动合并；任务-000121状态闭环为已完成。任务-000115必须另行基于最新main执行，不因本任务自动完成。

## 数据与安全影响

只修改治理脚本、测试和治理记录；不读取或写入服务器、数据库、市场数据、原始数据、生产服务、凭据或真实账户。

## 已知限制

本任务只修复可信载体间的类型识别，不提供成本、延迟、覆盖或交易证据，也不改变任务-000106阻塞事实。

## 回滚方式

回滚本任务PR即可恢复旧类型解析；不删除或覆盖任务-000115、任务-000106、任务-000116历史合同和批次。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000121-stage1-conflict-type-v1`
- 开始时间：`2026-08-15T03:41:00+08:00`
- 实现提交SHA：`8da15432712ec3a9779ae315440351369ca196fa`
- Pull Request：[#347](https://github.com/xk320/zhishi/pull/347)
- 交付物：阶段1固定变更类型识别、任务-000106目标参数传递和失败关闭测试。
- 验证结果：跨载体专项通过；Python编译通过；变更Markdown 3份为0 issues；`git diff --check`通过。
- 已知限制：任务-000115需基于包含本修复的最新main重新执行；本PR不提供成本、延迟或交易证据。
- 数据与安全影响：未访问服务器、数据库、市场数据、原始数据、凭据、生产或真实交易路径。
- 回滚方式：回滚PR #347恢复旧类型解析，不删除或覆盖任务合同与历史证据。
