# 任务-000074：阶段1最终审计增量复验

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 类型：数据审计
- 阶段：阶段1最终审计与阶段2门禁增量复验
- 优先级：P0
- 执行方案：方案A“全链指纹复验、逐叶子硬门裁决与保守阶段结论”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅使用已合并仓库证据，不请求方案选择，不访问未授权外部数据
- 并行规则：禁止并行；唯一前序依赖任务-000073；不得与其他阶段1审计、数据复采、质量、重放或成本任务并行
- 执行分支：`codex/task-000074-stage1-final-audit-recapture-v1`
- 开始时间：`2026-08-07T16:25:32+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000073（已完成）。
- 输入依赖：任务-000029至任务-000037历史合同与批次、任务-000069至任务-000073追加合同与批次、最新缺口清单和规则指纹。
- 当前阻塞原因：无；输入缺失、指纹漂移、证据冲突或门禁无法判定时必须发布失败安全结论，不得请求人工补齐。
- 解除条件：同一精确范围的8个叶子全部取得来源、时间、质量、重放、成本、血缘、容量和恢复硬门的完整通过证据；否则阶段2保持阻塞。

## 背景

任务-000037的最终审计早于任务-000069至任务-000073。后续任务追加了证据，但任务-000072和任务-000073
已明确来源身份与三类时间缺口仍未关闭。必须在不改写历史证据的前提下重新复验，避免沿用过期结论或
把局部复采误报为阶段放行。

## 任务目标

1. 冻结并复算任务-000029至任务-000037及任务-000069至任务-000073的全部输入、规则、成员和内容指纹。
2. 对BTC、ETH分别构建4小时、8小时、24小时、48小时8个最终叶子，保持场所、市场、合约、对象和时间范围未知时的阻塞。
3. 逐叶子裁决来源身份、三类时间与研究尺度、质量、历史重放、成本与执行、血缘、容量和恢复八道硬门。
4. 生成追加式最终审计批次、缺口变化和唯一阶段2门禁结论；不因任务完成数量或局部证据通过而放行。

## 固定执行方案

1. 从最新`main`读取历史最终审计及任务-000069至任务-000073的不可变批次，计算并记录所有输入指纹。
2. 按标的和主研究尺度确定性生成8个叶子，逐门复算状态和证据定位，保留候选总体与全部状态计数。
3. 采用最低关键门聚合；任一门拒绝、无法判定、输入冲突或历史证据不可复算，叶子保持`阻塞`。
4. 追加新批次和报告，不覆盖历史批次；同步派生结论，但不改变上游任务合同或历史状态。

## 默认工程决策

- BTC与ETH独立裁决，缺失不得跨标的补偿；主研究尺度固定为4小时、8小时、24小时、48小时。
- 15分钟和1小时仅为事后结果观察窗口；不得成为状态、事件或许可尺度。
- `拒绝`、`无法判定`、`失败`、`未成熟`和`失效`均保留，状态计数必须守恒。
- 描述性差异不能解释为因果、预测优势、胜率、收益、研究准入或交易许可。
- 只有8个叶子所有硬门均为通过且范围字段完整时，阶段2才可精确解除；否则结论为禁止。

## 允许停止条件

- Git历史、不可变批次或可信规则无法取得且无法复算时停止相关叶子并记录阻塞。
- 发现需要修改历史产物、访问服务器或数据库业务正文、读取凭据、修改生产权限或执行不可逆操作时停止。
- 证据不足、结果为空或所有叶子阻塞不是请求人工选择的理由，必须完成失败安全报告。

## 输入合同

- 任务-000029至任务-000037的合并提交、最终审计批次、报告和缺口清单；只读。
- 任务-000069至任务-000073的合并提交、任务合同、追加批次、配置、规则和执行器指纹；只读。
- `AGENTS.md`、任务中心规范、研究准入规范、数据验证阶段执行规范和最新范围合同；只读。
- BTC、ETH及4小时、8小时、24小时、48小时精确研究范围；短窗口仅作为结果观察窗口。

## 输出合同

- `docs/审计/阶段1最终审计报告.md`的追加版本及对应不可变批次目录；
- `artifacts/审计/阶段1最终审计/<新批次>/`叶子裁决、统计、缺口和输入指纹；
- `docs/审计/数据缺口与补采清单.md`、README、总体计划和路线图的精确派生更新；
- 任务文件、看板、提交、PR、评审证据、验证命令、真实结果、限制与回滚说明。

## 工作范围

- 只读复算输入与内容指纹，构建8个叶子，执行八道硬门裁决和状态守恒核对。
- 检查历史批次未被覆盖，复核未来数据、跨标的补偿、短窗口升级和未经证据支持的结论。
- 运行阶段审计、研发中心、审计、数据、编译、MarkdownLint、敏感信息和差异验证。

## 不在范围

- 不访问服务器、Ubuntu、数据库业务正文或真实市场数据，不修改数据库、生产服务、权限、防火墙、原始数据或账户。
- 不训练模型、回测、模拟交易，不生成方向、仓位、订单、胜率、收益或交易许可。
- 不修复上游数据，不重写历史合同或批次，不把任务完成标记解释为阶段1通过。

## 安全边界

- 所有输入只读、追加式、可复算；历史批次和原始文件保持不变。
- 未知、冲突、漂移、未来数据、敏感信息或状态不守恒时失败关闭。
- 不记录地址、用户名、令牌、密码、原始业务记录或任何账户凭据。

## 验收标准

1. 所有输入提交、规则、成员和内容指纹复算一致；历史批次未被修改。
2. 8个叶子按BTC/ETH和4/8/24/48小时完整覆盖，候选总体与所有状态计数守恒。
3. 每道硬门均有状态、证据、限制和解除条件；未知不被默认值补齐。
4. 报告、缺口清单、README、总体计划、路线图、任务文件和看板结论一致。
5. 阶段2只有8个叶子全部通过且研究范围完整时才解除，否则明确保持阻塞。
6. 全部验证命令通过，PR经双只读评审和main可信复验后自动合并；任务状态闭环真实记录。

## 验证命令

```bash
python3 -m unittest tests/审计/test_阶段1最终审计.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/执行阶段1最终审计.py tests/审计/test_阶段1最终审计.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须复算全部输入和新批次指纹，核对任务与看板唯一映射，并如实记录资源预算和安全扫描结果。

## 完成定义

新增最终审计批次、报告、精确缺口派生状态和全部验证记录已提交；PR经双只读评审、main可信复验并自动合并，
随后独立状态闭环将任务-000074标记为`已完成`。任务完成不等于阶段1通过；阶段2只有报告明确证明8个叶子
全部硬门通过时才可解除，否则继续禁止并记录下一解除条件。

## 执行记录

- 交付状态：实现已完成，待创建Pull Request并进行双只读评审；未将阶段1或阶段2标记为通过。
- 执行分支：`codex/task-000074-stage1-final-audit-recapture-v1`；开始时间：`2026-08-07T16:25:32+08:00`。
- 增量审计批次：`final-20260807T082700Z-v4`；批次内追加证据索引绑定任务-000069至任务-000073的合并提交、任务文件和批次文件SHA-256。
- 结果：候选总体2520、已观察2520、拒绝56、无法判定2464、失败0、未成熟0、失效0；BTC与ETH各1260候选，8个叶子全部阻塞，允许研究范围为空，阶段2继续禁止。
- 验证结果：阶段1专项5项、研发中心194项、审计168项、数据65项通过；Python编译通过；MarkdownLint 178个文件0 issues；`git diff --check`通过；批次输出和追加索引指纹复算通过。
- 已知限制：来源身份、三类时间、质量、重放、成本、容量和恢复硬门仍未形成完整通过证据；本任务不访问服务器、数据库业务正文或真实市场数据。
- 数据与安全：只读仓库内不可变证据；未修改历史批次、原始数据、生产服务、权限、账户或凭据；未训练模型、回测、模拟交易或生成交易许可。
