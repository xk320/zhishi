# 任务-000116：建立阶段1合同修订的受控资格路径

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：治理
- 阶段：研发中心可信合同治理
- 优先级：P0
- 执行方案：方案A“固定双映射与失败关闭验证”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务完成前不得执行任务-000115的目标合同修订PR
- 开始时间：`2026-08-15T00:40:56+08:00`
- 执行分支：`codex/task-000116-governed-contract-revision-route-v1`
- Pull Request：[#325](https://github.com/xk320/zhishi/pull/325)
- 实现提交SHA：`edc3dbe710d4754c059a271ad13e2b7ec3296167`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000105已完成；任务-000115登记PR #322已进入main。
- 前置事实：任务-000115首次阻塞闭环PR #323因其基线合同缺少`当前阻塞原因`字段而被可信规则失败关闭；本任务只修复治理入口，不伪造任务-000115或任务-000106状态。
- 目标映射一：任务-000116→任务-000115，仅允许补齐任务-000115首次阻塞所需的唯一`当前阻塞原因`字段，目标保持`待执行`，看板保持不变。
- 目标映射二：任务-000115→任务-000106，新增独立变更类型`阶段1覆盖受限合同修订`，仅允许在任务-000115完成执行后按其合同修订任务-000106当前适用规则；历史批次、执行记录和原始数据不可变。
- 当前阻塞条件：可信资格器、自动合并工作流和治理协议尚未登记上述两个映射。
- 解除条件：双映射、路径白名单、目标状态、合同指纹和负向测试进入main并通过双只读评审；随后任务-000116完成，任务-000115才可恢复执行。

## 背景

普通任务交付不能修改另一个阻塞任务合同，现有可信规则只登记历史任务-000056→000055和任务-000086→000084映射。任务-000115需要在不绕过任务中心的前提下修订任务-000106的阶段1适用规则；同时其首次阻塞闭环缺少基线字段。必须先建立精确、不可泛化的治理入口。

## 任务目标

1. 在main可信校验器、自动合并工作流和治理文档中固定任务-000116→任务-000115的单字段基线补齐路径。
2. 固定新的变更类型`阶段1覆盖受限合同修订`及唯一执行任务-000115→目标任务-000106映射。
3. 为两个映射补齐正向、错目标、错状态、额外路径、合同漂移、看板漂移和生产/数据越界负向测试。
4. 保持普通任务登记、任务交付、状态闭环、既有阻塞合同修复和任务合同冲突修复规则不放宽。

## 固定执行方案

1. 在可信规则中增加固定常量和最小解析分支，不实现通用合同编辑器。
2. 工作流严格识别新的变更类型，仍从main可信脚本验证完整base/head SHA、PR正文和路径事实。
3. 映射一只允许在任务-000115仍为`待执行`时新增一行固定`当前阻塞原因`，不得改状态、看板、其他字段或数据。
4. 映射二只允许任务-000115在基线为`待执行`、头部为`待评审`时，目标任务-000106在基线和头部均为`阻塞`；目标只可按任务-000115合同更新当前适用规则及其派生入口，历史执行记录、批次、SHA和原始数据保持不变。
5. 任一未知任务、重复引用、额外任务、状态迁移、路径越界、合同指纹漂移、未来数据、服务器/数据库/交易路径均失败关闭。

## 默认工程决策

- 复用现有Python标准库、变更类型解析、任务合同指纹和看板复算器；不新增依赖。
- 新变更类型使用中文固定字符串`阶段1覆盖受限合同修订`，不修改既有类型语义。
- 测试保持单进程，沿用现有内存与超时边界；错误默认拒绝。
- 只记录脱敏治理事实，不读取服务器、数据库、凭据或业务网络数据。

## 允许停止条件

- 需要修改生产服务、数据库、权限、防火墙、原始数据、真实账户或不可逆资源。
- 无法把映射限制为固定执行任务、固定目标、固定字段/路径和固定状态。
- 新规则会放宽未来数据、跨标的补偿、真实交易、研究准入或自动合并评审门。
- 既有规则回归测试无法保持全绿，或双只读评审发现P0/P1且三轮修复仍无法消除。

## 输入合同

- `scripts/研发中心/验证自动合并资格.py`及其现有任务登记、任务交付、状态闭环规则。
- `.github/workflows/pr-auto-merge-eligibility.yml`、`.github/workflows/pr-auto-merge.yml`。
- `docs/治理/PR自动合并策略.md`、`docs/研发中心/任务规范.md`和任务-000115/000106合同。
- 任务-000115登记PR #322合并事实及失败关闭PR #323的脱敏原因。

## 输出合同

- 双映射可信资格规则、自动合并工作流变更类型同步和专项正反测试。
- 治理策略与任务中心规范中的固定映射说明。
- 任务-000116执行记录、PR和验证证据；不修改任务-000115/000106合同或数据事实。

## 工作范围

- 可信合并资格脚本、自动合并工作流变更类型解析、研发中心治理文档和专项测试。
- 固定映射的状态、路径、合同指纹、看板和失败安全验证。

## 不在范围

- 不修改任务-000115、任务-000106、阶段计划、数据缺口、历史批次或原始数据。
- 不执行覆盖受限研究、不采集市场数据、不访问服务器/数据库/主网、不训练模型、不回测。
- 不生成订单、交易信号、胜率、收益、方向、仓位或交易许可。

## 安全边界

- 新规则只允许治理自动化路径，权限继续为只读检查和现有合并流程。
- 目标任务状态、看板和历史证据不可由本任务迁移或伪造。
- 错误、缺证据、API/JSON失败、路径或SHA不一致均失败关闭。

## 验收标准

1. 任务-000116→任务-000115单字段补齐路径能通过正向测试，任何额外字段/路径/状态迁移失败。
2. `阶段1覆盖受限合同修订`只能引用任务-000115并固定目标任务-000106；目标保持阻塞，历史执行记录/批次/SHA不变。
3. 既有任务登记、任务交付、合并后状态闭环和两条历史阻塞合同修复路径回归通过。
4. 工作流解析新变更类型并保持GH_TOKEN、main可信脚本、完整SHA和失败关闭规则。
5. 负向测试覆盖错任务、错目标、重复引用、额外路径、合同漂移、看板漂移、真实交易和数据越界。
6. Python编译、研发中心专项测试、MarkdownLint、敏感扫描和`git diff --check`通过；双只读评审P0/P1为零。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

双映射可信规则、工作流同步、正反测试和治理文档进入main并自动合并；任务-000116状态闭环为已完成。任务-000115/000106状态和合同仍按后续独立PR处理。

## 数据与安全影响

仅修改治理脚本、工作流、测试和治理文档；不访问或修改服务器、数据库、原始数据、凭据、生产或真实交易。

## 已知限制

本任务只建立受控资格入口，不代表任务-000115或任务-000106完成，不代表阶段1、研究准入或交易许可通过。映射二执行时仍必须以任务-000115合同和目标历史事实为准。

## 回滚方式

回滚任务-000116 PR即可移除新增变更类型和双映射规则；不修改任务-000115/000106历史合同、失败PR或数据批次。

## 执行记录

- 执行分支：`codex/task-000116-governed-contract-revision-route-v1`
- 开始时间：`2026-08-15T00:40:56+08:00`
- 尝试命令：按任务合同运行可信资格专项测试、Python编译、MarkdownLint和`git diff --check`。
- 结果：双映射规则、工作流变更类型、治理文档和正反测试已完成；167项研发中心专项测试通过，Python编译通过，4份变更Markdown为0 issues，diff-check通过。
- 外部证据：PR #325已创建；实现提交SHA为`edc3dbe710d4754c059a271ad13e2b7ec3296167`；未访问服务器、数据库、市场数据或真实交易。
- 阻塞原因：无；等待双只读评审及main可信复验。
- 解除条件：两个评审角色均APPROVE且P0/P1为零，main可信工作流以PR #325当前完整头SHA复验并合并。
- 数据与安全：仅治理脚本、工作流、测试和Markdown变更；未修改任务-000115/000106合同、历史批次、原始数据、生产或凭据。
