# 任务-000080：来源身份声明证据入口复核与缺口冻结

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 执行分支：`codex/task-000080-source-identity-entry-review-v1`
- 开始时间：`2026-08-08T05:54:13+08:00`
- 实现提交SHA：`e10f7b2cef24edefc42313bf4fc8675fb040c9cd`
- Pull Request：[#210](https://github.com/xk320/zhishi/pull/210)
- 合并时间：2026-08-07 22:37:14 +0000
- 合并提交SHA：`43ac38f835461342897929afa9c03b1450297d49`
- 类型：数据治理
- 阶段：阶段1来源身份证据闭环
- 优先级：P0
- 执行方案：方案A“复核显式声明入口、逐字段证据可见性与失败安全缺口冻结”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；仅使用最新main、已合并任务-000079批次和已登记的只读证据入口，不请求方案选择或重复批准
- 并行规则：禁止并行；唯一前序依赖任务-000079；不得与来源身份、时间、质量、重放、成本或最终审计任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000079已完成，但其最终批次630个BTC/ETH成员中已证明0、拒绝12、无法判定618，`ZS-DATA-GAP-001`仍阻塞。
- 输入依赖：任务-000029来源身份合同、任务-000071结构证据、任务-000075至任务-000079不可变批次、当前缺口台账、当前阶段1审计报告和执行时最新`main`。
- 执行前阻塞：如果任务-000079最终批次、成员指纹、声明配置或缺口记录不能从`main`复算，必须停止并记录输入漂移，不得重建或覆盖历史证据。
- 结果阻塞：显式声明入口为空、字段缺少唯一证据定位、授权或Schema指纹缺失时，只能输出`无法判定`和解除条件，不得标记身份门通过。
- 解除条件：每个候选成员的九个身份字段均有版本化声明、唯一证据定位、当前成员指纹、Schema/授权指纹和可撤销状态；BTC与ETH分别满足，且复验批次可重放。

## 背景

任务-000079已建立声明与证据合同并完成一次失败安全复采，但当前结构化`身份声明`仍为空。继续推进前必须把“没有入口”“入口不完整”“入口存在但证据不匹配”三类事实分开记录，避免把路径、文件名、表名、共同目录或结构观察误当作来源身份。该任务只冻结证据可见性和解除条件，不猜测缺失事实。

## 任务目标

1. 从最新`main`复核任务-000079最终批次、声明配置、输入指纹和缺口记录的一致性。
2. 枚举仓库中已登记的显式来源身份声明入口、证据文件、字段定位、Schema指纹和授权边界；入口为空或字段缺失必须逐项登记。
3. 对BTC、ETH分别按候选资产和身份成员统计入口状态：已登记、可定位、指纹匹配、无法判定、拒绝、失败、未成熟、失效。
4. 追加不可变失败安全复核记录，精确更新`ZS-DATA-GAP-001`的覆盖范围和解除条件；不得关闭其他缺口或阶段门。

## 固定执行方案

1. 读取并冻结任务-000029、任务-000071、任务-000075至任务-000079合同、设计、批次清单和审计报告；保存字节指纹与确定性成员顺序。
2. 只从仓库合同和已登记的结构化证据入口读取声明元数据；不从聊天、Issue、PR描述、路径、名称或常识推断声明值。
3. 按九个字段逐成员检查声明来源、唯一定位、证据文件指纹、当前成员SHA-256、Schema/授权指纹和撤销事实；任一缺失即为`无法判定`。
4. BTC与ETH独立计数；不跨标的补偿，不缩小分母，不重排或覆盖历史批次。
5. 生成一次追加式复核批次和缺口记录；若没有新的可验证入口，批次必须如实记录入口为空和保持阻塞的解除条件。

## 默认工程决策

- 证据、可复现和失败安全优先；复用标准库、既有冻结引擎和任务-000079批次格式，不引入新服务、数据库客户端或常驻系统。
- 默认不访问服务器；只有任务合同已登记的脱敏只读入口存在且无需新增权限时，才可按白名单规则执行最小探针。
- 主研究尺度固定为4小时、8小时、24小时、48小时；15分钟和1小时只能作为事后结果观察窗口。
- 资源上限：单进程、最多两个只读评审者、Node堆上限256MiB、不创建额外工作树；测试串行执行。
- 普通文件组织、脚本实现和验证方式由Codex按安全、证据、简单、可回滚顺序自主决定，不向用户发起A/B/C选择。

## 允许停止条件

- 需要新增账号、凭据、服务器、生产权限、数据库写入、原始数据读取/迁移或不可逆操作。
- 发现来源入口要求读取业务正文、价格、成交、订单簿、账户、环境变量、凭据或未登记路径。
- 输入指纹、成员顺序、状态计数或批次不可复算，或历史证据将被覆盖。
- 发现未来数据、跨标的补偿、结果可见后更换分类或缩小分母。
- 证据不足不是请求人工方案选择的理由；应按失败安全输出`无法判定`并记录解除条件。

## 输入合同

- `AGENTS.md`、`README.md`、`docs/研发中心/README.md`、`docs/研发中心/任务规范.md`、`docs/研发中心/看板.md`、`docs/研发中心/总体计划.md`。
- 任务-000029、任务-000071、任务-000075至任务-000079的任务合同、设计、配置、批次清单、审计报告和缺口台账。
- 任务-000079最终批次`source-identity-declaration-20260808T051806+0800-dd1e3d74dca4`及其输入/规则/执行器指纹。
- 仓库中已登记的来源身份声明入口及其结构化证据；入口不存在时，输入事实为“空集”，不得补造样本。

## 输出合同

- `docs/研究/来源身份声明证据入口复核合同.md`：定义入口类型、字段状态、定位和解除条件。
- `artifacts/数据/来源身份声明证据入口复核/<批次>/`：不可变入口清单、逐成员状态、计数摘要、输入/规则/执行器指纹和失败原因。
- `docs/审计/数据缺口与补采清单.md`、`docs/审计/阶段1最终审计报告.md`：只追加当前真实结果和精确覆盖范围。
- 本任务文件、看板、提交、PR、双只读评审证据与合并后状态闭环记录。

## 工作范围

- 复核并冻结来源身份声明入口和逐字段证据定位的可见性。
- 对BTC、ETH候选成员分别统计入口存在性、证据完整性、指纹匹配和失败安全状态。
- 生成一次可复算、追加式、无敏感信息的复核批次并精确维护`ZS-DATA-GAP-001`。
- 运行专项、数据、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`。

## 不在范围

- 不访问或读取市场业务正文、价格、成交、订单簿、账户、凭据或未登记数据库表。
- 不修改服务器、生产服务、数据库权限、防火墙、原始数据、历史批次或账户。
- 不训练模型、回测、模拟或真实交易，不生成方向、仓位、订单、胜率、收益、因果、预测优势、研究准入或交易许可。
- 不关闭`ZS-DATA-GAP-001`以外的缺口，不把本任务完成解释为阶段1或阶段2放行。

## 安全边界

- 默认仓库内只读；如需远端探针，只允许任务合同登记的逻辑别名和脱敏元数据，不保存地址、用户名、凭据或原始响应。
- 远端写入、数据库业务记录读取、原始业务记录落盘、生产修改和权限变更固定为`false`。
- 批次追加式、原子、不可覆盖；敏感输出、越界、超时、状态不守恒和未来数据均失败关闭。
- `已登记`、`可定位`或结构观察不能升级为`已证明`；仅完整九字段证据匹配才可形成精确解除范围。

## 验收标准

1. 任务-000079最终批次和所有输入指纹可从最新`main`复算，历史目录字节不变。
2. 入口清单明确记录声明入口、字段、唯一定位、证据指纹、Schema/授权指纹和缺失原因；空集必须如实保留。
3. BTC与ETH分别计数，候选总体、已登记、可定位、已证明、拒绝、无法判定、失败、未成熟和失效守恒。
4. 不存在通过路径、名称、表名、共同目录、结构观察或跨标的补偿形成身份结论的路径。
5. `ZS-DATA-GAP-001`只按真实覆盖范围更新；无完整证据时继续阻塞，其他缺口和阶段门不变。
6. 专项、数据、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
7. 交付PR通过双只读评审、main可信复验并自动合并；独立状态闭环PR合并后任务才可标记`已完成`。

## 验证命令

```bash
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile <本任务新增或修改的脚本与测试>
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须保存一次可复算的入口复核批次；若入口为空或证据不足，必须记录`无法判定`、失败原因和解除条件，不得虚构通过结果。

## 完成定义

来源身份声明入口复核合同、追加式复核批次、精确缺口记录和全部验证证据已形成；交付PR经双只读评审、可信复验并自动合并，随后独立状态闭环PR标记任务完成。任务完成不等于`ZS-DATA-GAP-001`关闭；只有所有BTC/ETH研究叶子满足阶段1硬门时，阶段1才可完成。

## 需要人工决策

- 无。若发现新增权限、凭据、不可逆操作或合同冲突，停止相关部分并记录阻塞原因；普通证据缺失按失败安全处理。

## 执行记录

- 执行分支：`codex/task-000080-source-identity-entry-review-v1`；开始时间：`2026-08-08T05:54:13+08:00`；实现提交：`e10f7b2cef24edefc42313bf4fc8675fb040c9cd`；Pull Request：[#210](https://github.com/xk320/zhishi/pull/210)。
- 交付物：来源身份声明证据入口复核合同、复核脚本及专项测试、最终批次`source-identity-entry-review-20260808T055600+0800-v9`、数据缺口与阶段1审计追加记录、同步后的输入指纹配置。
- 最终批次：候选成员630；BTC与ETH各315；显式声明入口0；两标的入口状态均为`未登记`315；已证明0；输出清单SHA-256=`505e1ef245bb5c7220b80f13589055dea088d685747c35d3a2c601d5a9eb63ca`；`ZS-DATA-GAP-001`继续阻塞。
- 验证结果：专项测试4通过；数据测试89通过；审计测试172通过；研发中心测试194通过；Python编译通过；MarkdownLint扫描195个文件、0问题；`git diff --check`通过；敏感模式扫描仅命中既有`smoke-only`示例，未新增凭据。
- 已知限制：两个显式声明配置的`身份声明`数组为空，630个成员均未登记，不能形成已证明来源身份范围；任务完成不等于阶段1或阶段2放行。
- 数据与安全影响：仅读取仓库合同、配置和已合并不可变批次；未访问服务器业务数据、数据库、真实市场数据或凭据；未写入远端、数据库、原始数据或生产服务；未生成交易、收益、胜率或研究许可结论。
- 交付状态：已提交并推送，等待双只读评审和main可信复验；合并后须通过独立状态闭环PR标记`已完成`。
