# 任务-000013：建立 PR 自动合并策略与审批规则

- 状态：待评审
- 类型：治理
- 来源：架构优化
- 优先级：P1
- 阶段：0.5 理论体系建设
- 依赖：任务-000012（已完成）
- 关联问题：PR 合并流程存在人工阻塞
- 执行分支：`agent/000013-pr-auto-merge-v1`
- 开始时间：2026-07-23 14:52:15 +0800
- 完成实现时间：2026-07-23 15:14:45 +0800
- 实现提交 SHA：`537fd6026b99d0526d9584617ecc69afaa84e982`
- Pull Request：[#15](https://github.com/xk320/zhishi/pull/15)

## 架构决策记录

- 核验时间：2026-07-23 14:48:37 +0800
- 仓库当前为 GitHub Free 私有仓库，仓库配置返回
  `allow_auto_merge=false`；读取 `main` 分支保护与仓库规则集均返回 HTTP 403，
  因此当前阶段不使用原生 Auto Merge、Branch Protection 或完整 Quality Gate。
- 2026-07-23，用户明确决定不升级 GitHub Pro、不公开仓库、不以套餐升级阻塞
  研究和架构阶段，并授权在当前套餐能力内采用轻量、可维护的替代实现。
- 当前阶段的架构评审授权主体固定为仓库所有者`xk320`。自动化只接受所有者针对
  PR当前头提交 SHA 发出的精确批准指令；提交变化后旧批准立即失效。
- GitHub Pro、Branch Protection、必需评审和完整 Quality Gate 延后到阶段2实施，
  不作为本任务验收条件。

### 能力核验证据

```bash
env -u GITHUB_TOKEN gh api repos/xk320/zhishi \
  --jq '{visibility,allow_auto_merge}'
# {"allow_auto_merge":false,"visibility":"private"}

env -u GITHUB_TOKEN gh api repos/xk320/zhishi/branches/main/protection
# HTTP 403：Upgrade to GitHub Pro or make this repository public to enable this feature.

env -u GITHUB_TOKEN gh api repos/xk320/zhishi/rulesets --paginate
# HTTP 403：Upgrade to GitHub Pro or make this repository public to enable this feature.
```

GitHub 官方文档同时说明：私有仓库的受保护分支与原生自动合并能力需要
GitHub Pro、Team 或 Enterprise；原生自动合并在必需评审和状态检查满足后执行。

## 目标

建立适用于当前 GitHub 套餐的轻量 PR 自动化机制，使已完成架构评审、提交未变化
且满足低风险边界的变更可以自动合并。任何无法证明安全资格的变更默认转人工，
不得为追求自动化而放宽《知势》的研究、风险或数据纪律。

目标流程：

任务中心
↓
Codex执行
↓
创建PR
↓
自动检查
↓
架构评审通过
↓
自动Merge
↓
Codex按任务中心协议更新任务状态

## 输入合同

- `AGENTS.md`
- `README.md`
- `docs/研发中心/README.md`
- `docs/研发中心/任务规范.md`
- `docs/研发中心/看板.md`
- `docs/研发中心/总体计划.md`
- `docs/研发中心/Codex自动执行提示词.md`
- `docs/研发中心/任务/`下全部任务文件
- GitHub官方自动合并、Actions权限、事件触发与脚本注入安全文档
- 仓库当前可见性、合并设置、Actions权限、分支保护与规则集只读核验结果

## 输出合同

- `docs/治理/PR自动合并策略.md`
- `.github/workflows/pr-auto-merge-eligibility.yml`
- `.github/workflows/pr-auto-merge.yml`
- `scripts/研发中心/验证自动合并资格.py`
- `tests/研发中心/test_验证自动合并资格.py`
- `tests/研发中心/test_自动合并工作流.py`
- `docs/plans/2026-07-23-pr-auto-merge-design.md`
- `docs/superpowers/plans/2026-07-23-pr-auto-merge.md`
- 更新`.gitignore`以排除Python字节码
- 更新`AGENTS.md`
- 更新`README.md`
- 更新`docs/研发中心/README.md`
- 更新`docs/研发中心/任务规范.md`
- 更新`docs/研发中心/Codex自动执行提示词.md`
- 更新`docs/研发中心/总体计划.md`
- 更新`docs/白皮书/知势白皮书.md`的自动化授权边界
- 更新本任务文件与`docs/研发中心/看板.md`

## 工作范围

1. 定义文档、治理、研究规范三类低风险任务的自动合并资格。
2. 定义仓库所有者针对当前提交 SHA 的可审计架构批准指令。
3. 建立只读资格检查工作流和最小写权限合并工作流。
4. 合并前重新检查PR状态、基线、来源仓库、任务合同、变更路径、批准SHA和资格检查。
5. 对高风险、无法分类、提交变化、检查失败或合并冲突统一拒绝自动合并并转人工。
6. 保持Codex完成任务后自动创建PR的既有流程。
7. 把付费平台保护能力登记为阶段2治理增强。

## 不在范围

- 不升级 GitHub 套餐，不改变仓库可见性。
- 不建立复杂审批系统、外部机器人、数据库或服务。
- 不自动合并任务-000013自身的引导PR。
- 不让GitHub Actions直接修改`main`中的任务状态；合并后的状态证据仍由Codex按
  任务中心协议提交，阶段2再评估端到端状态自动化。
- 不修改生产代码、交易代码、资金管理、生产配置或数据流程。

## 安全边界

- 合并工作流只接受`xk320`以`OWNER`身份创建的精确架构批准评论。
- 批准指令必须包含40位PR头提交SHA；PR提交变化后必须重新评审。
- 合并工作流只能运行已进入`main`的可信脚本，不执行PR分支中的代码或工作流。
- 自动合并只允许任务文件已在`main`存在、任务类型明确为`文档`、`治理`或
  `研究规范`，且变更全部属于允许的Markdown路径。
- `.github/`、脚本、源代码、配置、数据、数据库迁移和任何无法分类的路径禁止
  自动合并。
- 工作流令牌使用最小权限，不读取或提交自定义密钥、密码、令牌或账户凭据。
- 不访问服务器、数据库、市场数据或真实交易账户，不执行真实资金交易。

## 实施要求

新增 PR 自动化规则，包括但不限于：

1. 建立自动合并策略文档。
2. 建立 GitHub Actions 自动合并流程。
3. 定义不同任务类型的合并权限。
4. 更新 AGENTS.md。
5. 更新 Codex 自动执行规范。
6. 更新任务中心规范。

## 自动合并边界

允许自动合并：

- `类型：文档`
- `类型：治理`
- `类型：研究规范`

禁止自动合并：

- 交易执行代码
- 资金管理代码
- 生产环境配置
- 数据删除或迁移
- 涉及真实资金风险的修改
- 缺少任务文件、任务类型或架构批准SHA的修改
- 修改`.github/`、脚本、源代码、配置或数据文件的PR
- 来自外部仓库、存在合并冲突、提交已变化或资格检查未通过的PR

## 验收标准

- [x] 存在明确的 PR 自动合并策略文档。
- [x] 自动合并触发条件明确。
- [x] 仓库所有者针对当前提交SHA的架构评审结果可以作为自动合并依据。
- [x] 高风险变更仍保留人工审批。
- [x] Codex 执行规则已同步。
- [x] 资格判定脚本包含允许、拒绝和边界测试，测试通过。
- [x] GitHub Actions语法、最小权限和不执行PR代码的安全检查通过。
- [x] 相关Markdown格式和`git diff --check`通过。
- [x] 阶段2正式平台保护能力已登记，不阻塞当前任务。

## 验证命令

```bash
python3 -m unittest \
  tests/研发中心/test_验证自动合并资格.py \
  tests/研发中心/test_自动合并工作流.py -v
python3 scripts/研发中心/验证自动合并资格.py --help
env GOPROXY=https://proxy.golang.org,direct GOSUMDB=sum.golang.org \
  go run github.com/rhysd/actionlint/cmd/actionlint@v1.7.12 \
  .github/workflows/*.yml
ruby -e "require 'yaml'; %w[
  .github/workflows/pr-auto-merge-eligibility.yml
  .github/workflows/pr-auto-merge.yml
].each { |file| YAML.load_file(file) }"
npx --yes markdownlint-cli2 \
  AGENTS.md \
  README.md \
  docs/治理/PR自动合并策略.md \
  docs/研发中心/README.md \
  docs/研发中心/任务规范.md \
  docs/研发中心/Codex自动执行提示词.md \
  docs/研发中心/总体计划.md \
  docs/白皮书/知势白皮书.md \
  docs/研发中心/任务/任务-000013.md \
  docs/研发中心/看板.md
git diff --check
```

## 执行记录

### 交付物

- `docs/治理/PR自动合并策略.md`
- `.github/workflows/pr-auto-merge-eligibility.yml`
- `.github/workflows/pr-auto-merge.yml`
- `scripts/研发中心/验证自动合并资格.py`
- `tests/研发中心/test_验证自动合并资格.py`
- `tests/研发中心/test_自动合并工作流.py`
- 自动合并设计与实施计划
- AGENTS、README、白皮书、研发中心规范、Codex提示词和阶段2计划同步

### 验收与验证

- Python单元与结构测试：通过，22项测试，0失败。
- `actionlint v1.7.12`：通过，两个工作流0问题。
- GitHub托管工作流：通过。PR #15头提交
  `537fd6026b99d0526d9584617ecc69afaa84e982`的
  [PR自动合并资格运行 #1](https://github.com/xk320/zhishi/actions/runs/29987602680)
  状态为`completed/success`。
- YAML解析：通过。Ruby输出一条本机`iStats`扩展未构建警告，不影响YAML解析退出码。
- Markdown检查：通过，12个相关文件，0问题。
- 任务中心一致性：通过，13个任务与看板一一对应。
- CI/CD安全扫描：通过。未发现凭据前缀、`pull_request_target`、浮动Action版本或
  评论正文直接拼接进Shell。
- `git diff --check origin/main...HEAD`：通过。

### 已知限制

- PR #15是引导变更，修改`.github/`、脚本和测试，且新工作流尚未进入`main`，
  因此不适用自动合并，必须人工评审和合并。
- 当前GitHub Free私有仓库没有Branch Protection、规则集、必需评审或原生
  Auto Merge；阶段2再评估正式平台能力。
- GitHub Actions不直接写入`main`更新任务状态；合并后仍由Codex按任务中心协议
  记录完成证据。
- 合并API只能原子绑定头提交SHA；工作流在合并前额外复核基线SHA，但平台不提供
  同时原子绑定基线和头提交的接口。任何复验失败均停止并转人工。

### 数据与安全影响

- 未访问服务器、数据库、市场数据或真实交易账户。
- 未删除、覆盖或迁移数据，未修改生产代码或生产配置。
- 未提交密钥、密码、令牌或账户凭据。
- 未实现交易执行、资金管理、模型、回测或真实资金交易。
- 写权限只存在于所有者批准工作流，并受身份、任务类型、文件路径、检查、
  基线SHA和头SHA共同约束。

### 需要人工决策

- 架构评审并人工合并PR #15。不得使用本PR尚未进入`main`的工作流自我合并。

## 完成定义

- 交付策略、脚本、测试和工作流，全部验收与验证通过。
- Codex自动创建引用任务-000013的PR并记录真实验证结果。
- 任务状态更新为`待评审`，等待架构评审。
- 任务-000013引导PR由人工合并；机制进入`main`后对后续合格PR生效。
- 合并后由Codex按任务中心协议更新任务状态，不自行虚构完成。
- 不降低《知势》的安全和质量门槛。
