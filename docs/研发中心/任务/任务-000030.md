# 任务-000030：建立三类时间与数据质量合同

<!-- markdownlint-disable MD013 -->

- 状态：阻塞
- 类型：数据治理
- 阶段：阶段1 时间与质量语义
- 优先级：P0
- 执行方案：方案A“来源绑定、三类时间显式化与对象级质量规则冻结”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领

## 依赖与阻塞条件

- 唯一前序依赖：任务-000029通过评审、合并并提供确切来源身份批次；
- 当前阻塞原因：任务-000029尚未完成，不能对未冻结身份的数据猜测时间和质量语义；
- 解除条件：任务-000029标记`已完成`且交付指纹可复算。

## 背景

既有审计无法证明事件时间、到达时间、采集时间、时区、频率、业务键、迟到、修订、
重复和断档语义。字段名称或文件修改时间不能替代正式合同。

## 任务目标

为任务-000029已证明身份的BTC、ETH数据对象分别冻结三类时间与质量规则，建立读取任何
统计结果前确定的规则版本、内容指纹、适用范围和失败语义。

## 固定执行方案

1. 只接受任务-000029确切来源与资产版本；
2. 逐对象定义事件、到达、采集时间字段、时区、精度、允许范围和时钟漂移；
3. 冻结频率、业务键、排序、迟到、补录、撤销、修订、重复、断档和异常规则；
4. 建立合同校验器，未知或冲突字段输出`无法判定`，不自动猜测；
5. 生成带指纹的不可变合同清单，为任务-000031提供唯一输入。

## 默认工程决策

优先使用上游正式Schema或可验证来源说明；缺少到达时间时不以事件时间、采集时间或文件
时间代替；频率不明时不计算断档；BTC与ETH、不同市场和合约分别建立合同。

## 允许停止条件

只有需要新增凭据或权限、修改生产系统或原始数据，或两个权威来源合同直接冲突且无法
从严标记未知时才停止。时间或质量语义无法证明时继续交付`无法判定`合同。

## 输入合同

- 任务-000029来源身份批次、成员清单、规则和输出指纹；
- 获批来源Schema、公开协议或现有白名单元数据；
- 既有质量审计仅作历史反证，不得沿用其未知字段为已证明；
- 标的固定BTC、ETH，主研究尺度固定4小时、8小时、24小时、48小时。

## 输出合同

- `docs/数据/三类时间与数据对象质量合同.md`；
- `config/数据/时间与质量规则.json`；
- `scripts/数据/验证时间与质量合同.py`及对应测试；
- `artifacts/数据/时间质量合同/<批次>/合同清单.json`；
- 逐成员三类时间、频率、业务键、修订、质量状态及内容指纹；
- 本任务和看板状态、提交、PR与验证记录。

## 工作范围

设计并实现合同格式、Schema校验、指纹、确定性排序和负向测试；在授权范围内只读核对
元数据，不运行全量正文质量统计。

## 不在范围

- 不全量扫描市场数据，不执行任务-000031审计；
- 不修改时间戳、填补缺失、去重、重排或修订原始记录；
- 不实现重放、成本、模型、回测、交易信号、仓位或订单。

## 安全边界

只读访问且不输出原始行；合同不保存秘密值；未知语义必须显式拒绝；使用有界超时、内存、
成员数和输出；任何规则不得根据统计结果或研究表现事后修改。

## 验收标准

1. 每个上游已证明成员都有时间与质量合同或明确无法判定原因；
2. 三类时间分别定义，缺少任一必需语义时不能证明无时间穿越；
3. 频率、业务键、迟到、修订、重复、断档和异常规则在结果前冻结；
4. BTC、ETH及不同场所、市场、合约和尺度不互相补偿；
5. 合同清单排序确定、指纹可复算、旧版本不可覆盖；
6. 测试、Markdown、敏感信息扫描和`git diff --check`通过。

## 验证命令

```bash
python3 -m unittest tests/数据/test_验证时间与质量合同.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
npx --yes markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
```

## 完成定义

全部合同、校验器、测试和不可变清单完成并创建引用任务-000030的PR后更新为`待评审`；
未知成员可以保留无法判定但不能伪造合同。PR合并后标记`已完成`并解除任务-000031。
