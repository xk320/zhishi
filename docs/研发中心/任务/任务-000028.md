# 任务-000028：统一阶段状态与BTC、ETH研究范围

- 状态：已完成
- 类型：治理 / 范围管理
- 阶段：阶段1 数据闭环修复与范围治理
- 优先级：P0
- 执行分支：`codex/000028-btc-eth-scope-stage-governance-v1`
- 开始时间：2026-08-03 04:53:49 +0800
- 完成实现时间：2026-08-03 07:23:50 +0800
- 最新实现提交SHA：`323b8aec3a0f958d355ab4750f7d0c797e353b5e`
- Pull Request：[#38](https://github.com/xk320/zhishi/pull/38)
- 合并时间：2026-08-03 07:30:24 +0800
- 合并提交SHA：`e138bd589a5bde38c81f48d38b7c449f6f13df37`
- 执行方案：方案A“前向双标的收缩、历史证据保留与阶段门重建”
- 方案状态：已批准执行
- 方案批准记录：2026-08-03，仓库所有者要求修复全部状态漂移、取消SOL并按阶段1缺口顺序执行
- 自动评审授权：2026-08-03，仓库所有者授权Codex组织子智能体评审、自动修复并在通过后自动合并，无需人工批准
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：不允许与任务-000029至任务-000037并行执行

## 依赖与阻塞条件

- 任务-000002、000007至000025和000027的已完成事实必须保留；
- 任务-000003至000006的审计任务已经完成，但阶段1能力门结论仍为禁止；
- 任务-000027真实批次必须保持不可变，不能因为范围收缩改写315个历史候选成员；
- 本任务不依赖任务-000026。任务-000026尚未进入`main`，不能成为执行事实来源；
- `main`中的任务-000028旧合同存在状态、方案、阶段、范围和最低字段漂移，本任务先修复
  自身合同再执行治理交付。

若修复需要修改历史审计批次、已合并任务执行记录、原始数据、生产系统或真实账户，必须
停止相应部分。普通文档取舍、任务拆分、测试组织和保守状态更新不构成阻塞。

## 背景

任务-000028最初登记为“研究数据闭环真实化与样本资产建设”，状态为`待执行`，但方案
状态为`待评审`，且缺少任务规范要求的输入合同、输出合同、安全边界、验收命令和完成
定义。看板同时把任务-000001至000025压缩为一行，破坏任务文件与看板一一映射。

现行文档还存在三类漂移：README和总体计划仍写服务器未恢复；多个规范仍写任务-000003
至000006尚未完成；阶段标签把已完成的理论合同误写成实际运行能力。另一方面，用户已经
明确将前向研究范围收缩为BTC和ETH，不再研究SOL。

最新真实质量批次仍包含历史315个候选成员，其中通过0、拒绝3、无法判定312。范围收缩
不能把这些历史结果删除或改写，也不能自动解决BTC和ETH的来源、时间、质量、重放、成本
和血缘缺口。

## 任务目标

1. 建立单一、可自动检查的阶段现状，修复服务器、任务和能力状态漂移；
2. 将所有现行规范和未来工作范围统一为BTC、ETH，不再把SOL列为前向必需标的；
3. 保留历史任务和审计产物中的SOL事实，明确历史证据与现行范围的边界；
4. 恢复任务文件与看板逐项唯一映射；
5. 登记任务-000029至000037的阶段1严格依赖链，授权Codex按顺序直接执行；
6. 保持阶段2被阶段1证据门阻塞，禁止以文档完成或删除SOL替代真实数据验收。

## 固定执行方案

采用方案A“前向双标的收缩、历史证据保留与阶段门重建”：

1. 任务状态只读任务文件；阶段状态集中在`docs/研发中心/总体计划.md`，其他入口引用该
   结论，不维护更宽松的平行状态；
2. 维护显式现行规范清单，通过自动测试拒绝SOL前向范围、旧服务器状态和旧任务状态；
3. 历史任务文件、已发布审计报告、CSV、JSON和批次索引保持原文和指纹，不做机械替换；
4. 将旧市场状态顶层设计标记为历史草案，由现行市场状态架构取代；
5. 后续数据工作拆为来源、时间、质量、重放、成本、双标的闭环、试点、容量恢复和最终
   门禁九个串行任务；
6. 每项任务先形成失败测试和冻结输入，再生成不可变新证据，后序门不得补偿前序失败。

## 默认工程决策

合同范围内存在等价实现时，依次选择：

1. 不改历史证据、原始数据、生产系统和账户；
2. BTC与ETH分别判定，失败安全且不跨标的补偿；
3. 单一事实来源、显式清单、自动检查和内容指纹；
4. 复用Python标准库、既有审计器和研发中心测试；
5. 磁盘、内存、输出和超时最小且有界；
6. 最容易复现、评审和回滚的最小修改。

## 允许停止条件

只有以下情况允许停止并请求人工决策：

- 需要新增凭据、账号、生产权限或付费平台；
- 需要修改服务器、数据库、权限、防火墙、原始数据或真实账户；
- 需要删除、覆盖或重写已发布审计批次和历史任务证据；
- 权威合同存在无法通过从严处理解决的直接冲突；
- 缺少会改变BTC、ETH范围、阶段门或风险底线的外部事实。

历史证据仍出现SOL、阶段结论为禁止、数据无法判定、普通文件组织和测试选择不属于停止
条件，必须按本合同继续并如实记录。

## 输入合同

本任务只读取：

- `AGENTS.md`、`README.md`和《知势宣言》；
- `docs/研发中心/`全部入口、任务规范和任务文件；
- 白皮书、路线图、系统蓝图、决策、研究、架构、治理和方法论文档；
- 已发布审计报告、批次清单和任务交付记录，只用于界定历史事实；
- Git历史和当前开放Pull Request元数据，只用于防止编号和在途工作冲突。

输入中的历史SOL、旧阶段结论和旧任务状态不得直接删除。必须先判断其属于历史证据还是
现行规范，再决定保留、加注或修正。

## 输出合同

本任务至少交付：

- `docs/superpowers/specs/2026-08-03-btc-eth-scope-stage1-remediation-design.md`；
- `docs/superpowers/plans/2026-08-03-btc-eth-scope-stage1-remediation.md`；
- `tests/研发中心/test_项目范围与阶段状态.py`；
- 修正后的`AGENTS.md`、`README.md`、《知势宣言》、总体计划、路线图、白皮书、系统蓝图
  及全部现行规范；
- 标记为历史草案的旧市场状态设计；
- 完整任务文件与看板唯一映射；
- 任务-000029至任务-000037的完整合同；
- 本任务执行记录、验证结果和Pull Request。

## 工作范围

1. 修复任务-000028合同、状态、阶段、方案和范围漂移；
2. 恢复任务-000001至000028在看板中的逐项映射；
3. 建立阶段状态和双标的现行范围自动检查；
4. 更新所有规范性文档的现状与BTC、ETH范围；
5. 保留并清楚标注历史SOL证据，不改变历史计数和指纹；
6. 建立任务-000029至000037的串行任务合同和看板记录；
7. 运行研发中心、审计回归、Markdown和差异检查；
8. 提交、推送、创建Pull Request并更新为`待评审`。

## 不在范围

- 不执行任务-000029至000037的服务器或数据工作；
- 不访问服务器、数据库业务正文或市场接口；
- 不修改、删除、迁移、修复、去重或补采原始数据；
- 不重写历史任务执行记录、审计报告、CSV、JSON或批次索引；
- 不实现采集、标准化、重放、成本、存储、备份或恢复运行时；
- 不训练模型、运行策略回测、计算胜率收益或生成交易许可；
- 不创建SOL替代标的，也不把BTC证据外推给ETH；
- 不处理尚未进入`main`的任务-000026或Pull Request #33。

## 安全边界

- 本任务只修改仓库文档和研发中心测试；
- 历史审计产物必须保持与`origin/main`相同；
- 不记录IP地址、用户名、密钥、密码、令牌或账户凭据；
- 不使用未来数据，不生成市场结论或交易动作；
- 任何自动范围检查必须使用显式文件清单，不能误改历史证据；
- 本机磁盘空间有限，不创建额外工作树或大体积临时产物；
- 未知冲突从严保留历史并阻止阶段放行。

## 验收标准

1. 任务-000028最低合同完整，方案为已批准执行，执行分支与开始时间已记录；
2. 每个任务文件在看板中恰好出现一次，状态与分区一致，不使用范围行压缩；
3. 总体计划、README和路线图对阶段0、0.5、1至7的当前状态一致；
4. 所有现行规范的前向研究范围只包含BTC、ETH；
5. 历史任务和审计产物未修改，历史SOL计数与指纹保持不变；
6. 不再出现“服务器未恢复”或“任务-000003至000006尚未完成”的现行断言；
7. 旧市场状态设计明确标记为历史草案且不再声明未证明的现有能力；
8. 任务-000029至000037均具有完整合同、唯一批准方案、串行依赖和安全边界；
9. 阶段2仍由真实数据门控制，范围收缩不会自动放行；
10. 研发中心、审计回归、Markdown和`git diff --check`全部通过。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_项目范围与阶段状态.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
npx --yes markdownlint-cli2 \
  AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check origin/main...HEAD
```

另需核对历史审计路径相对`origin/main`无差异、任务编号唯一、看板映射唯一、现行范围清单
无SOL前向声明，以及阶段2没有被文档完成状态自动放行。

## 执行记录

### 交付物

- 阶段1修复设计与执行计划；
- 统一后的README、AGENTS、总体计划、路线图、白皮书及全部现行决策、研究、架构和
  风控规范；
- 历史草案和历史评估的明确归档标记；
- PR #38合并时36个任务文件的看板唯一映射；任务-000026未进入本任务事实源且已明确排除；
- 任务-000029至000037的完整、串行、已批准执行合同；
- `tests/研发中心/test_项目范围与阶段状态.py`自动治理检查。

### 验收与验证

- 范围与阶段治理：通过。现行规范前向范围只包含BTC、ETH；README、总体计划和路线图
  使用相同阶段结论，阶段2仍被阶段1证据门阻塞。
- 任务中心：通过。PR #38合并时36个任务文件编号唯一，每个任务在看板中恰好出现一次且状态一致；
  任务-000029至000037全部具有唯一前序依赖、已批准方案和直接执行授权。
- 历史证据：通过。三份历史审计报告、持续验证清单和批次索引与`origin/main`无差异，
  受保护SHA-256全部复算一致。
- 自动化回归：研发中心38项、审计119项通过，0失败、0错误。
- Markdown检查：84个文件，0 issues；`git diff --check origin/main...HEAD`通过。

### 实际验证命令

```bash
python3 -m unittest tests/研发中心/test_项目范围与阶段状态.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
npx --yes markdownlint-cli2 \
  AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check origin/main...HEAD
git diff --quiet origin/main -- \
  docs/审计/数据资产审计报告.md \
  docs/审计/数据源清单.md \
  artifacts/审计/数据源清单.csv \
  docs/审计/数据质量审计报告.md \
  artifacts/审计/数据质量结果.csv \
  docs/审计/历史现场重放验证.md \
  artifacts/审计/历史重放结果.csv \
  artifacts/审计/数据质量持续验证/dqv-20260803T035557+0800-87273a8d253a/验证清单.json \
  artifacts/审计/数据质量持续验证/批次索引.csv
```

### 已知限制

- 本任务没有执行任务-000029至000037，也没有生成新的真实数据批次；
- 当前BTC和ETH仍未通过完整来源、时间、质量、重放、成本、血缘、容量和恢复硬门；
- 允许预测性研究范围为空，阶段2继续阻塞；
- 任务-000026和Pull Request #33不属于本任务范围。

### 数据与安全影响

- 未访问服务器、数据库或真实市场数据；
- 未修改原始数据、历史审计产物、生产服务、权限、防火墙或账户；
- 未训练模型、回测或生成胜率、收益、方向、仓位、订单和交易许可；
- 新增内容只包含治理文档、任务合同和本地自动检查。

### 需要人工决策

- 无。仓库所有者已授权当前PR由至少两个独立子智能体评审，主执行器自动修复阻断问题，
  最终头提交复验通过后按精确SHA自动合并；
- 本授权不涉及服务器、真实数据、生产系统、原始数据、权限或真实资金操作。

## 完成定义

全部交付物和验证证据形成后，提交、推送并创建引用任务-000028的Pull Request，将任务与
看板更新为`待评审`。至少两个独立子智能体必须对最终头提交完成只读评审；发现的问题由
主执行器修复，新提交使旧评审失效并重新评审。最终阻断问题为零、全部验证通过且PR仍指向
同一精确SHA时自动合并。合并后任务才能标记为`已完成`。
