# 任务-000070：专用只读身份下的未完整扫描对象正文复采

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000070-body-audit-v1`
- 开始时间：`2026-08-06T20:50:00+08:00`
- 类型：数据审计
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“复用任务-000064正文合同并绑定专用只读强制入口的追加式复采”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；用户已授权建立和使用远程专用只读身份，不得使用root读取业务数据，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000069完成后执行；不得与其他数据审计、数据工程或正文复采任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000069
- 依赖事实：任务-000069已在最新`main`完成专用远程只读身份、固定强制入口、权限隔离和身份批次验证；任务-000064、任务-000066、任务-000061的输入合同和历史批次继续有效。
- 当前可执行条件：使用已验证的专用只读身份；一次性root管理动作仅限安装或撤销root-owned固定强制入口，不得读取业务数据。数据库表级只读授权、敏感日志固定入口、对象白名单和资源预算必须在执行前按任务-000064/000066指纹复核。
- 当前阻塞原因：无；数据库只读授权和版本化正文固定入口已形成可复核事实，本任务已按合同认领执行。
- 解除条件：已满足；执行仍必须使用同一专用身份、固定入口、冻结对象清单和资源合同，任一指纹漂移即失败安全。

## 背景

任务-000066在旧的固定授权入口下形成了94个对象的正文复验，但最终仍有`无法判定91、失败1、未成熟2、通过0`，且该入口未证明数据读取身份与订单簿隔离边界。任务-000069已建立独立专用只读身份，但其身份探针本身不等于正文可读，也不关闭ZS-DATA-GAP-003/005。必须在同一专用身份和固定强制入口下重新取得正文级脱敏证据，保留历史批次，不把访问成功解释为数据质量通过。

## 任务目标

1. 将任务-000064的对象级正文质量合同绑定到任务-000069已验证的专用只读身份和固定强制入口，禁止使用root、任意远程命令或未登记密钥读取业务数据。
2. 对任务-000063冻结的92个数据库对象和2个敏感系统日志按确定性顺序执行一次真实、串行、只读、追加式正文复采；数据库仅执行已证明索引路径的有界时间字段样本，日志仅返回合同允许的脱敏字段。
3. 冻结任务文件、对象清单、合同/授权/规则/代码指纹、数据截止时点、资源预算和成员顺序，逐对象保留`通过`、`拒绝`、`无法判定`、`失败`、`未成熟`、`失效`、`未执行`。
4. 分别记录BTC与ETH及4小时、8小时、24小时、48小时主研究尺度的证据覆盖和缺口；15分钟、1小时只能作为事后结果观察窗口，不能升级为市场状态、事件探测或交易许可尺度。
5. 只有完整、可复算的正文证据才能作为关闭ZS-DATA-GAP-003/005的输入；本任务不直接关闭阶段1总门、不放行阶段2，也不生成研究或交易结论。

## 固定执行方案

1. 从最新`main`冻结任务-000064/000066合同、任务-000063覆盖矩阵、任务-000069身份批次、输入清单、授权截止和资源预算；任何指纹漂移均失败安全。
2. 通过专用只读身份的root-owned固定强制入口发送唯一版本化请求，入口只接受本任务的合同版本、清单指纹、截止时点和对象白名单；拒绝任意脚本、任意参数、交互终端、代理转发、端口转发、X11转发和未登记对象。
3. 如需一次性root管理动作，只能安装或复核固定入口及其root-owned文件指纹、权限和撤销时间；root不执行数据库查询、日志读取、业务数据复制或结果生成。
4. 复用`scripts/审计/执行未完整扫描对象正文.py`的审计语义，改为通过专用固定入口获取结构化脱敏结果；数据库串行、单对象最多65536字节/30秒、批次最多600秒/8MiB，日志单对象最多32768字节、批次最多65536字节/30秒，内存上限512MiB，远端临时写入关闭。
5. 对超时、越权、敏感输出、未来时间、输入漂移、授权失效、成员遗漏、输出不完整或资源超限立即失败关闭；保留未执行/无法判定/失败等状态，不重试为成功。
6. 生成新的不可变批次和脱敏报告，运行合同列出的全部验证；若外部入口或资源不满足，提交阻塞状态闭环，不伪造批次或覆盖历史证据。

## 默认工程决策

- 优先复用任务-000064、任务-000066和任务-000069既有合同、脚本、标准库和追加式批次模式；不引入常驻服务、新数据库、新依赖或新的数据源。
- 证据与安全优先于覆盖率：专用身份、固定入口、授权指纹、资源预算和脱敏输出任一缺失时保持`阻塞`或`无法判定`。
- BTC与ETH分别统计，禁止跨标的、交易场所、合约或尺度补偿；SOL不进入前向研究。
- 主研究尺度固定为4小时、8小时、24小时、48小时；15分钟、1小时仅记录为事后结果观察窗口。
- 描述性差异不得解释为因果关系、预测优势、胜率、收益、研究准入或交易许可；不得事后更换状态分类、分母、对象筛选或分组。
- 单进程执行；本机可用内存低于20%或磁盘可用空间低于5GiB时停止新批次；不创建额外工作树。

## 允许停止条件

- 专用只读身份、固定强制入口、对象授权、资源预算、截止时点或指纹无法复核；不得改用root、通用`ubuntu`命令或猜测性凭据。
- 需要读取白名单以外的路径、数据库业务正文之外的字段、未登记日志、真实交易账户、原始数据或生产服务。
- 需要修改、删除、覆盖、迁移原始数据、数据库、订单簿逻辑、权限、防火墙或生产服务；需要不可逆操作或新付费能力。
- 发现未来数据、时间语义混淆、输出包含敏感信息、成员顺序/计数不守恒、资源超限、固定入口绕过或历史批次被覆盖。
- 继续执行会把`无法判定`、`失败`、`未成熟`或`未执行`改写为通过，或会把局部正文证据升级为阶段2/交易许可。

## 输入合同

- `AGENTS.md`、README、研发中心全部入口、任务规范、全部任务文件和看板。
- 任务-000061/000062/000063的不可变清单、覆盖矩阵、来源/时间/质量缺口报告及其确切指纹。
- 任务-000064正文授权与资源预算设计、任务-000066既有审计脚本/批次、任务-000069专用只读身份批次及固定入口指纹。
- `scripts/审计/执行未完整扫描对象正文.py`及其测试；执行时的专用只读密钥仅从受限本地路径读取，不进入仓库、日志或批次。
- 数据截止时点、授权有效期、对象白名单、会话/权限快照、入口版本、规则版本、输入成员顺序和资源上限必须在批次元数据中绑定。

## 输出合同

- `artifacts/审计/Ubuntu未完整扫描对象正文质量复验/<新批次>/`追加式不可变批次：仅含对象指纹、脱敏计数、状态、失败类别、资源实测、合同/授权/规则/代码/清单指纹和确定性成员顺序。
- `docs/审计/Ubuntu未完整扫描对象正文质量复验报告.md`：记录批次身份、状态守恒、授权与入口事实、资源事实、缺口矩阵、解除条件和阶段1影响，不输出用户名、地址、凭据、原始日志或业务正文。
- 如需固定入口实现，新增的仓库文件仅限审计脚本、测试和对应设计说明；远端入口必须由root-owned固定文件提供内容指纹、权限和撤销事实。
- 更新本任务文件、看板、提交、PR、双只读评审证据和真实验证记录；不得写入交易、模型、回测、胜率、收益、方向、仓位、订单或交易许可。

## 工作范围

- 复核专用只读身份和固定入口边界，并按任务-000064对象白名单执行一次正文级脱敏质量复采。
- 保存不可变批次、逐对象状态和资源事实，更新阶段1缺口报告所需的证据引用。
- 运行数据、审计、研发中心、Python编译、Markdown、敏感信息和差异验证。

## 不在范围

- 不使用root读取业务数据，不执行任意SSH命令，不访问白名单外路径，不读取未登记数据库/日志对象。
- 不修改、删除、覆盖、迁移原始市场数据、数据库内容、订单簿逻辑、生产服务、权限或防火墙；不提交密钥、密码、令牌或账户凭据。
- 不训练模型、不实现运行时、不回测、不模拟或真实交易、不生成交易信号、方向、仓位、订单、胜率、收益或许可结论。
- 不将SOL加入前向范围，不将15分钟/1小时升级为市场状态、事件探测或交易许可尺度。
- 不因本任务局部通过自动关闭任务-000037、ZS-DATA-GAP-001至ZS-DATA-GAP-013或阶段2门禁。

## 安全边界

- 远端仅允许专用只读身份和root-owned固定强制入口；固定入口采用精确协议、字段白名单、对象白名单、确定性顺序、单进程和资源硬门，拒绝原始命令和任意脚本。
- root只进行一次性身份/入口管理，不读取业务正文、不生成业务结果、不接触数据库查询输出；专用密钥只在受限本地路径使用。
- 本地批次只保存脱敏结构化事实和指纹；不保存原始日志、业务字段值、数据库连接串、用户名、地址、公钥正文、私钥、令牌或密码。
- 所有批次追加式、不可覆盖；失败、未知、未成熟、失效和未执行必须完整保留；任何敏感输出、越权、超限或漂移失败关闭。

## 验收标准

1. 执行只经过任务-000069验证的专用只读身份和固定强制入口；root未读取业务数据，入口拒绝任意命令、未登记对象和未知协议。
2. 92个数据库对象和2个敏感系统日志按冻结清单和确定性顺序均有脱敏结果或明确未执行/无法判定原因；状态计数守恒，历史批次字节不变。
3. 数据库权限/会话、日志入口、合同、规则、代码、对象清单、数据截止时点和资源事实可由指纹复算；资源硬门和失败安全有实测证据。
4. BTC、ETH分别保留证据；主研究尺度仅为4小时、8小时、24小时、48小时，15分钟/1小时仅为结果观察窗口；不进行跨标的补偿或事后筛选。
5. 报告保留通过、拒绝、无法判定、失败、未成熟、失效、未执行及缺口解除条件；描述性差异不升级为因果、预测优势、胜率、收益、研究准入或交易许可。
6. 不访问或修改生产数据库、订单簿逻辑、原始数据、服务器权限、防火墙或真实账户；不生成模型、回测或交易输出。
7. 任务中心、数据/审计测试、Python编译、MarkdownLint、敏感信息扫描和`git diff --check`通过；交付PR经双只读评审、main可信复验并自动合并，随后独立状态闭环。

## 验证命令

```bash
python3 scripts/审计/执行未完整扫描对象正文.py --help
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/审计/*.py tests/审计/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另需执行一次真实专用只读复采命令（参数、截止时点和入口由本任务批次元数据固定），复算批次/对象/授权/入口/资源指纹，扫描仓库及批次不得出现凭据、地址、用户名、原始日志、业务正文或未来数据。

## 完成定义

专用只读身份下形成新的不可变正文质量批次、脱敏报告、缺口矩阵和全部真实验证记录；交付PR经双只读评审、主执行器验证、main可信复验并精确头SHA自动合并；再通过独立状态闭环PR将任务-000070标记为`已完成`。即使部分对象通过，本任务也不自动放行阶段2或交易许可；任何未证明项必须保留并成为下一解除条件。

## 执行记录

- 执行分支：`codex/task-000070-dedicated-body-recapture-v1`
- 开始时间：`2026-08-06T18:09:52+08:00`
- 尝试命令：显式指定专用只读身份执行固定`zhishi-ro/1`身份请求；发送`body-audit`请求验证固定入口拒绝未知操作；在专用身份上下文执行常量能力探针验证数据库只读授权。
- 结果：身份探针通过（UID非零、无管理员组、无sudo、无远端写入、无数据库/市场数据读取）；正文请求被固定入口拒绝；数据库能力探针认证失败；未生成正文批次、未读取业务记录。
- 外部证据：仅使用白名单逻辑别名`ubuntu`；固定入口返回版本化脱敏身份响应；数据库能力探针返回认证拒绝；日志固定入口仅返回既有脱敏状态，不输出日志正文。
- 阻塞原因：当前专用身份没有可核验的数据库表级`SELECT`只读授权和正文固定入口；使用root继续读取或直接创建数据库授权会违反本任务边界。
- 解除条件：补齐不依赖root业务读取的现有数据库只读授权、固定正文入口和授权/资源指纹，并通过专用身份最小能力探针；之后从最新`main`通过状态闭环PR恢复为`待执行`或`需修复`再执行。
- 数据与安全：未读取、复制、修改或删除数据库业务记录、原始市场数据、订单簿数据或日志正文；未修改数据库、生产服务、权限、防火墙或账户凭据；仅进行一次性只读入口/配置权限管理和失败安全探针。

## 本次执行记录

- 执行分支：`codex/task-000070-body-audit-v1`
- 执行时间：`2026-08-06T20:50:00+08:00`至`2026-08-06T20:54:03+08:00`
- 真实批次：`批次-20260806T125400Z-v2`
- 批次指纹：`78f401b70703c0f6dea9148c343c196e1ff2a96296adec67f7e705cbc1cd6fb7`
- 交付摘要：92个数据库对象和2个敏感系统日志均有脱敏结果；状态计数为`通过0、拒绝0、无法判定91、失败1、未成熟2、失效0、未执行0`，计数守恒94。
- 固定入口事实：协议`zhishi-ro/2`、入口版本`zhishi-ro-body-audit-1.0`；入口指纹、对象清单指纹、数据库会话指纹、权限快照指纹和专用密钥指纹均写入批次元数据，未写入凭据或原始正文。
- 资源事实：数据库单对象最多65536字节/30秒、批次最多600秒/8MiB、最多64行、串行、内存512MiB、远端临时写入关闭；日志合同保持32768字节/对象、65536字节/批次、30秒。
- 验证结果：正文协议单元测试2/2、数据测试55/55、审计测试156/156、研发中心测试通过；Python编译、MarkdownLint（168文件0问题）和`git diff --check`通过；真实批次复算通过。
- 已知限制：91个对象仍为`无法判定`、1个对象因未来时间样本为`失败`、2个敏感日志保持`未成熟`、通过0；本批次不关闭ZS-DATA-GAP-003/005，不放行阶段2，不生成研究或交易结论。
- 数据与安全：仅读取任务白名单内的有界时间字段样本和敏感日志脱敏结果；未使用root读取业务数据，未修改或覆盖原始数据、数据库、订单簿逻辑、生产服务、权限或防火墙；未保存原始字段、日志正文、地址或凭据。
