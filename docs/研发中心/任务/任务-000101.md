# 任务-000101：建立任务-000100冻结输出合同修复可信通道

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“精确输出段替换与冻结文档旁路”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；只修改仓库治理控制面、测试和文档，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务只处理PR #273双评审确认的合同死锁；暂停任务-000100直至任务-000102完成且目标合同修复PR合并
- 一次性领取顺序：任务-000100已在PR #273待评审且因基线输出合同与历史指纹冻结冲突无法合并；本任务获准先于目标任务闭环；任务-000101完成后的下一步仅为登记并完成任务-000102，随后执行目标合同修复PR；在该修复合并前不得恢复任务-000100
- 执行分支：`codex/task-000101-contract-repair-governance-v1`
- 开始时间：`2026-08-12T23:03:30+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000099已完成；任务-000100已在main登记且PR #273保持开放。
- 当前阻塞原因：无。任务-000100基线合同、历史输入指纹测试和两份独立评审均可本地复算；不依赖Ubuntu、数据库、市场数据、账户或凭据。
- 失败安全：若修复需要改变任务-000100目标、数据结论、SQL安全边界、资源门、阶段门或交易纪律，必须停止；只允许逐字固定的唯一输出条目精确替换，验收标准不得修改。

## 背景

任务-000100输出合同要求更新`docs/审计/阶段1最终审计报告.md`和
`docs/审计/数据缺口与补采清单.md`。这两个文件已被旧任务配置按字节冻结；直接更新会使
数据测试和历史证据链失效。PR #273选择保留历史字节并新增独立追加报告，双评审确认该安全
选择正确，但也确认它与基线输出合同直接冲突。

本任务只建立一次性可信修复通道。目标修复由后续独立任务执行，不在本任务中修改
任务-000100。

## 任务目标

1. 建立任务-000101治理交付后可由唯一后继任务修复任务-000100固定输出段的可信规则。
2. 固定旧输出合同和新输出合同全文指纹；新合同必须保留两个历史文件字节，改为新增或更新独立追加报告及派生入口。
3. 只允许目标在修复PR的基线和头部均保持`待执行`；不得改变目标、输入、范围、安全边界、成本门或阶段结论。任务-000102作为独立授权源任务必须先完成并进入main，目标修复PR只使用其已完成授权，不迁移任务-000102状态。
4. 为未知执行任务、未知目标、部分替换、额外合同改写和历史文件夹带增加负向测试。

## 固定执行方案

1. 从最新main冻结任务-000100、自动合并策略、资格校验器、跨载体校验器和相关测试。
2. 在可信规则中新增一次性固定映射：任务-000102执行，任务-000100为目标。
3. 固定唯一旧文本为`- 更新阶段1最终审计报告、数据缺口清单、README、总体计划、任务文件和看板；`；固定唯一新文本为`- 保持docs/审计/阶段1最终审计报告.md和docs/审计/数据缺口与补采清单.md字节不变；新增docs/审计/阶段1成本与执行证据报告.md，并更新README、总体计划、任务文件和看板；`。验收标准现有8项均不修改，禁止自行选择或新增“对应验收条目”。
4. 本任务交付只修改可信治理代码、测试、治理文档、本任务文件和看板，不修改任务-000100。
5. 双评审、可信资格和精确头SHA通过后自动合并，再独立状态闭环。

## 默认工程决策

- 使用固定执行任务、固定目标任务和旧/新全文常量，不实现通用合同编辑器。
- 复用现有任务-000095到任务-000094的两步模式：授权源任务先完成，随后目标修复PR保持目标`待执行`且看板不变；PR #273之后同步最新main，才在普通任务交付中迁移任务-000100到`待评审`。
- 历史冻结文件、旧配置和旧批次不得改写；目标修复仅改变未来交付载体说明。
- 普通测试、字段组织和错误信息由Codex自主处理。

## 允许停止条件

- 需要改写历史审计文件、旧配置、旧批次或原始数据。
- 需要修改任务-000100的研究目标、SQL/网络白名单、资源门、数据结论或阶段门。
- 需要访问服务器、数据库、账户、凭据或生产系统。
- 需要绕过双评审、可信资格、精确SHA或GitHub可合并状态。

## 输入合同

- 最新main中的任务-000100合同、任务规范、看板和AGENTS.md。
- PR #273两份独立只读评审的合同冲突结论。
- 现行自动合并策略、资格校验器、跨载体校验器和研发中心测试。
- 冻结两个历史文档的既有配置指纹和数据测试，仅作只读复算。

## 输出合同

- 更新`docs/治理/PR自动合并策略.md`，记录一次性任务-000102到任务-000100映射。
- 更新`AGENTS.md`，以同一精确映射同步项目最高层执行规范。
- 更新`scripts/研发中心/验证自动合并资格.py`、`scripts/研发中心/验证跨载体冲突.py`及相关测试。
- 固定上述唯一旧/新输出条目、目标状态、允许路径和负向边界；任务-000100验收标准保持不变。
- 更新本任务文件、看板、PR证据和验证记录。

## 工作范围

仅限研发中心可信治理控制面、治理测试和治理文档。

## 不在范围

- 不在本任务PR修改或执行任务-000100。
- 不修改历史审计报告、数据缺口清单、旧配置、证据批次或原始数据。
- 不访问Ubuntu、数据库、网络、账户、凭据或生产服务。
- 不训练、回测、模拟下单或产生交易结论。

## 安全边界

可信规则只从main执行；目标修复必须逐字匹配固定旧段并生成固定新段。任何额外任务、
路径、状态、合同、数据或生产变更失败关闭。文件仍须通过普通Git对象、UTF-8、大小和敏感
信息硬门。

## 验收标准

1. 任务-000102到任务-000100的一次性映射只能替换本合同逐字固定的一个旧输出条目为一个新输出条目；验收标准不得修改。
2. 目标在修复PR基线和头部均保持待执行、看板不变；目标、输入、范围、安全、资源、门禁和数据结论逐字不变。
3. 两个历史文档、旧配置、旧批次和原始数据均不能出现在允许变更路径。
4. 未知执行任务/目标、部分替换、额外字段、状态迁移和历史文件夹带全部失败关闭。
5. 研发中心全部测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
6. 双只读评审、main可信复验和自动合并完成，随后独立状态闭环。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py scripts/研发中心/验证跨载体冲突.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md docs/治理/PR自动合并策略.md docs/研发中心/任务/任务-000101.md docs/研发中心/看板.md
git diff --check
```

## 完成定义

可信规则、治理文档和正反测试已建立一次性精确修复通道；双评审、可信复验、自动合并和
独立状态闭环完成。完成只表示修复通道可用，不表示任务-000100合同已修复或阶段1完成。

## 数据与安全影响

仅修改仓库治理控制面和测试；不访问或修改服务器、数据库、市场数据、账户、凭据、生产
系统或交易链路。

## 已知限制

本任务只处理任务-000100已知的冻结输出合同冲突，不建立通用任务合同编辑入口。目标合同
仍须由任务-000102独立修复并重新评审。

## 回滚方式

回滚本任务合并提交即可移除一次性可信映射；若目标修复已合并，还须通过固定反向合同修复
或普通revert恢复，禁止改写Git历史。

## 执行记录

- 实现提交SHA：`aebf882d390fa7929e2ed47b2e600acfb03b27c9`
- Pull Request：[#275](https://github.com/xk320/zhishi/pull/275)
- 交付物：任务-000102到任务-000100的一次性固定映射、可信资格与跨载体校验、正反测试、AGENTS与自动合并策略同步。
- 验证结果：研发中心215项、资格定向109项、跨载体定向28项通过；Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
- 已知限制：只建立可信修复通道；任务-000102尚未登记和执行，任务-000100合同尚未修复，阶段1尚未完成。
- 数据与安全：仅仓库治理代码、测试和文档；未访问Ubuntu、数据库、市场数据、账户、凭据、历史证据或生产系统。
- 需要人工决策：无。
