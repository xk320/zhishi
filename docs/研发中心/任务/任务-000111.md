# 任务-000111：修复压缩合并事实复算兼容

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 开始时间：`2026-08-14T05:30:00+08:00`
- 执行分支：`codex/task-000111-squash-merge-fact-repair-v1`
- 类型：治理
- 阶段：研发中心自动合并可信事实治理
- 优先级：P0
- 执行方案：方案A“最小压缩合并事实解析与负向测试”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；修复已发现的可信状态闭环兼容缺口，不再询问方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；唯一前序依赖为任务-000110；不得修改任务-000110研究交付内容或历史批次

## 依赖与阻塞条件

- 唯一前序依赖：任务-000110已完成代码交付并已进入main，但其状态闭环因压缩合并事实解析缺口暂未完成。
- 当前阻塞原因：可信复算器对非标准双父合并提交无法绑定真实PR合并事实，导致任务-000110状态闭环被安全拒绝。
- 解除条件：新增解析分支只接受可验证Git事实，保留双父路径，专项测试和双只读评审通过。

## 背景

PR #304已经以真实压缩合并提交进入main；状态闭环复算器仅支持标准双父合并，若直接标记完成会违反可验证原则。本任务只修复复算器兼容性。

## 任务目标

1. 定义压缩合并事实的最小可验证输入与绑定规则。
2. 补充正向（真实压缩合并）和负向（无PR事实、非main提交、伪造正文）测试。
3. 使任务-000110可通过独立状态闭环PR完成，且不修改任务-000110合同或研究产物。

## 固定执行方案

1. 读取Git对象、任务文件和PR元数据事实；不读取业务数据。
2. 优先使用目标提交父链、任务记录和可信PR元数据的确定性绑定；任一字段不一致则失败关闭。
3. 保持标准双父合并路径完全不变，新增逻辑只覆盖压缩合并且禁止猜测。

## 默认工程决策

- 复用Python标准库和现有可信复算器；不新增依赖或服务。
- 采用小范围白名单和固定字段，不把Issue、评论、聊天摘要或提交正文单独当作合并证明。
- 普通取舍按安全、证据、简单、可复现和可回滚顺序自主决定。

## 允许停止条件

- 无法从Git对象和可信PR元数据唯一绑定压缩合并。
- 需要放宽评审、任务合同、敏感扫描、状态迁移或生产权限。
- 出现跨任务状态漂移、伪造事实或测试无法复现。

## 输入合同

- `scripts/研发中心/验证自动合并资格.py`、相关研发中心测试、任务-000110状态闭环事实和现有工作流合同。
- PR #304的main提交、PR头SHA、PR编号、目标分支和合并时间等已验证元数据。

## 输出合同

- 可信合并事实解析器及专项测试。
- 任务-000111执行记录、看板状态和PR验证记录。
- 不改写任务-000110的研究合同、批次、脚本或报告。

## 工作范围

- 压缩合并与标准双父合并的事实识别、绑定和负向测试。
- 自动合并资格复验和状态闭环兼容验证。

## 不在范围

- 不修改研究代码、市场数据、原始数据、数据库、服务器、账户权限或生产服务。
- 不改变任务合同唯一事实源、评审门槛、自动合并白名单范围或真实交易关闭状态。
- 不伪造PR、提交、时间、评审或完成状态。

## 安全边界

- 只读Git对象和治理元数据；失败安全，不以无法判定替代合并事实。
- 单进程、固定资源上限、不执行网络业务请求、不记录凭据。
- 未来数据、真实资金、订单、方向、仓位、模型和回测均禁止。

## 验收标准

1. 真实压缩合并`f83f15568398449ae912b21f2b4adb61501ad9bc`可由main、PR #304、任务字段和可信元数据唯一复算。
2. 标准双父合并现有测试保持通过；伪造或缺字段压缩合并均失败关闭。
3. 任务-000110状态闭环PR的可信资格通过；无任务合同或看板漂移。
4. 研发中心测试、MarkdownLint、敏感扫描和`git diff --check`通过。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_合并事实复算.py -q
PYTHONHASHSEED=0 python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

压缩合并事实解析和负向测试进入main；双只读评审、可信资格复验和自动合并完成；任务-000110可在独立状态闭环PR中如实标记已完成。

## 数据与安全影响

仅修改治理脚本、测试和任务文档；不访问或写入服务器、数据库、原始数据、凭据、生产服务或真实交易账户。

## 已知限制

本任务只覆盖可由可信GitHub PR元数据绑定的压缩合并，不把任意单父提交、Issue或提交正文解释为合并证明；任务-000110状态闭环需在本任务完成后另行提交。

## 回滚方式

回滚本任务PR及其状态闭环即可恢复原有双父合并解析逻辑，不改动任务-000110交付代码、批次或历史事实。

## 执行记录

- 交付状态：待评审，已创建Pull Request，尚未合并。
- 执行分支：`codex/task-000111-squash-merge-fact-repair-v1`
- 开始时间：`2026-08-14T05:30:00+08:00`
- 实现提交SHA：`36d30449e75001f4fb2fc9c12e206bbdfc7634a8`
- Pull Request：[#307](https://github.com/xk320/zhishi/pull/307)
- 交付物：压缩合并事实解析、GitHub PR元数据绑定、标准双父兼容测试及负向测试。
- 验证结果：合并事实/自动合并/工作流专项141项通过；Python编译通过；`git diff --check`通过。
- 已知限制：仅支持可信工作流元数据绑定的压缩合并；不修改任务-000110交付、研究批次或生产系统。
- 数据与安全影响：只读Git对象和PR治理元数据；未访问服务器业务数据、数据库、原始数据、凭据或交易系统。
- 回滚方式：回滚本PR即可恢复原双父合并事实解析，不改动任务-000110交付。
