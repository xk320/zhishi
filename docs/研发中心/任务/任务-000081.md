# 任务-000081：来源身份声明证据获取与九字段闭环复验

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：数据治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“显式来源声明入口冻结、逐字段证据复验与失败安全批次”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；复用已合并任务-000069的专用只读身份和既有白名单规则，不得再次要求方案选择或重复批准；不授予新的生产、数据库写入或凭据权限
- 并行规则：禁止并行；唯一前序依赖为任务-000080；不得与其他数据证据、来源身份、时间质量、重放、成本或阶段审计任务并行
- 执行分支：`codex/task-000081-source-identity-evidence-v1`
- 开始时间：`2026-08-08T07:01:26+08:00`
- 实现提交SHA：`8ec27ba`
- Pull Request：[#214](https://github.com/xk320/zhishi/pull/214)
- 合并时间：2026-08-08 00:07:14 +0000
- 合并提交SHA：`4867fd006a8d55acdd470aa6d6b36ed6ce50117d`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000080已完成；其最终批次确认BTC、ETH各315个成员均未登记显式来源声明，`ZS-DATA-GAP-001`继续阻塞。
- 输入依赖：任务-000029来源身份合同、任务-000069只读身份隔离证据、任务-000071结构证据合同、任务-000074至任务-000080不可变批次、最新缺口台账、阶段1审计报告和执行时最新`main`。
- 当前阻塞原因：无；“没有显式声明入口”是本任务必须验证的失败安全结果，不得在执行前据此伪造通过或跳过复验。
- 执行中阻塞：若没有任务合同登记的结构化声明入口，或入口要求新增权限、读取业务正文、环境变量、凭据、未登记路径或不可逆操作，必须发布脱敏阻塞记录并停止，不得改用路径、表名、目录、结构相似性或聊天内容补齐身份。
- 解除条件：BTC与ETH每个候选成员均有当前版本九字段声明、唯一证据定位、声明内容指纹、成员输入指纹、Schema指纹、授权指纹和可撤销事实；来源、交易场所、市场类型、标的、精确合约、数据对象和版本可独立复算，且复验批次可重放。

## 背景

任务-000080已经复核仓库内显式声明入口为空。当前630个身份成员（BTC 315、ETH 315）不能因路径、文件名、表名、服务名称、目录归属或数据库结构观察而形成来源身份结论。阶段1最终审计因此仍有八个BTC/ETH逐尺度叶子阻塞。本任务只负责寻找并验证合同明确允许的显式声明证据；找不到证据时必须把缺口冻结为阻塞事实，不把任务完成解释为阶段放行。

## 任务目标

1. 冻结并枚举当前允许的结构化来源身份声明入口，明确入口版本、所有者、用途、唯一定位、内容指纹、Schema指纹、授权边界和撤销事实。
2. 对任务-000080冻结的630个BTC/ETH成员逐项复验九个身份字段：来源提供者、交易场所、市场类型、标的身份、精确合约、数据对象、Schema确切版本、授权边界和字段中文映射。
3. 保存成员输入指纹、声明指纹、证据定位、Schema/授权指纹、原因代码和可重放的确定性成员顺序；BTC与ETH独立统计，不跨标的补偿。
4. 追加不可变、失败安全的证据批次并精确更新`ZS-DATA-GAP-001`。无入口或证据不完整时输出`未登记`或`无法判定`，不得虚构来源身份、质量、收益、胜率、研究准入或交易许可。

## 固定执行方案

1. 从最新`main`只读冻结任务-000029、任务-000069、任务-000071、任务-000074至任务-000080、配置、报告、缺口台账和历史批次的字节指纹及成员顺序。
2. 仅读取任务合同已登记的结构化声明配置或白名单只读入口；不扫描未登记目录，不猜测远端路径，不把数据库结构元数据当作来源声明。
3. 如需远端复核，只能通过逻辑别名`ubuntu`和任务-000069形成的专用只读入口执行串行、无交互、有界请求；只允许声明元数据、Schema摘要和授权快照，不读取业务记录、价格、成交、订单簿、账户、环境变量或凭据。
4. 对每个成员按固定顺序匹配九字段、唯一证据定位、内容/成员/Schema/授权指纹和撤销事实；任一缺失、冲突、漂移、超时、越界或输出异常均失败安全。
5. 采用追加式临时目录和原子不可覆盖发布；历史批次只读保留。若入口为空，批次必须明确记录入口条数为0、630个成员的状态和解除条件。

## 默认工程决策

- 复用任务-000079/000080的标准库、冻结批次格式、哈希算法、失败状态和原子发布引擎；不引入服务、数据库客户端、模型或常驻进程。
- 测试串行执行，最多两个只读评审者，不创建额外工作树，Node堆上限256MiB；批次总超时600秒、单成员超时5秒、最大成员数1000、最大输出8MiB、日志32KiB。
- 证据、可复现和失败安全优先；普通文件组织、测试结构和可逆实现由Codex按安全、简单、可验证、可回滚顺序决定，不向用户发起A/B/C选择。
- 任何状态计数、输入指纹或成员顺序无法复算时，保留`无法判定`并停止相关发布，不覆盖历史证据。

## 允许停止条件

- 需要新增账号、凭据、服务器、生产权限、数据库写入、原始数据读取/迁移、账户权限或不可逆操作。
- 声明入口要求读取业务正文、价格、成交、订单簿、账户、环境变量、凭据或未登记路径。
- 发现未来数据、跨标的补偿、结果可见后更换分类、修改分组、缩小分母或把描述性差异解释为因果、预测优势、胜率、收益、研究准入或交易许可。
- 历史批次将被覆盖、输入指纹漂移无法解释、计数不守恒、探针越界或敏感信息无法清除。
- 证据不足不是请求人工方案选择的理由；应形成脱敏失败安全批次并按合同进入`阻塞`。

## 输入合同

- 根目录`AGENTS.md`、`README.md`、`docs/研发中心/README.md`、`docs/研发中心/任务规范.md`、`docs/研发中心/看板.md`、`docs/研发中心/总体计划.md`。
- 任务-000029、任务-000069、任务-000071、任务-000074至任务-000080的任务合同、设计合同、配置、审计报告、缺口台账和不可变批次。
- 任务-000080最终入口复核批次`source-identity-entry-review-20260808T055600+0800-v9`及其输入/规则/执行器指纹。
- 已登记的结构化来源身份声明入口；入口不存在时输入事实为“空集”，不得从聊天、Issue、PR、文件路径、服务名、表名或常识补齐声明。

## 输出合同

- `docs/研究/来源身份声明证据获取与九字段复验合同.md`：定义入口白名单、九字段、证据定位、指纹绑定、状态机、阻塞和解除条件。
- `artifacts/数据/来源身份声明九字段复验/<批次>/`：批次清单、成员清单、计数摘要、输入/规则/执行器指纹、资源事实和失败原因；不保存凭据、地址、原始业务字段或敏感响应。
- `docs/审计/数据缺口与补采清单.md`、`docs/审计/阶段1最终审计报告.md`：只追加本次真实结果和`ZS-DATA-GAP-001`精确覆盖范围，不关闭其他缺口或阶段门。
- 任务文件、看板、提交、Pull Request、双只读评审证据和合并后状态闭环记录。

## 工作范围

- 复核允许的显式声明入口及九字段证据完整性、版本、指纹、授权和撤销事实。
- 对BTC、ETH各315个成员独立统计入口状态、已证明、拒绝、无法判定、失败、未成熟和失效，并保持计数守恒。
- 生成一次可复算、追加式、无敏感信息的真实验证批次，精确更新`ZS-DATA-GAP-001`。
- 运行专项、数据、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`。

## 不在范围

- 不训练模型、回测、模拟或真实交易，不生成方向、仓位、订单、收益、胜率或交易信号。
- 不访问或保存价格、成交、订单簿、账户、环境变量、凭据、未登记数据库表或业务正文。
- 不修改服务器、数据库、生产服务、防火墙、权限、订单簿逻辑、原始数据、历史批次或账户。
- 不把任务完成解释为`ZS-DATA-GAP-001`关闭、阶段1通过、阶段2放行、研究准入或交易许可。
- 不改变主研究尺度4小时、8小时、24小时、48小时；15分钟和1小时只能作为事后结果观察窗口，不能升级为状态、事件或交易许可尺度。

## 安全边界

- 默认仓库内只读；远端仅允许已登记逻辑别名`ubuntu`的专用只读入口，远端写入、临时文件、业务记录读取、原始数据落盘、生产修改和权限变更均为`false`。
- 批次必须追加式、原子、不可覆盖；敏感输出、越界、超时、未来数据、状态不守恒或输入冲突均失败关闭。
- 只有九字段声明与全部证据指纹匹配的成员才可标记`已证明`；`已登记`、结构观察或路径存在不能升级为证明。
- BTC与ETH分别统计，禁止跨标的补偿缺失；所有描述性差异保持非因果、非预测、非交易许可解释。

## 验收标准

1. 任务-000080最终批次、成员顺序和所有输入指纹可从最新`main`复算，历史目录字节不变。
2. 入口清单明确记录入口状态、九字段、唯一定位、声明/成员/Schema/授权指纹、撤销事实和缺失原因；入口为空时逐项保留`未登记`。
3. BTC与ETH各315成员的候选总体、已登记、可定位、已证明、拒绝、无法判定、失败、未成熟和失效计数守恒，不跨标的补偿。
4. 不存在通过路径、名称、表名、结构观察、结果可见后筛选或缩小分母形成身份结论的路径。
5. `ZS-DATA-GAP-001`只按本批次真实覆盖范围更新；无完整证据时继续阻塞，其他缺口和阶段门不变。
6. 专项、数据、审计、研发中心测试、Python编译、MarkdownLint、敏感扫描和`git diff --check`全部通过并记录实际结果。
7. 交付PR通过双只读评审、main可信复验、精确头SHA自动合并；独立状态闭环PR合并后任务才可标记`已完成`。若外部条件不能满足，按证据进入`阻塞`。

## 验证命令

```bash
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -q
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile <本任务新增或修改的脚本与测试>
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

另须保存一次真实、串行、白名单只读的九字段复验批次；入口为空或证据不足时必须原样记录失败原因、解除条件和资源事实，不得虚构完成。

## 完成定义

来源身份声明九字段合同、不可变复验批次、精确缺口记录和全部验证证据已形成；交付PR经双只读评审、可信复验并自动合并，随后独立状态闭环PR标记任务完成。任务完成不等于阶段1通过；阶段1只有八个BTC/ETH逐尺度叶子全部满足硬门时才能放行。

## 需要人工决策

- 无。普通证据缺失、结果为空和`无法判定`按失败安全处理；只有新增权限、凭据、生产/数据库写入、不可逆操作或合同直接冲突才停止并报告。

## 执行记录

- 交付状态：已提交，等待双只读评审与main可信复验；不得在评审前标记`已完成`。
- 交付物：九字段复验合同、只读复验脚本、专项测试、真实脱敏批次、阶段审计与缺口台账追加记录、输入指纹同步。
- 批次：`source-identity-nine-fields-20260808T074100+0800-v4`；批次清单SHA-256=`230316378417f43098f8033c16ae1ee19f54098dbbd5b8ac2f5b61a45dfb9d41`；输出清单SHA-256=`74a1034fdee473da1d4e750debf474a9b3f171c913ded93f514cf4f0648ba524`；输入快照SHA-256=`17a51632948c8234ac00f17c8a6b5a7f073a57f72b60826bf9d655d9f42d8f31`；规则与执行器SHA-256=`fa6071e64e9acbe2130901ad9d94b2e553cf3ae26aa327b8a37cdf97f302889a`；630条成员（BTC/ETH各315），声明入口0，九字段全部`无法判定`，已证明0。
- 验证结果：专项9通过；数据98通过；审计172通过；研发中心194通过；Python编译通过；MarkdownLint 198个文件0问题；敏感信息扫描无命中；`git diff --check`通过。
- 已知限制：`ZS-DATA-GAP-001`继续阻塞，八个研究叶子、阶段1和阶段2门禁不变；任务不产生研究准入、胜率、收益或交易许可。
- 数据与安全影响：仅追加不可覆盖仓库证据和合同；未访问服务器、数据库业务记录或真实市场数据，未读取凭据，未修改原始数据、生产系统、权限或账户。
