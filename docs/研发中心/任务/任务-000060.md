# 任务-000060：Ubuntu 数据证据增量复验

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/000060-ubuntu-data-evidence-v1`
- 开始时间：`2026-08-05T12:02:16+08:00`
- Pull Request：[#142](https://github.com/xk320/zhishi/pull/142)
- 实现提交SHA：`0889ce527ee9b5183e680643e70ea328aebf13e9`
- 类型：数据审计
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“复用既有只读审计器的增量批次复验”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案或批准合并
- 并行规则：禁止并行；任务-000031完成后执行；不得与其他数据审计任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000031已完成并进入main。
- 当前阻塞原因：无；任务合同已授权固定逻辑别名`ubuntu`的白名单只读访问。
- 解除条件：从最新main认领任务并重新冻结输入；若探针不可达或资源边界无法满足，则记录
  脱敏证据并将本任务标记为`阻塞`，不得伪造批次。

## 背景

任务-000031已形成历史质量批次，但其结果只代表当时冻结输入和当时的外部状态。用户要求在
Ubuntu恢复可达后重新取得数据证据，必须创建新的不可变批次，不能覆盖旧批次、借用聊天结论
或把预检可达扩展为数据可用。

## 任务目标

1. 在最新main上冻结当前资产清单、规则版本、审计器版本、资源上限和数据截止信息。
2. 使用现有持续质量审计器通过`ubuntu`白名单只读探针生成新的聚合证据批次。
3. 按BTC、ETH及4小时、8小时、24小时、48小时独立保留候选总体、已观察、通过、拒绝、
   无法判定、失败、未成熟和失效计数；未知不能以零填充。
4. 记录批次身份、规则指纹、资产清单指纹、代码指纹、资源实测、成员顺序和失败原因，
   并保持历史批次字节不变。

## 固定执行方案

1. 从任务-000031、任务-000029和任务-000030读取确切输入合同，验证清单和规则指纹。
2. 运行既有 `scripts/审计/持续验证数据质量.py`，使用现有配置与资源上限；不改审计器语义。
3. 远端只通过逻辑别名`ubuntu`执行白名单探针，远端无落盘；本地只保存脱敏聚合事实和指纹。
4. 对输入身份漂移、超时、SSH失败、非法输出、资源超限和缺失证据失败关闭；部分成员失败
   仍须继续记录并保留状态守恒。
5. 生成新报告、批次索引和验证清单，完成回归测试后提交PR。

## 默认工程决策

- 复用仓库已有脚本、配置和标准库，不引入新依赖。
- 内存上限优先于速度；测试单进程，Node堆上限256 MiB；可用内存低于20%或磁盘低于5 GiB时
  停止新批次和评审。
- BTC与ETH独立统计，不跨标的、市场、合约或尺度补偿。
- 数据不足只输出`无法判定`或阻塞，不生成胜率、收益、方向、仓位、订单或交易许可。

## 允许停止条件

- 需要读取白名单以外的服务器路径、数据库业务正文或敏感日志；
- 需要修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；
- 需要新增凭据、付费数据、真实账户或不可逆操作；
- 权威输入指纹冲突且无法在不改写历史证据的情况下保守处理。

## 输入合同

- `AGENTS.md`、README、研发中心入口、任务规范和全部任务文件；
- 任务-000029、任务-000030、任务-000031在main中的确切提交与指纹；
- `artifacts/审计/数据源清单.csv`、`config/审计/数据质量持续验证.json`；
- `scripts/审计/持续验证数据质量.py`及其既有底层只读审计器；
- 固定逻辑别名`ubuntu`、白名单根、单成员/总超时、内存、输出和日志上限。

## 输出合同

- `artifacts/审计/数据质量增量复验/<批次>/`不可变脱敏批次目录和追加索引；
- `docs/审计/Ubuntu数据证据增量复验报告.md`；
- 批次身份、清单/规则/代码指纹、资源实测、状态计数、失败/无法判定原因和验证清单；
- 更新后的任务文件、看板、提交、PR和结构化评审证据。

## 工作范围

- 冻结输入并执行一次真实白名单只读质量证据复验；
- 生成可复算的聚合批次和脱敏报告；
- 运行任务合同指定的审计、研发中心、Markdown、编译、敏感信息和差异验证。

## 不在范围

- 不修复、删除、覆盖、迁移或重排原始数据；
- 不访问服务器白名单之外路径，不修改数据库、生产服务、权限或防火墙；
- 不训练模型、不回测、不做模拟或真实交易、不生成交易信号或许可；
- 不将SOL加入前向研究，不把15分钟/1小时升级为主研究尺度；
- 不因新批次局部通过而自动解除阶段2门禁。

## 安全边界

- SSH目标只允许逻辑别名`ubuntu`，命令、产物和文档不得记录IP、用户名、密钥、密码、令牌
  或原始日志；
- 远端只读、无落盘；本地产物只保存聚合事实、元数据、指纹和脱敏错误；
- 任何失败、超时、漂移和未知均保持失败安全，不重试为成功；
- BTC与ETH分别形成证据，不互相外推。

## 验收标准

1. 新批次不覆盖历史批次，批次目录、索引和报告可通过指纹复算。
2. 每个白名单成员均有结果或明确未执行原因，无静默遗漏；状态计数守恒。
3. BTC、ETH及四个主研究尺度独立统计，短窗口仅作为事后观察窗口。
4. 远端只读、无落盘、资源上限和敏感信息门均有运行证据。
5. 证据不足时明确为`无法判定`或`阻塞`，不生成胜率、收益或交易许可。
6. 历史任务-000031批次和原始数据未被修改；研发中心、审计、Markdown、编译和差异检查通过。

## 验证命令

```bash
python3 scripts/审计/持续验证数据质量.py --inventory artifacts/审计/数据源清单.csv --plan config/审计/数据质量持续验证.json --ssh-target ubuntu --batch-root artifacts/审计/数据质量增量复验 --timeout 3600
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m py_compile scripts/审计/*.py tests/审计/test_*.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

真实只读复验形成新的不可变批次、报告、指纹和验证记录，创建引用任务-000060的PR并更新为
`待评审`；双只读评审、主执行器验证和main可信复验通过后自动合并，再通过独立状态闭环PR
标记`已完成`。若Ubuntu或资源边界阻止批次形成，则按证据将任务标记`阻塞`，不得提交假批次。

## 执行记录

- 执行分支：`codex/000060-ubuntu-data-evidence-v1`
- 开始时间：2026-08-05 12:02:16 +0800
- 批次：`dqv-20260805T122014+0800-8cef70b7a7cf`
- 底层审计批次：`audit-20260805T122014+0800`
- 结果：成功生成追加式只读批次；主状态计数为拒绝3、无法判定312、失败0、未成熟0、失效0、通过0。
- 验证单元：315；完成扫描218（完整扫描记录27523291条）；仅元数据92（含估计记录数的合计81293526不代表完整扫描）；输入漂移或未执行5；可用性结论全部为无法判定。
- 指纹：规则`331fbfd9360b7e319ec0d9b981d398c401d10713d7275ab80ce33edb3bd577cf`；
  清单`019010d64fe47d89c81bfaedafd458f1d886025bb34292c0c32eafcfa4392657`；结构
  `4d8a6e6bb13f40b83bd5b1376a4974385e68bcff3de547bfb8870f006b32d573`；审计器
  `f7ed60e09f552bfe43126d19be1eaa9c0ea9d871616b0831df8c729dd24e09b3`。
- 资源实测：执行前内存可用88%，磁盘可用约166 GiB；单进程执行，无新增依赖。
- 安全事实：仅使用逻辑别名`ubuntu`白名单只读探针；无远端落盘、无服务器或数据库写入、无原始数据修改、无凭据记录。
- 交付提交SHA：`0cd054dce847381d034b509eb1a1575440d1c69b`；Pull Request：[#142](https://github.com/xk320/zhishi/pull/142)；当前状态：待评审。
- 验证结果：审计命令成功；审计测试147项通过；研发中心测试183项通过；Python编译通过；
  全量MarkdownLint（143个文件）0 issues；`git diff --check`通过；跨载体检查`conflicts: []`，
  规则指纹`217d9173c35d36b9305fb5d6c5b8d8ecf42b1d6de70fe2578a17b836b38dcf41`。
- 交付物：`artifacts/审计/数据质量增量复验/dqv-20260805T122014+0800-8cef70b7a7cf/`、
  `artifacts/审计/数据质量增量复验/批次索引.csv`、`docs/审计/Ubuntu数据证据增量复验报告.md`。
