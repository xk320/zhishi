# 任务-000115：将阶段1成本证据门改为覆盖受限模式

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000115-bounded-cost-coverage-gate-v6`
- 开始时间：`2026-08-15T04:33:48+08:00`
- Pull Request：[#353](https://github.com/xk320/zhishi/pull/353)
- 实现提交SHA：`ca79ec7f70a2f32aabdf8990bf8aaf22621951fd`
- 类型：治理
- 阶段：阶段1数据闭环证据修订
- 优先级：P0
- 执行方案：方案A“覆盖受限成本证据门治理修订”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务登记、执行、评审、合并和状态闭环完成前不得执行其他阶段1合同修订任务

## 依赖与阻塞条件

- 唯一前序依赖：任务-000105已完成，作为阶段1最终门禁基线输入。
- 被修订对象：任务-000106当前为阻塞；任务-000106不是本任务的完成依赖，本任务不得伪造其已完成或自动解除其历史阻塞事实。
- 当前目标：将阶段1研究证据门从“完整多年覆盖+多年主网真实执行延迟”改为“按已验证覆盖窗口交付并显式保留缺失”。
- 阻塞条件：若修订会放宽真实交易、未来数据、跨标的补偿、原始数据修改、生产写入或任务-000106历史批次不可变性，则立即停止并保持本任务阻塞。
- 解除条件：新合同、设计说明、任务-000106适用规则、总体计划和缺口入口对覆盖窗口、缺失语义、时间尺度和真实交易关闭逐项一致，并通过可信治理评审。

## 背景

任务-000106把完整多年成本覆盖和多年主网真实执行延迟设置为阶段1交付硬门，导致现有已验证的有限窗口数据无法用于研究。当前项目明确不要求交易延迟达到主网高频级别，也不要求等待多年数据；系统仍处于研究和模拟交易阶段，真实资金交易关闭。本任务建立覆盖受限模式，使已有证据可以在其真实覆盖边界内使用，同时不把缺失伪装成通过。

## 任务目标

1. 定义覆盖受限模式：按标的、数据对象、主研究尺度和结果窗口记录实际覆盖起止、样本量、缺失日期、连续段和数据指纹。
2. 将多年主网真实执行延迟从阶段1研究证据硬门移出，固定为当前阶段“未纳入/无法判定”，不允许用网络往返、信号投递确认、Demo或本地模拟替代真实主网证据。
3. 将完整多年历史覆盖从当前阶段硬门改为覆盖能力说明；覆盖外不得外推、填充、跨标的补偿或生成交易许可。
4. 保持BTC、ETH独立统计，主研究尺度固定为4小时、8小时、24小时、48小时，15分钟和1小时只能作为事后观察窗口。
5. 通过治理PR修订任务-000106的当前适用规则和相关阶段入口，不修改其历史执行记录、批次、SHA或已产生的失败证据。

## 固定执行方案

1. 先冻结最新main、任务-000105结果和任务-000106历史批次身份，建立修订前后字段映射。
2. 只在任务合同、阶段计划、数据缺口入口和相应设计说明中做最小治理修改，不新增采集、运行时、模型、回测或交易代码。
3. 对每个成本要素分别标注`已验证覆盖`、`部分覆盖`、`缺失`或`无法判定`；任何缺失均保留候选总体和分母，不得缩小分母。
4. 研究输出只能声明覆盖窗口内的描述性证据；不得由描述性差异推导因果、预测优势、胜率、收益、研究准入或交易许可。
5. 运行任务中心可信资格、MarkdownLint、`git diff --check`和相关合同一致性测试；失败安全地保留任务-000106阻塞。

## 默认工程决策

- 复用现有任务-000105、000106、000099的版本、批次和缺失字段，不新增第三方依赖。
- 使用现有已验证官方数据和已合并脱敏批次；不为补齐“多年”而重复扫描854GiB归档或扩大下载。
- 研究计算仅在有覆盖的标的、对象、主研究尺度和时间窗口内进行；覆盖外统一输出`无法判定`。
- 交易延迟字段保留为研究限制和后续阶段输入，不设置临时数值、不用代理值回填。
- 普通文案、路径组织和测试取舍按安全、证据、简单、可复现、可回滚顺序自主决定。

## 允许停止条件

- 需要真实资金、主网账户、生产权限、凭据、数据库写入、原始数据覆盖或不可逆操作。
- 新规则无法同时保持未来数据禁止、分标的统计、主研究尺度边界或真实交易关闭。
- 任务-000106历史合同、批次、SHA或失败证据必须被删除、覆盖或改写才能完成。
- 任务中心可信校验器无法确定性复算修订范围，或双只读评审发现P0/P1且三轮修复仍无法消除。

## 输入合同

- `docs/研发中心/任务/任务-000105.md`及其已完成状态和正式批次指纹。
- `docs/研发中心/任务/任务-000106.md`当前合同、阻塞原因、执行记录和历史输出路径。
- `docs/研发中心/总体计划.md`、`docs/审计/数据缺口与补采清单.md`及相关阶段1入口。
- 已合并任务-000099、000100、000103、000105、000106产生的脱敏边界、覆盖和缺失事实。
- 本任务设计文档`docs/superpowers/specs/task-000115-bounded-cost-coverage-gate-design.md`。

## 输出合同

- 修订后的任务-000106当前适用规则，保留历史合同与执行记录不可变。
- 阶段计划与数据缺口入口中的覆盖受限模式、缺失语义和研究尺度边界。
- 一份可复算的覆盖字段映射：标的、数据对象、主研究尺度、覆盖起止、候选总体、已观察、拒绝、无法判定、缺失日期、连续段、数据指纹。
- 任务-000115执行记录、验证命令与结果、PR和合并事实；不生成真实交易、胜率、收益、订单或方向结论。

## 工作范围

- 任务合同、阶段计划、数据缺口入口、相关研究规范和设计说明的治理一致性修订。
- 现有覆盖窗口与缺失状态的字段、术语和验收规则定义。
- 研发中心专项测试、MarkdownLint、敏感信息扫描和`git diff --check`。

## 不在范围

- 不访问服务器、数据库、Binance主网账户或真实市场接口。
- 不新增或修改数据采集器、运行时、模型、回测、交易代码、生产服务或账户权限。
- 不下载多年全量历史、不删除或覆盖原始数据、不修改任务-000106历史批次和执行记录。
- 不生成胜率、收益、预测优势、方向、仓位、订单、交易信号或交易许可。
- 不将15分钟、1小时升级为市场状态尺度、事件尺度或交易许可尺度。

## 安全边界

- 所有缺失均显式保留并输出`无法判定`，禁止默认值、插值、外推和跨BTC/ETH补偿。
- 事件时间、到达时间和采集时间继续分离；不得使用未来数据。
- 真实资金自动交易当前关闭；覆盖受限模式不改变任何生产或交易授权。
- 变更仅限治理文档和测试；失败或回滚不触碰Ubuntu、数据库、原始数据和凭据。

## 验收标准

1. 任务-000106当前规则明确完整多年历史覆盖不是阶段1研究交付的必要条件，实际覆盖窗口成为适用边界。
2. 任务-000106当前规则明确多年主网真实执行延迟不作为阶段1研究硬门，缺失输出为`无法判定`，且Demo/网络往返/投递确认不得替代。
3. BTC、ETH分别统计；4/8/24/48小时仍为主研究尺度；15分钟、1小时仅为事后观察窗口。
4. 覆盖受限输出保留候选总体、已观察、拒绝、无法判定、缺失日期、连续段和指纹，分母不因缺失缩小。
5. 任务-000106历史执行记录、批次、SHA、失败证据和原始数据字节不变。
6. 总体计划、数据缺口入口、任务-000106和本设计文档无相互冲突的硬门语义。
7. 真实交易仍关闭，未新增服务器、数据库、网络业务数据、凭据、生产或交易路径。
8. 相关专项测试、Python编译（如有代码改动）、MarkdownLint、敏感扫描和`git diff --check`通过；双只读评审P0/P1为零。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

覆盖受限模式治理PR进入main并通过双只读评审、可信资格复验和自动合并；任务-000115状态闭环为已完成。任务-000106仍需按修订后的当前规则重新执行或重算，不能仅凭本任务自动标记已完成。

## 数据与安全影响

本任务只修改治理合同、阶段入口和测试；不读取、写入、迁移或删除服务器、数据库、原始市场数据、生产服务、真实账户或凭据，不产生真实订单。

## 已知限制

覆盖受限模式不证明多年成本、主网执行延迟、稳定胜率、收益或交易可行性；它只允许在已验证窗口内继续研究并把窗口外结论标记为无法判定。后续若进入生产或真实交易阶段，必须另行建立执行遥测和成本证据合同。

## 回滚方式

回滚任务-000115治理PR及其派生入口修改；保留任务-000106历史合同、执行记录、批次和失败证据不变。回滚后任务-000106继续按原阻塞规则执行，不删除任何外部数据。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000115-bounded-cost-coverage-gate-v6`
- 开始时间：`2026-08-15T04:33:48+08:00`
- 实现提交SHA：`ca79ec7f70a2f32aabdf8990bf8aaf22621951fd`
- Pull Request：[#353](https://github.com/xk320/zhishi/pull/353)
- 交付物：任务-000106覆盖受限规则、五个派生入口补充、任务115状态和看板迁移。
- 验证结果：研发中心专项170项通过；变更Markdown 8份默认规则0 issues；`git diff --check`通过。
- 已知限制：多年主网真实执行延迟仍为无法判定，任务-000106不因本PR自动完成；真实资金交易保持关闭。
- 数据与安全影响：未访问服务器、数据库、市场数据、原始数据、凭据、生产服务或真实交易路径。
- 回滚方式：回滚PR #353恢复原完整成本门语义，不删除任务-000106历史批次和失败证据。
