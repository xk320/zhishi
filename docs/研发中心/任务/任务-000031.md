# 任务-000031：完成不可变输入与全量只读质量审计

<!-- markdownlint-disable MD013 -->

- 状态：待执行
- 类型：数据审计
- 阶段：阶段1 完整质量审计
- 优先级：P0
- 执行方案：方案A“确切输入冻结、流式全量审计与追加式证据批次”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领
- 执行分支：`codex/000031-quality-audit-v1`
- 开始时间：`2026-08-04T10:41:54+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000030通过评审并合并；
- 当前阻塞原因：无；固定只读探针（2026-08-04T23:58:28+08:00）`timeout 12 ssh -o BatchMode=yes -o ConnectTimeout=5 -o ConnectionAttempts=1 -o UserKnownHostsFile=/dev/null -o StrictHostKeyChecking=no ubuntu true`脱敏结果为`probe_category=reachable`；运行时预检（2026-08-04T23:58:29+08:00）返回`preflight=python3_entry_reachable`（Python 3.10.12）。
- 解除条件：已满足；新的可达性与运行时预检证据已记录，后续必须从最新`main`重新认领并重新冻结输入。

## 背景

历史审计存在动态输入身份漂移、未完整扫描成员以及大量无法判定项。本任务必须在同一
不可变输入身份上完成结构与质量阶段，保留拒绝、失败和无法判定，不能为提高覆盖率修改数据。

## 任务目标

使用任务-000029和任务-000030的确切版本，对BTC、ETH授权成员执行有界、流式、全量、
只读质量审计，生成新的不可覆盖批次和逐成员可复验证据。

## 固定执行方案

1. 在远端读取前冻结清单、规则、脚本、资源边界和数据截止时间；
2. 对每个成员在开始和结束时复核身份与内容指纹，漂移即拒绝；
3. 复用并收敛既有审计器，以流式方式统计缺失、重复、乱序、断档、异常和延迟；
4. 保存候选总体、已观察、通过、拒绝、无法判定、失败、未成熟和失效计数；
5. 原子发布新批次并只追加索引，不覆盖历史审计产物。

## 默认工程决策

单成员超时或超限时记录失败并继续后续成员；内存上限优先于速度；不读取敏感日志正文；
不能证明的统计保留未知，不以零填充；BTC与ETH分别汇总。

## 允许停止条件

只有目标环境不可访问、所有获批成员均无法读取，或执行必须扩大权限、修改原始数据和
生产系统时停止并记录阻塞。部分成员失败或结论无法判定应如实完成批次。

## 输入合同

- 任务-000029确切来源身份批次和成员清单；
- 任务-000030确切时间与质量合同、规则和指纹；
- `ubuntu`白名单只读目标；
- 冻结的数据截止时间、逐成员超时、总超时、内存、输出和日志上限；
- BTC与ETH分别统计，4小时、8小时、24小时、48小时尺度分别判定。

## 输出合同

- 新的`artifacts/审计/数据质量/<批次>/`不可变目录和追加式索引；
- 逐成员质量、断档、异常、身份复核结果及验证清单；
- `docs/审计/数据质量复验报告.md`；
- 审计器必要修订及测试；
- 批次、规则、输入、输出、成员和报告指纹；
- 本任务、看板、提交、PR和验证记录。

## 工作范围

执行获批成员的结构与正文只读统计、身份复核、资源控制、状态守恒、不可变发布、脱敏和
回归测试；记录所有未执行与失败原因。

## 不在范围

- 不修改、修复、填补、清洗、去重、重排、迁移或删除原始数据；
- 不读取未授权数据库业务正文或敏感日志；
- 不完成历史决策重放、成本闭环、模型训练、回测或交易判断。

## 安全边界

远端禁止写入和落盘，文件与SQLite只读，数据库只在明确合同授权时读取；不得提升权限；
流式处理并设置有界资源；产物只保存聚合事实、元数据和指纹，不保存原始市场记录或凭据。

## 验收标准

1. 上游清单每个成员都有结果或明确未执行原因，无静默遗漏；
2. 输入开始与结束身份一致，漂移成员不产生通过结论；
3. 各状态互斥且计数与候选总体守恒，未知不以零代替；
4. BTC与ETH及各场所、合约、对象和尺度独立判定；
5. 新批次不可覆盖、全部指纹可复算、历史批次保持不变；
6. 真实只读执行、资源上限、失败安全和脱敏有自动化与运行证据；
7. 全部审计、研发中心、Markdown和差异检查通过。

## 验证命令

```bash
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m py_compile scripts/审计/*.py tests/审计/test_*.py
npx --yes markdownlint-cli2 AGENTS.md README.md 'docs/**/*.md'
git diff --check origin/main...HEAD
```

实际审计命令、冻结参数和批次身份必须在执行时记录，不得在合同中虚构。

## 完成定义

真实只读审计形成不可变批次，交付与验证记录完整并创建引用任务-000031的PR后更新为
`待评审`。结论可以为拒绝或无法判定；只有外部环境完全阻止批次形成时标记`阻塞`。
PR评审并合并后标记`已完成`并解除任务-000032。

## 执行记录

- 执行分支：`codex/000031-quality-audit-v1`
- 开始时间：`2026-08-04T10:41:54+08:00`
- 尝试命令：`python3 scripts/审计/持续验证数据质量.py --inventory artifacts/审计/数据源清单.csv --plan config/审计/数据质量持续验证.json --ssh-target ubuntu --batch-root artifacts/审计/数据质量 --timeout 3600`
- 结果：未生成批次；底层审计因远端返回非零状态而安全停止，未发布部分产物。
- 外部证据：获批只读目标`ubuntu`的SSH探针返回`Host is down`；记录已脱敏，不包含主机地址或原始日志。
- 阻塞原因：获批只读目标`ubuntu`当前不可达，无法形成真实审计批次。
- 解除条件：`ubuntu`可通过SSH稳定访问且在任务冻结的超时、内存、日志和输出边界内完成两阶段只读审计；恢复后从最新`main`重新认领并重新冻结输入。
- 数据与安全：未访问服务器文件、数据库业务正文或原始市场记录；未修改远端、原始数据、生产服务、权限或账户；未创建假批次。
