# 任务-000066：数据库与敏感日志正文只读审计授权执行

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/000066-body-audit-v2`
- 开始时间：`2026-08-06T12:30:00+08:00`
- 实现提交SHA：`eb632a33d3d0c08893dfe4bcee1f4dc549e03f4e`
- Pull Request：[#165](https://github.com/xk320/zhishi/pull/165)
- 类型：数据审计
- 阶段：阶段1 数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“按任务-000064合同逐对象只读正文审计，失败安全追加不可变批次”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；外部授权未满足前不得访问正文或请求猜测性替代方案
- 并行规则：禁止并行；任务-000064完成后执行；不得与其他数据审计或数据工程任务并行

## 依赖与阻塞条件

- 唯一前序依赖：任务-000064
- 当前阻塞原因：无；授权批次已分别核验92个数据库对象与2个敏感系统日志文件的独立只读入口。数据库权限快照按任务-000063矩阵精确匹配92/92个表级`SELECT`、非`SELECT`权限0、`GRANT OPTION`及管理权限0，集合指纹为`25500878d3a6ef217d0c7ecfc7044be1448e56340e83dc8a36217967218ea4df`，权限令牌指纹为`9f211a9dcf6edec099423b992eb8054bfcb9c3f8246eae45bfa69db4b5c22b13`，其中订单簿对象16/16同样仅`SELECT`；日志固定强制命令指纹为`7a5ff0dcb146fbe576e309d6a0d6d48391b61f5bf80091ac1062131573c6cdf9`，授权主体仅以角色指纹`b1623ea7ebe52923b35d3af2fee8cd9b325153ec23c13c0256ccbc417e8c28b3`记录，日志批次指纹为`a4eeec7ac4b1194edba0c0151861ec831c01200258da2dc6bbecdb794bcfcd58`；两文件仅输出脱敏计数和指纹，当前状态均为`未成熟`，未输出正文
- 解除条件：已满足；授权开始`2026-08-06T11:38:35+08:00`，截止`2026-08-07T00:00:00+08:00`；预算已冻结为数据库单对象最多读取65536字节、30秒、串行、内存512MiB、远端临时写入关闭，日志单对象32768字节、批次65536字节、30秒、内存512MiB、远端临时写入关闭；字段/时间范围、脱敏、输出指纹、失败安全和撤销条件继续严格采用任务-000064合同，批次完成、超限、泄漏、失败或截止时立即锁定入口、撤销表级授权并移除强制键

## 背景

任务-000062和任务-000063确认94个对象没有正文级质量证据，ZS-DATA-GAP-003和ZS-DATA-GAP-005仍阻塞。
任务-000064已经建立最小授权、资源和脱敏合同，但该合同本身不授予服务器、数据库或日志访问权限。
本任务把后续正文审计入口登记为唯一任务，等待外部事实满足后再执行，禁止因服务器可达性声明直接读取正文。

## 任务目标

1. 在授权满足后，逐对象读取最小必要正文并生成可复算质量证据；授权未满足时保持阻塞。
2. 绑定任务-000062/000063输入清单、任务-000064合同版本、成员顺序、截止时点和内容指纹。
3. 保留通过、拒绝、无法判定、失败、未成熟、失效和未执行状态，不缩小分母、不跨BTC/ETH补偿。
4. 只有对象级正文证据完整覆盖且质量规则可复算时，才提出关闭ZS-DATA-GAP-003/005的证据；不直接放行阶段2。

## 固定执行方案

1. 解除阻塞前只检查授权合同和仓库不可变输入，不访问服务器或正文。
2. 解除后按任务-000064设计逐对象顺序执行，资源超限、漂移、脱敏失败或输出不完整立即失败关闭。
3. 保存追加式批次、状态守恒、资源实测和脱敏错误；不得覆盖历史批次或原始数据。

## 默认工程决策

- 复用任务-000064设计、既有审计脚本和标准库；不引入服务、模型、回测或交易代码。
- BTC、ETH分别统计；SOL不进入前向范围；主研究尺度固定4小时、8小时、24小时、48小时，15分钟/1小时仅事后观察。
- 任何描述性差异不构成因果、预测优势、胜率、收益、研究准入或交易许可。

## 允许停止条件

- 授权、资源预算、脱敏规则、输入指纹或截止时点缺失或冲突；
- 需要访问白名单外路径、生产系统、原始数据、账户、凭据或执行不可逆操作；
- 资源超限、输入漂移、成员遗漏、输出不完整或敏感信息泄漏。

## 输入合同

- 任务-000064合同及设计；任务-000062/000063质量批次、清单、覆盖矩阵和缺口报告；
- `AGENTS.md`、README、研发中心入口、任务规范、全部任务文件和看板；
- 解除阻塞后实际提供的对象授权、资源预算、脱敏和截止事实。

## 输出合同

- 追加式不可变批次目录、逐对象脱敏状态和资源事实；
- `docs/审计/Ubuntu未完整扫描对象正文质量复验报告.md`；
- 更新任务文件、看板、提交、PR、双只读评审记录和验证结果。

## 工作范围

- 当前仅登记阻塞合同和解除条件；解除后仅按对象白名单执行正文质量审计。

## 不在范围

- 当前阻塞期间不访问服务器、数据库、敏感日志或真实市场数据；
- 不修改、删除、覆盖、迁移原始数据或历史批次；不训练模型、不回测、不生成交易信号或交易许可；
- 不将未执行或无法判定解释为通过，不因局部证据关闭阶段1。

## 安全边界

- 只读、最小必要字段、对象级授权、资源硬上限和脱敏输出同时满足才可解除阻塞。
- 禁止凭据、IP、域名、用户名、原始日志、业务正文进入Git；失败安全优先于完成数量。

## 验收标准

1. 任务文件和设计文档明确逐对象授权、资源、脱敏、输入指纹、状态守恒和解除条件。
2. 当前状态保持阻塞，未访问服务器、数据库、真实市场数据或正文。
3. 后续执行只能在任务-000064合同前置条件全部满足后开始；任一缺失条件均继续阻塞。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

本登记PR合并后任务保持`阻塞`，不标记已完成。只有解除条件有证据并经独立状态闭环PR恢复为待执行后，
才能认领执行；正文审计交付须另行PR、双只读评审、main可信复验和合并后状态闭环。

## 执行记录

- 交付状态：待评审；已完成一次真实、串行、失败安全的正文只读复验批次。
- 批次：`批次-20260806T043100Z-v5`；批次指纹：`921fad8452628fc6d19b4a20df3fb1d9243859c9bd044e5740035a753ad1dda1`。
- 交付物：`scripts/审计/执行未完整扫描对象正文.py`、`docs/审计/Ubuntu未完整扫描对象正文质量复验报告.md`及`artifacts/审计/Ubuntu未完整扫描对象正文质量复验/批次-20260806T043100Z-v5/`。
- 状态摘要：无法判定91、失败1、未成熟2、通过0、拒绝0、失效0、未执行0，合计94；未来时间对象保留为失败，未用于任何统计或决策。
- 验证结果：审计器编译与合同烟测通过；研发中心测试190项通过；markdownlint覆盖159个文件且0 issues；`git diff --check`通过。
- 数据与安全：数据库只在EXPLAIN证明索引路径后读取最多64行时间字段，并校验会话/SHOW GRANTS指纹；日志固定入口、密钥和字段白名单均校验；远端无临时写入，未修改原始数据、数据库、订单簿逻辑、权限或生产服务，Git未包含凭据、原始日志或业务正文。
- 已知限制：有界样本不能替代全量正文质量证明；91个无法判定、1个失败和2个未成熟必须保留，ZS-DATA-GAP-003/005与阶段2门禁不因本批次关闭。
- 回滚方式：回滚本PR提交即可移除审计器与批次产物；本批次无远端写入，无需服务器或数据库回滚。
- 修复轮次：针对评审意见重新执行批次`批次-20260806T044500Z-v6`；批次指纹`174c00e964ff5d145806b5b7419fc3c6d4772334dc1317d477604f8f1137addd`，状态仍为无法判定91、失败1、未成熟2、通过0，新增授权输入指纹、对象顺序/字段白名单、固定入口证明及对象/批次资源硬门；v5产物保留为历史证据，未覆盖。
- 最终批次：`批次-20260806T045500Z-v7`；批次指纹`784e821fc2a845d8234aff4541a333f68bf5cdf1edf7a59a8d003328adb0d7ce`，由最终脚本指纹`1f21e5b3798d8141783169b8b022dddf9a04168fb42631add02c2f25bac00b2a`执行；远端包装器实际回传并校验指纹`d63540742cc71bc07908c0d31d09e7e95c1ed8d89fc43c00d848277a62b6cbc3`，状态仍为无法判定91、失败1、未成熟2、通过0。
