# 任务-000004：审计数据质量、时间语义与可用性

- 状态：待评审
- 类型：数据审计
- 优先级：P0
- 阶段：阶段1数据质量审计
- 讨论关联：GitHub Issue #5（非任务事实来源）
- 前置任务：任务-000003
- 执行分支：`codex/000004-data-quality-audit-v1`
- 开始时间：2026-07-28 22:34:40 +0800
- 完成实现时间：2026-07-28 23:34:28 +0800
- 最新实现提交 SHA：`8972931437492cb93251c9e649c3bab8f388f74c`
- Pull Request：[#24](https://github.com/xk320/zhishi/pull/24)
- 依赖满足证据：任务-000003已通过PR #23合并，合并提交
  `3b80179191c0ec20fa5df5cb865753d8c215add0`；只读目标`ubuntu`可访问。

## 背景

任务-000003只证明运行环境和候选数据资产已被发现，不证明数据完整、时间语义明确、
可重放或可用于研究。下一阶段必须基于冻结的资产清单，以只读、可重复执行的方式量化
质量事实，并对无法证明的语义保留“无法判定”。

## 任务目标

对任务-000003发现的数据源执行缺失、重复、乱序、断档、异常、事件时间、到达时间、
采集时间和延迟审计，分别给出BTC、ETH、SOL的数据可用性结论和解除条件。

## 输入合同

1. 基线分支：`main`提交`ee0a7821407f0d7c5eab461c40ecfe7247127258`。
2. 资产清单：`artifacts/审计/数据源清单.csv`，发现批次和资产编号不得在审计中改写。
3. 资产说明：`docs/审计/数据源清单.md`。
4. 只读目标：SSH别名`ubuntu`；只允许访问资产清单已登记且位于任务-000003白名单根
   目录内的候选数据文件和数据库元数据。
5. 时间、唯一键、标的、市场、频率和异常规则必须来自可验证的字段合同、模式或来源
   文档。字段名称只能形成候选映射，不能单独证明业务语义。
6. 先完成模式发现并冻结审计规则，再读取质量统计结果。无法冻结字段语义、唯一键或
   预期频率的验证单元，对应指标记为`无法判定`，不得用经验阈值补齐。
7. 数据截止时间为审计运行开始时间；审计结果必须记录输入清单指纹、规则版本、脚本
   版本、运行环境和冻结时间。

## 输出合同

- `docs/审计/数据质量审计报告.md`：区分事实、判定和建议，分别记录BTC、ETH、SOL
  的精确作用域、结论、依据、限制、失效条件和解除条件。
- `artifacts/审计/数据质量结果.csv`：每个验证单元的范围、记录数、关键字段缺失、重复、
  乱序、三类时间、延迟、覆盖和结论证据。
- `artifacts/审计/数据断档结果.csv`：只有预期频率和事件时间语义已冻结时才量化断档；
  否则显式记录`无法判定`及原因。
- `artifacts/审计/数据异常结果.csv`：记录规则编号、异常类型、数量、比例、严重度和处置；
  不包含原始业务记录或敏感值。
- `scripts/审计/审计数据质量.py`：固定白名单、只读、可超时、可重复执行的审计入口。
- `tests/审计/test_审计数据质量.py`：覆盖合同校验、只读边界、时间语义、统计、脱敏、
  确定性输出和失败安全。

## 工作范围

1. 校验任务-000003资产清单的结构、指纹和已登记范围。
2. 只读发现候选文件和数据库对象的模式、字段及可验证合同证据。
3. 对具有足够合同证据的验证单元流式计算缺失、重复、乱序、断档、异常和延迟统计。
4. 对缺少语义、权限、支持格式或完整性证据的单元保留未执行事实和解除条件。
5. 按BTC、ETH、SOL分别形成结论，不跨标的、市场、来源或时间尺度外推。
6. 提供可复现命令、运行摘要、失败记录和安全影响说明。

## 不在范围

- 不清洗、填补、去重、重排、修订或迁移原始数据。
- 不执行任务-000005的历史现场重放，不执行任务-000006的最小数据闭环放行。
- 不训练模型、不运行正式回测、不生成胜率或收益结论。
- 不编写交易代码、不下单、不修改生产代码、服务、数据库结构或服务器配置。
- 不把文件名、目录名、列名或其他标的数据结论当作已证明的业务语义。

## 安全边界

1. 远程访问仅允许固定SSH目标和白名单路径，数据库只允许只读查询。
2. 禁止读取或输出环境变量、凭据文件、密钥、令牌、密码、私钥和账户信息。
3. 禁止任何远程写入、临时落盘、锁表、建表、修改、删除、服务控制和权限变更。
4. 审计采用有界资源、逐对象超时和失败安全；超时或异常不得重试为成功。
5. 仓库只保存汇总统计、指纹和脱敏证据，不保存原始记录、未脱敏样本或网络地址。
6. 原始数据保持不变；审计结果不得自动升级研究准入、知识关系或交易许可状态。

## 验收标准

1. 清单内每个候选数据文件和数据库元数据对象都有审计覆盖记录或明确未执行原因，且
   不存在静默遗漏。
2. 缺失、重复、乱序、断档、异常、事件时间、到达时间、采集时间和延迟均有数值证据
   或`无法判定`证据，口径与规则版本可追溯。
3. 阈值和字段合同在读取质量结果前冻结；报告不得根据结果事后改变阈值或排除范围。
4. BTC、ETH、SOL各自形成`可用`、`有限可用`、`不可用`或`无法判定`结论；在
   任务-000005与任务-000006未完成前，本任务不得单独给出`可用`结论。
5. 报告、三份CSV、脚本和测试完整，CSV列固定、行数可核对、输出排序确定且防止表格
   公式注入。
6. 只读边界、安全失败、敏感信息脱敏和不修改原始数据有自动化测试与运行证据。
7. Markdown格式正确，任务文件与看板状态一致，`git diff --check`通过。

## 验证方式

```bash
python3 -m unittest tests/审计/test_审计数据质量.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 scripts/审计/审计数据质量.py --inventory artifacts/审计/数据源清单.csv \
  --ssh-target ubuntu --output-dir artifacts/审计 \
  --report docs/审计/数据质量审计报告.md
python3 -m unittest tests/研发中心/test_验证自动合并资格.py -v
git diff --check
```

## 完成定义

实现、只读审计和全部验证如实完成，交付记录写回本文件与看板，提交并推送独立分支，
创建引用任务-000004的Pull Request后，状态变更为`待评审`。只有架构评审通过且PR
合并后才能标记为`已完成`。

## 解除阻塞记录

任务-000003已完成并提供可验证的数据源清单、只读发现脚本和安全边界。任务-000004
已于2026-07-28重新确认服务器访问、输入合同和方案A“合同先行、全量白名单审计”，
进入执行。

## 执行记录

### 交付物

- `scripts/审计/审计数据质量.py`
- `tests/审计/test_审计数据质量.py`
- `artifacts/审计/数据质量结果.csv`
- `artifacts/审计/数据断档结果.csv`
- `artifacts/审计/数据异常结果.csv`
- `docs/审计/数据质量审计报告.md`
- `docs/superpowers/specs/2026-07-28-data-quality-audit-design.md`
- `docs/superpowers/plans/2026-07-28-data-quality-audit.md`

### 验收与验证

- 真实只读审计：通过。批次`audit-20260728T233014+0800`覆盖315个验证单元；三份
  CSV各315行、资产编号唯一，批次、清单指纹和规则指纹一致。
- 内容审计边界：217个文件完整扫描；92个MySQL对象仅查询元数据；4个动态文件因
  两阶段身份漂移未执行；2个敏感系统日志保留覆盖记录但不读取正文。
- 失败真实性：6个未执行对象的记录数、字段数、缺失数、缺失率、重复数、覆盖和异常
  数量均为`无法判定`，没有用默认零值替代未执行事实。
- 标的结论：BTC、ETH、SOL的已登记候选验证单元分别为55、2、0；三者均为
  `无法判定`，没有从文件名、字段名或其他标的外推可用性。
- 质量口径：缺失率使用逐对象真实单元格分母；JSONL结构发现不静默截断；SQLite多表
  分母、时间字段后缀及WAL/SHM身份已覆盖；MySQL两阶段结构指纹不一致时拒绝结论。
- 异常口径：结构异常规则只在217个完整扫描对象执行，发现0项；其余98个对象不进入
  零异常分母。该结论不包含未定义的业务数值异常。
- 自动化回归：任务测试34项、审计目录55项、研发中心22项、自动合并资格12项均
  通过；Python编译检查通过。
- 格式与安全：5份Markdown为0 issues，CSV公式注入防护通过；IPv4、私钥、令牌、
  云密钥和明文凭据模式扫描0命中；`git diff --check`通过。
- 独立实现复核：提交`8972931`经只读复审判定无代码或产物阻断项；该结论不替代
  仓库所有者的架构评审。

### 验证命令

```bash
python3 -m unittest tests/审计/test_审计数据质量.py -v
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m py_compile scripts/审计/审计数据质量.py
python3 scripts/审计/审计数据质量.py \
  --inventory artifacts/审计/数据源清单.csv \
  --ssh-target ubuntu --timeout 3600 \
  --output-dir artifacts/审计 \
  --report docs/审计/数据质量审计报告.md
npx --yes markdownlint-cli2 \
  docs/superpowers/specs/2026-07-28-data-quality-audit-design.md \
  docs/superpowers/plans/2026-07-28-data-quality-audit.md \
  docs/审计/数据质量审计报告.md \
  docs/研发中心/任务/任务-000004.md \
  docs/研发中心/看板.md
python3 -m unittest tests/研发中心/test_验证自动合并资格.py -v
git diff --check origin/main...HEAD
```

### 已知限制

- 标的身份、市场、合约、三类时间、时区、预期频率和修订合同尚未证明，断档、乱序、
  延迟及BTC、ETH、SOL可用性均保持`无法判定`。
- MySQL只审计`information_schema`元数据，不读取业务表正文，记录数仅为元数据估计或
  `无法判定`。
- 4个动态文件在两阶段间发生身份漂移，本批次不形成其内容质量结论；20个大对象因
  重复集合上限不形成完整重复结论。
- 未定义业务异常阈值，不评价价格、数量、收益或盘口数值是否异常。
- 任务-000005历史决策现场重放和任务-000006最小数据闭环尚未完成，本任务不能单独
  放行正式研究、模型训练、回测或交易许可。

### 数据与安全影响

- 远端仅通过固定SSH逻辑别名`ubuntu`执行白名单内只读探针，程序经标准输入执行且
  远端无落盘；未修改服务器、服务、权限、防火墙、数据库或原始数据。
- SQLite使用只读URI；MySQL仅查询元数据；两个可能包含查询文本的系统日志不读取正文。
- 仓库只保存汇总统计、规则和指纹，不保存原始业务记录、未脱敏样本、网络地址或凭据。
- 未使用未来数据，未训练模型、运行正式回测、编写交易执行代码或访问真实交易账户。

### 人工决策与下一任务

- Pull Request #24包含Python脚本、测试和CSV审计产物，不符合低风险自动合并资格，
  必须由仓库所有者完成人工架构评审和合并；Codex不得自行合并。
- 任务-000005依赖本任务合并完成。在PR #24合并并将本任务标记`已完成`前，下一任务
  仍为阻塞，不得提前执行。
