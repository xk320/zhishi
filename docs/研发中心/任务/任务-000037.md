# 任务-000037：完成阶段1最终审计与阶段2门禁裁决

<!-- markdownlint-disable MD013 -->

- 状态：执行中
- 类型：数据审计
- 阶段：阶段1 最终审计与门禁
- 优先级：P0
- 执行方案：方案A“全链指纹复验、逐叶子硬门裁决与保守阶段结论”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 并行规则：禁止并行；只在前序任务合并后认领
- 执行分支：`codex/000037-stage1-final-audit-v1`
- 开始时间：`2026-08-05T05:53:30+08:00`

## 依赖与阻塞条件

- 唯一前序依赖：任务-000036通过评审并合并；
- 当前阻塞原因：无；任务-000036已完成。
- 解除条件：已满足。

## 背景

任务完成数量、服务器可访问、范围收缩、文档齐全或某个局部批次通过都不能代表阶段1
通过。最终裁决必须在同一确切叶子范围复核全部硬门，并允许结论继续禁止阶段2。

## 任务目标

对任务-000029至000036形成的BTC、ETH证据链执行独立最终审计，给出精确允许研究范围、
仍阻塞范围和阶段2是否解锁的唯一可复现结论。

## 固定执行方案

1. 冻结全部上游任务、提交、批次、规则、代码、成员和内容指纹；
2. 按标的、场所、市场、合约、数据对象、尺度和时间范围构建最终叶子；
3. 依次裁决身份、三类时间、质量、重放、成本、血缘、容量和恢复硬门；
4. 任一关键门失败或无法判定，对应叶子不得进入阶段2；
5. 生成不可变最终审计批次，同步总体计划、README、路线图和缺口清单的派生结论。

## 默认工程决策

聚合结论取最低关键门，不投票、不平均；BTC与ETH不能互相补偿；部分通过只精确放行对应
叶子，不扩大到其他场所、合约、尺度或时期；证据冲突时保持阶段2禁止。

## 允许停止条件

上游指纹无法取得且Git历史也不能恢复、权威合同直接冲突，或裁决需要修改历史证据时停止。
最终结果为禁止、部分范围为空或全部无法判定不属于停止条件，必须照实发布。

## 输入合同

- 任务-000029至000036的合并提交、不可变批次、规则、成员、报告和SHA-256；
- 现行数据验证、研究准入、最小闭环和任务中心合同；
- BTC、ETH及4小时、8小时、24小时、48小时精确范围；
- 历史审计批次只作基线，不得覆盖或改写。

## 输出合同

- `docs/审计/阶段1最终审计报告.md`；
- `artifacts/审计/阶段1最终审计/<批次>/`叶子裁决、缺口、验证清单和指纹；
- 最终允许研究范围：标的、场所、市场、合约、对象、尺度、时间范围和版本；
- 阶段2门禁唯一结论及证据、限制、失效和解除条件；
- 同步后的README、总体计划、路线图、缺口清单、任务和看板；
- 提交、PR和全部验证记录。

## 工作范围

只读复算上游指纹、构建最终叶子、执行硬门裁决、生成报告和派生状态、验证历史证据未改、
执行全量回归和敏感信息检查。

## 不在范围

- 不修复上游数据、不补采、不修改原始记录或重写历史批次；
- 不执行阶段2基准模型、正式回测、模拟交易或真实资金交易；
- 不因项目进度要求降低门槛或虚构允许范围；
- 不自行合并PR或把待评审任务写为已完成。

## 安全边界

仅只读访问仓库证据和必要的获批元数据；不访问真实账户、不修改生产系统；报告不得含
凭据、地址或原始记录；历史产物保持不可变；未知、冲突和失效默认阻止阶段放行。

## 验收标准

1. 任务-000029至000036全部确切输入和指纹复算一致；
2. 每个最终叶子八类硬门均有状态、证据、限制和解除条件；
3. BTC与ETH以及各场所、合约、对象、尺度和时间范围不互相补偿；
4. 候选总体和全部状态计数守恒，失败、未成熟和失效不删除；
5. 阶段2只有存在完整通过叶子和明确允许范围时才可精确解锁，否则保持阻塞；
6. README、总体计划、路线图、缺口清单、任务和看板结论一致；
7. 历史审计证据未修改，最终批次不可覆盖且可复现；
8. 研发中心、审计、数据、Markdown、敏感信息和差异检查全部通过。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -v
python3 -m unittest discover -s tests/审计 -p 'test_*.py' -v
python3 -m unittest discover -s tests/数据 -p 'test_*.py' -v
npx --yes markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check origin/main...HEAD
```

另须复算全部上游和最终批次指纹，核对任务与看板唯一映射，并扫描凭据、令牌、地址、
原始记录、未来数据和未经证据支持的胜率收益声明。

## 完成定义

最终审计批次、阶段门结论、派生状态和全部验证记录形成后创建引用任务-000037的PR并更新
为`待评审`。只有PR经架构评审并合并后任务才为`已完成`；阶段2是否解除只由最终报告的
精确证据决定，即使本任务完成也允许阶段2继续阻塞。

## 执行记录

- 交付状态：执行中，已形成待评审交付物，尚未合并。
- 最终审计批次：`final-20260805T061500Z-v1`。
- 上游输入：任务-000029至任务-000036的合并提交、验证清单文件指纹和双标的闭环成员；
  所有上游任务状态均为`已完成`且合并提交精确匹配。
- 最终叶子：8个（BTC、ETH分别覆盖4小时、8小时、24小时、48小时）；候选2520，已观察2520，
  拒绝56，无法判定2464，失败0，未成熟0，失效0；计数守恒。
- 阶段门结论：8个叶子全部`阻塞`；允许研究范围为空；阶段2保持阻塞；不生成胜率、收益、
  方向、仓位、订单、模型、回测或交易许可结论。
- 交付物：`docs/审计/阶段1最终审计报告.md`、`scripts/审计/执行阶段1最终审计.py`、
  `tests/审计/test_阶段1最终审计.py`、`artifacts/审计/阶段1最终审计/final-20260805T061500Z-v1/`、
  README、总体计划、路线图、数据缺口清单和数据来源合同指纹同步。
- 验证结果：阶段1最终审计专项5项、研发中心177项、审计147项、数据55项、MarkdownLint
  134个文件、Python编译和敏感信息扫描通过；`git diff --check`通过。
- 数据与安全：仅读取仓库内聚合证据和任务合同；未访问Ubuntu、服务器、数据库业务正文或
  真实市场数据，未修改原始数据、生产服务、权限、账户或凭据。
- 已知限制：来源、三类时间、质量、重放、成本、容量和恢复硬门仍未形成完整通过证据；
  本次不因外部可达性声明改变结论。
- 回滚方式：移除本任务交付提交及其PR，保留任务-000029至任务-000036历史批次和原始数据不变。
