# 任务-000122：兼容任务交付记录与默认Markdown结构校验

<!-- markdownlint-disable MD013 -->

- 状态：待评审
- 执行分支：`codex/task-000122-md022-contract-compat-v1`
- 开始时间：`2026-08-15T04:14:56+08:00`
- Pull Request：[#351](https://github.com/xk320/zhishi/pull/351)
- 实现提交SHA：`b7df98497f61b13c29b1e69227e08ceabc41231e`
- 类型：治理
- 阶段：研发中心可信合同治理
- 优先级：P1
- 执行方案：方案A“限定执行记录分隔兼容修复”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- 自动合并范围：治理自动化
- 并行规则：禁止并行；本任务完成前不得重提任务-000115覆盖受限合同修订PR

## 依赖与阻塞条件

- 唯一前序依赖：任务-000121已完成并进入main。
- 当前问题：任务-000115注册合同要求任务正文不可变；其执行记录紧贴回滚正文时，默认MarkdownLint触发MD022，补空行又会触发可信合同漂移。
- 目标：让可信合同比较仅对任务交付记录边界允许一个标准空行，同时继续冻结合同正文和执行记录内容。
- 阻塞条件：若修复会放宽任意任务合同、允许多余空白或跳过任务/路径/状态/安全校验，立即停止。
- 解除条件：限定兼容规则、负向测试和任务-000115默认MarkdownLint结果进入main，并通过双评审和可信资格复验。

## 背景

任务-000115的已批准合同布局与默认Markdown结构规则出现确定性冲突。当前只能用临时禁用MD022的方式验证，不能作为正式治理流程。需要建立最小、可复算且不扩大合同编辑能力的兼容规则。

## 任务目标

1. 仅允许任务交付记录标题前的一个标准空行不计入合同漂移比较。
2. 保持任务合同正文、状态元数据、执行记录字段、看板、路径和安全边界的既有硬门。
3. 增加正向兼容及多空行、正文改写、错误任务和额外路径失败关闭测试。

## 固定执行方案

1. 在可信资格器的任务交付合同比较中增加精确的单空行归一化，仅作用于`## 执行记录`紧邻前的分隔位置。
2. 不改变任务类型、状态迁移、任务映射、路径白名单、目标合同指纹或主可信阶段1规则。
3. 用最小单进程专项测试验证兼容边界，并以默认MarkdownLint重新验证任务-000115。

## 默认工程决策

- 复用Python标准库和现有测试；不新增依赖、配置文件或服务。
- 只对明确的交付记录边界做一次空行兼容；其他空白差异仍视为合同漂移。
- 以最新main、任务-000121完成事实和关闭的任务-000115候选PR为输入，不恢复旧PR或旧分支。

## 允许停止条件

- 需要修改任务-000115、任务-000106或任务-000121的合同正文、状态、历史批次或失败证据。
- 需要修改生产代码、服务器、数据库、原始数据、凭据、真实账户或交易路径。
- 兼容规则无法精确限制到一个执行记录前分隔空行，或任何负向测试不能失败关闭。

## 输入合同

- `scripts/研发中心/验证自动合并资格.py`的任务交付合同比较逻辑。
- `tests/研发中心/test_验证自动合并资格.py`现有任务交付和阶段1合同测试。
- `docs/研发中心/任务/任务-000115.md`的已批准合同与MD022验证证据。
- `docs/治理/PR自动合并策略.md`及任务-000121完成后的可信规则。

## 输出合同

- 限定单空行兼容的可信合同比较规则。
- 正向和负向专项测试及任务-000122执行记录。
- 可按默认规则通过MarkdownLint的任务-000115重提前置能力；不交付研究、数据或交易能力。

## 工作范围

- 研发中心可信资格器的最小合同归一化逻辑。
- 对应测试、任务-000122执行记录和看板派生状态。

## 不在范围

- 不修改任务-000115、任务-000106、任务-000121合同或状态。
- 不放宽阶段1路径、真实交易、未来数据、跨标的补偿、研究准入或交易许可。
- 不访问服务器、数据库、市场数据、生产服务、账户权限或凭据。

## 安全边界

- 空行兼容不改变合同内容，只解决Markdown结构与不可变比较的表示差异。
- 未知任务、错误状态、额外路径、正文改写、多个空行和任何敏感/生产/交易变更必须失败关闭。
- 不使用未来数据，不生成胜率、收益、方向、仓位、订单或交易许可。

## 验收标准

1. 任务-000115在执行记录标题前加入一个标准空行后，可信资格复算通过且默认MarkdownLint无MD022。
2. 两个或更多空行、正文任意改写、执行记录字段漂移、错误任务或额外路径均失败关闭。
3. 任务-000115、任务-000106和任务-000121相对基线合同与状态不变。
4. 研发中心专项测试、Python编译、默认MarkdownLint和`git diff --check`通过。
5. 变更无服务器、数据库、原始数据、生产、凭据或交易影响。

## 验证命令

```bash
PYTHONHASHSEED=0 python3 -m unittest tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_自动合并工作流.py tests/研发中心/test_任务看板模式.py tests/研发中心/test_项目范围与阶段状态.py -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 <本次变更Markdown文件>
git diff --check
```

## 完成定义

限定单空行兼容规则进入main并通过双只读评审、可信资格复验和自动合并；任务-000122状态闭环为已完成。任务-000115必须随后基于最新main重新执行，不因本任务自动完成。

## 数据与安全影响

仅修改治理脚本、测试和任务记录；不读取、写入、迁移或删除服务器、数据库、市场数据、原始文件、生产服务、凭据或真实账户。

## 已知限制

本任务只解决任务合同表示层的Markdown兼容，不改变任务-000106阻塞事实、覆盖证据、成本证据或交易许可。

## 回滚方式

回滚本任务PR即可恢复原合同比较逻辑；不删除或覆盖任何任务合同、批次或历史失败证据。

## 执行记录

- 交付状态：待评审，已创建Pull Request，等待双评审与可信合并。
- 执行分支：`codex/task-000122-md022-contract-compat-v1`
- 开始时间：`2026-08-15T04:14:56+08:00`
- 实现提交SHA：`b7df98497f61b13c29b1e69227e08ceabc41231e`
- Pull Request：[#351](https://github.com/xk320/zhishi/pull/351)
- 交付物：任务交付合同单空行兼容规则及正负向测试。
- 验证结果：研发中心专项170项通过；Python编译通过；变更Markdown 2份默认规则0 issues；`git diff --check`通过。
- 已知限制：本任务不改变任务-000115、任务-000106或任务-000121合同和状态；任务-000115需基于本PR合并后的最新main重新执行。
- 数据与安全影响：未访问服务器、数据库、市场数据、原始数据、凭据、生产或真实交易路径。
- 回滚方式：回滚PR #351恢复原合同比较逻辑，不删除任务合同或历史证据。
