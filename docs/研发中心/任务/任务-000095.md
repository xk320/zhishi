# 任务-000095：修复阶段1审计交付与阻塞治理死锁

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 类型：治理
- 阶段：阶段1数据闭环证据修复
- 优先级：P0
- 执行方案：方案A“审计C热路径窄白名单、固定进程组资源合同与任务-000094一次性修复入口”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；用户此前已明确授权放宽开发阶段白名单并自动解决不影响主机、数据库和原始数据的阻塞；不得再次要求选择方案
- 自动合并范围：治理自动化
- 执行分支：`codex/task-000095-audit-governance-exec-v1`
- 开始时间：`2026-08-12T12:00:00+08:00`
- 实现提交SHA：`dc14048c9319b8b97cbd8dbeaf06570fc9f17662`
- Pull Request：[#255](https://github.com/xk320/zhishi/pull/255)
- 合并提交SHA：`436e562fbb0dcf8abbfe52b714576780d5af1ee1`
- 合并时间：2026-08-12 04:38:43 +0000
- 并行规则：禁止并行；本任务仅用于解除任务-000094被双评审和状态闭环共同确认的治理死锁，完成前不得继续任务-000094交付或登记后续阶段1门禁任务
- 一次性领取顺序：依据用户已明确批准自动解决开发阶段治理阻塞的当前授权，本任务在任务-000094之前领取；该例外只适用于任务-000095本身，进入`main`后的可信实现必须固定并审计此映射，其他任务不得复用

## 依赖与阻塞条件

- 唯一前序依赖：任务-000093已完成；任务-000094已在`main`登记，PR #252因两份独立只读评审发现四项P1而关闭，PR #253又证明既有合同缺少解除条件时无法进入阻塞状态闭环。
- 当前阻塞原因：无。失败PR、可信资格日志、任务-000094基线合同和现有治理测试均在仓库或GitHub可验证；本任务不依赖服务器、数据库、凭据、市场数据或生产权限。
- 失败安全：任何实现若允许审计目录外`.c`、任意编译器/命令、远端或生产操作、未绑定进程组资源事实，或可修改任务-000094目标/研究门槛，必须失败关闭。

## 背景

任务-000094真实扫描使用Python主控、固定解压器和C扫描热路径。PR #252第一轮资格检查拒绝`.c`；将源码改为`.py`载体后虽通过路径检查，但两份独立评审正确认定其为扩展名规避，并发现批次只统计主进程RSS、任务合同与最终执行器指纹不一致。尝试把任务标记阻塞时，可信规则又因登记合同没有`解除条件`而拒绝迁移，形成无法通过正常交付或状态闭环解除的治理死锁。

本任务只修复治理控制面：允许极窄审计C源码路径，冻结最多三个进程的拓扑与整个进程组资源计量，并建立任务-000095到任务-000094的一次性完整合同修复入口。不得借此改变数据结论、阶段门槛或生产权限。

## 任务目标

1. 仅允许任务-000094交付精确路径`scripts/审计/阶段1时间质量扫描器.c`普通UTF-8源码，继续拒绝其他任务、路径、原生扩展名、可执行文件和二进制对象。
2. 固定任务-000094唯一拓扑为一个Python主控、一个固定`/usr/bin/unzip`解压子进程和一个由固定`/usr/bin/clang`从内容寻址源码编译的扫描子进程；任一时刻最多三个相关进程。
3. 要求任务-000094按主进程和全部子进程峰值RSS之和的保守上界记录资源，并保持512MiB硬门、单成员串行和总时限不变。
4. 建立任务-000095→任务-000094的一次性`任务合同冲突修复`入口，只允许完整替换已知旧段落、增加唯一解除条件并更新对应验证命令；状态保持`待执行`。
5. 为白名单、路径对象、进程拓扑、资源字段、精确段落指纹和负向边界增加可信测试与治理文档。

## 固定执行方案

1. 从最新`main`冻结任务-000094、自动合并策略、资格校验器、跨载体校验器和现有测试指纹。
2. 在受控研发路径规则中只为任务-000094增加精确路径`scripts/审计/阶段1时间质量扫描器.c`；沿用文件数量、单文件5MiB、总量25MiB、UTF-8、敏感信息、删除/重命名/符号链接/可执行文件拒绝规则。
3. 在可信规则中新增固定映射任务-000095→任务-000094；目标修复必须保持任务编号、状态、类型、阶段、优先级、方案、授权、依赖、目标、输入、安全边界和完成定义不降级。
4. 固定允许变化完整覆盖任务-000094中的任务目标第3项、默认工程决策进程段、允许停止条件资源段、输出合同执行器段、安全边界进程段、验收标准第6项和验证命令，并在`当前阻塞原因`后新增唯一`解除条件`；每个旧/新全文片段均由main可信代码逐字固定并测试，禁止任意合同编辑。
5. 修复后另开目标合同PR执行映射；不得在本任务交付PR内提前修改任务-000094。
6. 全部测试串行，Node堆256MiB；双只读评审通过后自动合并，再独立闭环本任务。

## 默认工程决策

- 优先复用现有路径事实、严格字段白名单、完整旧/新段落常量和负向测试，不引入新依赖、服务、数据库或通用策略语言。
- `.c`资格同时绑定任务-000094和精确路径`scripts/审计/阶段1时间质量扫描器.c`，不允许其他任务、路径、`.cc`、`.cpp`、头文件、动态库、对象文件、生成二进制或仓库内可执行权限。
- 进程组资源事实采用可复现的保守计量，至少分别记录主进程峰值、子进程峰值以及两者之和；缺字段、非整数、负值或总和超过512MiB时失败关闭。
- 一次性修复入口完成后保留固定映射和测试作为历史可信规则；后续未知任务不得复用。
- 任务领取的一次性自举例外只解决任务-000094无法被现有状态闭环标记阻塞的死锁；任务-000095进入`main`后先于任务-000094执行，完成后立即恢复全局普通优先级和编号排序。

## 允许停止条件

- 需要扩大到任务-000094与精确路径`scripts/审计/阶段1时间质量扫描器.c`之外的任何任务、目录、原生源码或二进制交付。
- 需要修改任务-000094的数据规则、拒绝标准、研究范围、阶段门、风险门槛或完成结论。
- 需要访问服务器、数据库、真实市场接口、账户、凭据、生产环境或原始数据。
- 需要绕过双评审、资格检查、精确SHA或GitHub可合并状态。

普通测试夹具、段落指纹、文件组织和错误信息由Codex自动处理，不属于暂停条件。

## 输入合同

- 最新`main`的`AGENTS.md`、`README.md`、研发中心入口、任务规范、看板、总体计划和全部任务文件。
- `docs/治理/PR自动合并策略.md`、`.github/workflows/pr-auto-merge*.yml`、`scripts/研发中心/验证自动合并资格.py`、`scripts/研发中心/验证跨载体冲突.py`及相关测试。
- 任务-000094登记合同、PR #252两份独立评审结论和PR #253资格失败原因；只读取脱敏治理事实。

## 输出合同

- 更新`docs/治理/PR自动合并策略.md`和必要的研发中心入口文档，明确窄白名单、固定进程组与一次性修复映射。
- 更新`scripts/研发中心/验证自动合并资格.py`、`scripts/研发中心/验证跨载体冲突.py`及必要可信工作流，使main规则精确复验上述边界。
- 更新`tests/研发中心/`，覆盖仅任务-000094精确C路径、拒绝的其他任务/C/二进制路径、固定任务映射、全部七处精确合同替换、资源事实和所有负向边界。
- 本任务文件、看板、PR、双只读评审证据、验证结果、限制、安全影响和回滚记录。

## 工作范围

- 研发中心自动合并可信控制面及其测试和治理文档。
- 任务-000094一次性合同修复入口的规则定义与测试。

## 不在范围

- 不在本任务交付PR中修改任务-000094或重新执行其数据审计。
- 不访问或修改Ubuntu、数据库、订单簿、市场数据、生产服务、权限或账户。
- 不改变质量规则、科学计数法裁决、BTC/ETH分母、阶段1八道门或阶段2禁止条件。
- 不实现模型、回测、模拟交易或真实交易。

## 安全边界

- 可信控制面只从`main`执行；PR头代码只能作为不可信文本和Git对象检查。
- 新白名单只接受`100644 blob` UTF-8源码，拒绝删除、重命名、符号链接、子模块、可执行位、NUL、控制字符和敏感信息。
- 固定编译器和解压器不授予任意命令、网络、服务器、数据库、生产或交易能力。
- 一次性任务修复必须逐字匹配固定旧合同并产生固定新合同；任何额外变化失败关闭。

## 验收标准

1. 只有任务-000094的精确`.c`热路径可通过路径资格，其他任务、目录或扩展名的原生源码、对象和二进制继续被拒绝。
2. 固定进程拓扑、串行成员、整个进程组资源事实和512MiB门在文档、可信校验与测试中一致。
3. 任务-000095→任务-000094映射只允许固定完整合同修复，覆盖任务目标、默认决策、停止条件、输出、安全、验收、验证和解除条件，目标状态保持`待执行`且看板不漂移。
4. 未知执行任务/目标、部分替换、多字段夹带、研究/安全边界变化和生产路径均失败关闭。
5. 研发中心全部测试、Python编译、MarkdownLint、敏感信息扫描和`git diff --check`通过。
6. PR经两个独立只读评审、main可信复验和精确头SHA自动合并；随后独立状态闭环将本任务标记为`已完成`。

## 验证命令

```bash
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py scripts/研发中心/验证跨载体冲突.py tests/研发中心/test_验证自动合并资格.py tests/研发中心/test_验证跨载体冲突.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

窄`.c`白名单、固定进程组资源合同和任务-000095→任务-000094一次性修复入口均由main可信规则实现并有正反测试；双评审、可信复验和自动合并完成，独立状态闭环将本任务标记`已完成`。之后才能运行目标合同修复PR并恢复任务-000094，不能把本治理任务本身解释为数据审计通过。

## 数据与安全影响

仅修改仓库治理控制面、测试和文档，不读取或修改真实数据，不访问Ubuntu、数据库、订单簿、账户、凭据或生产系统，不生成交易结论。

## 已知限制

本任务只修复任务-000094已知死锁，不建立通用原生代码白名单、通用领取例外或任意任务合同编辑入口。任务-000094仍须用最终代码重跑全量批次并重新评审；旧PR #252和旧批次不构成完成证据。

## 回滚方式

回滚本任务合并提交即可恢复既有治理规则；若任务-000094合同修复已另行合并，还须按相反固定合同修复或普通revert恢复，禁止改写Git历史。无数据、服务器或生产回滚动作。

## 执行记录

- 交付：精确C路径白名单、任务-000094八段合同修复、固定三进程资源门和一次性领取规则。
- 验证：研发中心208项测试通过（含任务-000095→000094真实Git基线/头路由及资源证据最终头绑定）；Python编译、MarkdownLint 234个文件和`git diff --check`通过。
- 安全：未访问服务器、数据库、真实数据、账户或交易系统，未扩大到其他任务、路径或原生代码。
