# 任务-000022：历史重放确定性与快照合同修复

- 状态：执行中
- 类型：架构修复 / 数据验证
- 阶段：阶段2 数据真实性验证阶段
- 优先级：P0
- 来源：任务-000005架构评审
- 依赖任务：任务-000005（历史重放验证）
- 阻塞条件：任务-000005重新评审通过前，不进入基准模型阶段
- 执行分支：`codex/000005-historical-replay-v1`
- 开始时间：2026-07-31 03:17:22 +0800
- 执行方案：方案A“内容寻址的不可变重放快照合同”
- 方案状态：已批准执行
- 执行授权：Codex直接执行，不得再次要求用户选择方案
- Pull Request策略：继续使用任务-000005的PR #26；两项任务修改同一验证器、测试和
  报告，拆分为堆叠PR会产生重叠交付与合并顺序风险

## 目标

修复历史重放验证中的确定性与快照合同缺口，使系统能够证明指定历史时间点可基于冻结输入集合进行一致性重放。

## 工作范围

包含：

- Replay Snapshot Contract设计；
- 输入数据版本与hash绑定；
- 重放输入集合冻结；
- 输出确定性验证；
- 无法判定原因分类；
- 历史重放报告增强。

## 不在范围

不包含：

- 修改原始数据；
- 补采数据；
- 模型训练；
- 回测系统；
- 真实交易执行。

## 输入合同

输入：

- 历史时间点；
- 数据版本；
- 数据hash；
- 输入资产集合；
- 可用字段状态。

## 输出合同

输出：

- Replay Snapshot记录；
- 重放结果hash；
- 一致性验证结果；
- 不可重放原因分类；
- 修复建议。

## 验收标准

- 支持固定快照重放；
- 同输入重复运行结果一致；
- 无法判定结果具有明确原因分类；
- 不引入未来数据；
- 与知识版本合同一致。

## 完成定义

- 提交PR；
- 架构评审通过；
- 合并后更新任务状态。
