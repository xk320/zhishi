# 任务-000087：修复阻塞合同修复目标路径解析

<!-- markdownlint-disable MD013 -->

- 状态：已完成
- 合并时间：2026-08-10 14:06:16 +0000
- 合并提交SHA：`e6aca85433e34506e4e9cbb65032093fbc29a209`
- 类型：治理
- 阶段：阶段1研发自动化治理与数据证据修复
- 优先级：P0
- 执行方案：方案A“受控目标任务加载与单一执行任务引用解析”
- 方案状态：已批准执行
- 执行授权：Codex直接执行；修复可信资格入口的任务文件加载与回归测试，不访问Ubuntu、不读取数据库、不修改任务-000084合同
- 自动合并范围：治理自动化
- 并行规则：禁止并行；必须在任务-000086已完成后执行；不得与任务-000084合同修复、来源身份、数据质量、重放、成本或阶段门禁任务并行
- 执行分支：`codex/task-000087-root-contract-path-exec-v1`
- 开始时间：`2026-08-10T21:54:24+08:00`
- 实现提交SHA：`e7526fd1e068b3335490734b8470c4b6c8b57090`
- Pull Request：[#230](https://github.com/xk320/zhishi/pull/230)

## 依赖与阻塞条件

- 唯一前序依赖：任务-000086已完成。
- 当前阻塞原因：`阻塞任务合同修复`的可信入口把目标任务文件路径追加进执行任务引用集合，导致合法的任务-000086→任务-000084单目标修复被错误判为多任务引用。
- 解除条件：可信入口仅把PR正文中的已登记执行任务作为任务引用；按固定映射加载目标任务用于合同校验；未知执行任务、未知目标、额外任务路径、状态迁移和越权路径仍失败关闭。

## 背景

任务-000086已建立任务-000086→任务-000084的root只读兼容合同修复映射。实际合同修复PR必须只修改任务-000084合同段落，但当前CLI从变更路径推导任务编号，会把任务-000084加入执行任务集合，触发映射长度硬门。该任务只修复可信入口的确定性解析，不放宽合同内容、状态、路径或安全边界。

## 任务目标

1. 固定`阻塞任务合同修复`的执行任务引用只来自PR正文。
2. 对已登记映射按执行任务加载目标任务正文，供合同、状态和段落指纹校验，但不把目标任务当作第二个执行任务。
3. 保持任务-000056→任务-000055与任务-000086→任务-000084两条映射的越权失败行为。
4. 增加CLI集成回归测试，覆盖合法root目标路径、未知目标、额外任务文件和错误执行任务编号。

## 固定执行方案

1. 从最新`main`读取任务规范、自动合并策略、任务-000086合同、任务-000084合同、可信资格校验器和现有测试。
2. 在可信资格CLI中区分“执行任务引用集合”和“受控校验所需目标任务加载集合”；只对受控阻塞合同修复映射追加目标任务加载。
3. 不改变任务文件合同、看板派生规则、root兼容段落内容、自动合并类型或跨载体安全门。
4. 运行治理专项、研发中心全量、Python编译、MarkdownLint和`git diff --check`。

## 默认工程决策

- 目标任务加载只由固定映射推导，不信任PR正文、Issue、评论、文件名之外的目标声明或聊天内容。
- 任务-000086→任务-000084仍要求执行任务已完成、目标任务保持阻塞、目标仅追加可信固定段落；本任务不得修改目标合同。
- 测试单进程，最多两个只读评审者，Node堆上限256MiB，不创建额外工作树。

## 允许停止条件

- 需要修改任务-000084、任务-000086或其他任务合同正文。
- 需要放宽状态、映射、路径、敏感信息、生产、数据库或远端访问边界。
- 可信基线、头SHA、评审证据或跨载体规则无法复算。

## 输入合同

- 最新`main`上的AGENTS、任务规范、自动合并策略、任务-000086合同、任务-000084合同和看板。
- `scripts/研发中心/验证自动合并资格.py`、`scripts/研发中心/验证跨载体冲突.py`及研发中心测试。
- 仅使用仓库文本、Git对象和合成夹具，不访问Ubuntu、数据库、Binance或真实市场数据。

## 输出合同

- 可信资格CLI的受控任务加载修复。
- 任务-000056与任务-000086映射的正向、越权和路径漂移回归测试。
- 任务文件、看板、PR、验证命令、评审证据和回滚事实。

## 工作范围

- 修改可信资格CLI的任务引用解析和受控目标加载逻辑。
- 新增研发中心集成回归测试。
- 更新本任务执行记录和看板状态。

## 不在范围

- 不修改任务-000084或任务-000086合同、状态、执行记录、看板语义或root段落。
- 不访问或修改Ubuntu、数据库、原始数据、订单簿、生产服务、权限、防火墙或账户。
- 不实现远端探针、数据读取、模型、回测、交易信号、仓位或订单。

## 安全边界

- 只处理本地Git树和合成测试夹具；任何未知路径、任务、映射、状态或合同漂移失败关闭。
- 不记录IP、用户名、密钥、密码、令牌、环境变量、原始业务数据或远端输出。
- 目标任务只读加载不改变任务引用身份，不能绕过任务文件唯一事实源和双评审自动合并门。

## 验收标准

1. 合法root合同修复PR正文只引用任务-000086、只修改任务-000084时，可信资格校验通过。
2. 未知执行任务、错误目标、额外任务文件、额外治理/看板路径、状态迁移和段落漂移均失败关闭。
3. 任务-000056既有映射行为不回归；普通交付、状态闭环、任务登记和任务合同冲突修复行为不改变。
4. 治理专项、研发中心全量、Python编译、MarkdownLint、敏感扫描和`git diff --check`通过。
5. 任务-000084仍为阻塞；本任务完成不代表root合同已写入或来源身份证据已补齐。

## 验证命令

```bash
python3 -m unittest tests/研发中心/test_验证自动合并资格.py -q
python3 -m unittest discover -s tests/研发中心 -p 'test_*.py' -q
python3 -m py_compile scripts/研发中心/验证自动合并资格.py tests/研发中心/test_验证自动合并资格.py
NODE_OPTIONS=--max-old-space-size=256 npx --yes --offline markdownlint-cli2 AGENTS.md README.md '《知势宣言》.md' 'docs/**/*.md'
git diff --check
```

## 完成定义

可信资格入口、映射加载逻辑、回归测试和治理记录已在独立PR中交付，经双只读评审、main可信复验和自动合并；任务-000084仍保持阻塞，后续合同修复必须独立PR。

## 数据与安全影响

仅修改本地治理脚本、测试和任务记录；不访问Ubuntu、数据库、真实市场数据、凭据、原始数据、生产服务或订单簿逻辑。

## 已知限制

本任务只修复可信入口的任务引用解析，不生成来源身份声明，不证明root只读，不解除`ZS-DATA-GAP-001`或阶段1门禁。

## 回滚方式

回滚本任务PR即可恢复原解析逻辑；不修改任务-000084合同、Ubuntu、数据库、原始数据、权限或历史批次。

## 执行记录

- 执行分支：`codex/task-000087-root-contract-path-exec-v1`
- 开始时间：`2026-08-10T21:54:24+08:00`
- 尝试命令：`python3 -m unittest tests/研发中心/test_验证自动合并资格.py -q`；`python3 -m unittest tests/研发中心/test_验证跨载体冲突.py -q`；`python3 -m py_compile scripts/研发中心/验证自动合并资格.py`；MarkdownLint；`git diff --check`
- 结果：资格专项99项、跨载体23项通过；Python编译、MarkdownLint和差异检查通过。
- 外部证据：无；本任务未访问Ubuntu、数据库、Binance或真实市场数据。
- 阻塞原因：无；交付等待双只读评审和main可信复验。
- 解除条件：PR #230经双只读评审、main可信复验并合并后，独立状态闭环标记任务已完成。
- 数据与安全：仅修改可信资格脚本与研发中心测试；未修改任务-000084/000086合同、服务器、数据库、权限、原始数据、订单簿或生产服务。
