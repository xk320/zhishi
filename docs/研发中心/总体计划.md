# 《知势》研发总体计划

## 一、阶段结构

```text
阶段0：已完成
    ↓
阶段0.5：核心理论合同已完成
    ↓
阶段1：数据闭环证据修复已完成，最终审计中
    ↓
阶段2：被阶段1证据门阻塞
    ↓
阶段3至阶段7：仅完成部分理论合同，运行能力未解锁
```

## 二、当前推进策略

当前外部事实以任务-000031最新脱敏执行记录为准；任务-000037不访问服务器，只复核仓库内
不可变证据。任务-000029至任务-000036已完成交付，但任务-000037最终审计的8个BTC/ETH
逐尺度叶子全部阻塞，允许预测性研究范围为空，阶段2继续禁止。任务完成不等于阶段门通过。

任务-000028已经完成。后续固定依赖链为：任务-000038先建立自动评审、修复、
合并和状态闭环，再按任务-000029至任务-000037串行推进：

1. 建立可信、低资源的自动评审与精确SHA合并治理；
2. 补齐来源身份、三类时间和数据对象质量合同；
3. 完成不可变输入和完整只读质量审计；
4. 建立可信重放来源、成本执行数据和双标的独立闭环；
5. 完成最小数据闭环试点、容量试采和隔离恢复；
6. 生成新最终审计批次并裁决阶段2门禁；当前批次为`final-20260805T061500Z-v1`，结论为阻塞。

## 三、硬门槛

在任务-000037最终审计明确放行前：

- 不训练复杂模型；
- 不报告正式胜率或收益；
- 不开展真实资金交易；
- 不使用无法证明时间对齐的数据做正式事件驱动回测；
- BTC与ETH必须分别形成证据，不得互相外推。

任务-000006只提供历史最小闭环基线；任务-000029至任务-000036负责形成新证据，只有
任务-000037有权根据同一精确范围的完整证据裁决阶段2门禁。

## 四、任务创建原则

新任务必须：

1. 对应一个明确的决策问题或工程问题；
2. 有输入合同、输出合同和安全边界；
3. 有可观察、可复现的验收标准；
4. 标明依赖、优先级和阻塞条件；
5. 能说明它减少哪类错误交易、提高哪类净期望或增强哪项风险约束；
6. 不与《知势宣言》《知势决策标准》和《研究准入规范》冲突。

## 五、阶段评审原则

阶段不是因为任务数量完成而通过，而是因为核心能力被证据证明。任何阶段结论均允许为“不通过”“部分通过”或“需补充证据”。

## 六、阶段2研发平台增强

进入阶段2正式开发后，评估并实施：

- GitHub Pro或适合的组织级付费方案；
- `main`分支保护或规则集；
- 必需架构评审、CODEOWNERS和必需状态检查；
- 原生Auto Merge；
- 完整Quality Gate；
- 合并后任务状态的端到端自动同步。

阶段0.5使用`docs/治理/PR自动合并策略.md`定义的轻量替代流程。上述付费能力和完整
质量门不作为任务-000013验收条件，不阻塞当前研究与架构任务。
