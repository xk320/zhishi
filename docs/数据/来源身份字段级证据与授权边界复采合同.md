# 来源身份字段级证据与授权边界复采合同

<!-- markdownlint-disable MD013 -->

本合同对应任务-000076。它在任务-000075当前输入指纹和成员批次上追加字段级证据复采，目标是区分“结构可观察”与“来源身份、字段语义和授权边界可证明”。它不覆盖历史合同或批次，不把未知改写为通过。

## 输入与版本

- 成员输入：任务-000075批次`source-identity-input-drift-20260807T173144+0800-592622eddd8e`，成员630（BTC 315、ETH 315）。
- 配置：`config/数据/来源身份字段级证据复采.json`，合同版本`source-identity-field-evidence-recapture-1.0`。
- 执行器：`scripts/数据/复采来源身份字段级证据.py`，探针版本`source-identity-field-evidence-probe-1.0`。
- 主研究尺度固定为4小时、8小时、24小时、48小时；15分钟和1小时仅为事后结果观察窗口。

## 字段级证据合同

每个成员必须绑定以下字段：来源提供者、交易场所、市场类型、标的身份、精确合约、数据对象、Schema确切版本、授权边界和字段中文映射。字段映射、Schema摘要、授权快照、任务-000075成员指纹、输入指纹、规则指纹和执行器指纹必须可复算；成员按标的、资产编号和成员编号确定性排序，BTC与ETH不可互相补偿。

`已证明`要求所有身份字段、结构证据和授权边界同时完整且指纹一致。任一缺失为`无法判定`，输入漂移、越权或显式冲突为`拒绝`或`失败`。候选总体、已观察、拒绝、无法判定、失败、未成熟和失效均须保留并守恒。

## 只读范围

白名单仅允许逻辑别名`ubuntu`。数据库元数据范围登记为`information_schema.TABLES`、`information_schema.COLUMNS`和`information_schema.TABLE_PRIVILEGES`；候选文件只读取白名单`stat`。本批次不读取价格、成交、订单簿、持仓量、资金费率、账户信息或其他业务正文，不读取环境变量和凭据，不向远端写入或落盘。

## 不可变批次

真实批次：`source-identity-field-evidence-20260807T182805+0800-2fca470a6756`。

结果为候选资产315、身份成员630；已证明0、拒绝12、无法判定618，BTC和ETH各已证明0、拒绝6、无法判定309。字段级证据和授权快照均为未取得，未发布任何身份通过范围。CSV与JSON载荷SHA-256记录在批次清单中，目录追加发布且未覆盖任务-000075批次。

## 结论边界

本合同不证明数据质量、三类时间、历史覆盖、重放、成本、研究准入、预测优势、胜率、收益或交易许可；`ZS-DATA-GAP-001`仍阻塞，阶段1和阶段2不放行。后续任务必须继续提供当前版本的字段语义与授权边界证据，不能以本批次的结构描述性差异推导因果或交易结论。
