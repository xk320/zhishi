# 《知势》数据质量复验报告

<!-- markdownlint-disable MD013 -->

本报告对应任务-000031本次从最新 `main` 重新冻结并执行的首个真实只读验证批次。报告只记录
可由批次产物复算的聚合事实，不把“无法判定”改写为通过，也不替代原始数据或业务合同。

## 批次身份

- 验证批次：`dqv-20260805T002810+0800-e85f8d999b07`
- 底层审计批次：`audit-20260805T002810+0800`
- 冻结时间：`2026-08-05T00:28:10.623416+08:00`
- 资产清单指纹：`019010d64fe47d89c81bfaedafd458f1d886025bb34292c0c32eafcfa4392657`
- 方案指纹：`bab8e512b49187b1139e878699c43699ba9e997c868cac9b5fbc3313d5849ca4`
- 规则版本与指纹：`dq-rules-1.0` / `bc8924c509b2f516cc2361c9395afa02ef3d2ddbccde19d8122bcc71c0d68187`
- Schema指纹：`fe4d5c085630a1b1320bdae57a6fab2e962a6e49c1c7af7b5356488f18d9cb20`
- 作用域指纹：`51875961fe7d2eb91af388dd5026287c445aca9dde136580991655cf768f19f5`
- 底层审计器指纹：`a81d6c5c1cf204a5bbc132c2db16309923e7847b4bbde43e8883b1d61f4a07b7`
- 远端逻辑目标：`ubuntu`；运行时预检：Python 3.10.12 / Linux-x86_64
- 比较状态：无前序批次；本次批次仅追加，不覆盖历史产物。

批次根目录为
`artifacts/审计/数据质量/dqv-20260805T002810+0800-e85f8d999b07/`，其中的
`验证清单.json`保存确定性成员顺序及失效记录，`批次索引.csv`保存追加式索引。

## 执行与计数

实际执行命令：

```bash
python3 scripts/审计/持续验证数据质量.py --inventory artifacts/审计/数据源清单.csv --plan config/审计/数据质量持续验证.json --ssh-target ubuntu --batch-root artifacts/审计/数据质量 --timeout 3600
```

| 状态 | 计数 |
| --- | ---: |
| 候选总体 | 315 |
| 通过 | 0 |
| 拒绝 | 3 |
| 无法判定 | 312 |
| 失败 | 0 |
| 未成熟 | 0 |
| 失效 | 0 |

另有218个文件完成结构与质量扫描，累计记录27,523,291条，结构空值或空文本2,805,365项，
量化规范记录重复152条；92个MySQL对象只读取元数据。3个动态文件因两阶段身份漂移拒绝形成
内容结论，2个敏感系统日志未读取正文。断档结果全部保持“无法判定”，未把未知填为零。

## 标的结论与边界

| 标的 | 结论 | 依据 |
| --- | --- | --- |
| BTC | 无法判定 | 候选映射未证明精确标的、交易场所、合约、三类时间、频率、重放和最小闭环证据 |
| ETH | 无法判定 | 候选映射未证明精确标的、交易场所、合约、三类时间、频率、重放和最小闭环证据 |

本批次不覆盖SOL。以上结果不构成因果、预测优势、胜率、收益、回测结论、研究准入或交易
许可；在任务-000005、任务-000006及后续阶段门完成前，正式回测、模型训练和真实资金交易均
保持禁止。15分钟和1小时不作为市场状态或交易许可尺度，主研究尺度仍限于4小时、8小时、
24小时和48小时。

## 安全影响与限制

- 通过白名单固定只读SSH目标执行；远端不落盘，文件与SQLite只读，MySQL仅元数据。
- 未修改服务器、数据库、生产服务、权限、防火墙、原始数据或账户；仓库产物不含原始记录、
  未脱敏样本、密钥或凭据。
- 结构缺失和重复是可复算的结构统计，不等同于业务必填违约或业务主键重复。
- 未定义业务异常阈值，未评价价格、数量、收益或盘口数值异常；MySQL业务记录未被扫描。
- 所有结论只对上述批次身份有效；输入、规则或合同变化必须新建批次，不得回写本批次。

## 可复验产物

- `验证报告.md`
- `验证清单.json`
- `数据质量结果.csv`
- `数据断档结果.csv`
- `数据异常结果.csv`
- `artifacts/审计/数据质量/批次索引.csv`

上述产物共享批次、规则和资产清单指纹，可由固定脚本和任务记录复算。
