# Ubuntu 数据证据增量复验报告

<!-- markdownlint-disable MD013 -->

- 报告版本：`1.0`
- 任务：任务-000060
- 验证批次：`dqv-20260805T120241+0800-74a72e689e15`
- 底层审计批次：`audit-20260805T120241+0800`
- 执行时间：`2026-08-05T12:02:41+08:00`
- 远端逻辑目标：`ubuntu`
- 执行方式：白名单、只读、远端无落盘；本地只保存脱敏聚合事实、元数据与指纹

## 结论

本批次执行成功，但没有解除任何研究或交易门槛。315个验证单元均未形成可用性证据；
主状态计数为：拒绝3、无法判定312、失败0、未成熟0、失效0、通过0。任务结果只能证明
本次只读审计完成并保留了可复算批次，不能证明数据可用于回测、模型、研究准入或交易许可。

## 批次身份与指纹

| 项目 | 值 |
| --- | --- |
| 合同版本 | `dq-continuous-1.1` |
| 规则版本 | `dq-rules-1.0` |
| 规则指纹 | `01938b5b3b9ea940a41c9dd9534ea84cbca061ea2baccd571cd1d940132ee6e8` |
| 资产清单指纹 | `019010d64fe47d89c81bfaedafd458f1d886025bb34292c0c32eafcfa4392657` |
| 结构指纹 | `c277924885ea573fb3aa22a766c8830ec8a4b9fdbdf91a8a6318099c0cde8f90` |
| 底层审计器指纹 | `0cb87624795f88a88415e3bf0ad2a7d753350fd007855d2fd680fda9727ab9d5` |
| 代码文件指纹 | `834036678696b199b5789aae190468f043262ece4a3c625775241b344eefb08d` |
| 批次目录 | `artifacts/审计/数据质量增量复验/dqv-20260805T120241+0800-74a72e689e15/` |

## 结果摘要

- 资产清单共315个验证单元；218个完成内容扫描，92个仅保留元数据，5个因输入漂移或未执行保守退出。
- 完成扫描记录数为81293526；结构空值或空文本为2805365；已量化的规范记录重复为152。
- 事件时间、到达时间、采集时间状态均为`无法判定`；没有正式频率合同，因此不计算断档结论。
- 所有验证单元的可用性结论和实际覆盖范围均为`无法判定`；不得把未知填充为零或跨资产补偿。
- 候选映射出现BTC或BTC、ETH字样时仍未证明精确标的、交易场所、合约和主研究尺度；BTC与ETH不互相外推。

## 研究尺度与范围边界

前向研究范围仍只允许BTC、ETH及4小时、8小时、24小时、48小时主研究尺度。15分钟和1小时
只能作为事后结果观察窗口；本批次没有足够的标的、合约、时间语义和频率合同，不能为任何
主研究尺度或观察窗口建立可用证据。SOL不进入前向研究。

## 安全与限制

- 未修改服务器、数据库、生产服务、权限、防火墙或原始数据；没有生成或记录凭据、密钥、令牌或密码。
- 产物只保存统计、指纹、失败/未知原因和验证清单，不保存原始记录、未脱敏样本或原始日志正文。
- `拒绝`和`无法判定`均按失败安全处理；不能解释为数据质量通过、预测优势、胜率、收益或交易许可。
- 历史任务-000031批次保持不变；本批次是追加的新目录和索引行，不覆盖历史证据。

## 可复现入口

批次文件、批次索引和本报告共同构成证据入口。复验命令及其结果记录在任务-000060执行记录和
同一目录的`验证清单.json`、`验证报告.md`中；任何输入指纹漂移、远端不可达或资源超限都必须
新建批次并保守标记为`阻塞`，不得沿用本批次结论。
