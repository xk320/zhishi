# 阶段1成本与执行证据报告

<!-- markdownlint-disable MD013 -->

- 报告版本：`stage1-cost-execution-report-1.0`
- 任务编号：任务-000100
- 上游任务：任务-000099
- 正式批次：`stage1-cost-execution-20260812T165500Z-7cd1992a98c0`
- 清单SHA-256：`7ed2fc957f37981e11d40bdccf68ec6d39a0a8225aa5a984d875efb321a8610b`
- 摘要SHA-256：`7ed4906c02c311455c9c022503ed60bb9a4993171d4c9a95c9494f839e180f65`

## 一、结论

任务-000100完成了成本与执行来源的低负载只读验证，但没有证明成本门通过。
32个标的、方向、入场/退出和4/8/24/48小时分组全部为`无法判定`。
原因已从泛化的“缺少成本数据”缩小为两个可执行问题：

1. 现有订单簿及派生数据不覆盖任务-000099的多年正式输入；
2. 尚无版本化模拟委托生命周期，不能证明发送、确认/成交、排队与撤单延迟。

因此成本与执行总门、阶段1和阶段2继续阻塞。本报告不覆盖被冻结的
`阶段1最终审计报告`，后者保持历史字节不变，避免破坏旧任务输入指纹。

## 二、数据库证据

- 只读登记5张候选表、62列和28条索引记录。
- 4张时间序列表具有标的与时间复合索引。
- 4张时间表各执行1个最早/最晚覆盖聚合，`EXPLAIN`全部为`range`。
- 合计估算扫描410行，实际返回276字节，重试0；前后元数据8条、业务4条，非`EXPLAIN` SQL共12条。
- `symbol_metadata`没有合格的标的与时间复合索引，因此未执行其业务查询。
- MySQL客户端禁用默认配置文件；计划、SQL、解释和结果形成逐级SHA绑定，执行前后远端身份、
  授权、Schema和索引不变。
- 原始盘口覆盖2026-08-09至2026-08-12。
- 特征桶覆盖2026-07-13至2026-08-12。
- 市场结构与公开上下文最早覆盖到2026-06-24。

以上边界对BTCUSDT和ETHUSDT分别保存，不跨标的补偿。

## 三、公开接口证据

通过Ubuntu、IPv4和正常TLS验证串行请求Binance官方公开接口。
`exchangeInfo`以及BTC/ETH的`depth`、`premiumIndex`和`fundingRate`
共7个请求全部以`HTTPS_REQUEST_FAILED_28`超时。

未关闭证书验证，未访问账户或签名端点，未使用密钥，也未把当前接口值回填为历史证据。

## 四、分母与状态

| 项目 | 计数 |
| --- | ---: |
| 候选总体 | 32 |
| 已观察 | 32 |
| 拒绝 | 0 |
| 无法判定 | 32 |
| 失败 | 0 |
| 未成熟 | 0 |
| 失效 | 0 |
| 通过 | 0 |

行情事件、到达和采集时间差没有被解释为执行延迟。数据库短期覆盖和API失败均作为
事实保留，不能推导因果、预测优势、胜率、收益、研究准入或交易许可。
32组分别保存手续费、价差、深度、冲击、资金费率、行情可见性延迟、可成交量和执行延迟的
原因代码与解除条件；BTC和ETH的资金费率观察状态独立裁决，不使用合并布尔值互相补偿。

## 五、剩余缺口与下一解除条件

| 缺口 | 当前证据 | 唯一解除条件 |
| --- | --- | --- |
| 多年历史成本覆盖 | 原始盘口约3天，最早派生表约49天 | 只读发现并冻结同一历史现场中的更长成本来源；缺失继续无法判定 |
| 模拟执行生命周期 | 没有发送、确认/成交、排队、撤单时间 | 建立版本化、可重放的模拟委托生命周期 |
| 研究容量 | 本批次只是低负载边界查询 | 在真实研究转换与查询负载下单进程试采 |
| 隔离恢复 | 本批次没有执行恢复 | 对不可变副本做隔离恢复并复核指纹和记录数 |

后续不得重新扫描854GB归档，不得用当前快照回填历史，不得把行情可见性延迟替代
执行延迟，也不得以BTC证据补偿ETH缺失。

## 六、资源与安全

- 正式采集耗时24.157秒，峰值RSS 25,165,824字节。
- 正式批次文件合计66,427字节，数据库响应共276字节。
- 正式目录只在完整语义回读和资源硬门通过后原子发布；没有遗留待发布目录。
- 没有保存价格或数量正文。
- 没有远端写入、数据库写入、权限修改或服务修改。
- 没有读取凭据、训练模型、回测或生成交易判断。

## 七、产物

正式证据位于：
`artifacts/数据/阶段1成本执行/stage1-cost-execution-20260812T165500Z-7cd1992a98c0/`。

七个早期追加批次保留为历史，不作为当前交付批次：

- `stage1-cost-execution-20260812T135611Z-2938b4b2736f`
- `stage1-cost-execution-20260812T140231Z-ab53ea5b9ab1`
- `stage1-cost-execution-20260812T140402Z-96e21fe4635a`
- `stage1-cost-execution-20260812T142654Z-f405e8a7c9d2`
- `stage1-cost-execution-20260812T143746Z-7c30e25b6a19`
- `stage1-cost-execution-20260812T160700Z-81f61b9fae06`
- `stage1-cost-execution-20260812T165000Z-7cd1992a98c0`
