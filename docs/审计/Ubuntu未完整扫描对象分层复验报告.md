# Ubuntu 未完整扫描对象分层复验报告

<!-- markdownlint-disable MD013 -->

本报告对应任务-000063。它把任务-000062中97个未形成完整扫描结论的对象按授权边界分层，
只对3个普通文件输入漂移对象执行白名单`stat`复验；数据库业务正文和敏感系统日志正文均未读取。
本报告不构成完整质量通过、研究准入、预测优势、胜率、收益或交易许可。

## 批次与输入绑定

| 项目 | 值 |
| --- | --- |
| 任务 | 任务-000063 |
| 前序质量批次 | `dqv-20260805T143246+0800-b1c06c04eda7` |
| 输出目录 | `artifacts/审计/数据质量增量复验/任务-000063范围绑定/` |
| 覆盖矩阵 | `覆盖矩阵.csv`，97个对象 |
| 对象编号顺序SHA-256 | `5ddfdb8f52028c264b4f05a99468e334d19ae04599e9d92b95632d49d4a10767` |
| 任务-000062清单指纹 | `019010d64fe47d89c81bfaedafd458f1d886025bb34292c0c32eafcfa4392657` |
| 作用域 | BTC、ETH；主研究尺度4小时/8小时/24小时/48小时；15分钟/1小时仅事后观察窗口 |

97个对象与前序315个候选验证单元按资产编号确定性绑定：3个输入漂移、92个数据库元数据
范围、2个敏感系统日志。未限定标的的对象没有被分配给BTC或ETH，不能跨标的补偿缺失。

## 分层结果

| 复验层 | 对象数 | 结果 | 正文读取 |
| --- | ---: | --- | --- |
| 普通文件输入漂移 | 3 | 拒绝3 | 否 |
| 数据库元数据范围 | 92 | 未执行94中的92个 | 否 |
| 敏感系统日志 | 2 | 未执行94中的2个 | 否 |
| 合计 | 97 | 拒绝3、未执行94 | 否 |

### 3个普通文件身份复验

复验仅运行固定逻辑别名`ubuntu`上的`stat`，没有打开SQLite或读取任何文件正文。

| 资产 | 冻结字节数 | 复验字节数 | 冻结最后修改时间 | 复验最后修改时间 | 裁决 |
| --- | ---: | ---: | --- | --- | --- |
| DS-000001 | 3,095,396,352 | 3,284,889,600 | 2026-07-28T19:40:13.438028+08:00 | 2026-08-05 15:05:01.683231478 +0800 | 拒绝：身份漂移 |
| DS-000080 | 1,114,583,040 | 1,114,583,040 | 2026-07-27T21:35:09.653243+08:00 | 2026-07-27 21:35:09.653242520 +0800 | 拒绝：精确时间指纹漂移 |
| DS-000194 | 225,280 | 237,568 | 2026-07-13T05:01:18.161310+08:00 | 2026-07-31 03:46:50.550672853 +0800 | 拒绝：身份漂移 |

这些对象必须重新发现并冻结与只读元数据一致的候选资产版本后，才可能进入下一次正文质量
合同；本任务没有用新版本替换旧版本，也没有覆盖前序批次。

### 92个数据库元数据对象与2个敏感系统日志

92个数据库对象仍仅有获批Schema元数据范围，2个敏感系统日志仍受正文禁止读取边界约束。
本任务将它们记录为未执行，不将未执行解释为零异常、完整扫描或质量通过。解除条件分别是：

- 数据库对象：新的独立业务正文只读授权、对象级范围、资源预算和脱敏输出合同；
- 敏感系统日志：新的独立授权、脱敏方案、资源预算和最小必要字段合同。

## 缺口与阶段影响

| 缺口 | 本次新增事实 | 裁决 |
| --- | --- | --- |
| ZS-DATA-GAP-003 对象级质量 | 3个身份复验继续拒绝；92个数据库对象和2个敏感日志没有正文级质量证据 | 仍阻塞 |
| ZS-DATA-GAP-005 完整扫描 | 97个对象均未形成新的完整正文扫描结论；3个只做`stat`，94个未执行 | 仍阻塞 |

任务-000063没有产生任何通过对象，也没有关闭阶段1硬门。阶段2、模型、正式回测、模拟交易
和真实交易继续禁止；主研究尺度不变，短窗口不升级为市场状态、事件探测或交易许可尺度。

## 资源与安全事实

- 3个`stat`对象复验约2.99秒；执行前本机磁盘可用约165 GiB。
- SSH目标仅为逻辑别名`ubuntu`，远端无写入、无临时文件；未访问白名单外路径。
- 未读取数据库业务正文、敏感系统日志正文或原始记录；未修改服务器、数据库、生产服务、
  权限、防火墙、原始数据、账户或历史批次。
- 输出仅包含状态、元数据、指纹、计数、脱敏原因和解除条件；未保存IP、用户名、密钥、令牌、
  密码、原始日志或业务正文。

## 结论

任务-000063形成了可复算的97对象分层覆盖矩阵和3个输入漂移的最新失败安全证据，但没有
关闭ZS-DATA-GAP-003或ZS-DATA-GAP-005。下一步只有在新的授权和证据合同满足时，才能继续
尝试正文级质量复验；不得因本次局部复验或任务完成数量变化而放行阶段2。
