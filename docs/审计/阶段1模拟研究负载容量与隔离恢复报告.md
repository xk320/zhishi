# 阶段1模拟研究负载容量与隔离恢复报告

<!-- markdownlint-disable MD013 -->

- 任务编号：任务-000104
- 正式批次：`stage1-simulated-load-recovery-20260812T204723Z-c315888f1fd7`
- 清单SHA-256：`fc66d363221bcd7bd9ef3dc7d0af7b310a233fae9d997f5bd7d3ce2ace44f578`
- 摘要SHA-256：`d966753305416d162b37399635c44be99edb7a3a67cdf7ab2f26230bea352455`
- 来源批次：`stage1-simulated-lifecycle-20260812T195103Z-f0f4ff04c8e8`

## 一、结论

任务-000104对任务-000103已经提交的最终脱敏批次完成1×、2×、4×单进程串行压力复验和一次文件级隔离恢复演练。三个档位每次都重新读取并验证同一批次，规范结果指纹均为`5318c7bd53bcc73ce8cf9b88054c4701db1bb17871647ff25bf7c9aa6c69d649`。正式成员分母始终为512，没有把重复处理次数解释为新增市场样本。

一次性故障副本中的`summary.json`被追加字节后确定性失败；未修改的隔离备份随后恢复出11个普通文件，逐文件字节数、SHA-256以及BTCUSDT/ETHUSDT各256个成员、6144条生命周期事件和1056个分组均与源批次一致。任务工作目录及临时根清理完成后才原子发布正式批次。

因此，当前512成员正式模拟研究负载的本地串行处理能力和仓库内证据文件级恢复已经形成可复算证据。多年历史容量、共享数据库或Ubuntu主机灾备、真实交易所延迟仍为`unknown`；本报告不形成研究准入、阶段2放行或交易许可。

## 二、输入身份

| 输入 | SHA-256 |
| --- | --- |
| 任务-000103清单 | `668b5affccbb1fe898205e26c944d8e65a13339741f9024cf2b846d98b537c24` |
| 任务-000103摘要 | `7ad51875c9081ac7b7b05cb720a704ba2f38406841c5964168ee11fd53f6fe54` |
| 任务-000103生命周期结果 | `867706b2394f29cec7ce6877436b92ff9a4b7b20694e9be9663afecdbfcb3ee0` |
| 任务-000104配置 | `a3bd62ef4a6bfdffd2f868fb6add5153a4d6beb882e931484e9f223575aa643f` |
| 任务-000104执行器 | `e1cfe043444e2f76811fba19ecc68866a45a6ff2b87f0cc67b12deedc68d8955` |

来源清单声明10个内容文件、4,519,200字节；加上清单本身，本次每次完整验证读取11个普通文件、4,520,611字节。所有文件拒绝符号链接、非普通文件、硬链接漂移、未声明文件和路径逃逸。

## 三、容量测量

| 档位 | 正式成员分母 | 处理成员次数 | 处理生命周期次数 | 处理字节 | 耗时秒 | 峰值RSS字节 | 结果 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 1× | 512 | 512 | 6,144 | 4,520,611 | 0.071742 | 38,928,384 | 通过 |
| 2× | 512 | 1,024 | 12,288 | 9,041,222 | 0.143079 | 42,713,088 | 通过 |
| 4× | 512 | 2,048 | 24,576 | 18,082,444 | 0.292298 | 47,595,520 | 通过 |

全批次累计耗时0.846338秒，正式发布前观测进程峰值RSS为50,855,936字节，低于256MiB硬门；可用内存比例26.38%，高于20%硬门；可用磁盘114,540,339,200字节，高于5GiB硬门。每次复验都复用任务-000103最终验证器的成员级状态机，以流式方式重建生命周期规范SHA、计数和分组，不只核对文件哈希，也不在内存中保留完整结果副本。吞吐只描述本机本次文件验证，不外推为服务器、数据库、多年历史或生产容量。

## 四、隔离恢复

1. 只把任务-000103清单精确声明的11个普通文件复制到随机任务目录，备份字节数4,520,611。
2. 在独立故障副本追加5个字节，文件清单验证按`FILE_DRIFT`失败。
3. 从未修改备份恢复11个文件，恢复字节数4,520,611；文件事实与源批次逐项一致。
4. 恢复批次重新验证BTCUSDT 256、ETHUSDT 256、生命周期事件6144、分组1056及重放结果指纹。
5. 清理时间为`2026-08-12T20:47:24.135544Z`；正式发布时间为`2026-08-12T20:47:24.143196Z`，清理早于发布且任务临时根无残留。

## 五、安全事实

- Ubuntu、SSH、数据库、Binance及其他外部网络访问：`false`；
- 市场数据新增读取、源批次写入、其他项目写入：`false`；
- 模型、回测、方向、仓位、订单和交易判断：`false`；
- 源批次、`/Volumes/data`现有数据、生产系统、权限和原始数据修改：`false`。

## 六、限制与后继

本批次证明当前提交的模拟研究工作负载可在固定资源内串行复验并从文件级隔离副本恢复。它不证明多年历史盘口和成本覆盖、真实交易所确认/成交延迟、共享数据库恢复、Ubuntu主机灾备或生产服务恢复。

任务-000104完成状态闭环后，后继只能登记阶段1最终审计任务。最终审计必须把本证据与任务-000094全量时间质量扫描、任务-000099重放、任务-000100成本执行和任务-000103模拟生命周期共同裁决；仍为未知的市场证据不得由本地容量结果替代。

## 七、复现

```bash
PYTHONHASHSEED=0 python3 scripts/审计/验证阶段1模拟负载容量恢复.py \
  --repo-root . \
  --batch <新的追加式批次编号>
PYTHONHASHSEED=0 python3 -m pytest -q tests/审计/test_阶段1模拟负载容量恢复.py
```
