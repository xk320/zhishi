# 《知势》数据缺口与补采清单

<!-- markdownlint-disable MD013 -->

> 本清单只记录证据缺口和候选责任任务，不授权采集、补采、修改服务器或执行研究。

- 清单版本：`data-gap-register-3.0`
- 依据：任务-000003至任务-000006、任务-000022、任务-000092至任务-000094
- 现行标的范围：BTC、ETH；历史范围退出项保留在旧版本和Git历史，不进入活动缺口
- 当前安全降级：任务-000094新正式输入的8个BTC/ETH逐尺度叶子全部阻塞；禁止进入简单基准模型阶段；预测性研究范围为空
- 最新阶段1审计：`stage1-time-quality-20260812T061600Z-6968246516ef`；候选5387、正式成员5180、来源拒绝207、质量已证明4789、质量拒绝391、执行失败0；来源身份、三类时间、质量和血缘通过，其他四门仍阻塞
- 路线边界：任务-000084旧630成员路线保持历史阻塞，但不再是当前阶段门禁；不得用旧成员补偿新正式输入缺口
- 状态说明：只有形成独立任务文件并进入`main`的事项才可执行

## 一、缺口优先级

- `P0`：阻断来源、时间、质量、重放、成本或闭环硬门。
- `P1`：硬门通过后仍需完成的容量、恢复或范围校准。
- `P2`：不影响当前禁止结论的可维护性增强。

## 二、当前新路线硬门

| 硬门 | 当前状态 | 最新证据 | 解除条件 |
| --- | --- | --- | --- |
| 来源身份 | 通过 | 5180个成员绑定Binance对象、CHECKSUM、内容SHA、Schema、授权与正式输入指纹 | 后继处理必须保持同一正式输入身份 |
| 三类时间 | 通过 | 5180个成员逐行事件时间、官方归档可见时间和本批次采集时间已绑定；本地文件时间未使用 | 后继历史研究必须遵守官方归档可见边界 |
| 质量 | 通过 | 16,121,999,478行全量审计；已证明4789、拒绝391、失败0；连续段均覆盖最长48小时尺度 | 后继研究只使用通过连续段且不得跨缺口 |
| 历史重放 | 阻塞 | 没有绑定新正式输入的历史决策现场 | 冻结决策记录、代码、配置和可见性并重放 |
| 成本与执行 | 阻塞 | 无同版本手续费、价差、深度、冲击、资金费率和延迟证据 | 建立成本执行数据闭环 |
| 血缘 | 通过 | 内容SHA可从任务-000092成员定位到任务-000093八叶子 | 后继转换继续内容寻址 |
| 容量 | 阻塞 | 本次逐行审计耗时3914.254秒、进程组保守峰值RSS 188,923,904字节；不等于研究流水线容量 | 在真实研究转换和查询负载下试采 |
| 恢复 | 阻塞 | 未执行新正式输入隔离恢复 | 完成副本、恢复、指纹和记录数复核 |

## 三、历史缺口登记与新路线映射

| 编号 | 优先级 | 缺口 | 证据 | 影响范围 | 当前降级 | 解除条件 | 候选责任任务 | 复验入口 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| ZS-DATA-GAP-001 | 已关闭（新路线） | 新正式输入的来源、市场、标的和合约身份已冻结 | 任务-000093采纳5180个任务-000092已证明成员 | 当前正式输入 | 来源身份门通过；旧630路线历史非门禁 | 后继保持正式输入指纹与内容寻址 | 任务-000093 | 复算正式输入和门1 |
| ZS-DATA-GAP-002 | 已关闭（归档研究） | 三类时间语义与官方归档可见边界已冻结 | 5180个成员事件、来源可见和采集时间完整 | 归档历史研究 | 禁止把归档可见时间解释为实时到达 | 后继维持任务-000094规则和正式输入指纹 | 任务-000094 | 时间门通过 |
| ZS-DATA-GAP-003 | 已关闭（连续段约束） | 业务键、顺序、重复、日期、数值和结构规则已逐行执行 | 已证明4789、质量拒绝391；175个连续段 | 质量、快照和重放 | 拒绝日和缺日切断连续段 | 后继只使用通过连续段且不得跨缺口 | 任务-000094 | 质量门通过 |
| ZS-DATA-GAP-004 | 历史非门禁 | 旧路线4个输入身份漂移 | `input_identity_drift`共4个 | 任务-000084旧成员 | 不进入新正式输入 | 历史证据保持不变；不迁移到新路线 | 历史输入身份漂移修复 | 仅历史重放 |
| ZS-DATA-GAP-005 | 历史非门禁 | 旧路线94个单元未完整扫描 | `input_scan_incomplete`共94个 | 任务-000084旧成员 | 不进入新正式输入 | 历史证据保持不变；新路线质量由任务-000093成员重新审计 | 历史只读审计 | 仅历史复核 |
| ZS-DATA-GAP-006 | P0 | 缺少真实历史决策记录 | `decision_record_missing`共217个 | 历史现场重放 | 不进入正式重放第二门 | 提供来源可证明、唯一编号、带时区和数据截止时间的不可变决策记录 | 历史决策记录合同 | 重跑门4 |
| ZS-DATA-GAP-007 | P0 | 缺少可信正式重放来源入口 | 当前正式构建器拒绝调用方自报证据 | 全部正式重放 | 只有`smoke-only`算法测试，不发布通过行 | 建立只读来源登记或加载器，绑定内容指纹并禁止调用方自报 | 可信重放来源加载器 | 任务-000022合同复验 |
| ZS-DATA-GAP-008 | P0 | 成本、流动性和执行证据未闭环 | 未形成手续费、滑点、资金费率、盘口深度和延迟的统一版本链 | 成本门、执行门和净期望 | 禁止成本后优势 | 按标的、市场、方向和尺度冻结成本与执行数据版本 | 成本与执行数据闭环 | 重跑门3、门5 |
| ZS-DATA-GAP-010 | 已关闭（质量门已裁决） | ETH已形成2243个独立正式成员 | 任务-000094逐行裁决ETH已证明2100、质量拒绝143，另有199个来源拒绝缺日 | ETH全部尺度 | 不跨标的补偿；所有拒绝和缺日切断连续段 | 后继只使用ETH通过连续段且不得跨缺口 | 任务-000094 | 时间与质量门通过 |
| ZS-DATA-GAP-011 | P0 | 最小闭环尚未部署验证 | 当前仅有`minimum-data-loop-1.0`架构 | 全部研究输入 | 禁止冻结正式研究快照 | 实现来源、原始段、标准化、质量、成本、血缘、快照和只读查询的最小试点 | 最小数据闭环试点 | 门5与门6 |
| ZS-DATA-GAP-012 | P1 | 容量参数未经真实试采校准 | 3、6、12个月结果均标记为规划假设 | 存储与恢复 | 不形成服务器容量结论 | 只读试采记录实际增长、压缩、索引、峰值、日志和副本开销 | 容量试采与校准 | 重跑容量评估器 |
| ZS-DATA-GAP-013 | P1 | 备份和恢复没有演练证据 | 现有清单没有容量、备份介质、恢复时间或恢复指纹 | 数据可恢复性 | 备份状态无法判定 | 在隔离目录完成副本、恢复、指纹、记录数和快照清单复核 | 备份恢复演练 | 门5恢复验收 |

## 三、补采与修复顺序

按硬门依赖顺序推进：

```text
来源与标的身份
  → 三类时间、频率、业务键和修订合同
  → 输入身份与完整质量审计
  → 真实决策记录和可信重放入口
  → 成本、流动性和执行数据
  → BTC、ETH独立闭环
  → 容量试采、备份和恢复
  → 新的最终审计与允许研究范围
```

后序工作可以为诊断提前收集只读证据，但不能将前序缺口标记为已解除。

## 四、补采合同最低字段

任何未来补采任务至少必须预先冻结：

```text
任务编号与获批唯一方案
来源版本、提供者、交易场所、市场、标的和合约
字段中文映射、类型、单位、精度和空值语义
事件、到达、采集时间、时区和数据截止时间
频率、延迟、迟到、补录、修订和撤销规则
业务键、排序、重复和异常规则
数据范围、样本范围和明确排除范围
原始段、标准化、质量、成本、血缘和快照版本
只读安全、资源预算、超时、失败与回滚方式
交付物、验证命令和解除条件
```

缺少上述字段的补采事项应保持阻塞，不能因服务器可访问而直接运行。

## 五、禁止的补采方式

- 通过文件名、目录、表名或字段相似度猜来源、标的和时间语义；
- 用文件修改时间替代到达时间；
- 把事后补采记录写入旧快照或旧决策现场；
- 覆盖、删除、重排或静默去重原始版本；
- 读取凭据、环境变量、完整配置或未授权业务正文；
- 用BTC或ETH数据补足另一标的，或用未限定数据补足任一标的；
- 为得到足够样本而降低质量、成本、重放或研究门槛；
- 用容量规划假设宣称磁盘、备份或恢复已经通过；
- 在没有任务文件的情况下执行本清单中的候选建议。

## 六、解除与关闭规则

1. 缺口只能由新的不可变证据关闭，不能仅凭评论、Issue、PR描述或口头说明。
2. 关闭记录必须引用任务编号、输入批次、输出版本、指纹和验收结果。
3. 部分解除必须精确限定标的、市场、字段、尺度和时间范围，未列范围继续阻塞。
4. 新数据、修订、规则或代码变化后重新打开受影响缺口并创建新验证批次。
5. 所有P0缺口关闭且六道门通过前，基准模型阶段保持禁止。

## 七、数据与安全影响

本清单的初始记录未执行采集、补采、服务器访问、数据库读取、数据迁移、模型训练、回测或交易；后续任务的实际动作以各自追加记录为准。它不包含网络地址、凭据、原始记录或未脱敏样本，也不构成任何生产操作授权。

## 八、任务-000072来源身份复采记录（追加）

- 批次：`source-identity-recapture-20260807T132937+0800-bc866b5c5cdb`
- 任务：`任务-000072`；合同版本：`source-identity-recapture-1.0`
- 覆盖范围：资产清单中的315个候选资产（223个候选数据文件、92个数据库元数据），BTC与ETH分别形成315个身份成员，共630个成员。
- 结果：已证明0；拒绝12个成员（资产`DS-000001`、`DS-000042`、`DS-000122`、`DS-000164`、`DS-000192`、`DS-000194`各覆盖BTC和ETH）；无法判定618个成员（BTC 309、ETH 309）。拒绝原因均为冻结文件元数据漂移；未将路径、表名或字段名解释为身份证明。
- 证据：批次目录保存输入、规则、执行器、任务、成员和结果内容指纹；探针仅执行白名单文件`stat`和获批`information_schema`元数据读取，远端写入和业务记录读取均为`false`。
- 缺口裁决：`ZS-DATA-GAP-001`未关闭，当前复采没有形成任何可解除的身份范围；上述6个资产的两类标的继续拒绝，其余618个成员继续无法判定。其他P0/P1缺口和阶段2门禁不受影响。

## 九、任务-000073三类时间与可见性复采记录（追加）

- 批次：`time-visibility-recapture-20260807T145215+0800-81d077ed7db5`
- 任务：`任务-000073`；合同版本：`time-visibility-recapture-1.0`
- 覆盖范围：任务-000072来源身份批次的630个成员（BTC 315、ETH 315），每个成员固定绑定4小时、8小时、24小时、48小时四个主研究尺度并独立输出统计；15分钟、1小时仅登记为事后结果观察窗口。
- 结果：候选总体630、分母630、已观察630、拒绝12、失败12、无法判定618、未成熟0、失效0；BTC和ETH分别为候选315、拒绝6、失败6、无法判定309；每个主研究尺度均为候选630、拒绝12、失败12、无法判定618。失败仅来自来源身份已拒绝成员；其余成员因未读取业务正文而保持无法判定。
- 时间结论：事件时间、到达时间、采集时间的值、时区、精度、数据截止点、时钟漂移、迟到、补录、撤销、修订、重复、断档和异常规则均未取得当前结构化证据；文件修改时间未被用作替代。
- 探针与安全：通过逻辑别名`ubuntu`执行固定版本可达性元数据探针；远端写入`false`，业务正文和数据库业务记录读取`false`，未读取凭据或环境变量，未修改原始数据、数据库、生产服务；未使用未来数据。
- 缺口裁决：`ZS-DATA-GAP-002`未关闭，也未形成部分解除范围；所有三类时间和可见性缺口继续阻塞，其他P0/P1缺口和阶段2门禁不受影响。

## 十、任务-000074阶段1最终审计增量复验记录（追加）

- 批次：`final-20260807T082700Z-v4`；任务：`任务-000074`；合同版本：`stage1-final-audit-1.0`。
- 输入：任务-000029至任务-000037历史批次，以及任务-000069至任务-000073的合并合同、追加批次和规则指纹；所有历史批次只读，未覆盖或改写。
- 结果：候选总体2520、已观察2520、拒绝56、无法判定2464、失败0、未成熟0、失效0；BTC与ETH分别为1260候选、28拒绝、1232无法判定；8个叶子均为`阻塞`，允许研究范围为空。
- 复验结论：`ZS-DATA-GAP-001`、`ZS-DATA-GAP-002`及质量、重放、成本、容量、恢复相关缺口仍未形成完整通过证据；阶段2继续禁止，不产生研究准入或交易许可。
- 安全事实：仅读取仓库内证据；未访问服务器、数据库业务正文或真实市场数据，未训练模型、回测、模拟交易、修改生产服务、原始数据、权限或账户。

## 十一、任务-000075来源身份输入指纹修复与复采记录（追加）

- 批次：`source-identity-input-drift-20260807T173144+0800-592622eddd8e`；任务：`任务-000075`；合同版本：`source-identity-input-drift-recapture-1.0`。
- 输入漂移：当前main与任务-000072执行前基线的6个输入中，3个`未漂移`（资产清单、来源身份合同说明、任务-000071结构合同），3个为`内容变化`（历史来源身份配置、阶段1最终审计报告、缺口清单）；未将差异解释为来源证明。
- 复采结果：候选资产315、身份成员630；已证明0、拒绝12、无法判定618；BTC和ETH分别为已证明0、拒绝6、无法判定309；计数守恒。
- 远端与安全：仅通过白名单逻辑别名`ubuntu`执行固定版本`information-schema`只读元数据探针；远端写入、数据库业务记录读取、环境变量/凭据读取均为`false`，未保存地址、用户名、凭据或原始业务记录。
- 缺口裁决：`ZS-DATA-GAP-001`仍未关闭，没有形成可解除的身份范围；输入重签不等于来源身份、质量、时间、重放、成本、研究准入或交易许可通过；阶段2继续禁止。

## 十二、任务-000076来源身份字段级证据与授权边界复采记录（追加）

- 批次：`source-identity-field-evidence-20260808T020108+0800-7634991794d9`；任务：`任务-000076`；合同版本：`source-identity-field-evidence-recapture-1.0`。
- 覆盖范围：任务-000075当前630个身份成员（BTC 315、ETH 315）及315个候选资产；字段白名单固定为来源提供者、交易场所、市场类型、标的身份、精确合约、数据对象、Schema版本、授权边界和中文字段映射。
- 结果：已证明0、拒绝12、无法判定618；BTC和ETH各已证明0、拒绝6、无法判定309；字段级证据和授权快照均未取得，计数守恒。
- 远端与安全：仅执行白名单逻辑别名`ubuntu`的固定只读stat/元数据探针；不读取价格、成交、订单簿、持仓量、资金费率、账户或其他业务正文，不写入远端或数据库，不读取凭据，不保存原始字段值。
- 缺口裁决：`ZS-DATA-GAP-001`仍未关闭，没有形成可解除的身份范围；本批次不改变三类时间、质量、重放、成本、容量、恢复、阶段1或阶段2结论。

## 十三、任务-000077数据库元数据身份与Schema证据复采记录（追加）

- 批次：`database-metadata-identity-evidence-20260808T030243+0800-c3cb2624d197`；任务：`任务-000077`；合同版本：`database-metadata-identity-evidence-recapture-1.0`。
- 覆盖范围：任务-000076权威批次中的92个数据库元数据资产、184个身份成员；BTC和ETH各92个候选资产、92个成员；固定查询范围为`information_schema.TABLES`和`information_schema.COLUMNS`。候选资产分母与身份成员分母分开记录，不以92替代184。
- 结果：元数据`已观察`184；最终身份状态`拒绝`0、`无法判定`184、`失败`0、`未成熟`0、`失效`0；BTC与ETH分别为已观察92、无法判定92。结构观察没有被升级为来源身份`已证明`。
- 证据：成员SHA-256为`ae892e5694d963a4714ff8b144b60e52879cf5fdeb8e0c73bc402d3dc7b24a06`，资产清单SHA-256为`89295c43108e7b75c45a4f17cbc233fa8e93f858664f61c5d90f714293b357e0`，输出清单SHA-256为`424fb75257929ff85451cd7458b71c84625d0a5cc88ef86da5363c1601718d1f`；任务合同快照、执行器、规则、探针和授权边界指纹见批次清单；逐成员清单保留原因代码和证据定位。
- 远端与安全：仅通过白名单逻辑别名`ubuntu`执行无交互SSH固定只读探针；无业务表正文、价格、成交、订单簿、账户、凭据或写入，无原始数据修改和远端临时文件。
- 缺口裁决：`ZS-DATA-GAP-001`仍未关闭；数据库结构元数据可观察不证明来源提供者、交易场所、市场类型、精确合约、三类时间、数据质量、因果、预测优势、胜率、收益、研究准入或交易许可；阶段1和阶段2结论不变。

## 十四、任务-000078阶段1最终审计重算与门禁裁决记录（追加）

- 批次：`final-recompute-20260808T042000Z-v3`；任务：`任务-000078`；仅使用仓库不可变证据，不访问服务器、数据库或真实市场数据；绑定最新main提交SHA和任务-000029至任务-000077祖先证据。
- 证据范围：任务-000075/000076各有BTC、ETH独立身份成员315+315，最新身份状态为已证明0、拒绝12、无法判定618；任务-000077有BTC、ETH独立元数据身份成员92+92，元数据已观察184但最终身份已证明0、无法判定184。资产候选分母和身份成员分母分开保存。
- 最终状态：八个BTC/ETH逐尺度叶子（4小时、8小时、24小时、48小时）候选2520、已观察2520、拒绝56、无法判定2464、失败0、未成熟0、失效0；BTC和ETH分别候选1260、拒绝28、无法判定1232；计数守恒，八叶子全部阻塞。
- 缺口裁决：`ZS-DATA-GAP-001`、`ZS-DATA-GAP-002`、`ZS-DATA-GAP-003`、`ZS-DATA-GAP-012`和`ZS-DATA-GAP-013`继续阻塞；新增批次未形成任何部分解除范围。阶段2继续禁止。
- 研究边界：15分钟和1小时仅为事后结果观察窗口，不得升级为市场状态、事件探测或交易许可尺度；不因结果可见后更换分类、筛选事件、修改分组、缩小分母或寻找最佳状态/事件/窗口。
- 安全事实：未修改原始数据、数据库、生产服务、权限、防火墙或账户，未读取凭据，未训练模型、回测、模拟交易或生成交易结论；历史批次追加式保留。

## 十五、任务-000079来源身份声明与当前证据补采记录（追加）

- 批次：`source-identity-declaration-20260808T045203+0800-81295e1128b0`；任务：`任务-000079`；合同版本：`source-identity-declaration-recapture-1.0`。
- 覆盖范围：任务-000076权威批次的630个BTC/ETH身份成员（各315）及315个候选资产；主研究尺度固定为4小时、8小时、24小时、48小时，15分钟和1小时仅登记为事后结果观察窗口。
- 声明与证据：当前结构化`身份声明`清单为空；九个身份字段逐成员固定为`未知`，证据定位为声明清单空集，未使用路径、文件名、服务名、表名或结构观察补齐身份。远端仅执行Ubuntu白名单文件stat/元数据摘要探针，不读取业务正文。
- 结果：候选成员总体630；已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0；BTC和ETH分别已证明0、拒绝6、无法判定309，计数守恒。数据库元数据成员没有因结构可观察而升级为已证明。
- 证据指纹：配置SHA-256=`ae60e5f28a78965e8c1028458f74f174dc55917d7ee8d502527f6878ac2fde9c`；成员SHA-256=`4b942ddc41f5ceaa2f64051b89adbb01b6e87f0b5884a69bf6f631def9c94abe`；探针SHA-256=`f226d6a2b0eb2dce021c972a46456db1ab3b0247443abeedcb5579486fb328bb`；规则SHA-256=`2d50c1344a835ceeb832796935182aeff80bb03cecc5465c8cf3aaccbd446acd`；执行器SHA-256=`86129966e4bc55fac18f2d7602a2309d4403f85861102128a56a80a5a0ccc1ea`；输出清单SHA-256=`3bcabe8fcda8600c47b9a502353ca85b56816782850b5b2dadd2e29eeee7eec6`。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞，没有形成可解除的身份范围；不改变其他缺口、阶段门或阶段2结论。描述性差异不推导因果、预测优势、胜率、收益、研究准入或交易许可。
- 安全事实：远端写入、远端临时文件、数据库业务记录读取、凭据/环境变量读取、原始业务记录落盘、价格/成交/订单簿读取和生产修改均为`false`；历史批次未覆盖。

## 十六、任务-000079最终配置绑定批次（追加）

- 由于任务交付元数据规范化后配置文件字节发生变化，前一批次仅保留为历史事实；本次追加批次重新绑定最终配置，未覆盖任何历史目录。
- 批次：`source-identity-declaration-20260808T050813+0800-b2487b6c41a5`；配置SHA-256=`25925ef8f47870540a3333c51fe109509d1892a094f19e7ed97ee9e9ac0bbbe7`；成员SHA-256=`4b942ddc41f5ceaa2f64051b89adbb01b6e87f0b5884a69bf6f631def9c94abe`；探针SHA-256=`8d8ab2d57ab6c113fa71e8d791e24dbf60f08a601843653e8a2f67b7925cef52`；规则SHA-256=`2d50c1344a835ceeb832796935182aeff80bb03cecc5465c8cf3aaccbd446acd`；执行器SHA-256=`86129966e4bc55fac18f2d7602a2309d4403f85861102128a56a80a5a0ccc1ea`；输出清单SHA-256=`91a93db881f0d1697acb50c988647b774251401537ce65dc6a2f246bc690f57d`。
- 结果与前批次一致：候选成员630；已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0；BTC和ETH各已证明0、拒绝6、无法判定309；计数守恒。
- 缺口裁决和安全边界不变：`ZS-DATA-GAP-001`继续阻塞；不读取业务正文、价格、成交、订单簿、账户或凭据，不写入远端、数据库或生产系统，不产生交易结论。

## 十七、任务-000079最终可复算批次（追加）

- 因上游冻结配置需要同步本次审计追加记录，任务-000079再次在最终输入字节上重算并发布批次`source-identity-declaration-20260808T051008+0800-7cf99a5ed6b9`；此前批次均保留为不可变历史，不作为最终交付引用。
- 本批次结果：候选成员630；已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0；BTC和ETH各已证明0、拒绝6、无法判定309；`ZS-DATA-GAP-001`继续阻塞。
- 远端只读安全边界、研究尺度和结论边界不变；没有业务正文、真实市场数据、凭据、数据库写入、原始数据修改、交易判断或阶段放行。

## 十八、任务-000079最终交付批次指纹（追加）

- 最终交付批次：`source-identity-declaration-20260808T051102+0800-012a1b77a556`；配置SHA-256=`2922313a2608eaa0c5d6539ce5e10fa03426b995c33458f3654fe150bf8d7d96`；成员SHA-256=`4b942ddc41f5ceaa2f64051b89adbb01b6e87f0b5884a69bf6f631def9c94abe`；探针SHA-256=`d6de4658f9e1d3602bed41461d9a705955f04958c995881805ca93f3f02aeec6`；规则SHA-256=`2d50c1344a835ceeb832796935182aeff80bb03cecc5465c8cf3aaccbd446acd`；执行器SHA-256=`86129966e4bc55fac18f2d7602a2309d4403f85861102128a56a80a5a0ccc1ea`；输出清单SHA-256=`13ccdfd5a55d3736f57c793270d61da9eef347eb2b12e815e7c3ac24eebb9ff6`。
- 结果：候选成员630；已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0；BTC和ETH各已证明0、拒绝6、无法判定309。缺口`ZS-DATA-GAP-001`继续阻塞。
- 边界：仅白名单Ubuntu无交互stat/脱敏摘要；不读取业务正文、价格、成交、订单簿、账户或凭据，不写入远端、数据库、原始数据或生产系统；主尺度4/8/24/48小时，15分钟/1小时仅事后观察窗口。

## 十九、任务-000079当前最终交付批次（追加）

- 当前最终交付批次：`source-identity-declaration-20260808T051806+0800-dd1e3d74dca4`；配置SHA-256=`b99035eb31437b822b0d8ed9b728e2fbb4b5cd8b8861010049ee43d74fc08647`；成员SHA-256=`4b942ddc41f5ceaa2f64051b89adbb01b6e87f0b5884a69bf6f631def9c94abe`；探针SHA-256=`d462e41c3401b9042fcf2fbf02a5979868e16346a26ebe77cb4fcd922b2a3b5a`；规则SHA-256=`2d50c1344a835ceeb832796935182aeff80bb03cecc5465c8cf3aaccbd446acd`；执行器SHA-256=`a23c4d44f41c2fc245b02db92b59200f4b443888bdb532b601a5934f5c808064`；输出清单SHA-256=`0370a9b9dd45a76dbcdbfc58a8576ab377e664d5d818eacb60690267fb1c7f44`。
- 候选成员630；已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0；BTC和ETH各已证明0、拒绝6、无法判定309；`ZS-DATA-GAP-001`仍阻塞。
- 仅执行Ubuntu白名单无交互stat/脱敏摘要，未读取业务正文、真实市场数据、凭据，未写入数据库或生产；主尺度4/8/24/48小时，15分钟/1小时仅事后观察窗口。

## 二十、任务-000080来源身份声明证据入口复核（追加）

- 批次：`source-identity-entry-review-20260808T055600+0800-v1`；任务：`任务-000080`；仅读取最新`main`中的任务-000079最终批次、两个声明配置和仓库合同，不访问服务器、数据库业务记录或真实市场数据。
- 输入指纹：任务-000079批次清单SHA-256=`892fb6346e9d33ebde6d5eac7d2ba66985d944e4833a050b01623859763f4858`；任务-000079成员清单SHA-256=`0370a9b9dd45a76dbcdbfc58a8576ab377e664d5d818eacb60690267fb1c7f44`；声明配置SHA-256分别为`a743cceffb9380a2d134448172d2eddea53c72399a5c4549abea546452e05f2d`和`b99035eb31437b822b0d8ed9b728e2fbb4b5cd8b8861010049ee43d74fc08647`；入口条数为0。
- 结果：候选成员630，BTC 315、ETH 315；两标的入口均为`未登记`各315；完整九字段已证明为0。既有最终状态仍为已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0，未缩小分母或跨标的补偿。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞；当前没有可验证的显式九字段声明入口。本批次只冻结缺口事实和解除条件，不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 安全边界：服务器访问、数据库业务记录读取、真实市场数据读取、远端写入、原始数据修改、生产系统修改和凭据读取均为`false`；主研究尺度仍为4小时、8小时、24小时、48小时，15分钟/1小时仅事后观察窗口。

## 二十一、任务-000080当前最终入口复核批次（追加）

- 当前批次：`source-identity-entry-review-20260808T055600+0800-v2`；任务-000080；任务-000079批次清单SHA-256=`892fb6346e9d33ebde6d5eac7d2ba66985d944e4833a050b01623859763f4858`，成员清单SHA-256=`0370a9b9dd45a76dbcdbfc58a8576ab377e664d5d818eacb60690267fb1c7f44`。
- 两个声明配置的SHA-256分别为`9b9da85489333983fa50baccdeeb88456e79471a7f0ae6ece6402e287492e3f9`和`b99035eb31437b822b0d8ed9b728e2fbb4b5cd8b8861010049ee43d74fc08647`；显式声明入口条数为0。
- BTC与ETH各315个成员入口状态均为`未登记`，完整九字段已证明为0；既有最终身份状态仍为已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0。
- `ZS-DATA-GAP-001`继续阻塞；本批次仅记录入口空集和解除条件，不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论；安全边界和4/8/24/48小时主尺度不变。

## 二十二、任务-000080最终入口复核批次（追加）

- 当前最终批次：`source-identity-entry-review-20260808T055600+0800-v9`；任务-000080；任务-000079批次清单SHA-256=`892fb6346e9d33ebde6d5eac7d2ba66985d944e4833a050b01623859763f4858`，成员清单SHA-256=`0370a9b9dd45a76dbcdbfc58a8576ab377e664d5d818eacb60690267fb1c7f44`。
- 两个显式声明配置的字节指纹、入口条数和逐成员状态均以该批次`批次清单.json`及`来源身份声明入口复核清单.csv`为准；显式声明入口条数为0。
- 输出清单SHA-256=`505e1ef245bb5c7220b80f13589055dea088d685747c35d3a2c601d5a9eb63ca`；工作区与Git对象字节一致。
- BTC与ETH各315个成员入口状态均为`未登记`，完整九字段已证明为0；既有最终身份状态仍为已证明0、拒绝12、无法判定618、失败0、未成熟0、失效0。
- `ZS-DATA-GAP-001`继续阻塞，八个研究叶子继续阻塞，阶段1和阶段2不放行；本批次只冻结入口空集与解除条件，不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 未访问服务器、数据库业务记录或真实市场数据，未读取凭据，未修改远端、数据库、原始数据或生产系统；主研究尺度仍为4小时、8小时、24小时、48小时，15分钟/1小时仅为事后结果观察窗口。

## 二十三、任务-000081来源身份声明九字段复验（追加）

- 批次：`source-identity-nine-fields-20260808T071102+0800-2bc7f1e0a4d9`；任务：`任务-000081`；输入为任务-000080最终入口复核批次及两个结构化声明配置。
- 输入指纹：任务-000080批次清单SHA-256=`8f2ed8d68b18c34d1c368504f396c18d4de3a64b4198f10326dcb8f13a5a8dbb`；成员清单SHA-256=`505e1ef245bb5c7220b80f13589055dea088d685747c35d3a2c601d5a9eb63ca`；任务-000079来源成员清单SHA-256=`0370a9b9dd45a76dbcdbfc58a8576ab377e664d5d818eacb60690267fb1c7f44`；声明配置SHA-256分别为`4090e8085a8db77d8622c00ac13e18f722410283a71a3bcf17e0139db5ed9d48`和`696e0e2636f0a6d82041a4be46ca2b20222564afd75a5d8a8c8961ca635fc676`；声明入口条数为0。
- 结果：候选成员630；BTC与ETH各315；九字段状态均为`无法判定`；已证明0；计数守恒。输出清单SHA-256=`decb649f04afa9e7905f78321ecc1f953fe7279c92ad00c6c8c9527f1c9a2609`；批次清单SHA-256=`6089c2d748cd9e894c31117417870eeac302ef67d270e35eb86361bc6c3b3664`；执行器SHA-256=`8210782522d0647ef1edc9f0b8348d01aae51b6b422cd678a18fd84d8ce2467f`。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞；当前没有可验证的完整九字段声明入口，八个研究叶子继续阻塞，阶段1和阶段2不放行。
- 安全事实：仅读取仓库内冻结批次和结构化配置；未访问服务器、数据库业务正文或真实市场数据，未读取凭据，未写入远端、数据库、原始数据或生产系统；主研究尺度和15分钟/1小时事后观察窗口边界不变。

## 三十一、任务-000094时间质量缺口关闭记录（当前）

- 批次`stage1-time-quality-20260812T061600Z-6968246516ef`完整覆盖5180个正式成员、16,121,999,478行和854,743,871,570字节；已证明4789、质量拒绝391、执行失败0，207个来源拒绝成员和2个观察项继续保留。
- `ZS-DATA-GAP-002`在公开归档历史研究范围内关闭：事件时间、官方ZIP与CHECKSUM较晚`LastModified`形成的保守来源可见时间、本批次实际采集时间均逐成员保存。本地文件时间未使用，归档可见时间不得解释为实时到达延迟。
- `ZS-DATA-GAP-003`按连续段约束关闭：390个成员因科学计数法计价数量不满足冻结普通十进制规则而拒绝，另1个ETH成员发生业务键和事件时间回退；175个通过连续段保留全部缺口且分别裁决。BTC、ETH及数据对象禁止互相补偿。
- 八个叶子的三类时间和质量门通过，但历史重放、成本与执行、容量和恢复仍无法判定；阶段1和阶段2继续不放行，不产生因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 固定三进程串行流水线耗时3914.254秒；Python主控、clang、unzip和C扫描器分别计量后保守求和的峰值RSS为188,923,904字节；源目录前后指纹完全一致；输出载荷指纹为`0755c71b7694ae1cf0d8063400c11e5a3a7edf0f7217281037690a0bb2b6334c`。未访问Ubuntu、数据库、生产系统、账户或凭据，未修改原始数据。

## 二十四、任务-000081最终输入快照复验（追加）

- 批次：`source-identity-nine-fields-20260808T073132+0800-v2`；任务：`任务-000081`；批次输入绑定独立的`config/数据/任务-000081来源身份声明输入快照.json`，审计台账不参与该批次输入指纹，避免自引用哈希循环。
- 结果：候选成员630；BTC与ETH各315；入口状态均为`未登记`315；可定位均为`不可定位`315；九字段均为`无法判定`315；已证明0。最终身份状态按标的均为已证明0、拒绝6、无法判定309、失败0、未成熟0、失效0；所有计数守恒。
- 输出清单SHA-256=`5f92830b720788d728a6a373b1b72f5c113e03c9b3f77a36bf9c3dbaad300df2`；规则与执行器指纹以批次清单为准；历史批次未覆盖。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞，八个BTC/ETH逐尺度叶子继续阻塞，阶段1和阶段2不放行；本批次不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 安全事实：未访问服务器、数据库业务记录或真实市场数据，未读取凭据，未修改数据库、原始数据、生产服务、权限或账户；主研究尺度仍为4小时、8小时、24小时、48小时，15分钟/1小时仅为事后结果观察窗口。

## 二十九、任务-000085 Binance合约元数据身份复验（追加）

- 批次：`binance-contract-identity-20260810T165500+0800-final-v2`；任务：`任务-000085`；执行分支：`codex/task-000085-binance-contract-identity-exec-v1`；合同版本：`binance-contract-identity-recheck-1.0`。
- Binance公开`exchangeInfo`的USDⓈ-M与Coin-M端点均返回HTTP 200，保留BTC/ETH相关合约元数据分别13条和6条；响应SHA-256分别为`5ffda7f0bb92acdc821023feacf4759d58b638773559ea298425cd4c29e954ed`、`fde72da6098348fb62eac07e4e378269cb80f63168f19d352b07892bf306d843`，响应Schema指纹分别为`06485172c68cff2e1c3eb98fe0f8fa6974322f40a10b1c4ed271215da7e5df81`、`3ed393e4038906899229f64f12c815ecf5f92400577484df45d98948351668c3`。
- Ubuntu探针在扫描前发现固定SSH入口UID为0而非合同要求的专用只读UID 1001，立即失败安全退出：未遍历根目录、未读取候选文件、候选文件数0、扫描文件数0、失败原因`REMOTE_IDENTITY_NOT_DEDICATED`，原因指纹为`992ce89895194e2b936ba0aeb4e35ef9ec2d12bb9997754fa37e02facbcf941c`；未执行远端追加写入。
- 结果摘要：候选总体630，BTC与ETH各315；已观察0、已证明0、拒绝0、无法判定630、失败0、未成熟0、失效0；计数守恒。`ZS-DATA-GAP-001`继续阻塞，未生成空的任务-000084来源身份声明证据文件。
- 输出指纹：批次清单=`a7e69becad5eefb39fa3f9bb0b7c6349c302879d8fbd229f3ab0feb823153997`；Binance接口摘要=`24c0bbb6cab33273e72732f167c4172b83862dc2c9f5a1cc9d6a373c73b312f1`；Ubuntu候选摘要=`fb4f68e4329bd3f792bda23b26cec7081f7d0bb1256fdf6bf8fcfb06ab4212df`；成员状态摘要=`ecc63587f34bc6af5ab279fda2f6a11a9e837aee05b978765cd726d10a840148`。任务合同SHA按固定正文口径，完整任务文件SHA及三份输出SHA均写入清单。
- 历史处理：批次`binance-contract-identity-20260810T155134+0800-704cfd551e7b`、`binance-contract-identity-20260810T161957+0800-53e9e481df67`和`binance-contract-identity-20260810T164509+0800-3c537f9e128a`及其文件保持不可变，并分别由对应撤销事实停止其身份声明用途；最终替代批次为本批次。此前失败安全批次仍按各自配置指纹保留，不作为当前身份声明输入。
- 安全事实：未使用API密钥，未读取价格、成交、订单簿、账户、凭据、数据库业务记录或未知业务列，未写入数据库、原始数据、权限、生产服务或订单簿逻辑；任务配置冻结九字段完整映射门禁、扫描覆盖失败安全和Binance固定域名白名单。

## 二十八、任务-000083候选声明表失败安全修复与复验（追加）

- 批次：`source-identity-table-readonly-20260810T124500+0800-1cc091d879cf`；合同版本：`source-identity-table-readonly-1.0`；执行分支：`codex/task-000083-source-identity-table-readonly-exec-v1`。
- 修复范围：统一探针和本地校验的20个逻辑字段；候选表字段别名、资产路径、Schema指纹、入口指纹和证据定位绑定到冻结白名单；远端元数据/候选列失败、超时、输出超限、NULL值和超过630行均失败安全，不再伪装为空集；查询增加确定性成员排序和溢出探测；重复完整声明按冲突处理。
- 结果摘要：候选总体630、分母630、已观察0（口径：存在可定位的严格候选声明行）；BTC与ETH各候选总体315、分母315、已观察0；两标的入口均为`未登记`315、可定位均为`不可定位`315；最终状态各为已证明0、拒绝6、无法判定309、失败0、未成熟0、失效0；所有计数守恒。
- 指纹：成员顺序=`bf5b5dc916c394deb0366b26f0ee81453e16b5b63f7fc48ba71aeee0e0e32c5e`；探针=`039d96b02348cb4cb41189ea1c35ed84614e282e5c012d55caa6ea9e75c16451`；配置=`4475a8a280e1bcbc0604722510bfabe3b542726d319ba15daf7fce59d5862e35`；规则=`7e635374df02a62001f6ad444aa35e4ea68dc016917e4128d43b147a10741710`；执行器=`192eb09c2716254f1cf431589561ab0f6d8a94ce706c6cada06a0a603b75abea`；任务合同执行时=`35ccf70fc3e0ea193339649644c20076b56c2711fd28f7d0d6a7a95662ab6422`；输出CSV=`6455f2915227fd903b01202f2a158f1e031936756777433286cfd843aab3446a`；候选表=`37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570`。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞，未形成可验证的完整当前九字段声明；八个研究叶子、阶段1和阶段2不放行。本批次不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 安全事实：仅在逻辑别名`ubuntu`上串行执行`information_schema`元数据及严格20列白名单复验；未执行GRANT、DDL、数据库写入或业务记录读取，未读取价格、成交、订单簿、账户、环境变量、凭据或其他业务正文，未修改原始数据、订单簿逻辑、数据库或生产系统；历史批次追加式保留。

## 二十七、任务-000083来源身份声明表只读复验（追加）

- 批次：`source-identity-table-readonly-20260810T111227+0800-1cc091d879cf`；合同版本：`source-identity-table-readonly-1.0`；执行分支：`codex/task-000083-source-identity-table-readonly-exec-v1`。
- 输入与规则指纹：成员顺序SHA-256=`bf5b5dc916c394deb0366b26f0ee81453e16b5b63f7fc48ba71aeee0e0e32c5e`；探针SHA-256=`039d96b02348cb4cb41189ea1c35ed84614e282e5c012d55caa6ea9e75c16451`；配置指纹见批次清单；规则SHA-256=`7e635374df02a62001f6ad444aa35e4ea68dc016917e4128d43b147a10741710`；执行器SHA-256=`17a636c96bc041fe15d75512bb3d9d85be1f8bc3b2c2fe4494e81eddbdf10928`；任务合同执行时SHA-256=`76975ccbd5f6d49a7fa3f0fbbc3a19f2de225a65faf10d5dcd9eb9eb121a6277`。
- 结果：只读探针发现候选`BASE TABLE`数量为0、候选声明行数量为0；候选成员总体630，BTC与ETH各315；两标的入口均为`未登记`315、可定位均为`不可定位`315；最终状态各为已证明0、拒绝6、无法判定309、失败0、未成熟0、失效0；计数守恒。
- 输出指纹：来源身份声明表复验清单SHA-256=`0a1d33b53c5b365e6dae5b2625e21dc7f33ae7b6074dcf73dd6a5aaa44af26b9`；候选表清单SHA-256=`37517e5f3dc66819f61f5a7bb8ace1921282415f10551d2defa5c3eb0985b570`；批次清单SHA-256=`f7fe17f6d4c6f2b7d291350f7d38f730620551976fa233dd49ac08b40f295929`。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞；未形成可验证的完整当前九字段声明，八个研究叶子和阶段1继续阻塞，阶段2不放行。
- 安全事实：仅在逻辑别名`ubuntu`上串行执行`information_schema`元数据及严格20列白名单复验；未执行GRANT、写入、DDL或权限变更，未读取价格、成交、订单簿、账户、环境变量、凭据或其他业务正文，未修改原始数据、订单簿逻辑、数据库或生产系统；主研究尺度4/8/24/48小时，15分钟/1小时仅为事后观察窗口。

## 二十六、任务-000082来源身份声明入口发现与冻结（追加）

- 批次：`source-identity-entry-discovery-20260810T055620+0800-4dd0d2794c55`；任务：`任务-000082`；合同版本：`source-identity-entry-discovery-1.0`。仅在固定白名单`ubuntu`上串行执行声明文件和`information_schema.TABLES/COLUMNS`注释摘要探针。
- 入口边界：声明文件名固定为`来源身份声明.json`、`source-identity.json`、`data-source-manifest.json`、`dataset-identity.json`；数据库注释只有带固定前缀`知势身份声明:`的显式九字段JSON才进入候选；单文件上限256KiB，未读取业务正文。
- 结果：候选成员总体630，BTC与ETH各315；入口候选总体0；两标的入口状态均为`未登记`315、可定位均为`不可定位`315；最终状态各为已证明0、拒绝6、无法判定309、失败0、未成熟0、失效0；计数守恒。`ZS-DATA-GAP-001`继续阻塞。
- 证据：成员顺序SHA-256=`bf5b5dc916c394deb0366b26f0ee81453e16b5b63f7fc48ba71aeee0e0e32c5e`；配置SHA-256=`21fe99ab5fd80d097fb1865a0bbb9ab1585a0483b1f634b4bb6a17179adb37d6`；探针SHA-256=`f4e28fbe3afcd7f7d0c9009157a6a810036971b3941afeec921862f856a7c8e3`；规则SHA-256=`6d8c063c04eb02cc5c403067fed389705afe13e3dc1e8139af0c6163635c8bd2`；执行器SHA-256=`92e2317ab33808587c45d85260f8470a02dafb044af8ae83dbdfff20668a40b0`；输出CSV=`f8b59f5f74f2fac00a06ea141c7a221aa6f7b4885b887b5983e008162d7e0c61`；批次清单=`fefc47552bd191b3905e3c39a1f21d11a81b1a2b83c7151a5b27f06ce5d7e3b2`。
- 安全与结论：远端写入、远端临时文件、数据库业务记录读取、环境变量/凭据读取、原始业务记录落盘、价格/成交/订单簿读取、原始数据和生产系统修改均为`false`。入口空集不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论；阶段1和阶段2不放行，历史批次未覆盖。

## 二十五、任务-000081最终批次（追加）

- 批次：`source-identity-nine-fields-20260808T074100+0800-v4`；任务：`任务-000081`；输入使用独立来源身份声明快照，审计台账不参与批次输入指纹。
- 结果：候选成员630；BTC与ETH各315；入口状态均为`未登记`315；可定位均为`不可定位`315；九字段均为`无法判定`315；已证明0。最终身份状态按标的均为已证明0、拒绝6、无法判定309、失败0、未成熟0、失效0；所有计数守恒。
- 输出清单SHA-256=`74a1034fdee473da1d4e750debf474a9b3f171c913ded93f514cf4f0648ba524`；规则与执行器指纹以批次清单为准；历史批次未覆盖。
- 缺口裁决：`ZS-DATA-GAP-001`继续阻塞，八个BTC/ETH逐尺度叶子继续阻塞，阶段1和阶段2不放行；本批次不产生来源身份、因果、预测优势、胜率、收益、研究准入或交易许可结论。
- 安全事实：未访问服务器、数据库业务记录或真实市场数据，未读取凭据，未修改数据库、原始数据、生产服务、权限或账户；主研究尺度仍为4小时、8小时、24小时、48小时，15分钟/1小时仅为事后结果观察窗口。
