# 远程只读身份隔离复验报告

<!-- markdownlint-disable MD013 -->

- 批次身份：`remote-ro-identity-20260806T081014Z-v1`
- 冻结时间：`2026-08-06T08:10:14Z`
- 批次类型：专用远程只读身份与固定入口验证
- 任务：任务-000069
- 结论：通过身份与入口隔离硬门；不代表任何数据叶子或阶段门禁通过

## 复验范围

本批次只验证专用账户的非零UID、补充组隔离、锁定密码、唯一公钥、root-owned固定wrapper、固定请求协议、SSH强制命令和失败安全。没有读取数据库业务正文、敏感日志正文、订单簿正文、原始市场数据或生产服务。

## 身份与权限事实

| 检查项 | 结果 |
| --- | --- |
| UID | `1001`，非零 |
| 主组GID | `1001` |
| 补充组数量 | `0` |
| 管理员组 | `false` |
| 密码登录 | 已锁定 |
| sudoers条目 | `false` |
| 唯一公钥指纹 | `SHA256:oq45bCRm3+qAuQr/CVmB6P27cq2u1Z+3f0vrzi8GvyI` |
| SSH选项指纹 | `4d85e1316cbba1a814554df7d2bc9d93ae07cf91c29e5bceb96487d8bd27bc0a` |

## 固定入口与反向验证

- wrapper版本：`zhishi-ro-identity-probe-1.0`
- wrapper内容指纹：`bce456b7bd7b8717d0142c350331361596ff1e89c0c5463e75a3320e96e20b35`
- 协议仅接受`zhishi-ro/1`、`identity`和空对象载荷。
- 未知字段、非空载荷和原始远程命令均返回拒绝；拒绝响应不回显输入。
- PTY请求被SSH服务拒绝；端口转发连接返回`administratively prohibited`。
- 正向探针退出码为`0`，响应中的远端写入、数据库业务正文读取和真实市场数据读取均为`false`。
- 保护性目录权限检查显示root专属目录不可读写，受保护系统路径不可写。

## 不可变指纹与资源

- 规则指纹：`f7eb2779e60f3b1ac2547be2110d95915bde8195dc65616d1d846952682b1fb2`
- 身份隔离资产清单指纹：`5fc8cb8c39dbbf0e5c948d2fe109b3ed4a1d4e814923c275fdc30e13f3cf2611`
- 响应与元数据保存在`artifacts/审计/远程只读身份/remote-ro-identity-20260806T081014Z-v1/批次元数据.json`，采用新批次目录写入，不覆盖历史批次。
- 资源测量：可用内存约`72%`、可用磁盘约`159.4 GiB`、单探针上限`30秒`、批次上限`600秒`、串行执行。

## 解除条件与后续边界

身份隔离批次通过后，才允许另一个独立任务按对象白名单申请数据证据复采。任何wrapper指纹漂移、未知请求被接受、权限扩大、敏感输出、超时或root读取业务数据，都必须失败关闭并保持阶段1阻塞。本批次不授予数据库权限，不修改订单簿逻辑，也不解除阶段2门禁。
