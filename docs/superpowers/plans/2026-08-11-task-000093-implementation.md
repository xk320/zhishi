# 任务-000093实施计划

> **执行要求：** 按测试先行的顺序逐项实现；每次只处理一个行为，先观察失败，再写最小实现并复验。

**目标：** 将任务-000092的5180个已证明Binance归档成员冻结为阶段1正式输入，保留207个拒绝成员和2个观察项，并基于同一输入为BTC/ETH四个主尺度重算八类硬门。

**架构：** 新执行器只读任务-000092仓库证据和固定本地ZIP。输入层负责严格复算批次与成员身份；观察层有界读取ZIP单一CSV的首末有效行，生成日期覆盖和事件时间证据；裁决层仅复用精确兼容的证据，按八叶子最低硬门输出阻塞或通过。所有产物为追加式、确定性JSON，不保存业务值。

**技术栈：** Python 3标准库、`unittest`、JSON、ZIP/CSV流式只读、Markdown。

---

## 任务一：固定配置与测试接口

**文件：**

- 新增：`config/审计/任务-000093阶段1新候选集重算.json`
- 新增：`tests/审计/test_重算阶段1新候选集门禁.py`

1. 写入配置解析、固定批次、白名单根、三组数据、八叶子、资源和输出限制的失败测试。
2. 运行专项测试，确认因执行器尚不存在而失败。
3. 建立最小模块入口和配置校验，令该组测试通过。

## 任务二：正式输入采纳与失败安全

**文件：**

- 修改：`tests/审计/test_重算阶段1新候选集门禁.py`
- 新增：`scripts/审计/重算阶段1新候选集门禁.py`

1. 先测试只采纳`已证明`成员、确定排序、拒绝207成员不漂白、观察项不进入正式分母。
2. 先测试重复键、未知组、路径越界、符号链接、文件大小或内容SHA漂移均失败安全。
3. 实现最小发现、验证、排序和状态守恒逻辑并令测试通过。

## 任务三：有界ZIP与事件时间观察

**文件：**

- 修改：`tests/审计/test_重算阶段1新候选集门禁.py`
- 修改：`scripts/审计/重算阶段1新候选集门禁.py`

1. 先测试普通单CSV ZIP、无表头逐笔/聚合成交Schema、首末事件时间和成员日期边界。
2. 先测试多成员ZIP、异常路径、空文件、字段数错误、时间解析错误和日期越界进入保守状态。
3. 实现有界首末行读取；输出仅含时间、结构和计数，不保存价格、数量、成交ID或业务行。

## 任务四：八叶子门禁裁决与追加式发布

**文件：**

- 修改：`tests/审计/test_重算阶段1新候选集门禁.py`
- 修改：`scripts/审计/重算阶段1新候选集门禁.py`

1. 先测试BTC/ETH与4/8/24/48小时八叶子完整、不得跨标的补偿、任一硬门未知即阻塞。
2. 先测试输出目录不可覆盖、单文件/总量限制、源清单前后指纹一致和确定性输出指纹。
3. 实现正式输入、覆盖、异常、叶子、摘要、清单和资源事实发布。

## 任务五：真实批次与阶段文档

**文件：**

- 新增：`docs/数据/阶段1新候选集采纳合同.md`
- 新增：`artifacts/审计/阶段1新候选集重算/<批次>/`
- 修改：`docs/审计/阶段1最终审计报告.md`
- 修改：`docs/审计/数据缺口与补采清单.md`
- 修改：`README.md`
- 修改：`docs/研发中心/总体计划.md`
- 修改：阶段1路线派生文档（以仓库实际入口为准）

1. 运行一次真实、单进程、只读批次；记录正式输入、源前后指纹、事件时间/覆盖、八叶子和资源事实。
2. 对照真实结果更新阶段文档，只关闭确实有证据通过的门。
3. 若八叶子仍阻塞，把旧630身份问题替换为新输入上的具体时间、质量、重放、成本、容量或恢复解除条件。

## 任务六：完整验证、评审与交付

1. 执行任务合同全部验证命令和敏感信息扫描。
2. 检查原始数据前后指纹、仓库产物大小、Git对象类型与改动范围。
3. 将任务和看板更新为待评审，记录精确提交、PR、批次、验证、限制和安全影响。
4. 创建PR，组织治理/架构与范围/安全双只读评审；P0/P1自动修复后重新验证。
5. 双评审和main可信工作流通过后自动合并，再用独立状态闭环PR标记任务已完成。
