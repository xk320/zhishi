# 任务-000059设计：修复看板模式回归夹具

## 目标

使看板模式的重复表头负向测试真正针对当前机器合同中的表头/分隔行，避免旧版列数夹具
在看板为空分区或模式演进后静默失效。

## 固定方案

方案A“从机器合同读取现行分隔行并保持失败关闭”。仅修正测试夹具；不放宽看板模式校验，
不改变任务状态机、研究范围、阶段门或自动合并安全边界。

## 变更边界

- 允许：更新研发中心回归测试，使重复模式行来自当前看板模式合同。
- 禁止：修改生产代码、任务状态规则、看板机器合同内容、服务器、数据库、原始数据、
  账户权限或交易逻辑。

## 验收依据

1. 当前看板模式测试全部通过；重复表头夹具确实改变输入并被校验器拒绝。
2. 任务中心、范围与外部状态一致性测试通过。
3. `py_compile`、Markdown 检查和 `git diff --check` 通过。
4. 不新增 SOL 前向范围，不改变 BTC/ETH、阶段2或交易许可结论。

## 回滚

回滚本任务交付提交即可恢复原测试夹具；不影响历史审计产物和生产系统。
