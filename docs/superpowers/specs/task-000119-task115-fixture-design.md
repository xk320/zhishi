# 任务-000119设计说明：任务115生命周期测试夹具解耦

## 决策

测试必须使用固定合成基线，不把工作树中任务115或任务106的当前状态误当作资格测试输入。

## 固定基线

- 任务115：保留合同正文，移除执行/交付元数据并合成`待执行`状态。
- 任务106：若当前文本已有任务115固定覆盖章节，则按完整章节指纹移除一次，得到未修订基线。
- 任务116：继续使用已完成前置事实，并保留状态迁移与合同漂移负向断言。

## 安全与范围

仅改测试夹具，不改变可信资格器、自动合并工作流、任务合同、看板事实、数据、服务器或交易边界。

## 验证

研发中心资格、工作流、看板和项目范围专项测试全部通过后，才允许任务115继续执行。
