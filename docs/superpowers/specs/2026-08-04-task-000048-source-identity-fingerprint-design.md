# 任务-000048设计：冻结来源身份输入指纹同步

## 决策

任务-000046已将远端探针修复进入`main`，发现器文件字节指纹发生变化。只更新
`config/数据/数据来源与资产身份.json`中用途为“既有数据资产发现器”的单项SHA-256，
新值由当前文件字节计算；其它输入、合同字段和治理基线保持不变。

## 验证与边界

- 数据身份合同加载必须恢复，数据测试45项全部通过；
- 其它冻结输入逐项比对，不能借机更新无关指纹；
- 不访问服务器、数据库或真实市场数据，不改写原始数据、历史批次、生产或凭据；
- 指纹漂移继续失败关闭，任何不确定值均停止而非猜测。
