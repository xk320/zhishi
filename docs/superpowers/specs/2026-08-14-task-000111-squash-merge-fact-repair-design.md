# 任务-000111：压缩合并事实可复算支持设计

## 目标

补齐可信自动合并资格校验器对已验证 GitHub 压缩合并事实的解析，使状态闭环能够绑定真实 merge commit、PR 号、提交时间和交付头，不把压缩合并误判为无法复算。

## 边界

- 只修改仓库内可信复算脚本、专项测试和任务治理文档；不访问服务器、数据库、网络业务数据、账户、凭据或生产服务。
- 仅接受Git对象可复算的压缩合并：目标提交必须存在于main、任务记录必须包含PR与merge SHA/时间、PR头必须可由GitHub元数据或已冻结路径事实绑定。
- 不放宽任务合同、评审、范围、安全、敏感信息或状态迁移规则；不支持伪造提交、聊天声明或Issue替代Git事实。

## 交付与验收

- 为压缩合并新增最小、确定性复算分支和负向测试。
- 任务-000110状态闭环可在修复后绑定PR #304真实压缩合并提交`f83f155...`，并保持原有双父合并验证不变。
- 专项测试、研发中心测试、MarkdownLint和`git diff --check`通过。
