# 任务-000120设计：任务115交付期看板合成测试夹具

## 设计结论

阶段1覆盖受限合同修订的正向测试必须同时表达“任务115基线待执行”和“任务115交付头部待评审”。真实看板在执行过程中会发生迁移，因此测试应在内存中移除真实任务115行并插入固定四列表待执行行，再由测试构造待评审头部行。

## 固定边界

- 只修改`tests/研发中心/test_验证自动合并资格.py`。
- 任务115、任务116、任务106合同和真实看板不变。
- 不修改可信资格器、自动合并工作流、数据、服务器、数据库、生产或交易路径。
- 失败关闭仍覆盖分支、PR、优先级、标题、依赖、任务状态和任务116完成前置。

## 验收

使用任务中心专项测试、Python编译、MarkdownLint和`git diff --check`验证；所有测试在当前任务115生命周期状态下可复现。
