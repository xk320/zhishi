# 任务-000123：阶段1完成态测试夹具设计

## 目标

把阶段1覆盖受限合同正向测试从真实看板生命周期中隔离出来，在测试内固定构造任务115待评审和任务106未修订输入。

## 边界

只修改测试代码；真实任务合同、看板、可信规则、数据、服务器和交易能力不变。保留状态、合同、路径和安全漂移的失败关闭断言。

## 验证

运行研发中心专项测试、Python编译、MarkdownLint和`git diff --check`，结果记录在交付PR。
