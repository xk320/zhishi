# 任务-000125设计：阶段1覆盖受限V2跨载体类型注册

## 目的

为一次性任务-000124注册跨载体可识别的变更类型，消除任务正文、PR正文与可信跨载体检查器之间的类型解析漂移。

## 固定设计

- 类型字面量：`阶段1覆盖受限完成合同修订V2`
- 唯一执行者：任务-000124
- 目标合同：任务-000098、任务-000106（仅作引用约束，不在本任务修改）
- 允许实现路径：跨载体检查器、自动合并工作流及对应测试
- 未知类型、错任务、错目标、额外路径、状态迁移和数据/生产路径均失败关闭。

## 不改变的事实

任务-000124合同、任务-000098/000106合同、既有看板事实、历史批次、数据和交易边界均保持不变。本设计不执行V2合同，也不宣称阶段1完成；任务-000125自身状态元数据和派生看板行仍按生命周期协议更新。

## 验收

跨载体类型解析正向测试通过，越权负向测试失败关闭，相关测试、编译、MarkdownLint和diff-check通过。
