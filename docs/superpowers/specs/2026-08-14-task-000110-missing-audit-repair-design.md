# 任务-000110：缺失审计分母与执行器指纹修复设计

## 目标

在不改写任务-000109首批历史批次的前提下，补齐来源拒绝候选的BTC/ETH、对象和主研究尺度分母映射，并让执行器指纹绑定到实际包含脚本的提交。

## 边界

- 只读取任务-000109已合并的脱敏输入；不访问服务器、数据库、网络、原始市场数据、账户或凭据。
- BTC与ETH独立统计；主研究尺度固定4小时、8小时、24小时、48小时；15分钟和1小时仅为事后观察窗口。
- 不重新证明Binance来源，不实现运行时、模型、回测、交易、订单或研究准入。

## 交付与验收

- 新增追加式修复批次，不覆盖首批批次。
- `groups.json`和八个叶子逐组保存正式成员、来源拒绝、合计分母、状态计数、连续段数量和覆盖日期。
- `executor.json`同时保存包含执行器脚本的Git提交和脚本blob SHA，能够从该提交复现入口。
- 专项审计测试、审计测试、编译、MarkdownLint和`git diff --check`通过；来源拒绝与正式成员计数守恒。
