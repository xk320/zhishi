# 任务-000051：可信冲突检查器治理自动化修复设计

## 设计目的

为任务-000050发现的完成态身份校验和历史证据回归问题提供符合可信路径硬门的治理自动化
交付通道。任务合同、研究范围和历史证据保持不变。

## 设计决策

- 完成态普通任务交付继续校验执行分支、开始时间和PR；
- `合并后状态闭环`与`任务登记`仅在正文严格匹配时保留例外；
- 历史证据测试使用合成的`base-ref`和`head-ref`，不依赖当前分支拓扑；
- 变更路径仅限研发中心可信检查器和对应测试，由任务类型“治理”和“自动合并范围：治理自动化”授权。

## 安全与回滚

任何基线/头SHA、PR元数据、状态或资源不一致均失败关闭；回退本任务交付提交即可恢复，
不触碰服务器、数据库、原始数据、账户、凭据或生产服务。
