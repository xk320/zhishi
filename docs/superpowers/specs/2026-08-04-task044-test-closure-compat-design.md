# 任务-000044设计：状态闭环测试兼容性

任务-000043已将外部事实治理落到文档和纯函数合同，但其专项测试只覆盖交付等待评审态。
状态闭环是合法且必须的下一步，测试必须同时验证交付态与完成态，不能把完成态视为异常。

本任务只改测试断言：读取任务文件状态后，严格选择`待评审`或`已完成`分支；前者验证
执行分支、PR和待评审看板行，后者验证 PR #62、真实合并提交和已完成看板行。除这两个状态
外一律失败。四态外部词汇、证据时间新鲜度、唯一恢复路径、任务-000031阻塞和历史保留
测试继续运行。

不访问服务器、数据库或真实数据，不修改可信校验器和任务-000043状态。回滚本任务PR即可
恢复旧测试；恢复后不得跳过完成态测试。
