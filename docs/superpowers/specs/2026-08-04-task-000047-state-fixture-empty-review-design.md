# 任务-000047设计：按机器看板模式生成空分区状态夹具

## 决策

任务-000043完成闭环后，真实看板的“待评审”分区可以只有“无”，测试不能假设表格已经
存在。夹具构造应读取仓库唯一机器合同`docs/研发中心/任务看板模式.md`，在内存中插入标准
表头、分隔线和任务行，再执行既有状态映射断言。

## 约束

- 只修改研发中心状态一致性测试及任务记录；
- 不修改任务状态机、看板机器模式或可信合并脚本；
- 不访问服务器、数据库、真实市场数据或凭据；
- 夹具不落盘，失败语义不放宽。

## 验证

专项测试必须覆盖真实完成态和待评审态、空分区、错分区、缺失行、未知/缺失/重复状态及
历史记录缺失；提交前运行研发中心全量测试、Python编译、离线MarkdownLint和`git diff --check`。
