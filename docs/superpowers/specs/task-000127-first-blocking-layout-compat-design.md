# 任务-000127设计：任务126首次阻塞布局兼容

## 设计目的

任务-000126登记合同没有预置阻塞字段，首次进入阻塞时通用布局解析无法识别。建立一个只绑定任务126的一次性兼容，不放宽全局状态门。

## 固定规则

- 执行任务：任务-000127。
- 目标任务：任务-000126。
- 允许迁移：仅目标从`待执行`到`阻塞`，其他状态和任务均失败关闭。
- 必须保留：八字段结构化阻塞记录、任务合同正文、看板唯一映射和数据安全边界。

## 负向边界

错任务、缺字段、多字段、合同正文变化、看板漂移、目标非待执行、额外路径和任何研究/交易/生产改动均拒绝。

## 验收

正向合成夹具与任务126阻塞PR复算通过，越权夹具失败关闭，相关治理测试和格式检查通过。
