# 任务-000122：任务交付记录Markdown兼容设计

## 问题

任务-000115的注册合同要求正文不可变，而执行记录标题紧贴正文时默认MarkdownLint触发MD022；补空行会被旧合同比较视为正文漂移。

## 方案

只在可信资格器比较任务交付合同时，忽略`## 执行记录`标题前的一个标准空行。所有其他合同字符、状态元数据、执行记录、路径、任务映射和安全规则保持严格比较。增加多空行和正文改写的失败关闭测试。

## 边界

这是治理兼容修复，不是合同放宽；不访问数据或服务器，不改变研究、交易或生产权限。
