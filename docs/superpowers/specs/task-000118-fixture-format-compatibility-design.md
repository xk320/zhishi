# 任务-000118设计：阶段1测试夹具看板行格式兼容

## 目标

在测试内兼容任务-000116待评审和已完成两种合法看板行格式，同时保持合成输入与真实任务事实隔离。

## 约束

- 仅修改研发中心测试夹具与任务派生记录。
- 只允许识别带优先级和不带优先级的任务116行，不泛化为任意看板格式。
- 不修改可信资格器、工作流、任务合同、生产或数据。

## 验证

运行研发中心资格、工作流、看板和范围专项测试，全部通过后才进入状态闭环。
