# 任务-000117设计：任务116完成后的阶段1测试夹具修复

## 目标

让阶段1覆盖受限合同修订测试使用独立、确定性的输入基线，不依赖任务-000116当前真实状态或看板分区。

## 约束

- 只改测试夹具和任务派生记录。
- 保留任务-000116未完成、错路径、合同漂移、看板漂移和目标状态迁移的失败关闭断言。
- 不修改可信资格器、工作流、任务合同、看板事实、服务器、数据库、原始数据或交易逻辑。

## 验证

使用研发中心资格、自动合并工作流、看板和项目范围专项测试；全部通过后才允许进入状态闭环。
