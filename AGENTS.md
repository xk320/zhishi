# 《知势》Codex 执行规范

> **宁可错过，不做低胜率交易。**

本文件适用于在本仓库工作的所有 Codex、自动化代理和开发者。

## 一、项目定位

《知势》是一套以高质量交易许可为最终输出的智能交易决策系统，不是行情解说工具，也不是强制每日交易的自动下单程序。

第一阶段只研究BTC、ETH以及4小时、8小时、24小时、48小时时间尺度。每日允许0～2次高质量机会，优势不足时主动空仓。真实资金自动交易当前关闭。

## 二、最高优先级

发生冲突时依次服从：

1. 资金与数据安全；
2. 《知势宣言》和风险纪律；
3. 研发中心任务合同与验收标准；
4. 数据可追溯、可重放、无时间穿越；
5. 简单、可解释、可验证；
6. 性能与工程便利。

任何收益提升都不能越过前五项。

## 三、任务文件是唯一事实来源

所有任务必须由`docs/研发中心/任务/任务-NNNNNN.md`中的独立任务文件定义。任务状态、
类型、依赖、优先级、范围、交付物和验收标准只以任务文件为准，具体规则见
`docs/研发中心/任务规范.md`。

`docs/研发中心/看板.md`是任务文件的派生视图，`docs/研发中心/总体计划.md`只表达阶段路线和依赖。两者不得覆盖任务文件；发现不一致时，以任务文件为准并同步修复看板。

GitHub Issue只能用于讨论、提问、证据附件和关联，不是任务合同或任务状态来源。没有任务文件的Issue禁止执行。Pull Request是交付与评审载体，必须引用任务编号，但不能替代或扩大任务合同。

Codex不得仅根据聊天摘要、Issue、PR描述、标签、口头描述或自己的推测启动工作。

任务状态只允许：`待执行`、`执行中`、`阻塞`、`待评审`、`需修复`、`已完成`、`已取消`。

## 四、自动任务选择规则

每次运行必须：

1. 拉取远程最新主分支并确认工作区状态；
2. 阅读本文件、README、`docs/研发中心/任务规范.md`及研发中心全部入口文件；
3. 优先处理`需修复`任务，其次处理`待执行`任务；
4. 只选择依赖满足、没有阻塞的最高优先级任务；
5. 同优先级按任务编号从小到大；
6. 每次只执行一个任务，除非任务明确允许并行且不存在文件冲突；
7. 认领前检查该任务是否已有远程执行分支或未关闭PR，并读取在途分支中的任务文件防止重复执行；
8. 没有任务文件的Issue或评论请求不得进入执行队列；
9. 无可执行任务时不得制造空提交。

`待执行`或`需修复`任务如果写明`方案状态：已批准执行`，Codex必须按任务文件中的唯一
方案直接执行，不得再次向用户提供A/B/C选项、询问采用哪种实现或要求重复批准。普通
工程取舍按安全、证据、简单、可复现、可回滚的顺序自主决定，并在PR中记录依据。

只有需要新增生产权限或凭据、修改生产系统或原始数据、执行不可逆操作、合同直接冲突，
或缺少会改变业务目标与风险门槛的外部事实时，才允许请求人工决策。数据不足导致
`无法判定`、普通技术选型、文件组织和测试方式不属于暂停条件。

## 五、任务执行协议

开始前：

- 将任务文件和看板状态更新为`执行中`；
- 记录分支和开始时间；
- 创建独立分支。

执行中：

- 严格遵守输入合同、输出合同、范围、安全边界和验收标准；
- 按已批准的唯一执行方案自主推进，不把内部取舍转交用户选择；
- 优先补测试和验证脚本；
- 不得顺手扩展任务范围；
- 发现任务设计矛盾时记录证据并停止相关部分，不得自行降低门槛。

完成实现后：

- 运行全部相关验证；
- 自审未来数据泄漏、成本遗漏、异常输入、敏感信息和破坏性操作；
- 提交、推送并创建Pull Request，PR必须引用任务编号；
- 将任务更新为`待评审`，记录提交SHA、PR、交付物、验证结果和已知限制；
- 同步更新看板；
- 由主执行器组织两个独立只读子智能体评审；子智能体只报告问题，不修改文件、不使用凭据。

架构评审与合并：

- 默认最多同时运行两个只读评审者，分别覆盖治理/架构与范围/安全；测试保持单进程；
- 任一P0或P1问题由主执行器自动修复；新提交使全部旧评审失效，最多修复三轮；
- 最终两个评审均`APPROVE`、P0=P1=0、验证通过后，以结构化证据绑定基线SHA、头SHA、
  任务编号、验证命令和资源预算；
- `main`可信工作流复验同仓库来源、`main`目标、非草稿、任务合同、变更路径、未解决
  线程、`CHANGES_REQUESTED`、当前头检查和GitHub可合并状态，并携带精确头SHA合并；
- 不再要求仓库所有者逐PR人工批准；规则、证据或实时状态不满足时失败关闭，不得绕过；
- `任务登记`只允许一个完整新任务合同、看板和对应设计文档，编号必须为基线最大编号
  加一，历史缺号不得复用；
- 自动交付类型精确限定为`文档`、`治理`、`研究规范`、`数据治理`、`数据审计`、
  `数据工程`、`基础设施验证`、`策略研究`、`研究工程`、`模拟交易`、`测试`和`工具`；
- 受控研发交付还必须通过路径、扩展名、普通Git对象、文件数量、单文件与总大小及敏感
  信息硬门。只有基线任务明确`自动合并范围：治理自动化`时，才允许受限修改自动合并
  工作流和研发中心可信脚本；研发中心测试仍按普通测试路径校验，不得借此修改可信脚本；
- 当前系统只进行策略研究和基于实时数据的模拟交易；真实资金、生产写入、凭据、原始
  数据破坏和不可逆操作不会因自动合并白名单扩大而获得授权；
- 具体触发格式、资格和回滚见`docs/治理/PR自动合并策略.md`。

只有双子智能体评审通过且PR合并后，任务才能标记`已完成`。合并后状态闭环使用独立PR，
允许单一任务的`待评审→已完成`、`待执行/执行中/需修复→阻塞`或`阻塞→待执行/需修复`，
不得直接写入`main`。阻塞首次进入时必须保留固定执行元数据和脱敏结构化记录；恢复只能由
新的状态闭环PR触发，不得自动解除或夹带合同改写。

## 六、当前硬门槛

在数据资产审计和最小数据闭环通过前：

- 不训练复杂模型；
- 不宣称稳定胜率或收益；
- 不启动真实资金交易；
- 不以无法证明时间对齐的数据做正式回测；
- 不将研究结果标记为“已纳入”；
- BTC与ETH必须分别形成证据，不得互相外推。

服务器可达性不得由本文件、聊天或历史批次推断。当前外部事实以任务-000031最新脱敏执行
记录为准；该记录显示白名单逻辑别名`ubuntu`不可达，因此任务-000031及其后继仍保持阻塞。
只有在任务合同资源边界内取得新的脱敏探针和真实只读审计证据后，才能通过独立状态闭环恢复；
任何真实环境读取仍须由任务合同授权，并坚持只读、白名单和资源上限。

## 七、语言与命名

1. 所有用户可见指标、参数、状态、报告和界面文本使用中文；
2. 第三方接口、协议和原始字段可以保留英文，但必须提供中文映射；
3. 源代码标识符可使用英文，对应业务语义必须在中文文档中定义；
4. 新增业务术语前先更新《知势词典》；
5. 禁止使用“感觉偏强”“大概率”“可以考虑”等模糊表达替代可执行结论。

## 八、研究纪律

任何指标、规则、模型或数据源必须遵守《研究准入规范》：

- 假设必须可证伪；
- 必须有简单基准；
- 时间序列不得随机打乱替代前推验证；
- 必须计算手续费、滑点、资金费率和执行延迟；
- 必须报告交易次数、空仓比例、胜率、盈亏比、单笔期望和最大回撤；
- 必须展示失败样本和反证；
- 参数轻微变化后结果不能立即崩溃；
- 淘汰是正常研究结果。

严禁未来数据泄漏、只展示最佳参数、以少数异常交易支撑结论、先定结论再找证据。

## 九、决策与风控纪律

- 确定性、预测胜率、赔率和证据完整度必须分开；
- 未经校准的固定胜率门槛不得进入生产规则；
- 交易许可必须通过数据、样本、优势、成本、风险、执行、频率与纪律质量门；
- 任一硬门失败，最终必须为`交易许可：禁止`；
- 仓位由账户权益、最大允许损失、止损距离和交易成本推导；
- 杠杆不等于确定性；
- 不设置强制每日收益目标。

## 十、数据与安全

未经明确授权，禁止：

- 删除、覆盖或迁移原始市场数据；
- 修改生产数据库、生产服务、权限或防火墙；
- 写入真实交易账户；
- 记录或提交密钥、令牌、密码和账户凭据；
- 执行不可逆操作；
- 虚构数据、测试结果、胜率、收益、机构背书或完成状态。

数据处理必须区分事件时间、到达时间和采集时间，并显式记录缺失、延迟、乱序、重复和异常。

## 十一、工程与提交要求

- 优先小模块、明确接口和可重复执行；
- 新依赖必须说明必要性、许可证和替代方案；
- 关键计算必须有测试；
- 数据管道必须幂等或有明确去重策略；
- 时间、金额、比例、价格和数量必须说明时区、精度与单位；
- 随机过程必须固定种子并记录版本。

提交前缀：`docs:`、`research:`、`feat:`、`fix:`、`test:`、`refactor:`、`chore:`。

Pull Request必须包含：关联任务编号、变更摘要、验收结果、验证命令、数据与安全影响、
已知限制和回滚方式。GitHub Issue只能作为可选讨论关联。符合低风险资格的PR可以按
`docs/治理/PR自动合并策略.md`在双子智能体评审、主执行器验证和`main`可信复验通过后
自动合并；不再等待逐PR人工批准。不符合资格的PR失败关闭并保留证据，不得绕过。

## 十二、最终判断原则

每项新增能力必须回答：

> 它减少了哪类错误交易，提升了哪类机会的净期望，或者增强了哪项风险约束？

无法明确回答时，不进入《知势》主决策链路。
