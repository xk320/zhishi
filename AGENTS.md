# 《知势》Codex 执行规范

> **宁可错过，不做低胜率交易。**

本文件适用于在本仓库中工作的所有 Codex、自动化代理和开发者。它定义项目优先级、语言、研究纪律、安全边界、任务执行方式和交付标准。

## 一、项目定位

《知势》是一套智能交易决策系统，不是行情解说工具，也不是强制每日交易的自动下单程序。

第一阶段只研究：

- BTC
- ETH
- SOL
- 4小时、8小时、24小时、48小时时间尺度
- 每日 0～2 次高质量机会
- 优势不足时主动空仓

当前阶段不接入真实资金自动下单。

## 二、优先级顺序

发生冲突时，严格按以下顺序决策：

1. 资金与数据安全。
2. 《知势宣言》和风险纪律。
3. 任务验收标准。
4. 数据可追溯、可重放、无时间穿越。
5. 简单、可解释、可验证的实现。
6. 性能与工程便利。

任何收益提升都不能越过前五项。

## 三、语言与命名

1. 所有用户可见的指标、参数、状态、报告、日志摘要和界面文本使用中文。
2. 第三方接口字段、协议字段、数据库原始字段可以保留英文，但必须提供中文映射。
3. 源代码标识符可以使用英文，以保证生态兼容和可维护性；对应业务语义必须在中文文档中定义。
4. 新增业务术语前，先更新 `docs/方法论/知势词典.md`。
5. 禁止用“感觉偏强”“大概率”“可以考虑”等模糊表达替代可执行结论。

## 四、当前硬门槛

在 `任务-000001：完成数据资产审计与最小数据闭环设计` 验收通过前：

- 不训练复杂模型。
- 不宣称任何稳定胜率。
- 不启动真实资金交易。
- 不以无法证明时间对齐的数据做正式回测。
- 不将研究结果标记为“已纳入”。

允许开展的工作：

- 项目治理与接口合同。
- 数据审计与数据质量工具。
- 可重放数据骨架。
- 最简单的基准定义与测试框架。
- 不依赖真实结果的文档和安全规范。

## 五、任务执行协议

每项任务必须包含：

- 任务目标。
- 决策问题或工程问题。
- 输入与输出合同。
- 工作范围与明确不做的内容。
- 安全边界。
- 可机器验证的验收标准。
- 交付物路径。
- 验证命令。
- 完成定义。

执行步骤：

1. 阅读相关 Issue、README、系统蓝图、研究准入规范和本文件。
2. 检查任务是否与当前阶段门槛冲突。
3. 在独立分支完成工作。
4. 优先补测试和验证脚本，再补复杂实现。
5. 运行所有相关检查。
6. 自审未来数据泄漏、成本遗漏、异常输入和破坏性操作。
7. 提交 Pull Request，说明交付物、验证结果、已知限制和下一步。
8. 未达到验收标准时继续迭代，不得以“代码已写完”作为完成。

## 六、研究纪律

任何指标、规则、模型或数据源必须遵守 `docs/研究/研究准入规范.md`。

强制要求：

- 假设必须可证伪。
- 必须有简单基准。
- 时间序列不得随机打乱替代前推验证。
- 必须计算手续费、滑点、资金费率和执行延迟。
- 必须报告交易次数、空仓比例、胜率、盈亏比、单笔期望和最大回撤。
- 必须展示失败样本和反证。
- 参数轻微变化后结果不能立即崩溃。
- 研究可以被淘汰，淘汰是正常结果。

严禁：

- 未来数据泄漏。
- 只展示最佳参数。
- 用少数异常交易支撑结论。
- 只提高胜率却恶化净期望或最大回撤。
- 先定结论再寻找支持样本。

## 七、决策与风控纪律

1. “确定性”“预测胜率”“赔率”“证据完整度”必须分开记录。
2. 未经校准的固定胜率门槛不得进入生产规则。
3. 交易许可必须通过数据、样本、优势、成本、风险、执行和频率质量门。
4. 任一硬门槛失败，最终结果必须为“禁止交易”。
5. 仓位由账户权益、最大允许损失、止损距离和交易成本推导，不由情绪或杠杆倍数推导。
6. 杠杆不等于确定性。
7. 不能为了追回错过的行情降低下一笔交易标准。
8. 不设置强制每日收益目标；系统优化净期望、回撤和长期稳定性。

## 八、数据安全

未经明确授权，禁止：

- 删除、覆盖或迁移原始市场数据。
- 修改生产数据库结构。
- 写入真实交易账户。
- 记录或提交密钥、令牌、账户凭据。
- 对不可逆操作使用默认确认。

数据处理必须：

- 区分事件时间与到达时间。
- 保留数据源、版本和质量状态。
- 能证明决策时只使用当时已到达的数据。
- 对缺失、延迟、乱序、重复和异常值做显式标记。

## 九、工程要求

- 优先小模块、明确接口和可重复执行。
- 不为未来假想需求提前引入复杂框架。
- 新依赖必须说明必要性、许可证和替代方案。
- 关键计算必须有单元测试。
- 数据管道必须有幂等性或明确去重策略。
- 所有时间字段必须说明时区和精度。
- 所有金额、比例、价格和数量必须说明单位。
- 随机过程必须固定种子并记录版本。

## 十、提交与 Pull Request

提交信息使用以下前缀之一：

- `docs:` 文档与规范
- `research:` 研究与实验
- `feat:` 新能力
- `fix:` 缺陷修复
- `test:` 测试
- `refactor:` 不改变行为的重构
- `chore:` 工程维护

Pull Request 必须包含：

- 关联任务。
- 变更摘要。
- 验收标准逐项结果。
- 验证命令与结果。
- 数据与安全影响。
- 已知限制。
- 回滚方式。

## 十一、完成报告格式

```text
任务：
分支：
提交：
交付物：
验收结果：通过 / 未通过
验证命令：
关键结果：
已知限制：
数据与安全影响：
下一步建议：
```

## 十二、最终判断原则

每次新增能力都必须回答：

> 它减少了哪类错误交易，提升了哪类机会的净期望，或者增强了哪项风险约束？

无法明确回答时，不进入《知势》主决策链路。